# Local Pi runtime state
.atl/
tmp/
# Hypothesis scratch — not part of the distributable
.tmp/

# Build output — produced by tools/build.sh, never committed
dist/

# gsd-tools runtime state — a process lock and a derived cache regenerated
# from STATE.md on demand, never project source
.planning/milestone.lock
.planning/state.json
