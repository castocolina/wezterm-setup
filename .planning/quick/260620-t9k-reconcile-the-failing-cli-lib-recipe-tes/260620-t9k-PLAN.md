---
phase: quick-260620-t9k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [cli/lib/recipe_test.lua]
autonomous: true
requirements: [SCEN-04]

must_haves:
  truths:
    - "The recipe_test.lua `2.9d ai.toml` expected table matches the shipped scenes/ai.toml field-for-field"
    - "scenes/ai.toml is left byte-identical (seed is canonical — only the test fixture changes)"
    - "The `2.9d` expectation key names/shape match recipe.lua recipe_to_args real output (pane, not panes; follow_pane_color carried as a boolean)"
  artifacts:
    - path: "cli/lib/recipe_test.lua"
      provides: "Reconciled 2.9d ai.toml expected args block (color=yellow, follow_pane_color=true)"
      contains: "2.9d ai.toml"
  key_links:
    - from: "cli/lib/recipe_test.lua (2.9d expected)"
      to: "scenes/ai.toml (shipped seed)"
      via: "field-for-field equality (color, follow_pane_color, icon, title, panes)"
      pattern: "color = \"yellow\""
    - from: "cli/lib/recipe_test.lua (2.9d expected)"
      to: "cli/lib/recipe.lua recipe_to_args"
      via: "output shape (pane key, follow_pane_color boolean)"
      pattern: "pane = \\{"
---

<objective>
Reconcile the single red test in the suite: `cli/lib/recipe_test.lua`'s `2.9d ai.toml`
expected-args block has drifted from the shipped seed `scenes/ai.toml`. The seed is
the locked source of truth (maintainer decision); update the TEST fixture to match it.

The drift is exactly two fields in the `2.9d` expected table:
- tab `color` is `"pink"` in the test but `"yellow"` in the seed (scenes/ai.toml line 53)
- tab `follow_pane_color` is `false` in the test but `true` in the seed (scenes/ai.toml line 55)

Everything else already matches: `layout = "tall"`, `title = "@{cwd} AI work"`,
`icon = "ai"`, and both `pane` entries
(`cmd=claude, color=purple, title=@{cwd} AI Session, focus=true, icon=ai` and
`cmd=shell, color=red, title=@{cwd} AI Shell, icon=shell`). The `@{cwd}` prefix is an
intentional, uniform convention across all three seeds — DO NOT touch it.

The stale block comment above the assertion (test lines ~352-354) also claims
"explicit follow_pane_color=false (the documented default, written out)" — that is wrong
for the current seed (which ships `true`). The comment must be corrected alongside the
assertion so the fixture documents reality.

This closes deferred item D-1 in
`.planning/phases/06.3-distribution-channels-inserted/deferred-items.md`.

Purpose: Get the suite back to all-green (test-fixture reconciliation, no production
change) so the lone known-red assertion no longer masks future regressions.
Output: An updated `cli/lib/recipe_test.lua` whose `2.9d` expectation mirrors
`scenes/ai.toml` and the real `recipe_to_args` output shape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@scenes/ai.toml
@cli/lib/recipe.lua
@cli/lib/recipe_test.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the 2.9d ai.toml expected args (and its stale comment) with the seed</name>
  <files>cli/lib/recipe_test.lua</files>
  <action>
    Edit ONLY the `2.9d ai.toml` expectation and its preceding block comment in
    `cli/lib/recipe_test.lua` (the `do` block around lines 352-364). DO NOT touch any
    other test case, and DO NOT touch any production file (scenes/ai.toml, recipe.lua,
    scene.lua are off-limits — seed is canonical).

    In the `teq("2.9d ai.toml maps to the D-03/D-05/D-14 args", ai, { ... })` expected
    table, change exactly two fields to match the shipped scenes/ai.toml:
      - `color = "pink"`  ->  `color = "yellow"`   (matches scenes/ai.toml line 53)
      - `follow_pane_color = false`  ->  `follow_pane_color = true`  (matches scenes/ai.toml line 55)
    Leave every other field of the expected table exactly as-is: `layout = "tall"`,
    `title = "@{cwd} AI work"`, `cwd = nil`, `icon = "ai"`, and BOTH `pane` entries
    unchanged (they already match the seed and the recipe_to_args output shape — the
    key is `pane`, not `panes`, and each entry is the comma-joined multi-field spec).

    Correct the stale block comment immediately above the assertion (currently
    "...explicit follow_pane_color=false (the documented default, written out)...").
    Rewrite it to describe the real seed: pink->yellow tab color, and
    follow_pane_color=true (the opt-in is ON in this seed — a colorless-tab tracker,
    though here the tab has an explicit color so it is informational). Keep the comment
    accurate and comma-free in spirit with the rest of the file; do not invent behavior
    beyond what scenes/ai.toml + recipe.lua actually do.

    Rationale anchor: recipe.lua `recipe_to_args` (lines 138-160) carries top-level
    `color`/`title`/`cwd`/`icon`/`follow_pane_color` verbatim from the parsed recipe and
    emits panes under the `pane` key — so the seed's `color = "yellow"` and
    `follow_pane_color = true` flow through as the literal string "yellow" and the boolean
    true. That is precisely the reported "got" table, confirming the seed is canonical and
    the test fixture was the drifted side.
  </action>
  <verify>
    Structural-only (this Intel Mac has NO Lua interpreter — `lua`/`lua5.4` absent,
    Phase 7 gap C-1 — so `./tools/run-tests.sh` CANNOT run here):
    (a) Re-read the edited `2.9d` block in cli/lib/recipe_test.lua and confirm field-for-field
        against scenes/ai.toml: `color = "yellow"` (seed line 53), `follow_pane_color = true`
        (seed line 55), `icon = "ai"` (line 54), `title = "@{cwd} AI work"` (line 56), and the
        two pane specs matching the [[panes]] blocks (claude/purple/focus/ai; shell/red/shell).
    (b) Confirm the expected table's key names/shape match recipe.lua recipe_to_args output:
        top-level `color`/`title`/`cwd`/`icon`/`follow_pane_color` + a `pane` key (NOT `panes`).
    (c) Confirm `git diff --stat` shows ONLY cli/lib/recipe_test.lua changed (no seed / no
        production .lua touched).
    A live full-suite run (expected: recipe_test green, 2.9d passing) is DEFERRED to CI /
    a Lua-capable host. Do NOT claim the suite is green from this machine — only that the
    fixture now matches the seed and the real output shape by inspection.
  </verify>
  <done>
    cli/lib/recipe_test.lua `2.9d` expected table reads `color = "yellow"` and
    `follow_pane_color = true` (all other fields unchanged); its block comment no longer
    claims follow_pane_color=false; scenes/ai.toml and all production .lua are untouched;
    git diff is limited to cli/lib/recipe_test.lua.
  </done>
</task>

</tasks>

<verification>
- `git diff --name-only` lists ONLY `cli/lib/recipe_test.lua`.
- The `2.9d ai.toml` expected table matches `scenes/ai.toml` field-for-field
  (color=yellow, follow_pane_color=true, icon=ai, title=@{cwd} AI work, both panes).
- Expected-table key names match recipe.lua recipe_to_args (top-level scalars + `pane` list).
- Live suite run deferred to CI / a Lua-capable host (no Lua interpreter on this machine).
</verification>

<success_criteria>
- The lone red assertion (`2.9d ai.toml`) is reconciled to the canonical seed by editing
  the test fixture only.
- No production code or seed file is modified.
- The fix is structurally verified by inspection; a live green-suite claim is explicitly
  deferred to CI / a Lua-capable host (this machine cannot run the suite).
- Deferred item D-1 in 06.3 deferred-items.md is addressed.
</success_criteria>

<output>
Create `.planning/quick/260620-t9k-reconcile-the-failing-cli-lib-recipe-tes/260620-t9k-SUMMARY.md` when done.
</output>
