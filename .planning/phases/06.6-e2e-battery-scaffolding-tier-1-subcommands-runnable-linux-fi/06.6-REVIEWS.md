# Phase 06.6 — Plan Review Feedback (REVIEWS.md) — Round 3 (final)

**Source:** gsd-plan-checker validation after round-2 replan (verdict: SOUND, 1 non-blocking WARNING)
**Date:** 2026-06-24
**Verdict:** SOUND — resolve the single WARNING below, then ready for execution.

Rounds 1 & 2 are fully resolved (verified SOUND). This round folds in the ONE remaining issue.
Do not touch anything else.

---

## WARNING (fix before execution — protects a Plan 04 acceptance criterion)

### WR1 — `make test` will double-run the e2e tests against the stale PATH `wez`
`tools/run-tests.sh` discovers EVERY `tests/**/*_test.lua` and excludes only
`*_integration_test.lua`. The new `tests/e2e/tier1/*_e2e_test.lua` files match `*_test.lua`, so
`make test` will ALSO run them — but WITHOUT `WEZ_BIN=./dist/wez` (the e2e files resolve
`WEZ = os.getenv("WEZ_BIN") or "wez"`, i.e. the stale PATH binary `~/.local/bin/wez`, not the
freshly built `./dist/wez`). This directly threatens Plan 04's "`make test` exits 0 after the
cherry-picks" acceptance criterion, and re-introduces exactly the stale-binary footgun the battery
exists to catch.

**Required fix (in Plan 01 Task 2):**
- Add `tools/run-tests.sh` to Plan 01 Task 2 `files_modified` and `<files>`.
- In Task 2 `<action>`, modify `tools/run-tests.sh` test discovery to ALSO exclude
  `*_e2e_test.lua` (alongside the existing `*_integration_test.lua` exclusion) — the e2e battery
  is `make e2e`'s domain, not `make test`'s. Keep the edit bash-3.2-safe (no new mapfile/etc.).
- Add `tools/run-tests.sh` to Task 2 `<read_first>` (the current discovery block, ~lines 50-65, so
  the exclusion is grafted onto the real `find … ! -name '*_integration_test.lua'` predicate).
- Add an `<acceptance_criteria>` entry: `make test` does NOT run any `tests/e2e/tier1/*_e2e_test.lua`
  file (e.g. grep the run-tests.sh discovery for the `*_e2e_test.lua` exclusion, and/or assert the
  `make test` output contains no `tier1` e2e file name).
- Update the `<artifacts_this_phase_produces>` / `must_haves` as needed to note run-tests.sh is
  modified (the exclusion), and Plan 01 `key_links` if appropriate.

This keeps the `make test` (unit) / `make e2e` (battery) boundary clean and protects Plan 04.

---

## What PASSED (preserve — do not regress)
Everything from rounds 1 & 2: the icon module attribution (title.resolve_icon + color builders,
shared WEZTERM_TAB_ICON), install-state isolation seam (WEZTERM_CONFIG_FILE/HOME), run_new + 
decide_wez_update grounding, harness module-level API, Plan 04 conflict-resolution reads, the
Wave-2/Wave-3 deferred split, PRD A.1 coverage, bash-3.2 safety, scope discipline, OSC
source-grounding, no same-wave files_modified overlap.
