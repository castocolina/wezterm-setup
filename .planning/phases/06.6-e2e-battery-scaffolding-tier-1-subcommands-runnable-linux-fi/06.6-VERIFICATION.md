---
phase: 06.6-e2e-battery-scaffolding-tier-1-subcommands-runnable-linux-fi
verified: 2026-06-24T00:00:00Z
status: passed
score: 18/18 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 06.6: E2E Battery Scaffolding + Tier 1 (Subcommands) Verification Report

**Phase Goal:** A runnable, Linux-first E2E battery with the full deterministic subcommand layer (PRD Phase 1) — `tests/e2e/` scaffold + `tools/run-e2e.sh` (bash-3.2-safe) + `make e2e` / `make e2e-setup` + platform module + self-skip gates; Tier 1 exit-code/output-shape contracts + emitted-OSC-byte well-formedness assertions; cherry-pick the 4 independently-verified fixes from `archive/phase-7-macos`. Done-bar: Tier 1 MUST-PASS, `make e2e` exits 0, `make test` excludes the e2e battery, bash-3.2-safe tooling, macOS-only paths skip-gated (not run) on Linux.
**Verified:** 2026-06-24
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1 | `make e2e` runs the battery and exits 0 | ✓ VERIFIED | Ran `make e2e` → exit 0; `run-e2e: all 6 file(s) passed` |
| 2 | run-e2e.sh discovers via bash-3.2-safe while-read+array (no mapfile/readarray/declare -A) | ✓ VERIFIED | `tools/run-e2e.sh:67-69` `while IFS= read -r f; do ... done < <(find tests/e2e ... \| sort)`; no bash-4 constructs |
| 3 | `make test` does NOT run any `tests/e2e/tier1/*_e2e_test.lua` (clean unit/battery boundary) | ✓ VERIFIED | `make test` → exit 0, 34 files; e2e exclusion in `run-tests.sh:65` (`! -name '*_e2e_test.lua'`) + belt-and-braces `tests/e2e/*` case arm; only lints run-e2e.sh syntax, never runs it |
| 4 | harness.lua exposes module-level check/run_capture/run_capture_all/shquote/scratch_dir/footer/WEZ (+ M.new) | ✓ VERIFIED | `tests/e2e/lib/harness.lua` (148 lines); all 6 Tier-1 files `require` it; spot-checked exports used at module level |
| 5 | skip.lua exposes require_env/require_tool/soft_skip with loud-logged never-silent LIVE-ASSERTED contract | ✓ VERIFIED | `tests/e2e/lib/skip.lua` (70 lines); keys-siblings test emits loud `SKIPPED (reason=macOS …)` on Linux |
| 6 | platform.lua returns a pure Mac↔Linux deltas table loadable under plain lua5.4 | ✓ VERIFIED | `lua5.4 -e dofile("tests/e2e/platform.lua")` → `type==table OK` (114 lines, no wezterm dep) |
| 7 | `wez version` exits 0 + prints spec.VERSION (0.1.0), compared against spec not a literal | ✓ VERIFIED | `./dist/wez version` → `wez 0.1.0` exit 0; `diagnostics_e2e_test.lua:83` compares `out:find(spec.VERSION)` |
| 8 | `wez doctor` exit 0 gate lines; `wez keys` exit 0 non-empty; `wez keys --json` exit 0 + valid JSON | ✓ VERIFIED | `doctor` exit 0; `keys --json` emits parseable JSON decoded via dkjson (`diagnostics_e2e_test.lua:115-119`) |
| 9 | `wez completions bash`/`zsh` exit 0 with non-empty completion script | ✓ VERIFIED | bash 2666 bytes, zsh 2142 bytes, exit 0 |
| 10 | `wez seed-scenes` into scratch WEZTERM_SETUP_DIR copies 3 recipes (copy-if-absent; re-run keeps) | ✓ VERIFIED | `install_cycle_e2e_test.lua` asserts copy-if-absent; e2e file PASS |
| 11 | edge cases: unknown→exit 1; empty recipes→exit 2; path-traversal→exit 1; `--force` reinstall→exit 0 | ✓ VERIFIED | `scene_edge_e2e_test.lua` 6/6 PASS (unknown exit 1, empty exit 2, traversal exit 1) |
| 12 | install-state fresh installs managed block in isolated config; uninstall (binary present) exit 0; idempotent (absent); update comparator current/system-skip offline, smoke reachability-gated | ✓ VERIFIED | `install_cycle_e2e_test.lua` PASS; update smoke bounded-reachability gated (soft_skip offline) |
| 13 | `wez pane color` emits well-formed OSC-11 + OSC-1337 base64 BEL | ✓ VERIFIED | `osc_bytes_e2e_test.lua` 35/35 PASS; bytes recomputed from `cli.lib.color.build_osc11/build_osc1337` |
| 14 | `wez pane color reset` emits OSC-111 BEL reset; pane title/clear + pane icon emit well-formed OSC-1337 | ✓ VERIFIED | `osc_bytes_e2e_test.lua` asserts `\27]111\7` + base64 user-var bytes; PASS |
| 15 | `wez tab color`/reset + tab icon emit well-formed OSC bytes (tab title shells out, asserted in Plan 02) | ✓ VERIFIED | `osc_bytes_e2e_test.lua` tab assertions PASS; tab color emits NO OSC-11 (correct) |
| 16 | captured bytes compared against the real emitter (color.lua frame + title.resolve_icon glyph), no hardcoded literals | ✓ VERIFIED | `osc_bytes_e2e_test.lua:60-67` `require("cli.lib.color")` + `title.resolve_icon`; recompute, not literals |
| 17 | The 4 archive fixes cherry-picked forward test-first; dev-launcher Lua 5.4 (39aa3b6) lands → `make build` produces working dist/wez; install-cycle (7e72bf5); idempotent uninstall (9d117cd); keys-siblings re-shaped + documented | ✓ VERIFIED | Git history shows test→fix pairs (gbk, gn7, install-cycle/7e72bf5, keys-siblings); `dist/wez version` works; `resolve_install_state_args` in setup.sh; `dist/wez` fallback in uninstall.sh; keys-siblings comment in bootstrap-wezterm.sh:477-488 |
| 18 | `make e2e` AND `make test` both exit 0 after all cherry-picks | ✓ VERIFIED | Both ran → exit 0 (e2e 6/6 PASS, test 34/34 PASS) |

**Score:** 18/18 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `tools/run-e2e.sh` | bash-3.2-safe orchestrator | ✓ VERIFIED | 106 lines; `while IFS= read -r`; `bash -n` OK; wired into `make e2e` |
| `tools/run-tests.sh` | excludes `*_e2e_test.lua` | ✓ VERIFIED | `! -name '*_e2e_test.lua'` + case arm; **CR-01 mapfile removed** (while-read idiom, commit 6d6d77b) |
| `tests/e2e/lib/harness.lua` | shared TAP-ish helpers + WEZ resolver | ✓ VERIFIED | 148 lines; required by all 6 Tier-1 files |
| `tests/e2e/lib/skip.lua` | self-skip gate | ✓ VERIFIED | 70 lines; loud-logged LIVE-ASSERTED contract |
| `tests/e2e/platform.lua` | Mac↔Linux deltas table | ✓ VERIFIED | 114 lines; loads under plain lua5.4 |
| `Makefile` | `make e2e` + `make e2e-setup` | ✓ VERIFIED | `e2e:` → `@./tools/run-e2e.sh`; `e2e-setup:` → `--setup` |
| `tests/e2e/README.md` | tier matrix skeleton | ✓ VERIFIED | 74 lines, substantive (tier matrix, boundary doc, layout) — REVIEW IN-01 (absent) now resolved |
| `tests/e2e/tier1/diagnostics_e2e_test.lua` | version/doctor/keys/completions/scene contracts | ✓ VERIFIED | 199 lines; e2e PASS |
| `tests/e2e/tier1/install_cycle_e2e_test.lua` | seed-scenes/install-state/uninstall/update | ✓ VERIFIED | 316 lines; e2e PASS |
| `tests/e2e/tier1/scene_edge_e2e_test.lua` | unknown/empty/traversal edge exit codes | ✓ VERIFIED | 121 lines; 6/6 PASS |
| `tests/e2e/tier1/osc_bytes_e2e_test.lua` | OSC byte well-formedness | ✓ VERIFIED | 226 lines; 35/35 PASS |
| `tests/e2e/tier1/keys_siblings_e2e_test.lua` | OS-detect + skip-gated keys-siblings | ✓ VERIFIED | 117 lines; xplat 6/6 PASS + macOS layer loud-SKIPs on Linux |
| `tests/e2e/tier1/install_resolver_e2e_test.lua` | post-7e72bf5 resolver contract | ✓ VERIFIED | 179 lines; asserts `resolve_install_state_args` + `dist/wez` fallback; PASS |
| `tools/build.sh` | dev-launcher Lua 5.4 resolution | ✓ VERIFIED | lua5.4 resolution present; `dist/wez version` works |
| `tools/setup.sh` | `resolve_install_state_args` | ✓ VERIFIED | defined line 68, used line 245 |
| `tools/uninstall.sh` | `dist/wez` fallback + idempotency | ✓ VERIFIED | `DIST_WEZ` line 42; binary-absent loud warn line 63 |
| `tools/bootstrap-wezterm.sh` | keys-siblings macOS comment | ✓ VERIFIED | lines 477-488 document sibling-symlink requirement + cross-ref platform.lua |
| `tests/cli/bootstrap_macos_test.lua` | (macOS installer test) | ⚠️ ABSENT — DEFERRED | Intentionally NOT created (D-06.6-01); owned by Phase 7. Not a gap. |

### Key Link Verification

| From | To | Via | Status |
| ---- | -- | --- | ------ |
| Makefile | tools/run-e2e.sh | `@./tools/run-e2e.sh` | ✓ WIRED |
| tools/run-e2e.sh | tests/e2e/**/*_e2e_test.lua | `find tests/e2e \| sort` | ✓ WIRED |
| tools/run-tests.sh | tests/e2e/**/*_e2e_test.lua | discovery EXCLUSION | ✓ WIRED (excluded) |
| Tier-1 files | tests/e2e/lib/harness.lua | `require('harness')` | ✓ WIRED (all 6) |
| diagnostics test | cli/spec.lua | `spec.VERSION` source of truth | ✓ WIRED |
| osc_bytes test | cli/lib/color.lua | recompute build_osc11/build_osc1337 | ✓ WIRED |
| osc_bytes test | cli/lib/title.lua | resolve_icon glyph | ✓ WIRED |
| install_resolver test | tools/setup.sh | `resolve_install_state_args` | ✓ WIRED |
| tools/uninstall.sh | dist/wez | fallback when installed binary absent | ✓ WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| make e2e exits 0 | `make e2e` | exit 0, 6/6 files PASS | ✓ PASS |
| make test exits 0, no e2e run | `make test` | exit 0, 34 files, no e2e tests executed | ✓ PASS |
| wez version | `./dist/wez version` | `wez 0.1.0` exit 0 | ✓ PASS |
| wez doctor | `./dist/wez doctor` | exit 0 | ✓ PASS |
| wez keys --json valid JSON | `./dist/wez keys --json` | valid parseable JSON, exit 0 | ✓ PASS |
| wez completions bash/zsh non-empty | `./dist/wez completions bash\|zsh` | 2666 / 2142 bytes, exit 0 | ✓ PASS |
| platform.lua loads under lua5.4 | `lua5.4 -e dofile(...)` | table OK | ✓ PASS |
| all tooling bash-3.2-safe | `bash -n tools/*.sh` | all OK; no bash-4 constructs (only 1 comment) | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| E2E battery | `make e2e` (`tools/run-e2e.sh`) | exit 0, all 6 file(s) passed | PASS |
| Unit suite | `make test` (`tools/run-tests.sh`) | exit 0, all 34 file(s) passed | PASS |

### Requirements Coverage (PRD-document tags)

| Tag | Source | Description | Status | Evidence |
| --- | ------ | ----------- | ------ | -------- |
| PRD-T1 / A.1 | PRD Tier 1 / Appendix A.1 | Per-subcommand exit-code + output-shape + OSC-byte contracts | ✓ SATISFIED | All Tier 1 subcommands covered (diagnostics/install_cycle/scene_edge/osc_bytes); 6/6 e2e files PASS |
| PRD-A.5 | PRD platform parity matrix | platform-expectations table | ✓ SATISFIED | `tests/e2e/platform.lua` bundle_siblings parity row + Mac↔Linux deltas |
| PRD-Migration | Migration & Reset Strategy | cherry-pick 4 verified fixes test-first | ✓ SATISFIED | 3 fixes landed + verified on Linux; 4th (keys-siblings) re-shaped + deferred (D-06.6-01) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | No TBD/FIXME/XXX debt markers in any phase-modified file | — | — |

The `_TBD (06.9)_` cells in `tests/e2e/README.md` are intentional placeholder rows for future tiers (06.9), not debt in shipped code.

### Deferred Items

| # | Item | Addressed In | Evidence |
| - | ---- | ------------ | -------- |
| 1 | keys-siblings macOS install fix (archive 4c765e1) + full `tests/cli/bootstrap_macos_test.lua` | Phase 7 (D-18/D-06.6-01) | ROADMAP Phase 7 section explicitly carries D-06.6-01; substrate (`install_macos`) is a design-only stub on Linux; re-shaped as OS-detect + skip-gated test that loud-SKIPs on Linux and FIRES on macOS once Phase 7 lands |

### Human Verification Required

None. This phase produces deterministic, headless, runnable code; both `make e2e` and `make test` were executed and exit 0. macOS-only paths are skip-gated (deferred to Phase 7) and cannot be exercised on this Linux mainline by design — that is verified behavior, not a pending human check.

### Gaps Summary

No gaps. All 18 must-have truths verified against the live codebase:

- `make e2e` and `make test` both exit 0; the e2e battery (6 Tier-1 files) is owned exclusively by `make e2e` and excluded from `make test` (belt-and-braces: filename predicate + path case arm).
- The CR-01 BLOCKER (`mapfile` in `tools/run-tests.sh`) is confirmed FIXED — the bash-3.2-safe while-read+array idiom is in place, no `mapfile`/`readarray`/`declare -A` invocation anywhere in tooling (only documentary comments).
- OSC-byte assertions recompute expected bytes from the real emitters (`cli/lib/color.lua` + `cli/lib/title.lua`), not hardcoded literals.
- All 4 archive fixes are accounted for: 3 cherry-picked test-first and Linux-verified; the 4th (keys-siblings macOS) intentionally re-shaped as a skip-gated test and deferred to Phase 7 (D-06.6-01) because its substrate (`install_macos`) does not exist on the Linux mainline — correct and intended, not a gap.
- `tests/e2e/README.md` exists and is substantive (REVIEW IN-01's "absent" finding is now resolved on disk).

The REVIEW.md warnings (WR-01..WR-06) are robustness/consistency nits in test-harness code (relative-vs-absolute WEZ path, fallback hardening, unchecked io.open). None break the phase goal; every test passes on the verified Linux platform today. They are quality hardening, not blockers — out of scope for goal achievement.

---

_Verified: 2026-06-24_
_Verifier: Claude (gsd-verifier)_
