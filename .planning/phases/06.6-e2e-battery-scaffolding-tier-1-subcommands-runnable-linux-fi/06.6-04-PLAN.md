---
phase: 06.6-e2e-battery-scaffolding-tier-1-subcommands-runnable-linux-fi
plan: 04
type: execute
wave: 3
depends_on: ["06.6-02", "06.6-03"]
files_modified:
  - tools/build.sh
  - tools/setup.sh
  - tools/uninstall.sh
  - tools/bootstrap-wezterm.sh
  - tests/cli/build_dev_launcher_test.lua
  - tests/cli/uninstall_idempotent_test.lua
  - tests/cli/setup_sh_test.lua
  - tests/cli/uninstall_test.lua
  - tests/cli/bootstrap_macos_test.lua
  - tests/e2e/tier1/install_resolver_e2e_test.lua
autonomous: true
requirements: [PRD-Migration, PRD-T1]
user_setup: []

must_haves:
  truths:
    - "the 4 independently-verified archive/phase-7-macos fixes are cherry-picked forward onto main, each brought in only AFTER its Tier 1 / unit contract is green (test-first)"
    - "the dev-launcher Lua 5.4 resolution fix (39aa3b6) lands and `make build` produces a working dist/wez on Linux"
    - "the install-cycle fix (7e72bf5) lands: the no-flag `--force`-DEFAULT non-interactive reinstall (exit 0, via setup.sh resolve_install_state_args) + the dist/wez uninstall fallback (exit 0) behave per a Plan-04-owned Tier 1 contract (tests/e2e/tier1/install_resolver_e2e_test.lua) on Linux"
    - "the idempotent-uninstall fix (9d117cd) lands: `make uninstall` is idempotent when the binary is absent, verified on Linux"
    - "the keys-siblings symlink fix (4c765e1) lands; its bootstrap test passes; the macOS-only symlink path is documented as Linux-unverifiable (Phase 7) via a code comment"
    - "`make e2e` and `make test` both exit 0 after all cherry-picks (the Tier 1 battery proves the install/uninstall contracts on the real binary)"
  artifacts:
    - path: "tools/build.sh"
      provides: "dev-launcher Lua 5.4 resolution (39aa3b6 forward)"
      contains: "lua5.4"
    - path: "tools/setup.sh"
      provides: "non-interactive reinstall path via resolve_install_state_args (7e72bf5 forward)"
      contains: "resolve_install_state_args"
    - path: "tools/uninstall.sh"
      provides: "dist/wez fallback + binary-absent idempotency (7e72bf5 + 9d117cd forward)"
      contains: "dist/wez"
    - path: "tools/bootstrap-wezterm.sh"
      provides: "macOS bundle sibling symlinks (4c765e1 forward)"
    - path: "tests/cli/uninstall_idempotent_test.lua"
      provides: "binary-absent + dist/wez fallback regression (dec5d59/2798d6f forward)"
    - path: "tests/e2e/tier1/install_resolver_e2e_test.lua"
      provides: "Plan-04-owned post-7e72bf5 Tier 1 contract: no-flag-DEFAULT reinstall (exit 0) + dist/wez uninstall fallback (exit 0) — the assertions that only become true AFTER the resolver lands"
      contains: "resolve_install_state_args"
  key_links:
    - from: "tools/uninstall.sh"
      to: "dist/wez"
      via: "uninstall fallback when installed binary absent"
      pattern: "dist/wez"
    - from: "tests/cli/build_dev_launcher_test.lua"
      to: "tools/build.sh"
      via: "regression guarding the dev-launcher Lua 5.4 resolution"
      pattern: "lua5"
    - from: "tests/e2e/tier1/install_resolver_e2e_test.lua"
      to: "tools/setup.sh"
      via: "the no-flag-default reinstall contract is enabled by resolve_install_state_args (7e72bf5)"
      pattern: "resolve_install_state_args"
---

<objective>
Execute the Migration & Reset reuse step: cherry-pick the 4 independently-verified fixes
forward from `archive/phase-7-macos` onto main — dev-launcher Lua 5.4 resolution (39aa3b6),
install-cycle non-interactive reinstall + dist/wez uninstall fallback (7e72bf5), idempotent
uninstall when the binary is absent (9d117cd), and macOS bundle sibling symlinks (4c765e1) —
each brought forward only AFTER its Tier 1 / unit contract is green (test-first), and
re-verified on Linux because the archived changes touched cross-platform code never run on
Linux in that form.

Purpose: bring back ONLY the proven fixes (the suspect scene-styling / keybinding-hybrid /
tab-truncation changes are rebuilt test-first in 06.7, NOT reapplied here). The Plan 02/03
Tier 1 battery is the safety net that proves each fix holds on the real binary. This plan also
OWNS the post-resolver Tier 1 assertions (install_resolver_e2e_test.lua) that only become true
after 7e72bf5 lands — the no-flag-DEFAULT reinstall (exit 0) and the dist/wez uninstall
fallback (exit 0) — which Plan 02 deliberately deferred to this wave.

Output: 4 cherry-picks applied (with their tests) + the Plan-04-owned install_resolver_e2e_test
.lua, `make build` produces a working dist/wez, `make e2e` and `make test` exit 0 on Linux.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-e2e-battery-scaffolding-tier-1-subcommands-runnable-linux-fi/06.6-CONTEXT.md
@.planning/phases/06.6-e2e-battery-scaffolding-tier-1-subcommands-runnable-linux-fi/06.6-PATTERNS.md
@docs/prds/e2e-testing-battery-v1.0-prd.md
</context>

<artifacts_this_phase_produces>
NEW symbols (exclude from drift/source-grounding):
- Brought-forward test files (new on main): tests/cli/build_dev_launcher_test.lua,
  tests/cli/uninstall_idempotent_test.lua, tests/cli/setup_sh_test.lua,
  tests/cli/bootstrap_macos_test.lua (and additions to tests/cli/uninstall_test.lua).
- NEW Plan-04-owned Tier 1 file: tests/e2e/tier1/install_resolver_e2e_test.lua (the post-7e72bf5
  no-flag-default reinstall + dist/wez uninstall fallback assertions).
- Modified existing: tools/build.sh, tools/setup.sh, tools/uninstall.sh,
  tools/bootstrap-wezterm.sh.
- Source commits (from origin/archive/phase-7-macos): 39aa3b6 (+59f803e), 7e72bf5 (+dec5d59),
  9d117cd (+2798d6f), 4c765e1 (+959dac0).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick the dev-launcher + install-cycle + idempotent-uninstall fixes (test-first); add the Plan-04-owned post-resolver Tier 1 contract; verify on Linux</name>
  <files>tools/build.sh, tools/setup.sh, tools/uninstall.sh, tests/cli/build_dev_launcher_test.lua, tests/cli/uninstall_idempotent_test.lua, tests/cli/setup_sh_test.lua, tests/cli/uninstall_test.lua, tests/e2e/tier1/install_resolver_e2e_test.lua</files>
  <read_first>
    - tests/e2e/tier1/install_cycle_e2e_test.lua (Plan 02 — the Wave-2 install/uninstall/seed contract this task must keep green; it asserts the explicit-`--force` reinstall + the binary-ABSENT exit-1-warn contract that hold BEFORE 7e72bf5)
    - tests/e2e/lib/harness.lua (Plan 01 — scratch_dir + run_capture_all + WEZ; require it in the new install_resolver file, do NOT re-declare the harness)
    - tools/build.sh (the dev-launcher path 3 that 39aa3b6 fixes — current Linux form on main, read BEFORE cherry-picking so the conflict is resolved against the live form)
    - tools/setup.sh (CURRENT form on main — the install-cycle target 7e72bf5 reshapes; setup.sh's resolve_install_state_args is the resolver that makes the no-flag reinstall default to --force; read it BEFORE cherry-picking so a conflict is resolved against the live form)
    - tools/uninstall.sh (CURRENT 62-line form on main — Phase 6.3 reshaped it: it locates the binary via PATH else `${WEZ_BIN_DIR:-$HOME/.local/bin}/wez`, prints `wez binary not found … already gone` + `exit 1` when absent, and has NO dist/wez fallback yet; 7e72bf5 ADDS the dist/wez fallback. Read this current form BEFORE cherry-picking so the dist/wez-fallback conflict is resolved against the live shape, not a stale one)
    - tests/cli/uninstall_idempotent_test.lua (CURRENT form on main — BOTH 7e72bf5 and 9d117cd touch this file, so read its current assertions BEFORE cherry-picking and reconcile so both the dist/wez-fallback and the binary-absent-idempotency assertions survive the merge)
    - cli/commands/install_state.lua (the DECIDE dispatcher: present + no-flag + no-TTY returns exit 3 today; the resolver in setup.sh injects --force so the no-flag path becomes exit 0)
    - the 4 source commits: `git show 39aa3b6 59f803e 7e72bf5 dec5d59 9d117cd 2798d6f` (read the diffs before applying so a conflict is resolved knowingly, not blindly)
  </read_first>
  <action>
    Bring forward, in dependency order, TEST-FIRST per the project rule (the test/RED commit lands
    or is verified failing-then-passing BEFORE/with its fix):

    (1) dev-launcher Lua 5.4 resolution — `git cherry-pick 59f803e 39aa3b6` (test then fix). This
    is the foundation: a broken dev launcher makes the whole battery untrustworthy. Resolve any
    conflict by reading the diff (build.sh path-3 dev-launcher block) against the current main
    build.sh. After applying, run `make build` and confirm dist/wez is produced AND a smoke
    `WEZ_BIN=./dist/wez ./dist/wez version` exits 0 with the version string — the Linux
    re-verification (the archived change targeted macOS keg-only Lua; confirm it does not regress
    the Linux system-lua path).

    (2) install-cycle non-interactive reinstall + dist/wez uninstall fallback — `git cherry-pick
    dec5d59 7e72bf5` (tests then fix). These touch setup.sh + uninstall.sh + uninstall_test.lua +
    setup_sh_test.lua + uninstall_idempotent_test.lua, and introduce setup.sh's
    resolve_install_state_args resolver (the no-flag reinstall now defaults to --force). Resolve
    conflicts against the CURRENT main setup.sh / uninstall.sh / uninstall_idempotent_test.lua
    (Phase 6.3 reshaped uninstall.sh — all three were read in read_first above). After applying, run
    the Plan 02 install_cycle Tier 1 test (`WEZ_BIN=./dist/wez lua5.4
    tests/e2e/tier1/install_cycle_e2e_test.lua`) and confirm it STILL passes (its explicit-`--force`
    reinstall + binary-absent exit-1-warn contracts must not regress).

    (3) idempotent uninstall when the binary is absent — `git cherry-pick 2798d6f 9d117cd` (test
    then fix). Touches uninstall.sh + uninstall_idempotent_test.lua. NOTE: 7e72bf5 and 9d117cd both
    modify uninstall_idempotent_test.lua — apply in the listed order and reconcile the test file so
    BOTH the dist/wez-fallback and the binary-absent-idempotency assertions survive. After
    applying, run `make test` and confirm uninstall_idempotent_test.lua + uninstall_test.lua +
    setup_sh_test.lua are green, and re-run the Plan 02 install_cycle Tier 1 test — still exit 0.

    (4) AFTER 7e72bf5 has landed, CREATE the Plan-04-owned post-resolver Tier 1 contract
    tests/e2e/tier1/install_resolver_e2e_test.lua (require the Plan 01 harness; scratch HOME /
    scratch WEZTERM_CONFIG_FILE + scratch WEZ_BIN_DIR only, exactly like install_cycle). It asserts
    the TWO contracts Plan 02 deferred to this wave because they cannot hold until the resolver
    ships:
      (a) NO-FLAG-DEFAULT reinstall: in an isolated config (scratch HOME and/or WEZTERM_CONFIG_FILE)
          with a managed wezterm.lua already present, run the non-interactive `wez install-state`
          path THROUGH setup.sh's resolve_install_state_args (no explicit --force, no TTY) -> exit 0
          AND exactly one managed block (the resolver injects --force by default; this is the
          no-op-safe reinstall that returned exit 3 before 7e72bf5).
      (b) dist/wez UNINSTALL FALLBACK: with no `wez` on PATH and none at the scratch WEZ_BIN_DIR but
          a built ./dist/wez present, run uninstall and assert exit 0 via the dist/wez fallback (the
          fallback that 7e72bf5 adds to uninstall.sh — Plan 02 asserts the pre-7e72bf5 exit-1-warn
          contract; THIS file asserts the post-7e72bf5 exit-0 fallback).
    Footer + os.exit. Add the file to discovery so `make e2e` runs it.

    Commit discipline: group as ONE logical "install-cycle reuse" change-set if the conflicts force
    re-authoring, OR keep the original per-fix commits when they cherry-pick cleanly — prefer the
    original commit boundaries (test+fix pairs) when clean; otherwise amend into cohesive
    per-fix commits. The new install_resolver_e2e_test.lua belongs with the 7e72bf5 install-cycle
    change-set (it is that fix's Tier 1 contract). Do NOT collapse all of this into one monolith and
    do NOT leave WIP granular commits.
  </action>
  <verify>
    <automated>make build ; make test ; WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/install_cycle_e2e_test.lua ; WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/install_resolver_e2e_test.lua</automated>
  </verify>
  <acceptance_criteria>
    - `make build` produces `dist/wez`; `./dist/wez version` exits 0 and prints the version string.
    - `make test` exits 0 with build_dev_launcher_test.lua, uninstall_idempotent_test.lua, uninstall_test.lua, setup_sh_test.lua all PASS.
    - `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/install_cycle_e2e_test.lua` exits 0 (the Plan 02 Wave-2 contracts — explicit-`--force` reinstall exit 0, binary-absent exit-1 warn — still hold and did not regress).
    - `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/install_resolver_e2e_test.lua` (the Plan-04-OWNED file) exits 0 and asserts BOTH post-resolver contracts: the no-flag-DEFAULT non-interactive reinstall exits 0 (resolve_install_state_args injects --force) AND the dist/wez uninstall fallback exits 0.
    - The conflict resolution is grounded in the CURRENT main forms read in read_first: setup.sh carries resolve_install_state_args, uninstall.sh carries the dist/wez fallback, and uninstall_idempotent_test.lua keeps BOTH the dist/wez-fallback and the binary-absent-idempotency assertions (grep: all three tokens present).
    - `git log --oneline` shows the cherry-picked fixes as cohesive test+fix commits (not WIP granular, not one monolith); each references the source SHA in its body; install_resolver_e2e_test.lua ships with the 7e72bf5 install-cycle change-set.
    - The suspect changes (scene styling / keybinding hybrid / tab truncation) are NOT present (grep the diff of this plan's commits — only build.sh/setup.sh/uninstall.sh + their tests + the new install_resolver file changed).
  </acceptance_criteria>
  <done>The dev-launcher, install-cycle, and idempotent-uninstall fixes are forward on main with
  their tests; the Plan-04-owned install_resolver_e2e_test.lua asserts the post-resolver no-flag
  -default reinstall (exit 0) + dist/wez uninstall fallback (exit 0); make build + make test + the
  install_cycle and install_resolver Tier 1 tests are all green on Linux.</done>
</task>

<task type="auto">
  <name>Task 2: Cherry-pick the keys-siblings symlink fix (test-first); document the macOS-only path as Linux-unverifiable</name>
  <files>tools/bootstrap-wezterm.sh, tests/cli/bootstrap_macos_test.lua</files>
  <read_first>
    - tools/bootstrap-wezterm.sh (the macOS .app symlink branch 4c765e1 extends — current form on main)
    - the source commits: `git show 959dac0 4c765e1` (the failing test + the sibling-symlink fix)
    - tests/e2e/lib/platform.lua (Plan 01 — the bundle sibling-symlink parity row: wezterm-gui, wezterm-mux-server, strip-ansi-escapes; this fix is the macOS expectation that module encodes)
  </read_first>
  <action>
    `git cherry-pick 959dac0 4c765e1` (test then fix) to bring forward the macOS bundle
    sibling-symlink fix (symlink ALL WezTerm bundle binaries — wezterm-gui, wezterm-mux-server,
    strip-ansi-escapes — so `wez keys` resolves on macOS). Resolve any conflict against the current
    bootstrap-wezterm.sh macOS branch. Run `make test` and confirm bootstrap_macos_test.lua is
    green (the test asserts the symlink LIST via a pure shell-function harness — it does NOT need a
    real macOS bundle, so it runs deterministically on Linux). CRITICAL Linux-verification caveat:
    the actual symlink CREATION runs only in the macOS install branch, which cannot execute on
    Linux — add (or confirm present) a code comment in the bootstrap-wezterm.sh macOS symlink
    branch recording that the runtime macOS path is verified by the unit test's symlink-list
    assertion here, with on-hardware verification deferred to Phase 7 (D-18). Cross-reference the
    Plan 01 platform.lua bundle sibling-symlink parity row so the expectation has a single source.
    Commit as one cohesive test+fix change-set referencing 4c765e1. (The same Phase-7-deferral note
    is ALSO recorded in 06.6-04-SUMMARY.md at plan-close, but the verifiable-during-task artifact is
    the code comment, not the summary.)
  </action>
  <verify>
    <automated>make test ; lua5.4 tests/cli/bootstrap_macos_test.lua  (both exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0; `lua5.4 tests/cli/bootstrap_macos_test.lua` exits 0 (the symlink-list assertion passes deterministically on Linux).
    - `tools/bootstrap-wezterm.sh` symlinks the three bundle siblings (grep: wezterm-gui, wezterm-mux-server, strip-ansi-escapes present in the macOS branch).
    - A CODE COMMENT in the bootstrap-wezterm.sh macOS symlink branch records that the runtime macOS symlink CREATION is Linux-unverifiable and on-hardware-deferred to Phase 7 (D-18); the unit test asserts the list, not the live link (grep: a `Phase 7` / `D-18` comment near the symlink branch). This is the AC verifiable DURING the task.
    - The cherry-pick is one cohesive test+fix change-set referencing 4c765e1.
    - [plan-close] The same Phase-7-deferral note is recorded in 06.6-04-SUMMARY.md — checked at plan-close, not during the task.
  </acceptance_criteria>
  <done>The keys-siblings symlink fix is forward on main with its bootstrap test green on Linux;
  the macOS-only runtime path is documented as on-hardware-deferred (Phase 7) via a code comment in
  bootstrap-wezterm.sh (and again in the SUMMARY at plan-close); make test exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cherry-pick source -> main | the 4 commits come from origin/archive/phase-7-macos, each independently verified this session; reuse list is closed |
| setup.sh/uninstall.sh -> user filesystem | install/uninstall run sudo-free in user paths; the Tier 1 contract exercises them against scratch dirs |
| bootstrap macOS branch | symlink creation runs only on macOS; the Linux unit test asserts the list, never creates real links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-11 | Tampering | cherry-pick scope creep | mitigate | bring forward ONLY the 4 listed commits + their tests; the suspect scene/keybinding/truncation changes are explicitly excluded (rebuilt test-first in 06.7) |
| T-06.6-12 | Elevation of Privilege | setup.sh / uninstall.sh | mitigate | sudo-free; the brought-forward changes do not add privilege; the Tier 1 install_cycle + install_resolver contracts verify behavior on the real binary against scratch dirs |
| T-06.6-13 | Repudiation | unverified-on-Linux archived code | mitigate | each fix re-verified on Linux via make build/test + the Tier 1 install_cycle/install_resolver tests; the macOS-only symlink path is documented as Phase-7-deferred |
| T-06.6-SC | Tampering | npm/pip/cargo installs | accept | no package-manager installs; cherry-picks are git operations + Lua/bash; zero runtime deps |
</threat_model>

<verification>
- `make build` produces a working dist/wez; `./dist/wez version` exits 0.
- `make test` exits 0 (all brought-forward tests + the existing suite green).
- `make e2e` exits 0 (Tier 1 battery green on the real binary, proving the install/uninstall contracts — including the Plan-04-owned install_resolver post-7e72bf5 contracts).
- `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/install_cycle_e2e_test.lua` exits 0 (Plan 02 Wave-2 contracts still hold).
- `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/install_resolver_e2e_test.lua` exits 0 (no-flag-default reinstall exit 0 + dist/wez uninstall fallback exit 0).
- `lua5.4 tests/cli/bootstrap_macos_test.lua` exits 0.
- The diff of this plan's commits touches ONLY build.sh/setup.sh/uninstall.sh/bootstrap-wezterm.sh
  + their tests + the new install_resolver_e2e_test.lua — no scene-styling / keybinding-hybrid /
  tab-truncation changes.
</verification>

<success_criteria>
- All 4 verified fixes are forward on main as cohesive test+fix change-sets, each referencing
  its source SHA.
- Each fix was brought in test-first and re-verified on Linux (make build/test + the Tier 1
  install_cycle/install_resolver tests); the macOS-only symlink runtime path is documented as
  Phase-7-deferred via a code comment (and the SUMMARY at plan-close).
- The Plan-04-owned install_resolver_e2e_test.lua asserts the post-7e72bf5 contracts (no-flag
  -default reinstall exit 0 + dist/wez uninstall fallback exit 0) that Plan 02 deferred to this
  wave; Plan 04's Task 1 acceptance criterion references THIS file, not Plan 02's.
- make e2e + make test both exit 0 — the deterministic battery proves the install/uninstall
  contracts hold on the real binary.
- No suspect (scene/keybinding/truncation) change was reapplied.
</success_criteria>

<output>
Create `.planning/phases/06.6-e2e-battery-scaffolding-tier-1-subcommands-runnable-linux-fi/06.6-04-SUMMARY.md` when done
</output>
