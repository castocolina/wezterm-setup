---
phase: 06.6-e2e-battery-scaffolding-tier-1-subcommands-runnable-linux-fi
plan: 02
type: execute
wave: 2
depends_on: ["06.6-01"]
files_modified:
  - tests/e2e/tier1/diagnostics_e2e_test.lua
  - tests/e2e/tier1/install_cycle_e2e_test.lua
  - tests/e2e/tier1/scene_edge_e2e_test.lua
autonomous: true
requirements: [PRD-T1, PRD-A.1]
user_setup: []

must_haves:
  truths:
    - "`wez version` exits 0 and prints the spec.VERSION string (0.1.0)"
    - "`wez doctor` exits 0 with gate lines present; `wez keys` exits 0 non-empty; `wez keys --json` exits 0 + valid parseable JSON"
    - "`wez completions bash` and `wez completions zsh` exit 0 with a non-empty completion script"
    - "`wez seed-scenes` into a scratch WEZTERM_SETUP_DIR copies the 3 recipes (copy-if-absent; re-run keeps existing)"
    - "edge cases: unknown scene -> exit 1; empty recipes dir -> exit 2; path-traversal -> exit 1; reinstall over an existing block with explicit `wez install-state --force` -> exit 0 (holds today; the no-flag `--force`-DEFAULT is added in Plan 04 via setup.sh resolve_install_state_args and is verified there)"
    - "install-state fresh installs a managed block in an ISOLATED config (WEZTERM_CONFIG_FILE=<scratch>/wezterm.lua and/or a redirected HOME — install_state.lua's config_path honors WEZTERM_CONFIG_FILE or falls back to $HOME, it does NOT read WEZTERM_SETUP_DIR); `wez uninstall` with the binary PRESENT (driven in a scratch HOME) removes the managed block/config/binary and exits 0; uninstall is idempotent when the binary is ABSENT — today's contract is exit 1 with the loud 'wez binary not found … already gone' warn (the dist/wez fallback that turns binary-absent into exit 0 ships in Plan 04 via cherry-pick 7e72bf5 and is verified THERE, not at this wave); `wez update`'s pure comparator decide_wez_update returns 'current' (no published release / already-latest) and 'system-skip' (P6-D09) OFFLINE; a live `wez update` smoke is reachability-gated (soft_skip when offline) — never an ungated network call"
  artifacts:
    - path: "tests/e2e/tier1/diagnostics_e2e_test.lua"
      provides: "version/doctor/keys/keys --json/completions bash|zsh + scene new spec-echo contracts"
      contains: "require"
    - path: "tests/e2e/tier1/install_cycle_e2e_test.lua"
      provides: "seed-scenes copy-if-absent + install-state(fresh + explicit --force reinstall)/uninstall(binary-present exit 0 + binary-absent exit-1 warn)/update non-interactive contracts; the dist/wez uninstall fallback (exit 0) + no-flag-default reinstall are Plan 04 contracts (post-7e72bf5), asserted there"
      contains: "scratch_dir"
    - path: "tests/e2e/tier1/scene_edge_e2e_test.lua"
      provides: "unknown(1)/empty(2)/traversal(1) scene-launch edge exit-code contracts (reinstall --force exit 0 lives in install_cycle per checker H-1)"
      contains: "WEZTERM_SETUP_DIR"
  key_links:
    - from: "tests/e2e/tier1/*_e2e_test.lua"
      to: "tests/e2e/lib/harness.lua"
      via: "require('harness') for check/run_capture/scratch_dir/WEZ"
      pattern: "require%(.harness"
    - from: "tests/e2e/tier1/diagnostics_e2e_test.lua"
      to: "cli/spec.lua"
      via: "spec.VERSION + SUBCOMMANDS enumeration as source of truth"
      pattern: "VERSION"
    - from: "tests/e2e/tier1/scene_edge_e2e_test.lua"
      to: "cli.commands.scene.run_launch"
      via: "child lua5.4 -e with WEZTERM_SETUP_DIR set (real exit code)"
      pattern: "lua5%.4 %-e"
---

<objective>
Build the deterministic Tier 1 exit-code + output-shape contract tests for every `wez`
subcommand per PRD Appendix A.1, EXCEPT the OSC-byte assertions (Plan 03 owns those). Three
test files, each requiring the Plan 01 harness: diagnostics (version/doctor/keys/completions +
scene-new spec echo), install cycle (seed-scenes/install-state/uninstall/update), and scene
edge cases (unknown/empty/traversal/reinstall).

Purpose: lock the exit-code + output-shape contract for the whole subcommand surface so the
next N runtime commits cannot silently regress it. Enumerate against cli/spec.lua SUBCOMMANDS
so a newly added subcommand is detectable as untested.

Output: tests/e2e/tier1/diagnostics_e2e_test.lua, install_cycle_e2e_test.lua,
scene_edge_e2e_test.lua — all green under `make e2e`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.6-e2e-battery-scaffolding-tier-1-subcommands-runnable-linux-fi/06.6-CONTEXT.md
@.planning/phases/06.6-e2e-battery-scaffolding-tier-1-subcommands-runnable-linux-fi/06.6-PATTERNS.md
@docs/prds/e2e-testing-battery-v1.0-prd.md
@cli/spec.lua
</context>

<artifacts_this_phase_produces>
NEW symbols (exclude from drift/source-grounding):
- Files: tests/e2e/tier1/diagnostics_e2e_test.lua, tests/e2e/tier1/install_cycle_e2e_test.lua,
  tests/e2e/tier1/scene_edge_e2e_test.lua.
- Consumes Plan 01: harness.check/run_capture/run_capture_all/shquote/scratch_dir/WEZ/footer/new.
- Consumes existing: cli/spec.lua SUBCOMMANDS + M.VERSION; cli.commands.scene.run_launch +
  cli.commands.scene.run_new; cli.commands.update.decide_wez_update.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Diagnostics + completions + scene-new contract tests</name>
  <files>tests/e2e/tier1/diagnostics_e2e_test.lua</files>
  <read_first>
    - tests/e2e/lib/harness.lua (Plan 01 — require it; do NOT re-declare check/run_capture)
    - tests/integration/keys_integration_test.lua (144-147 exit+shape assertion; 256-276 dkjson valid-JSON + optional jq cross-check)
    - cli/spec.lua (35 M.VERSION="0.1.0"; 56-70 SUBCOMMANDS closed allow-list — enumerate against this)
    - cli/commands/version.lua (prints `wez <VERSION>`)
    - cli/commands/scene.lua (run_new IS public, returns M; `run_new(args)` reads `args.pane or args.panes` = a list of raw --pane spec strings; Step-0 VALIDATE-BEFORE-EMIT block at 249-278: zero panes (#raw_panes == 0) -> stderr `error: wez scene new requires at least one --pane (got 0)` + exit 2; a bad args.layout -> scenelib.validate_layout fails -> exit 2; ALL mux spawn/split-pane runs textually AFTER this block, so a child lua5.4 -e hitting these paths never materializes a mux — this is the no-mux contract the test asserts, mirroring how scene_edge cites run_launch)
  </read_first>
  <action>
    Create tests/e2e/tier1/diagnostics_e2e_test.lua. Header: the package.path bootstrap with
    repo_root = this_dir.."/../../.." (THREE `..` — file is at tests/e2e/tier1/, three levels below
    repo root) plus tests/e2e/lib/?.lua and cli/vendor/?.lua; `require("harness")`, bind check/
    run_capture/run_capture_all/shquote/WEZ. Assert per A.1 row using run_capture_all(shquote(WEZ)
    .. " <subcmd>"): `wez version` -> exit 0 AND output contains the spec.VERSION string (require
    cli.spec, compare against M.VERSION "0.1.0", not a hardcoded literal — so a version bump cannot
    silently break the test); `wez doctor` -> exit 0 AND output contains gate lines (assert a known
    gate token such as `[PASS]`/`[FAIL]` or the gate-name prose the doctor prints); `wez keys` ->
    exit 0 AND non-empty; `wez keys --json` -> exit 0 AND `dkjson.decode` returns a table (pcall
    require dkjson), with an optional `command -v jq` cross-check piping the JSON through `jq .`
    only when jq is present (soft_skip otherwise); `wez completions bash` and `wez completions zsh`
    -> exit 0 AND non-empty script (assert a known token like `complete -F` / `#compdef`). Add a
    `wez scene new` NO-MUX assertion. NOTE (verified against cli/commands/scene.lua run_new): there
    is NO pure spec-echo happy path — after the Step-0 validate-before-emit block, run_new does live
    mux materialization (spawn/split-pane), so the only deterministic, headless contract for `scene
    new` is the validate-before-emit / usage path that returns BEFORE any mux call. CONTEXT
    CORRECTION (traceability): this supersedes CONTEXT.md line 66 / PRD A.1's "`wez scene new …` ->
    emits the expected recipe/spec text (no mux needed)" — the shipped run_new has no such pure
    spec-echo branch, so the headless Tier 1 contract is the error/usage path below, NOT a recipe-text
    echo. (gsd-plan-checker confirmed this as a correct source-grounded scope correction.) Assert that path
    via a child `lua5.4 -e` that requires cli.commands.scene and `os.exit(s.run_new{...})` (the same
    child-lua idiom scene_edge uses for run_launch, so no mux runs): zero `--pane` (pane={}) -> exit
    2 AND stderr contains the exact copy `error: wez scene new requires at least one --pane (got 0)`;
    an unknown `--layout` (e.g. layout="bogus", pane={"sh"}) -> exit 2 AND the scenelib layout-error
    copy. Do NOT assert the mux happy-path materialization here — that is Tier 2 / 06.7. Also add an
    enumeration guard: require cli.spec, iterate
    SUBCOMMANDS, and check() that every non-hidden, non-mux subcommand referenced here is present
    in the allow-list (so a renamed/removed subcommand fails loudly). Footer: print passed/failed
    and os.exit(failed == 0 and 0 or 1).
  </action>
  <verify>
    <automated>WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/diagnostics_e2e_test.lua  # exits 0 (all ok lines, no FAIL)</automated>
  </verify>
  <acceptance_criteria>
    - `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/diagnostics_e2e_test.lua` prints only `  ok` lines and exits 0.
    - The version assertion compares against `require("cli.spec").VERSION`, not a hardcoded `0.1.0` literal (grep: the test contains `spec.VERSION` or `require("cli.spec")`).
    - `wez keys --json` is decoded by `dkjson.decode` to a table; the jq cross-check is gated behind `command -v jq` (soft_skip when absent).
    - Both `completions bash` and `completions zsh` assertions are present and assert a non-empty script token.
    - The `scene new` no-mux contract is asserted via a child `lua5.4 -e` calling `s.run_new{pane={}}` (exit 2 + exact `at least one --pane (got 0)` copy) and `s.run_new{layout="bogus", pane={"sh"}}` (exit 2 + layout-error copy); NO mux spawn/split-pane is driven (grep: `run_new` reached via `lua5.4 -e`, not the dev-launcher subprocess).
    - The file requires `harness` and does NOT re-declare `check`/`run_capture` locally (grep: `require("harness")` present; no second `local function check` definition).
  </acceptance_criteria>
  <done>diagnostics_e2e_test.lua asserts exit 0 + output shape for version/doctor/keys/keys
  --json/completions bash|zsh + the no-mux scene-new validate-before-emit contract (zero --pane
  and bad layout each -> exit 2 with exact UI-SPEC copy, via child lua5.4 -e calling run_new) + the
  SUBCOMMANDS enumeration guard, all green under WEZ_BIN=./dist/wez.</done>
</task>

<task type="auto">
  <name>Task 2: Install-cycle (seed-scenes/install-state/uninstall/update) + scene edge-case exit-code contracts</name>
  <files>tests/e2e/tier1/install_cycle_e2e_test.lua, tests/e2e/tier1/scene_edge_e2e_test.lua</files>
  <read_first>
    - tests/e2e/lib/harness.lua (Plan 01 — scratch_dir + run_capture_all + WEZ)
    - tests/cli/seed_scenes_test.lua (105-109 scratch dir; copy-if-absent / keep-existing contract + the child-lua5.4 env idiom)
    - tests/cli/scene_launch_test.lua (58-90 the child `lua5.4 -e` calling s.run_launch{name=…} with WEZTERM_SETUP_DIR set — the ONLY way to get the REAL exit code for env-dependent paths; exit 2 no-name empty dir, exit 1 unknown, exit 1 traversal)
    - cli/commands/install_state.lua (config_path() at 369-374 honors WEZTERM_CONFIG_FILE, ELSE falls back to $HOME/.config/wezterm/wezterm.lua — it does NOT read WEZTERM_SETUP_DIR; the DECIDE dispatcher: present block + no-flag + no-TTY returns exit 3 today (D-03, never silently overwrite) — so isolate install-state by setting WEZTERM_CONFIG_FILE to a scratch wezterm.lua and/or redirecting HOME, NEVER by WEZTERM_SETUP_DIR)
    - cli/commands/update.lua (decide_wez_update at 85: `M.decide_wez_update(have_version, latest_version, install_kind)` -> "current"|"update"|"system-skip"; install_kind=="system" -> "system-skip" FIRST (P6-D09); empty/nil latest_version -> "current"; the pure OFFLINE comparator the test drives)
    - tools/uninstall.sh (CURRENT 62-line form — binary located via PATH else `${WEZ_BIN_DIR:-$HOME/.local/bin}/wez`; confirm the binary-ABSENT contract: when `wez` is not on PATH and not at that bin dir, it prints the two `[uninstall] ERROR:` lines including `wez binary not found … already gone` and `exit 1`; there is NO dist/wez fallback yet — that ships in Plan 04 via 7e72bf5. Assert THIS exit-1-with-warn contract, not a fabricated exit 0. WEZ_BIN_DIR is the correct scratch seam for the binary location)
    - scenes/ (ai.toml, dev.toml, docker.toml — the 3 recipes seed-scenes copies)
  </read_first>
  <action>
    Create tests/e2e/tier1/install_cycle_e2e_test.lua (require harness). seed-scenes: make a
    scratch WEZTERM_SETUP_DIR via scratch_dir, run `WEZTERM_SETUP_DIR=<dir> WEZ_SEED_SRC_DIR=
    <repo>/scenes <WEZ> seed-scenes` (env-prefixed run_capture_all), assert exit 0 AND the 3
    recipes (ai/dev/docker) now exist under <dir>/scenes (check files present), then run AGAIN and
    assert exit 0 AND the existing files are byte-unchanged (copy-if-absent keeps existing — read a
    file's bytes before/after). install-state: ISOLATE the config target so the user's real
    ~/.config/wezterm/wezterm.lua is NEVER touched — set `WEZTERM_CONFIG_FILE=<scratch>/wezterm.lua`
    (the seam install_state.lua's config_path() honors) and/or redirect `HOME` to a scratch dir
    (config_path() falls back to $HOME when WEZTERM_CONFIG_FILE is unset); do NOT use
    WEZTERM_SETUP_DIR for install-state isolation — config_path() ignores it. Seed the scratch
    wezterm.lua, run install-state fresh -> exit 0 AND a managed sentinel block present; run again
    with EXPLICIT `wez install-state --force` -> exit 0, exactly one block. IMPORTANT
    (cli/commands/install_state.lua DECIDE + checker H-1): a present block with NO TTY and NO flag
    returns NON-ZERO today (exit 3 — D-03, never silently overwrite); the no-flag `--force`-DEFAULT
    comes from setup.sh resolve_install_state_args, which only lands in Plan 04 (Wave 3). At THIS
    wave assert ONLY the explicit-`--force` reinstall (exit 0); do NOT assert a bare no-flag
    reinstall exits 0 here — that contract is verified in Plan 04 after the resolver lands.
    uninstall: drive a scratch HOME (and point WEZ_BIN_DIR at a scratch bin) so the user's real
    config + installed binary are never touched, and assert TWO rows of PRD A.1. (a) binary PRESENT:
    stage a managed wezterm.lua (via WEZTERM_CONFIG_FILE / scratch HOME) + a dummy `wez` binary at
    the scratch WEZ_BIN_DIR, run uninstall, assert exit 0 AND the managed block/config/binary are
    removed. (b) binary ABSENT: with no `wez` on PATH and none at the scratch WEZ_BIN_DIR, assert
    TODAY'S contract — exit 1 AND the loud `wez binary not found … already gone` warn (this is the
    idempotent-when-already-gone signal at this wave; the dist/wez fallback that turns binary-absent
    into exit 0 lands in Plan 04 via 7e72bf5 and is asserted THERE). Do NOT assert a dist/wez-fallback
    exit 0 here — it cannot hold until 7e72bf5 ships in Plan 04. WAVE-CONTRACT NOTE (traceability):
    PRD Appendix A.1 line 284 documents the END-STATE `uninstall (binary absent, no dist/wez) ->
    exit 0` — that is the post-cherry-pick contract; this Wave-2 test deliberately asserts the
    CURRENT pre-7e72bf5 exit-1 behavior, and Plan 04 Task 1 flips it to the PRD's exit-0 once the
    fallback lands. Staged contract, not a PRD contradiction.
    update: do NOT make
    an ungated live network call (hang + CI-flake risk against the <60s/no-hang contract). Assert the
    PURE comparator OFFLINE via a child `lua5.4 -e` requiring cli.commands.update:
    `M.decide_wez_update(M.VERSION, "", "user") == "current"` (no published release / already-latest
    no-op) AND `M.decide_wez_update(M.VERSION, "v9.9.9", "system") == "system-skip"` (P6-D09 system
    guard wins first). THEN an OPTIONAL live `wez update` smoke gated behind a bounded reachability
    probe (`curl -fsS --max-time 3 https://api.github.com >/dev/null 2>&1`): only on probe success,
    run `wez update`, assert exit 0 AND the exact run() no-op/up-to-date message (read update.lua
    run() and pin the literal string); on probe failure soft_skip LOUDLY with a logged reason (never
    a silent pass, never a hang). Footer + os.exit.

    Create tests/e2e/tier1/scene_edge_e2e_test.lua (require harness). Use the child `lua5.4 -e`
    form (NOT the dev-launcher subprocess) to get the REAL exit code, exactly as
    scene_launch_test.lua run_launch_child: build a body that sets package.path, requires
    cli.commands.scene, and os.exit(s.run_launch{name=…}); invoke via `WEZTERM_SETUP_DIR=<dir>
    lua5.4 -e <body>`. Assert: unknown scene name (recipes present) -> exit 1; empty recipes dir
    (no name) -> exit 2; path-traversal `../../etc/passwd` -> exit 1 (the invalid-name guard). The
    reinstall-over-existing-block edge is asserted in install_cycle (Task-1's file) with EXPLICIT
    `wez install-state --force` -> exit 0 (per checker H-1 the no-flag default is a Plan-04 contract,
    NOT verifiable at this wave) — do not duplicate a bare no-flag reinstall here. Each scene_edge
    assertion checks the exact exit code AND the UI-SPEC error copy
    (`invalid scene name`, the no-recipes guidance, etc.). Footer + os.exit.
  </action>
  <verify>
    <automated>WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/install_cycle_e2e_test.lua ; lua5.4 tests/e2e/tier1/scene_edge_e2e_test.lua</automated>
  </verify>
  <acceptance_criteria>
    - `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/install_cycle_e2e_test.lua` prints only `  ok` lines and exits 0.
    - `lua5.4 tests/e2e/tier1/scene_edge_e2e_test.lua` prints only `  ok` lines and exits 0.
    - seed-scenes test asserts BOTH first-run-copies-3 AND re-run-keeps-existing-byte-identical.
    - install-state isolation uses `WEZTERM_CONFIG_FILE=<scratch>/wezterm.lua` and/or a redirected scratch `HOME` (the seams config_path() honors), NOT `WEZTERM_SETUP_DIR` (grep: `WEZTERM_CONFIG_FILE` present in the install-state block; the install-state path does not rely on WEZTERM_SETUP_DIR for the config target).
    - `wez uninstall` with the binary PRESENT (driven in a scratch HOME + scratch WEZ_BIN_DIR) exits 0 AND removes the managed binary (PRD A.1 mandatory row); the test stages the dummy binary at the scratch WEZ_BIN_DIR + managed config under the scratch HOME and asserts the binary is gone after the run.
    - `wez uninstall` with the binary ABSENT (no `wez` on PATH and none at the scratch WEZ_BIN_DIR) asserts TODAY'S contract: exit 1 AND the loud `wez binary not found … already gone` warn (NOT a fabricated exit 0; the dist/wez-fallback exit 0 is a Plan-04 contract verified after 7e72bf5).
    - The explicit-`--force` reinstall asserts exit 0; NO bare no-flag reinstall is asserted to exit 0 here (that no-flag-default contract is Plan 04's, post-resolver).
    - The edge-case exit codes are captured via a child `lua5.4 -e` (grep: `lua5.4 -e` present), NOT via a piped shell `$?` (which the dev launcher's exec would mask through head/od pipes).
    - Unknown=1, empty=2, traversal=1 are each asserted with the exact code AND the matching UI-SPEC error copy.
    - Tests write only under scratch dirs / scratch WEZTERM_CONFIG_FILE / scratch HOME / scratch WEZ_BIN_DIR (grep: no write to `$HOME/.config` without a scratch HOME or WEZTERM_CONFIG_FILE override).
  </acceptance_criteria>
  <done>install_cycle + scene_edge tests assert the seed-scenes copy-if-absent contract, the
  install-state(fresh + explicit-`--force` reinstall via WEZTERM_CONFIG_FILE/scratch-HOME isolation)/
  uninstall(binary-present exit 0 + binary-absent exit-1 warn, via scratch WEZ_BIN_DIR)/update
  non-interactive contracts, and the unknown(1)/empty(2)/traversal(1)/reinstall-force(0) edge exit
  codes, all green and touching only scratch dirs. The dist/wez uninstall fallback (exit 0) + the
  no-flag-default reinstall are Plan 04 contracts, verified there after 7e72bf5 lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> scratch config target | seed-scenes runs against a scratch WEZTERM_SETUP_DIR; install-state runs against a scratch WEZTERM_CONFIG_FILE and/or redirected HOME (the seams config_path honors) — never the user's real ~/.config/wezterm |
| test -> wez subprocess | exit codes captured via io.popen + p:close (real child code), or child lua5.4 -e for env-dependent paths |
| test -> scratch wez binary | uninstall is driven with WEZ_BIN_DIR pointed at a scratch bin, so the real installed binary is never removed |
| scene_edge traversal input | `../../etc/passwd` is asserted to be REJECTED (exit 1) — it is a guard test, not a vuln being introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.6-05 | Tampering | path-traversal edge case | mitigate | the test ASSERTS the existing guard rejects `../../etc/passwd` (exit 1); no file outside scratch is read or written |
| T-06.6-06 | Tampering | install-state / uninstall in tests | mitigate | scratch WEZTERM_CONFIG_FILE / redirected HOME (config target) + scratch WEZ_BIN_DIR (binary) only; never mutate the real wezterm.lua or installed binary |
| T-06.6-07 | Repudiation | update network call | mitigate | primary assertion is the OFFLINE pure comparator (decide_wez_update); the live `wez update` smoke is bounded-reachability-gated (curl --max-time 3) with a LOUD soft_skip, so an offline box neither hangs nor flakes |
| T-06.6-08 | Information Disclosure | committed test fixtures | mitigate | no secrets in fixtures; seed recipes are the existing committed scenes/*.toml |
</threat_model>

<verification>
- `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/diagnostics_e2e_test.lua` exits 0, only ok lines.
- `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier1/install_cycle_e2e_test.lua` exits 0, only ok lines.
- `lua5.4 tests/e2e/tier1/scene_edge_e2e_test.lua` exits 0, only ok lines.
- `make e2e` discovers and runs all three files and exits 0.
- Every A.1 non-OSC row reachable at this wave is covered: version/doctor/keys/keys --json/
  completions bash|zsh/seed-scenes/scene new/install-state(fresh + explicit --force reinstall)/
  uninstall(binary-present exit 0 + binary-absent exit-1 warn)/update + the 3 scene edge cases.
  The dist/wez uninstall fallback (exit 0) + the no-flag-default reinstall are deferred to Plan 04
  (post-7e72bf5) and verified there.
- install-state isolation uses WEZTERM_CONFIG_FILE / scratch HOME (config_path's real seams), never WEZTERM_SETUP_DIR.
</verification>

<success_criteria>
- All three Tier 1 contract files pass under `make e2e` on Linux with only `ok` lines.
- Exit codes are captured via the repo's io.popen+p:close idiom (or child lua5.4 -e for
  env-dependent paths), never a masked pipe `$?`.
- Tests require the Plan 01 harness rather than re-declaring it.
- install-state is isolated through WEZTERM_CONFIG_FILE and/or a redirected HOME (never
  WEZTERM_SETUP_DIR), and uninstall through a scratch WEZ_BIN_DIR — the user's real config and
  binary are untouched.
- Coverage maps to the PRD Appendix A.1 non-OSC rows reachable at Wave 2; the SUBCOMMANDS
  enumeration guard fires if a subcommand is renamed/removed. The two post-7e72bf5 rows
  (dist/wez uninstall fallback exit 0, no-flag-default reinstall exit 0) are Plan 04's contracts.
</success_criteria>

<output>
Create `.planning/phases/06.6-e2e-battery-scaffolding-tier-1-subcommands-runnable-linux-fi/06.6-02-SUMMARY.md` when done
</output>
