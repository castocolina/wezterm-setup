---
phase: 07-macos-parity
plan: 03
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - tools/ci-setup-toolchain.sh
  - tools/build.sh
  - .github/workflows/release.yml
  - tests/cli/ci_macos_toolchain_test.lua
autonomous: true
requirements: [INST-07]
must_haves:
  truths:
    - "ci-setup-toolchain.sh install_macos installs lua@5.4 (keg-only), NOT bare lua (5.5), and exposes the keg bin on $GITHUB_PATH"
    - "build.sh ad-hoc codesigns dist/wez on BOTH macOS arches at build time and records a codesign --verify evidence line (never gating on spctl)"
    - "release.yml builds darwin-x86_64 (macos-15-intel) AND darwin-aarch64 (macos-14) plus linux-x86_64, with an arm64 in-build smoke; macos-13 never named"
    - "a workflow_dispatch dry-run path builds + codesigns + uploads the macOS legs WITHOUT cutting a real v* tag"
  artifacts:
    - path: "tools/ci-setup-toolchain.sh"
      provides: "fixed macOS toolchain provisioning (lua@5.4 + keg PATH)"
      contains: "lua@5.4"
    - path: "tools/build.sh"
      provides: "build-time ad-hoc codesign for both macOS arches + verify evidence"
      contains: "codesign --force --sign -"
    - path: ".github/workflows/release.yml"
      provides: "3-leg strategy.matrix (ubuntu + macos-15-intel + macos-14) with arm64 smoke"
      contains: "macos-15-intel"
    - path: "tests/cli/ci_macos_toolchain_test.lua"
      provides: "TEXT gate over the lua@5.4 fix + codesign + matrix labels"
  key_links:
    - from: "tools/ci-setup-toolchain.sh install_macos"
      to: "brew --prefix lua@5.4 / $GITHUB_PATH"
      via: "keg-only PATH persistence"
      pattern: "brew --prefix lua@5\\.4"
    - from: "tools/build.sh build_with_luastatic"
      to: "codesign --force --sign -"
      via: "post-chmod macOS branch"
      pattern: "codesign --force --sign -"
    - from: ".github/workflows/release.yml"
      to: "macos-15-intel / macos-14"
      via: "strategy.matrix runner labels"
      pattern: "macos-15-intel"
---

<objective>
Restore and extend the macOS CI legs so the entire macOS build — both darwin-x86_64 and darwin-aarch64 — is produced, ad-hoc-codesigned, and uploaded with `.sha256` from CI (CONTEXT D-01 arm64-via-CI, D-06 codesign). Three correlated fixes: (1) fix the latent CI bug in ci-setup-toolchain.sh install_macos (`brew install lua` is now 5.5 — must pin `lua@5.4`, keg-only, wired onto `$GITHUB_PATH`); (2) add build-time ad-hoc codesign to tools/build.sh for BOTH macOS arches with a `codesign --verify` evidence line (never gate on `spctl --assess` — it always rejects ad-hoc); (3) re-introduce `strategy.matrix` in release.yml over `ubuntu-latest`/`macos-15-intel`/`macos-14` (NEVER `macos-13`), add a free arm64 in-build smoke (`codesign --verify` + `./dist/wez version` on the macos-14 runner), and provide a `workflow_dispatch` dry-run path so the agent can prove the macOS legs build+codesign+upload WITHOUT cutting a real `v*` tag.

This wires existing verified primitives (the publish.sh arm64 codesign, the install_linux CI step shape, the platform.sh asset naming) into a macOS matrix — no net-new architecture.

Purpose: deliver the arm64 + Intel macOS assets via CI/CD (the supply side INST-07/INST-08 ride on) so Plan 04 can fetch + verify them.
Output: fixed ci-setup-toolchain.sh, codesigning build.sh, 3-leg release.yml with dispatch dry-run, and a TEXT gate test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-macos-parity/07-CONTEXT.md
@.planning/phases/07-macos-parity/07-RESEARCH.md
@.planning/phases/07-macos-parity/07-PATTERNS.md
@tools/ci-setup-toolchain.sh
@tools/build.sh
@tools/publish.sh
@.github/workflows/release.yml
</context>

<artifacts_this_phase_produces>
- `tools/ci-setup-toolchain.sh install_macos()` — `brew install lua@5.4 luarocks` + `luarocks install luastatic` + `export PATH="$(brew --prefix lua@5.4)/bin:$PATH"` + `echo "$(brew --prefix lua@5.4)/bin" >> "$GITHUB_PATH"`; keeps assert_and_capture fail-loud gate.
- `tools/build.sh build_with_luastatic()` — post-`chmod +x`, when `platform_os`=macos: `codesign --force --sign - "${OUT}"` (both arches) + `codesign --verify --verbose "${OUT}"` evidence line. No `spctl` gate.
- `.github/workflows/release.yml` — `strategy.matrix` with `{runner: ubuntu-latest}`, `{runner: macos-15-intel}`, `{runner: macos-14}`; arm64-only in-build smoke step; prune kept as its own nightly-only step; `workflow_dispatch` already present → dry-run nightly path.
- `tests/cli/ci_macos_toolchain_test.lua` — new auto-discovered TEXT gate.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fix ci-setup-toolchain.sh macOS lua@5.4 + add build-time codesign to build.sh</name>
  <files>tools/ci-setup-toolchain.sh, tools/build.sh</files>
  <read_first>
    - tools/ci-setup-toolchain.sh (the latent-bug install_macos() lines 61-66 = `brew install lua luarocks`; the install_linux() analog lines 41-59 with `--local` PATH + `$GITHUB_PATH` echo; the assert_and_capture() gate lines 70-94)
    - tools/build.sh (build_with_luastatic() lines 101-158, the `chmod +x "${OUT}"` at line 156 — the codesign insertion point; have_luastatic() lines 92-96; the platform.sh source line 65)
    - tools/publish.sh (the existing arm64-only codesign lines 54-59 — the analog to lift + widen to both arches)
    - .planning/phases/07-macos-parity/07-RESEARCH.md (Pattern 2 keg-only path; Pattern 3 build-time codesign + the spctl NOTE; Pitfall 1/2/4)
    - .planning/phases/07-macos-parity/07-PATTERNS.md (§"ci-setup-toolchain.sh install_macos() lua@5.4 fix"; §"build-time ad-hoc codesign")
  </read_first>
  <action>
    In tools/ci-setup-toolchain.sh, rewrite install_macos(): replace `brew install lua luarocks` with `brew install lua@5.4 luarocks` (NEVER bare `lua` — Pitfall 1, it is 5.5), keep `luarocks install luastatic`, then resolve `PREFIX="$(brew --prefix lua@5.4)"`, `export PATH="${PREFIX}/bin:${PATH}"` (keg-only is not auto-on-PATH — Pitfall 2), and when `GITHUB_PATH` is set, `printf '%s\n' "${PREFIX}/bin" >> "${GITHUB_PATH}"` so the subsequent build step finds `lua5.4`. Leave assert_and_capture() unchanged — it already fails loud if `lua5.4`/`luastatic` are absent. If the macOS leg surfaces a luastatic link error for keg-only headers, the link flags `${PREFIX}/lib/liblua5.4.a -I${PREFIX}/include/lua5.4` are the documented fix (note in a comment; build.sh's pkg-config probe may already find them). In tools/build.sh build_with_luastatic(), AFTER the `chmod +x "${OUT}"` line, add a macOS-guarded codesign block: `if [ "$(platform_os)" = "macos" ]; then codesign --force --sign - "${OUT}"; codesign --verify --verbose "${OUT}"; fi` — sign BOTH arches (D-06, uniform; arm64 is the one that SIGKILLs unsigned, signing x86_64 too is harmless). Add a comment that `spctl --assess` is intentionally NOT used (it always rejects ad-hoc; the evidence is codesign --verify success — Pitfall 4). English only.
  </action>
  <verify>
    <automated>bash -n tools/ci-setup-toolchain.sh && bash -n tools/build.sh && bash tools/run-tests.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -nE 'brew install lua ' tools/ci-setup-toolchain.sh | grep -v '^[[:space:]]*#'` returns NOTHING (no bare `lua`); `grep -c 'lua@5.4' tools/ci-setup-toolchain.sh` >= 2.
    - SOURCE: `grep -c 'GITHUB_PATH' tools/ci-setup-toolchain.sh` >= 1 (keg bin persisted onto the job PATH).
    - SOURCE: `grep -c 'codesign --force --sign -' tools/build.sh` >= 1 AND `grep -c 'codesign --verify' tools/build.sh` >= 1, both inside a `platform_os" = "macos` guard.
    - SOURCE: `grep -nE 'spctl' tools/build.sh | grep -v '^[[:space:]]*#'` returns NOTHING (never gate the build on spctl).
    - BEHAVIOR: `bash -n tools/ci-setup-toolchain.sh` and `bash -n tools/build.sh` exit 0; `bash tools/run-tests.sh` exits 0 (the bash -n + shellcheck gate inside the suite covers both scripts).
  </acceptance_criteria>
  <done>ci-setup-toolchain.sh installs lua@5.4 with keg PATH; build.sh codesigns both macOS arches at build with a verify evidence line, no spctl gate; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-introduce the 3-leg matrix in release.yml + arm64 smoke + dispatch dry-run + TEXT gate test</name>
  <files>.github/workflows/release.yml, tests/cli/ci_macos_toolchain_test.lua</files>
  <read_first>
    - .github/workflows/release.yml (the whole file — the single hardcoded `build` job lines 51-229; the deferred-matrix comment lines 45-50; the `workflow_dispatch` + schedule triggers lines 24-38; the Resolve-channel D-07 step lines 71-82; the ci-setup-toolchain + build.sh steps lines 116-120; the Name+checksum step lines 133-144 with the `platform_os` asset naming + the `wez-macos-<arch>` note lines 129-132; the Ensure-release + Upload steps lines 161-193; the Prune step lines 204-228 with the `mapfile` Linux-only note)
    - .planning/phases/07-macos-parity/07-RESEARCH.md (Pitfall 7 macos-13 removed → macos-15-intel + macos-14; Open Q3 arm64 in-build smoke recommendation; A2 — workflow_dispatch dry-run before the real tag)
    - .planning/phases/07-macos-parity/07-PATTERNS.md (§"macOS legs in release.yml" — restore strategy.matrix, keep prune as its own step, add the arm64 smoke)
    - tests/cli/publish_test.lua (a TEXT-gate test analog for a tools script — shape for ci_macos_toolchain_test.lua)
  </read_first>
  <action>
    Add `strategy.matrix` to the `build` job over three runner labels: `ubuntu-latest` (linux-x86_64), `macos-15-intel` (darwin-x86_64), `macos-14` (darwin-aarch64). NEVER name `macos-13` (removed 2025-12-04 — Pitfall 7). Set `runs-on: ${{ matrix.runner }}` and a matrix `name` so each leg labels clearly. Keep EVERY existing step working per-leg: checkout, Resolve-channel (D-07 unchanged), Skip-if-unchanged (nightly guard), `./tools/ci-setup-toolchain.sh`, `./tools/build.sh` (now codesigns on macOS via Plan 03 Task 1), Name+checksum (the `platform_os` naming already yields `wez-macos-<arch>` on Darwin — keep the comment), Ensure-release, Upload. Keep the Prune step as its OWN factored nightly-only step (the comment at line 203 anticipates the matrix wrapping build/publish without touching prune) — guard it so it runs on ONE leg only (e.g. `if: matrix.runner == 'ubuntu-latest'` plus the existing nightly+unchanged guards) to avoid three legs racing the same delete. Add a NEW arm64-only in-build smoke step gated `if: matrix.runner == 'macos-14'`: run `codesign --verify --verbose dist/wez` and `./dist/wez version` (the runner IS arm64 — free arm64 evidence with no local Silicon; Open Q3). The existing `workflow_dispatch` trigger already gives the unattended dry-run (a non-tag ref resolves CHANNEL=nightly per the D-07 step) — confirm it builds+codesigns+uploads the macOS legs to a `nightly-YYYYMMDD` prerelease without a `v*` tag; add a comment documenting the dispatch dry-run as the unattended macOS-leg proof path. Run `actionlint` on the workflow if available. Author tests/cli/ci_macos_toolchain_test.lua: TEXT asserts release.yml contains `macos-15-intel` AND `macos-14` AND `strategy:` + `matrix:`, does NOT contain `macos-13`, has the arm64 smoke (`matrix.runner == 'macos-14'` + `codesign --verify` + `dist/wez version`), and that ci-setup-toolchain.sh uses `lua@5.4` not bare `lua` + build.sh has the `codesign --force --sign -` block (cross-file regression guard for Task 1). English only.
  </action>
  <verify>
    <automated>command -v actionlint >/dev/null 2>&1 && actionlint .github/workflows/release.yml; lua5.4 tests/cli/ci_macos_toolchain_test.lua 2>/dev/null || lua tests/cli/ci_macos_toolchain_test.lua; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'macos-15-intel' .github/workflows/release.yml` >= 1 AND `grep -c 'macos-14' .github/workflows/release.yml` >= 1 AND `grep -cE 'strategy:|matrix:' .github/workflows/release.yml` >= 2.
    - SOURCE: `grep -c 'macos-13' .github/workflows/release.yml` returns 0 (removed runner never named).
    - SOURCE: the arm64 smoke step exists — `grep -c "matrix.runner == 'macos-14'" .github/workflows/release.yml` >= 1 AND a `codesign --verify` + `dist/wez version` appear under it.
    - SOURCE: the Prune step is single-leg guarded (does not run on all three legs racing the same delete).
    - SOURCE: `workflow_dispatch` remains a trigger (the dry-run path) — `grep -c 'workflow_dispatch' .github/workflows/release.yml` >= 1.
    - BEHAVIOR: if `actionlint` is installed it exits 0 on the workflow; `lua5.4 tests/cli/ci_macos_toolchain_test.lua` reports `0 failed`, exit 0.
    - NOTE: live CI execution (the dispatch dry-run that proves the legs build+codesign+upload) is Plan 04's `gh run watch` step — this plan delivers a correctness-verified workflow (actionlint + greps + the TEXT gate), per the release.yml header convention "verified for CORRECTNESS, not via a live CI run" until pushed.
  </acceptance_criteria>
  <done>release.yml builds all three legs (no macos-13), arm64 smoke + dispatch dry-run present, prune single-leg; TEXT gate + actionlint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → release assets | CI builds + publishes public release assets; the workflow token crosses into write scope. |
| trigger ref → channel | `github.ref` decides stable vs nightly; a spoofed channel input would mis-route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | wrong Lua (5.5) silently links the CI build | mitigate | `lua@5.4` pin + keg PATH + the existing assert_and_capture fail-loud gate; the cross-file TEXT test guards against `brew install lua` regressing. |
| T-07-10 | Tampering | unsigned arm64 Mach-O / users disable Gatekeeper | mitigate | Build-time ad-hoc codesign (both arches) so end users never weaken Gatekeeper; `codesign --verify` records valid-signature evidence (D-06). |
| T-07-11 | Spoofing | malicious channel input | mitigate | `github.ref`-only channel resolution (no `workflow_dispatch` input field to spoof; a dispatch is a non-tag ref → nightly prerelease, excluded from /releases/latest). |
| T-07-12 | Elevation of Privilege | over-broad CI token | mitigate | `permissions: contents: write` (least privilege) retained — only the scope `gh release upload` needs. |
| T-07-SC | Tampering | brew/luarocks installs on the runner | mitigate | Same Approved formulae/rock as the RESEARCH audit; sudo-on-runner is the sanctioned CI provisioning scope (documented in ci-setup-toolchain.sh), NOT the user install path. No `[SUS]`/`[SLOP]` packages → no blocking-human checkpoint. |
</threat_model>

<verification>
- `actionlint .github/workflows/release.yml` clean (if installed); the TEXT gate test green.
- ci-setup-toolchain.sh uses lua@5.4 + keg PATH; build.sh codesigns both arches with verify, no spctl gate.
- release.yml: 3-leg matrix, no macos-13, arm64 smoke, single-leg prune, workflow_dispatch dry-run present.
- Live CI run is exercised in Plan 04 (`gh run watch --exit-status` on the dispatched dry-run).
</verification>

<success_criteria>
- ci-setup-toolchain.sh macOS path fixed (lua@5.4, keg on $GITHUB_PATH); latent CI bug closed.
- build.sh ad-hoc codesigns both macOS arches at build with a verify evidence line; never gates on spctl.
- release.yml restores the 3-leg matrix (ubuntu/macos-15-intel/macos-14), adds the arm64 in-build smoke, keeps prune single-leg, and offers the workflow_dispatch dry-run.
- ci_macos_toolchain_test.lua green; actionlint clean.
</success_criteria>

<output>
Create `.planning/phases/07-macos-parity/07-03-SUMMARY.md` when done
</output>
