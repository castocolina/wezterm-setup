---
phase: 07-macos-parity
plan: 07
type: execute
wave: 5
depends_on: ["07-04", "07-05", "07-06"]
files_modified:
  - tools/install.sh
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/MACOS-PARITY-AND-FOLLOWUPS.md
  - docs/macos-verification.md
autonomous: true
cross_ai: true

requirements: [INST-01, INST-06, INST-07, INST-08, FOUND-01, DIAG-05, PANE-01, PANE-02, PANE-03, PANE-04, SCEN-03, SCEN-04, SCEN-05, SCEN-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The D-07 quarantine decision (strip com.apple.quarantine in tools/install.sh, or explicitly do not) is made from the REAL evidence Plans 07-03 and 07-05 recorded on this Mac, never assumed."
    - "Every macOS-deferred requirement ID (INST-01/06/07, FOUND-01, DIAG-05, PANE-01..04, SCEN-03..06) has its REQUIREMENTS.md/ROADMAP.md status flipped to Done ONLY where this phase's plans actually recorded real evidence for it, citing the specific plan/runbook section that proved it."
    - "INST-08 (the cross-platform CI matrix + codesigned per-OS/arch release assets requirement Plan 07-04 actually closes — retagged from this plan's original INST-07 mistag per a cross-AI review of this phase's plans) is flipped to Done ONLY for the sub-parts 07-04's real GitHub Actions run actually proved. Specifically: the darwin-aarch64/Apple-Silicon sub-item is gated on 07-04 Task 3's macos-14 CI leg being recorded in its SUMMARY as having actually run AND passed (both the build step and the post-codesign smoke-test) — if that leg did not run or failed, INST-08 is left as an explicit partial/open status naming exactly which sub-part (darwin-aarch64) remains unresolved, NEVER folded into a blanket Done alongside the other 13 IDs."
    - "Any item this phase could NOT fully verify on this hardware (specifically: Apple Silicon runtime codesign/SIGKILL-avoidance confirmation, since this execution only had x86_64 available) is honestly documented as a remaining gap, not silently marked Done."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "every Phase 7 requirement ID's Traceability row (INST-01/06/07/08, FOUND-01, DIAG-05, PANE-01..04, SCEN-03..06) updated from '... macOS deferred D-18' / 'Pending' to 'Done (Phase 7, evidence: <plan/section>)', or left explicitly noted as a documented remaining gap — INST-08's darwin-aarch64 sub-item explicitly gated on 07-04's macos-14 CI leg outcome"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7's coverage table (including a NEW INST-08 row, since ROADMAP's own Phase 7 '**Requirements:**' line never listed it despite release.yml's own header deferring the macOS legs to Phase 7) and the four numbered Success Criteria annotated with which plan closed each, and an honest note on the Apple-Silicon-hardware gap"
    - path: "docs/macos-verification.md"
      provides: "a fully filled-in macOS Deviations table (real findings from Plans 07-01 through 07-06, not the placeholder rows), and every Results-table row set to PASS/FAIL/N/A from real evidence"
---

<objective>
Close the phase: decide the D-07 quarantine question from the real evidence Plans 07-03/07-05
recorded on this Mac (never assumed), apply the resulting fix to `tools/install.sh` if the evidence
shows Gatekeeper actually blocking (or explicitly document why no fix was needed if it does not),
then flip every macOS-deferred requirement ID's status in `.planning/REQUIREMENTS.md` and
`.planning/ROADMAP.md` to Done — but ONLY where a specific plan in this phase actually produced real
evidence for it, citing that plan/section, and leaving an honest, explicit note for the one item this
phase's hardware genuinely could not fully verify (Apple Silicon runtime codesign confirmation).

Scope note (per a cross-AI review of this phase's plans): this plan's requirement scope now also
includes INST-08 (the cross-platform CI matrix + codesigned per-OS/arch assets requirement Plan
07-04 actually closes) alongside the original 13 IDs — INST-08 was left off this plan's original
frontmatter despite `.github/workflows/release.yml`'s own header explicitly deferring its macOS legs
to Phase 7, and it is currently `Pending` (not "macOS deferred D-18") in both REQUIREMENTS.md and
ROADMAP.md. Unlike the other 13 IDs, INST-08's flip is SUB-ITEM-GATED: the darwin-aarch64/Apple-
Silicon half only flips to Done if Plan 07-04's macos-14 CI leg is recorded as having actually run
and passed; otherwise that sub-item stays explicitly open, and the overall blanket "flip all 13"
framing below is corrected to "flip all 14 IDs individually, on their own actual evidence."

Purpose: this is the ROADMAP's "final gate before v1 is declared done" — the requirement-traceability
bookkeeping this phase's whole verification effort exists to justify.

Output: a decided-and-applied (or explicitly-declined) quarantine fix, and REQUIREMENTS.md/
ROADMAP.md/the runbook all reflecting the real, evidenced state of macOS parity.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/MACOS-PARITY-AND-FOLLOWUPS.md
@.planning/phases/07-macos-parity/07-CONTEXT.md
@docs/macos-verification.md
@tools/install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the D-07 quarantine decision from recorded evidence</name>
  <read_first>
    - Plan 07-03's SUMMARY — the scratch-directory `xattr -p com.apple.quarantine` measurement
      against a freshly-downloaded `WezTerm.app` on this Mac.
    - Plan 07-05's SUMMARY — the real-install Gatekeeper observation against the actual
      `~/Applications/WezTerm.app` on this Mac (Task 1's `<how-to-verify>` step 2).
    - `tools/install.sh` in full (already read during planning) — the natural site for a POST-
      hand-off quarantine strip, if one is needed: after `WEZ_REMOTE_BOOTSTRAP=1 "$bootstrap_cmd"
      "$@"` succeeds, before `main` returns, gated on `uname -s` reporting `Darwin`.
    - `.planning/phases/07-macos-parity/07-CONTEXT.md` D-07 — "verify-then-decide": only add an
      `xattr -dr com.apple.quarantine` step if the on-Mac pass showed Gatekeeper actually blocking;
      otherwise document why not, keeping the runbook's manual `xattr -d`/right-click-open fallback
      as a note.
  </read_first>
  <files>tools/install.sh</files>
  <action>
    Read the two real measurements from Plans 07-03 and 07-05. If EITHER recorded that
    `com.apple.quarantine` was actually present on a downloaded `WezTerm.app` AND that Gatekeeper
    actually blocked or warned on first launch, add a small, macOS-gated post-install step to
    `tools/install.sh`'s `main()` (after the bootstrap hand-off succeeds): when `uname -s` reports
    `Darwin`, run `xattr -dr com.apple.quarantine` against the placed `WezTerm.app` (resolve its path
    the same way `install_macos` does — `${WEZTERM_APP_DIR:-${HOME}/Applications}/WezTerm.app`) and
    the installed `wez` binary path, logging what was stripped; treat a missing xattr (nothing to
    strip) as a normal, non-error outcome. If NEITHER measurement showed quarantine actually blocking
    anything (the more likely outcome for a plain `curl`/`ditto`-sourced download, per the CONTEXT.md
    discussion), make NO code change to `tools/install.sh` — instead, add one sentence to
    `docs/macos-verification.md`'s "Candidate deviations to watch for" quarantine bullet recording
    the real observed outcome (quarantine absent / present-but-non-blocking) and the plan that
    measured it, so this verify-then-decide question is answered with evidence rather than left an
    open question forever. Either way, do not touch anything else in `tools/install.sh` beyond the
    one gated block (if added).
  </action>
  <acceptance_criteria>
    - The quarantine decision is traceable to a specific, cited measurement from Plan 07-03 and/or
      07-05 — not asserted independently of that evidence.
    - If a fix was added, it is macOS-gated, non-destructive to a system that has nothing to strip,
      and touches only `tools/install.sh`'s `main()` post-hand-off block.
    - If no fix was added, `docs/macos-verification.md` explicitly records why, citing the real
      measurement.
  </acceptance_criteria>
  <verify>
    <automated>bash -n tools/install.sh && grep -c "com.apple.quarantine" tools/install.sh docs/macos-verification.md</automated>
    <fails_when>tools/install.sh has a syntax error, OR the quarantine decision (fix added, or explicitly-recorded no-fix) cannot be traced to a cited measurement from this phase's SUMMARYs.</fails_when>
  </verify>
  <reversibility rating="reversible">a one-block, macOS-gated `xattr` addition (or its absence) is trivially reverted by a follow-up commit if the real-world evidence this decision rests on later proves wrong.</reversibility>
  <done>The D-07 quarantine question is answered from real, cited evidence — either a macOS-gated fix landed in tools/install.sh, or an explicit, evidenced "not needed" note was added to the runbook.</done>
</task>

<task type="auto">
  <name>Task 2: Flip macOS-deferred requirement statuses to Done, citing evidence, with an honest note on the Apple-Silicon gap</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` Traceability table rows for INST-01, INST-06, INST-07, FOUND-01,
      DIAG-05, PANE-01, PANE-02, PANE-03, PANE-04, SCEN-03, SCEN-04, SCEN-05, SCEN-06 — each
      currently reads either "Pending" or a Linux-only "Done" with no macOS note (PANE-01..04 and
      SCEN-03/04/06 in fact still read bare "Pending" in the Requirements doc itself, distinct from
      the more detailed macOS-deferred phrasing already present in ROADMAP.md's own Coverage table —
      reconcile both documents to the same real state).
    - `.planning/REQUIREMENTS.md` line 91 and `.planning/ROADMAP.md` line 543 — INST-08's row, which
      currently reads bare "Pending" (attributed to Phase 6) in BOTH documents, with NO "macOS
      deferred D-18" phrasing at all. Per a cross-AI review of this phase's plans, INST-08 (the
      cross-platform CI matrix + codesigned darwin assets requirement) is what Plan 07-04 actually
      closes — ROADMAP.md's own Phase 7 `**Requirements:**` line (line 447) never listed INST-08
      despite `.github/workflows/release.yml`'s own header explicitly deferring its macOS legs to
      Phase 7. This task must ADD INST-08 to ROADMAP.md's Phase 7 Requirements line and Coverage
      table (a genuinely new row for this phase, not a re-flip of an already-listed one), citing
      Plan 07-04.
    - Plan 07-04's own SUMMARY — specifically whether its Task 3 (`checkpoint:human-verify`, the real
      GitHub Actions matrix run) recorded the `macos-14` (arm64) leg as having actually RUN and
      PASSED (both the build step and the post-codesign smoke-test). This is the ONLY evidence source
      for INST-08's darwin-aarch64 sub-item — if that leg did not run, was skipped, or failed, the
      darwin-aarch64 sub-item MUST stay open (see `<action>` below); the ubuntu-latest and
      macos-15-intel (x86_64) legs passing does NOT, by itself, justify flipping the arm64 half.
    - `.planning/ROADMAP.md`'s Coverage table and Phase 7's four numbered Success Criteria — the
      exact rows/criteria this task closes out.
    - This phase's own Plan SUMMARYs (07-01 through 07-06) — the actual evidence source for every
      flip; every "Done" this task writes must cite one.
    - `.planning/MACOS-PARITY-AND-FOLLOWUPS.md` section C (the by-capability checklist) — tick every
      box this phase's plans actually closed; leave any genuinely-unclosed box unticked with a note.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/MACOS-PARITY-AND-FOLLOWUPS.md, docs/macos-verification.md</files>
  <action>
    For each of the 13 original requirement IDs this phase covers, read the corresponding evidence
    from this phase's Plan SUMMARYs (07-02/07-03 for INST-06; 07-05 for INST-01/06/07's real-install
    half; 07-06 for FOUND-01, DIAG-05, PANE-01..04, SCEN-03..06) and update
    `.planning/REQUIREMENTS.md`'s Traceability table row to `Done (Phase 7, macOS: <plan-id>,
    runbook §<N>)` — using the REAL plan id and section number each item's evidence actually came
    from, never a generic "verified". Apply the SAME update to `.planning/ROADMAP.md`'s Coverage
    table rows for the same IDs.

    Handle INST-08 (a cross-AI review of this phase's plans found this ID belongs to Plan 07-04's
    work and was missing from both this plan's own frontmatter AND ROADMAP.md's Phase 7
    `**Requirements:**` line) as a SEPARATE, sub-item-gated flip, not folded into the blanket-13
    treatment above: read Plan 07-04's SUMMARY for its Task 3 real GitHub-Actions-run outcome.
      - If the `ubuntu-latest` AND `macos-15-intel` (x86_64) legs both passed: the non-Silicon half of
        INST-08 is evidenced — write `Done (Phase 7, macOS: 07-04, x86_64 + linux legs verified;
        darwin-aarch64: <see below>)`.
      - If the `macos-14` (arm64) leg ALSO passed (both its build step and its post-codesign
        smoke-test, per 07-04 Task 3's own recorded outcome): the darwin-aarch64 sub-item is
        evidenced too — the row becomes a clean `Done (Phase 7, macOS: 07-04, all three matrix legs
        incl. macos-14/arm64 verified)`.
      - If the `macos-14` leg did NOT run, was skipped, or failed: INST-08 stays an explicit PARTIAL
        status naming exactly the open sub-part, e.g. `Partial (Phase 7, macOS: 07-04 — ubuntu-latest
        + macos-15-intel/x86_64 verified; darwin-aarch64/macos-14 leg <did not run|failed> — needs a
        re-run once available)`. NEVER write a blanket `Done` for INST-08 in this case, and never let
        it get swept into a generic "all requirements flipped" closing statement elsewhere in this
        task's own output.
    Add INST-08 as a NEW row to `.planning/ROADMAP.md`'s Phase 7 `**Requirements:**` line (line 447)
    and Coverage table (it was never listed there despite `release.yml`'s own header deferring its
    macOS legs to Phase 7) — citing Plan 07-04, with the same sub-item-gated wording as above.

    Annotate Phase 7's four Success Criteria (in the Phase Details section) with which plan closed
    each — EXCEPT the Apple Silicon ad-hoc codesign runtime-confirmation half of Success Criterion 3,
    which stays gated on the SAME macos-14 leg evidence used for INST-08 above (cite the SAME
    Plan 07-04 SUMMARY fact, do not treat them as two independent judgment calls): if that leg passed,
    Success Criterion 3's Apple-Silicon codesign sub-item is closed by 07-04; if not, write it as an
    honest, explicit remaining gap ("ad-hoc codesign mechanism verified functionally on x86_64;
    SIGKILL-avoidance runtime confirmation on Apple Silicon hardware still pending — needs a real
    arm64 Mac or a passing macos-14 CI leg"), not as Done. Tick the corresponding boxes in
    `.planning/MACOS-PARITY-AND-FOLLOWUPS.md` section C for every item this phase's plans actually
    closed, leaving any genuinely-open box (e.g. A-2/A-3's pre-existing, always-deferred UX backlog,
    which is explicitly out of THIS phase's scope per CONTEXT.md) untouched. Finally, confirm
    `docs/macos-verification.md`'s Results table has no remaining blank cells for the 13 original
    covered requirement IDs, and its Deviations table lists every real divergence this phase's plans
    actually found (not the placeholder example rows) — if a placeholder row is now genuinely empty
    (no real deviation was ever logged there), remove it rather than leaving it as dead placeholder
    text.
  </action>
  <acceptance_criteria>
    - Every one of the 13 original requirement IDs has a `Done (Phase 7, macOS: ...)`-style row in
      BOTH `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, each citing a real plan id and,
      where applicable, a runbook section number.
    - INST-08 has a NEW row in `.planning/ROADMAP.md`'s Phase 7 Requirements line + Coverage table
      (it was absent before this task), and its status is `Done` ONLY if Plan 07-04's SUMMARY records
      ALL THREE matrix legs (including `macos-14`/arm64) as having passed; otherwise it is an explicit
      `Partial` status naming the darwin-aarch64 sub-item as open — never a blanket Done covering an
      unproven arm64 leg.
    - The Apple Silicon codesign runtime-confirmation gap (Success Criterion 3's sub-item) is gated on
      the SAME macos-14 leg evidence as INST-08's flip, and is explicitly documented as NOT closed
      when that leg did not pass, with a concrete reason and what would close it.
    - `.planning/MACOS-PARITY-AND-FOLLOWUPS.md` section C's boxes reflect the real, evidenced state
      — no box ticked without a citable plan behind it.
    - `docs/macos-verification.md`'s Results table has no blank cells for the 13 originally-covered
      IDs, and its Deviations table contains only real, evidenced entries (placeholder rows removed
      if unused).
  </acceptance_criteria>
  <verify>
    <automated>for id in INST-01 INST-06 INST-07 INST-08 FOUND-01 DIAG-05 PANE-01 PANE-02 PANE-03 PANE-04 SCEN-03 SCEN-04 SCEN-05 SCEN-06; do grep -q "$id" .planning/REQUIREMENTS.md || { echo "MISSING $id in REQUIREMENTS.md"; exit 1; }; grep -q "$id" .planning/ROADMAP.md || { echo "MISSING $id in ROADMAP.md"; exit 1; }; done; grep -qi "arm64\|apple silicon" .planning/ROADMAP.md && grep -qi "pending\|gap\|not.*verif\|partial" .planning/ROADMAP.md && echo "CLOSURE_OK"</automated>
    <fails_when>any of the 13 original requirement IDs is missing a Phase-7 evidence citation in REQUIREMENTS.md, OR INST-08 is missing from either REQUIREMENTS.md or ROADMAP.md, OR INST-08 is marked a blanket `Done` in ROADMAP.md without ROADMAP.md ALSO containing partial/gap/pending language covering the arm64/macos-14 sub-item when that leg did not pass, OR ROADMAP.md's Apple Silicon codesign item is marked Done without an honest gap note, OR MACOS-PARITY-AND-FOLLOWUPS.md's section C boxes were ticked without a citable plan.</fails_when>
  </verify>
  <done>REQUIREMENTS.md, ROADMAP.md, MACOS-PARITY-AND-FOLLOWUPS.md, and docs/macos-verification.md all reflect the real, evidenced state of macOS parity — every closed item (now including the newly-added INST-08 row) cites its proof, INST-08's darwin-aarch64 sub-item and Success Criterion 3's Apple-Silicon codesign sub-item are flipped together from the SAME macos-14 CI-leg evidence (never independently guessed), and the one genuinely-open gap when that leg did not pass is honestly documented rather than glossed over.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This plan's status flips -> the project's own record of what "v1 done" means | a false "Done" here would let the milestone close on unverified ground, defeating the entire purpose of this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-17 | Repudiation | flipping a requirement to Done without a citable evidence source | high | mitigate | Task 2's acceptance criteria hard-require a specific plan-id + section citation per flipped row; a flip with no citation fails this plan's own verify |
| T-07-18 | Tampering | quietly marking the Apple-Silicon codesign gap (or INST-08's darwin-aarch64 sub-item) Done to make the phase look fully closed | medium | mitigate | Task 2 explicitly requires both that specific sub-item AND INST-08's arm64 half to remain documented as an open/partial gap when 07-04's macos-14 CI leg did not pass, verified by a grep-based check for the honest-gap/partial language in ROADMAP.md |
</threat_model>

<verification>
- Every one of the 13 original requirement IDs is Done in both REQUIREMENTS.md and ROADMAP.md, each citing real evidence.
- INST-08 (newly added to ROADMAP.md's Phase 7 scope) is Done ONLY if 07-04's macos-14 leg passed; otherwise it is an explicit Partial status naming the darwin-aarch64 gap.
- The Apple Silicon codesign runtime-confirmation gap (Success Criterion 3) is gated on the SAME macos-14 leg evidence as INST-08, and is explicitly, honestly documented as open when that leg did not pass.
- The D-07 quarantine decision is applied (or explicitly declined) based on cited real measurements.
</verification>

<success_criteria>
Phase 7 closes with REQUIREMENTS.md and ROADMAP.md reflecting the real, evidenced state of macOS
parity — no requirement flipped to Done without proof, INST-08's darwin-aarch64 sub-item is only
Done when 07-04's macos-14 CI leg actually proved it, and the one hardware-bound gap this session
could not close is honestly left open for a future arm64 pass.
</success_criteria>

<output>
Create `.planning/phases/07-macos-parity/07-07-SUMMARY.md` when done
</output>
