---
phase: 07-macos-parity
plan: 06
type: execute
wave: 4
depends_on: ["07-05"]
files_modified:
  - docs/macos-verification.md
  - tools/verify-macos.sh
autonomous: false

requirements: [FOUND-01, DIAG-05, PANE-01, PANE-02, PANE-03, PANE-04, SCEN-03, SCEN-04, SCEN-05, SCEN-06]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CWD inheritance (OSC 7), Cmd+K clear-screen, the curated keybinding set, wez keys (grouping/conflicts/json), and every __complete value-completion context (including the A-1 scene new --layout/--color zsh confirmation the runbook has flagged as unconfirmed since 2026-06-14) are all driven for real in a live macOS WezTerm session, not asserted from reading code."
    - "Pane color/title/persistence and tab color/combined/priority/active-distinct are all visually confirmed on macOS, with the render-layer risk areas (emoji cell-width, tab-bar accent rendering) specifically reviewed, not just functionally probed."
    - "wez scene new (all 4 layouts) and wez scene launch (seeded ai/dev/docker recipes) are driven live, confirming no per-scene Aqua window ever appears (the windowing-parity risk area named in both the runbook and MACOS-PARITY-AND-FOLLOWUPS.md section C-7)."
    - "Every real divergence observed in this session is logged in docs/macos-verification.md's macOS Deviations table, and the Results table rows for FOUND-01/02/03/05, DIAG-02..05, PANE-01..04, TAB-01..05, SCEN-01..06 are filled in from this real run (D-03's evidence bar)."
  artifacts:
    - path: "docs/macos-verification.md"
      provides: "Sections 3-7's Results-table rows and Deviations entries, filled in from a real, agent-driven live-session pass on this Mac"
---

<objective>
Drive the entire live-WezTerm-session portion of the macOS verification runbook — Sections 3
(Foundation + doctor), 4 (Diagnostics), 5 (Pane and Tab identity), 6 (Ad-hoc scenes), and 7 (Named
scenes) — on the real, healthy install Plan 07-05 left in place, on this real Mac, with a dedicated
visual-review pass for the render-layer risk areas (emoji glyph width, tab-bar accent rendering,
windowing) per D-02's agent-driven verification model.

Purpose: this is the single largest block of "macOS deferred D-18" requirement IDs in REQUIREMENTS.md
(FOUND-01, DIAG-05, PANE-01..04, SCEN-03..06) — none of them can be closed without a live WezTerm
session, which cannot be automated headlessly. This plan is the phase's designated place for that
live-session work, following this project's own established convention of routing anything requiring
a real GUI session through a `checkpoint:human-verify` rather than pretending it can run unattended.

Output: the runbook's Sections 3-7 driven for real, with Results/Deviations filled in from actual
observation on this Mac.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-macos-parity/07-CONTEXT.md
@.planning/MACOS-PARITY-AND-FOLLOWUPS.md
@docs/macos-verification.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Foundation + Diagnostics live pass (Sections 3-4) — CWD/Cmd+K/bindings/doctor/keys/completions, including the outstanding A-1 zsh confirmation</name>
  <read_first>
    - `docs/macos-verification.md` Section 3 (Foundation + wez doctor) and Section 4 (Diagnostics)
      in full — the exact commands, expected outputs, and the specific `⚠ macOS risk to confirm`
      callouts for each (OSC 7 needing a NEW shell for the installer's rc line to be active; `Cmd+K`
      mapping to Command not Control; `wez keys` reading the macOS `wezterm show-keys --lua` shape
      cleanly; zsh `compinit` insecure-directory warnings; `bash-completion@2` for bash).
    - `.planning/MACOS-PARITY-AND-FOLLOWUPS.md` section A-1 — the outstanding, never-confirmed-on-
      macOS item: `wez scene new --layout <Tab>` / `--color <Tab>` value completion is proven in
      bash but the zsh arm (`${words[CURRENT-1]}` under `_arguments`) has never been runtime-
      confirmed on any Mac. This is the LAST open item under A-1 and this task is where it gets
      closed for real.
  </read_first>
  <precondition>Plan 07-05 left a healthy, real WezTerm + wez install on this Mac (wez doctor exits 0). Two cheap MEDIUM-severity tightenings from a cross-AI review of this phase's plans, folded into this task: (1) `tools/verify-macos.sh`'s non-interactive `hdr "2. __complete contexts"` block (its `chk_ctx` calls for `pane-colors`/`pane-icons`/`tab-colors`/`tab-icons`/`scene-layouts`/`scene-names`) is MISSING a `chk_ctx scene-colors` call entirely — add one alongside the existing `chk_ctx scene-layouts` line so the auto-gate's own non-interactive coverage matches this task's live zsh check. (2) Once THIS task's live zsh Tab-completion check below confirms A-1 is fully closed (both the bash-side wiring AND the zsh-runtime firing), also update `tools/verify-macos.sh`'s "Known bug A-1" `pass`/`note` messages (the `if grep -q "scene-layouts" ...` block) to drop the now-stale "KNOWN-BUG" framing — the check mechanism itself stays (it is already real pass/note logic, not something needing restructuring), only its wording changes from "appears FIXED" to a plain regression-guard label, since the bug it names is now fully closed rather than merely "appearing" fixed.</precondition>
  <what-built>
    A healthy real install is already in place from Plan 07-05. I will now, in a live WezTerm
    session: open a NEW shell to confirm the installer's OSC 7 rc registration is active, `cd` into a
    directory and split/open a tab to check CWD inheritance; press `Cmd+K` and confirm scrollback
    clears; exercise a spot-check of the curated bindings; run `wez doctor`, `wez keys`, `wez keys
    --json`; and, in a NEW zsh session, drive every `wez __complete` value-completion context
    (`pane-colors`, `pane-icons`, `tab-colors`, `tab-icons`, `scene-names`, AND `scene-layouts`/
    `scene-colors` via `wez scene new --layout <Tab>` / `--color <Tab>` — the specific A-1 zsh check).
  </what-built>
  <how-to-verify>
    1. I will report, per bullet in Sections 3-4, what actually happened (pass/fail/divergence),
       including the exact zsh Tab-completion result for `--layout`/`--color` (closing A-1).
    2. I will add `chk_ctx scene-colors` to `tools/verify-macos.sh`'s "2. __complete contexts" block
       (alongside the existing `chk_ctx scene-layouts` line) and, once the zsh A-1 confirmation above
       passes, update the "Known bug A-1" pass/note message wording to drop the stale "KNOWN-BUG"
       framing (mechanism unchanged, wording only) — both per the precondition note above.
    3. Review the reported results. Any macOS-specific divergence you want logged goes into the
       runbook's Deviations table before I move to Task 2.
  </how-to-verify>
  <resume-signal>Reply "approved" once you have reviewed the Section 3-4 results (including the A-1 zsh completion confirmation), or describe what diverged.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Identity + Scenes live pass (Sections 5-7) with a visual review of the render-layer risk areas</name>
  <read_first>
    - `docs/macos-verification.md` Section 5 (Pane and Tab identity), Section 6 (Ad-hoc scenes),
      Section 7 (Named scenes) in full — including the `⚠ macOS risk to confirm` callouts for OSC
      1337 user-var-to-background mapping, tab-bar accent/active-indicator/emoji-glyph rendering,
      and the windowing invariant (reuse-a-tab or spawn-a-new-tab-in-the-SAME-window, never a
      separate Aqua window per scene).
    - This project's `e2e-visual-review` skill (`.claude/skills/e2e-visual-review/SKILL.md`, landed
      in Phase 06.9) as the concrete mechanism for the render-layer visual judgment CONTEXT.md's D-02
      names generically as "agent-ui-ux-designer" — use this skill (or an equivalent dedicated
      visual-review pass if that skill is unavailable in this execution environment) to give the
      color/glyph/windowing checks the same structured, non-rubber-stamped scrutiny D-02 intends,
      rather than a single "looks fine" glance.
  </read_first>
  <precondition>Task 1 of this plan is complete and its results were approved.</precondition>
  <what-built>
    I will now, in the same live WezTerm session: set/reset pane color and title, split panes and
    switch focus to confirm persistence with no flicker; set tab color, combined color+title, and
    confirm pane-color-wins-over-tab-color when both are set on one tab; run all four `wez scene new`
    layouts plus the per-pane-startup-command form; launch the seeded `ai`/`dev`/`docker` recipes via
    `wez scene launch`; and confirm at every step that no separate Aqua window appears (reuse or
    same-window new-tab only).
  </what-built>
  <how-to-verify>
    1. I will report each identity/scene check's outcome, and specifically apply the visual-review
       mechanism above to the render-layer risk areas: does the tab-bar accent render correctly, does
       an emoji icon's cell width look right, does the active-vs-inactive tab distinction hold, and
       does every scene/launch stay inside the current WezTerm window.
    2. Review the reported results and any newly-logged Deviation. Fill the runbook's Results table
       for PANE-01..04, TAB-01..05, SCEN-01..06 accordingly. NOTE (per a cross-AI review of this
       phase's plans): TAB-01..05 are already `Done` in REQUIREMENTS.md with NO "macOS deferred"
       annotation (unlike PANE-01..04/SCEN-03..06) — filling their Results-table row here is useful
       confirmation, but it does NOT close a D-18 deferral that never existed for these IDs, and
       Plan 07-07 must not cite a TAB-* row as evidence for flipping any macOS-deferred status.
  </how-to-verify>
  <resume-signal>Reply "approved" once you have reviewed the identity/scenes visual results and the filled-in Results table, or describe what should change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Live WezTerm session -> visual/UX judgment | a rubber-stamped "looks fine" pass here would let a real rendering regression (emoji width, tab-bar accent) through unverified into the final closure plan's "Done" flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-15 | Repudiation | claiming a visual/UX check passed without a structured, recorded judgment | medium | mitigate | Task 2 requires the visual-review mechanism's structured output (or an equivalent explicit per-item judgment) to be recorded in the SUMMARY, not just a bare "looks good" |
| T-07-16 | Information Disclosure | this plan's checkpoints displaying no secrets, only rendered UI state | n/a | accept | no credentials or secret material are involved in any step of this plan |
</threat_model>

<verification>
- docs/macos-verification.md's Sections 3-7 Results/Deviations rows are filled in from real observation on this Mac.
- A-1's zsh scene-layouts/scene-colors completion confirmation is closed (pass or a logged divergence).
- No per-scene Aqua window is observed across any scene/layout check.
</verification>

<success_criteria>
FOUND-01, DIAG-05, PANE-01..04, and SCEN-03..06's macOS-deferred status has real, recorded live-
session evidence, and the runbook's remaining open item (A-1's zsh completion) is closed.
</success_criteria>

<output>
Create `.planning/phases/07-macos-parity/07-06-SUMMARY.md` when done
</output>
