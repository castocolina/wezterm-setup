---
phase: 07-macos-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/build.sh
autonomous: true
cross_ai: true

requirements: [INST-06]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The actual shipping build path (luastatic single-binary) produces a runnable Mach-O dist/wez on real Mac hardware — not just the dev source-launcher fallback, which is all tools/build.sh has ever exercised on macOS until now."
    - "MACOS-PARITY-AND-FOLLOWUPS.md section C-1's 'Xcode CLT / clang present; luastatic links a runnable Mach-O binary' item is closed with real, recorded evidence from this Mac."
    - "have_luastatic() itself — the gate that decides whether main() even ATTEMPTS build_with_luastatic() — no longer requires a bare `lua5.4` literally on PATH, which a keg-only Homebrew lua@5.4 install never satisfies; it resolves the same PATH → keg → confirmed-bare-lua order resolve_dev_lua() already uses, so the static-binary path is genuinely REACHABLE on a stock Homebrew Mac, not just theoretically correct once reached."
    - "The cflags/header resolution's macOS keg probe is a REAL, reachable arm of the probe chain itself — not a post-hoc 'if lua_cflags is still empty' check placed after an assignment that always succeeds via its own `|| echo` fallback, which a cross-AI review of this phase's plans found makes the keg path dead code as originally drafted."
  artifacts:
    - path: "tools/build.sh"
      provides: "a macOS-aware header/library resolution for the luastatic build path that works even when pkg-config is absent or names its .pc file differently than Linux does, by probing the Homebrew lua@5.4 keg directly, AND a have_luastatic() gate that recognizes the SAME keg instead of requiring lua5.4 literally on PATH"
    - path: "dist/wez"
      provides: "a real, luastatic-built, Mach-O wez binary produced on this Mac (superseding the dev-launcher fallback that verify-macos.sh's earlier runs silently relied on)"
  key_links:
    - from: "tools/build.sh build_with_luastatic()"
      to: "Homebrew lua@5.4 keg (brew --prefix lua@5.4)"
      via: "a brew --prefix lua@5.4 probe, reached as a REAL ordered step in the cflags resolution (not a post-hoc empty-check on an assignment that can never be empty), that supplies -I<keg>/include/lua and <keg>/lib/liblua.a when pkg-config cannot resolve lua5.4/lua-5.4/lua on this host"
      pattern: "brew --prefix lua@5.4"
    - from: "tools/build.sh have_luastatic()"
      to: "Homebrew lua@5.4 keg (brew --prefix lua@5.4)"
      via: "the same PATH → keg → confirmed-bare-lua resolution order resolve_dev_lua() already uses, so this predicate returns true on a stock keg-only Homebrew Mac instead of requiring lua5.4 literally on PATH"
      pattern: "brew --prefix lua@5.4"
---

<objective>
Make `tools/build.sh`'s luastatic single-binary path — the project's actual locked shipping
artifact (PROJECT.md: "packaging = vendored pure-Lua deps + luastatic single binary") — actually
work on macOS, then exercise it for real on this Mac. Live investigation this session found the
existing `pkg-config`-based header/library resolution cannot work on a stock Homebrew macOS install:
`pkg-config` itself is absent by default on this machine, and even where present, the Homebrew
`lua@5.4` keg ships its `.pc` file as `lua.pc` (not `lua5.4.pc`/`lua-5.4.pc`) with headers under
`include/lua/` (not `include/lua5.4/`) — none of which match `build_with_luastatic()`'s current
probe order or its `-I/usr/include/lua5.4` fallback. Until this plan, every macOS `tools/build.sh`
run has silently fallen back to the dev source-launcher (because `luastatic` itself was never
installed either), so the actual shipping build path has never been proven on real Mac hardware.

Purpose: close MACOS-PARITY-AND-FOLLOWUPS.md section C-1's Mach-O item with real evidence, and give
every later plan in this phase (the real `install_macos`, the real install/uninstall cycle, ad-hoc
codesign) a genuine static binary to build on instead of the non-shippable dev launcher.

Output: a macOS-portable `build_with_luastatic()`, and a real `dist/wez` Mach-O produced on this Mac.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-macos-parity/07-CONTEXT.md
@.planning/MACOS-PARITY-AND-FOLLOWUPS.md
@tools/build.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix build_with_luastatic()'s macOS header/library resolution (Homebrew lua@5.4 keg fallback)</name>
  <read_first>
    - `tools/build.sh` lines 101-158 (`build_with_luastatic`) — the current cflags resolution
      (lines 109-113) is ONE command-substitution chain:
      `lua_cflags="$(pkg-config --cflags lua5.4 2>/dev/null || pkg-config --cflags lua-5.4 2>/dev/null
      || pkg-config --cflags lua 2>/dev/null || echo '-I/usr/include/lua5.4')"`. A cross-AI review of
      this phase's plans flagged this as a HIGH-severity gap in the ORIGINAL draft of this plan: that
      final `|| echo '-I/usr/include/lua5.4'` arm means `lua_cflags` is NEVER empty once this line has
      run — a subsequent "if lua_cflags is still empty" check placed AFTER this assignment is dead
      code that can never execute. The fix in THIS revised plan therefore restructures the probe
      chain itself into discrete, ordered steps (see `<action>` below) rather than adding a post-hoc
      empty-check onto the existing one-liner. The liblua resolution (lines 119-128) walks a fixed
      candidate list of Linux-shaped paths (`/usr/lib/*/liblua5.4.a`, `/usr/local/lib/liblua5.4.a`,
      etc.) with no macOS/Homebrew entry at all — this part IS a real, reachable `for` loop (not
      dead-code-masked), so it only needs a new candidate appended, not a restructure.
    - `tools/build.sh` lines 92-96 (`have_luastatic`) — `command -v luastatic >/dev/null 2>&1 &&
      command -v lua5.4 >/dev/null 2>&1 && { command -v cc >/dev/null 2>&1 || command -v gcc
      >/dev/null 2>&1; }`. The SAME cross-AI review flagged this as a SECOND, independent HIGH-severity
      gap: this predicate requires `lua5.4` literally resolvable via `command -v`, which a keg-only
      Homebrew `lua@5.4` install (see the live-verified facts below) never satisfies — Homebrew
      deliberately does NOT symlink a keg-only formula's binaries onto PATH. Even after this task
      fixes the cflags/liblua resolution below, `have_luastatic()` would still return false on this
      exact Mac, so `main()` would still take `build_dev_launcher()` instead of ever reaching
      `build_with_luastatic()` at all. This function must ALSO be fixed, independently of the cflags
      restructure — one bug does not fix the other.
    - `tools/build.sh` lines 428-458 (`resolve_dev_lua`) — the EXISTING, working macOS keg-detection
      idiom to mirror for BOTH fixes above: `command -v brew` guard, then
      `keg="$(brew --prefix lua@5.4 2>/dev/null || true)"`, then test the keg path for the expected
      file. Reuse this exact idiom shape (same three-step order: PATH → keg → a bare `lua` ONLY when
      confirmed via `lua -v` to report `Lua 5.4`), do not invent a different one. Note: this function's
      own docstring comment (~line 437, "Mirrors the keg-only macOS idiom the luastatic static path
      already uses (lines ~202-209)") is ITSELF stale — lines ~202-209 land inside
      `resolve_stable_latest()`, not any keg-detection code; this task's own edits are the FIRST real
      implementation of that idiom in `build_with_luastatic()`/`have_luastatic()`, so fix that
      cross-reference to point at the correct new line range once this task's edits land.
    - Live-verified on this Mac (record these facts, they are the ground truth this fix targets):
      `pkg-config` is entirely absent (`command -v pkg-config` fails); `brew --prefix lua@5.4` resolves
      to `/usr/local/opt/lua@5.4` (a symlink into `/usr/local/Cellar/lua@5.4/5.4.x/`); that Cellar
      path contains `include/lua/{lua.h,lauxlib.h,lua.hpp,luaconf.h,lualib.h}` (note: `include/lua/`,
      NOT `include/lua5.4/`), `lib/liblua.a`, `lib/liblua.dylib`, and `lib/pkgconfig/lua.pc` (note:
      named `lua.pc`, NOT `lua5.4.pc`/`lua-5.4.pc` — so even if `pkg-config` WERE installed, the
      existing `pkg-config --cflags lua5.4`/`lua-5.4` probes would still both miss; only a bare
      `pkg-config --cflags lua` would match, and only if `PKG_CONFIG_PATH` includes this keg's
      `lib/pkgconfig/`, which it does not by default since the formula is keg-only); and `lua5.4` is
      NOT on PATH by default even though the keg's `bin/lua5.4` exists (confirming the `have_luastatic`
      gap above independently of the cflags gap).
  </read_first>
  <files>tools/build.sh</files>
  <action>
    In build_with_luastatic()'s lua_cflags resolution (currently lines 109-113, the single
    `A || B || C || echo default` command-substitution chain identified above as HIGH-severity dead
    code once a post-hoc empty-check is added): RESTRUCTURE the chain itself into ordered, discrete
    steps, so the Homebrew keg probe is a REAL, reachable arm — not a condition checked after an
    assignment that can never be empty:
      1. Keep `pkg-config --cflags lua5.4` and `pkg-config --cflags lua-5.4` as the first two tiers,
         unchanged, each still probed via `2>/dev/null` command substitution exactly as today.
      2. Only when BOTH of those returned empty AND `platform_os` reports `macos`: probe the
         Homebrew keg BEFORE ever trying the generic `pkg-config --cflags lua` — resolve
         `keg="$(brew --prefix lua@5.4 2>/dev/null || true)"` when `command -v brew` succeeds, and if
         `"${keg}/include/lua"` is a directory, set `lua_cflags="-I${keg}/include/lua"`. This
         ordering matters, not just the probe's existence: on a macOS host that also happens to have
         `pkg-config` and a generic `lua.pc` (now the 5.5.x formula), trying the generic `lua` name
         BEFORE the keg would silently pick up the wrong major version's headers — exactly the class
         of bug this whole plan exists to close.
      3. Only when `lua_cflags` is STILL unset after step 2 (always true on Linux, since
         `platform_os != macos` skips step 2 entirely there; also true on a macOS host where the keg
         probe itself failed): fall through to the existing generic `pkg-config --cflags lua` probe,
         then the final hardcoded `-I/usr/include/lua5.4` literal — both unchanged in shape and
         position relative to each other, now genuinely reachable as LAST-RESORT fallbacks instead of
         dead code guarded by a check that could never trigger.
      Implement this as a sequence of `if`/`elif` guards (each only running when the PRIOR step
      produced nothing), not as a single one-line `A || B || C || D` string — that shape is exactly
      what made the original keg probe unreachable.

    In have_luastatic() (currently lines 92-96, gated on `command -v lua5.4` which a keg-only
    Homebrew `lua@5.4` never satisfies — the SECOND, independent HIGH-severity gap identified above):
    keep the existing `luastatic` and `cc`/`gcc` checks unchanged, but replace the bare
    `command -v lua5.4 >/dev/null 2>&1` sub-check with the SAME 3-step resolution order
    `resolve_dev_lua()` already uses: (1) `lua5.4` on PATH → satisfied; (2) when `command -v brew`
    succeeds, resolve `keg="$(brew --prefix lua@5.4 2>/dev/null || true)"` and check
    `"${keg}/bin/lua5.4"` is executable → satisfied; (3) a bare `lua` on PATH whose `lua -v 2>&1`
    matches `Lua 5\.4` → satisfied. `have_luastatic()` stays a pure predicate (no side effects, no
    PATH mutation, no echoing a resolved path) — it only needs to answer "is a genuine Lua 5.4
    toolchain present", which build_with_luastatic()'s OWN cflags/liblua resolution (steps 1-3 above)
    independently locates for real use.

    In the liblua candidate-search loop (lines 119-128, unchanged in structure — this loop is a REAL
    reachable `for`, not dead code): after the existing Linux-shaped candidates all miss, add ONE more
    macOS-specific candidate before giving up: when `command -v brew` succeeds, resolve the same
    `lua@5.4` keg prefix and test `"${keg}/lib/liblua.a"` — if it exists, use it as liblua (mirror the
    existing `[ -f "${cand}" ] && { liblua="${cand}"; break; }` pattern; do not restructure the loop,
    just extend the candidate list).

    Finally, fix the stale cross-reference comment in `resolve_dev_lua()` (~line 437, "Mirrors the
    keg-only macOS idiom the luastatic static path already uses (lines ~202-209)") — those line
    numbers land inside `resolve_stable_latest()`, not any keg-detection code; this task's edits are
    the first REAL implementation of that idiom in `build_with_luastatic()`/`have_luastatic()`, so
    update the comment to cite the correct new line range once this task's edits land.

    Do not touch resolve_dev_lua(), build_dev_launcher(), or any non-macOS code path. The Linux
    pkg-config lua5.4 / /usr/lib/*/liblua5.4.a resolution must remain byte-identical and untouched.
  </action>
  <acceptance_criteria>
    - build_with_luastatic()'s cflags resolution is a sequence of discrete, ordered `if`/`elif`
      steps (not a single `A || B || C || D` command-substitution chain), with the macOS Homebrew-keg
      probe as a REAL reachable arm — gated on `platform_os == macos` AND both `pkg-config` name
      probes having failed — tried BEFORE the generic `pkg-config --cflags lua` probe, resolving to
      `-I$(brew --prefix lua@5.4)/include/lua`.
    - have_luastatic() no longer requires `lua5.4` literally on `command -v` PATH — it accepts the
      SAME 3-step order (`lua5.4` on PATH → the Homebrew `lua@5.4` keg → a bare `lua` confirmed via
      `lua -v` to report `Lua 5.4`) that `resolve_dev_lua()` already uses, as a pure predicate with no
      side effects.
    - The liblua candidate list includes $(brew --prefix lua@5.4)/lib/liblua.a as a fallback
      candidate.
    - `resolve_dev_lua()`'s stale cross-reference comment (formerly pointing at
      `resolve_stable_latest()`'s line range) now cites the correct location of the real keg-detection
      idiom this task added.
    - Every existing Linux-facing probe (pkg-config attempts, the /usr/lib/* liblua candidates,
      the final -I/usr/include/lua5.4 literal fallback) is unchanged, in the same relative order,
      still tried first on any non-macOS host.
  </acceptance_criteria>
  <verify>
    <automated>bash -n tools/build.sh && [ "$(grep -c "brew --prefix lua@5.4" tools/build.sh)" -ge 3 ] && sed -n '/^have_luastatic()/,/^}/p' tools/build.sh | grep -q "brew --prefix lua@5.4" && sed -n '/^build_with_luastatic()/,/^main()/p' tools/build.sh | grep -q "brew --prefix lua@5.4" && echo VERIFY_OK</automated>
    <fails_when>bash -n tools/build.sh reports a syntax error, OR fewer than 3 total "brew --prefix lua@5.4" occurrences exist in tools/build.sh (resolve_dev_lua's pre-existing one + a new one inside have_luastatic() + a new one inside build_with_luastatic()'s cflags/liblua resolution), OR have_luastatic()'s own function body (isolated via sed between its definition and its closing brace) does not contain the keg probe, OR build_with_luastatic()'s function body (isolated via sed up to the next top-level function, main()) does not contain the keg probe.</fails_when>
  </verify>
  <done>build_with_luastatic() resolves macOS headers/liblua via the Homebrew lua@5.4 keg as a REAL reachable step (not dead code after an always-non-empty assignment) when pkg-config cannot, have_luastatic() recognizes the same keg instead of requiring lua5.4 literally on PATH, every pre-existing Linux probe is left untouched, and bash -n is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Install luastatic on this Mac and produce a real luastatic-built Mach-O dist/wez</name>
  <read_first>
    - `tools/build.sh`'s `have_luastatic()` — after Task 1's fix, this predicate accepts the same
      PATH → Homebrew-keg → confirmed-bare-`lua` order `resolve_dev_lua()` uses, so it now returns
      true on this Mac without needing `lua5.4` literally on `command -v` PATH; this is the actual
      GATE `main()` checks before ever attempting `build_with_luastatic()` — live-verified this Mac
      already has `cc`/`clang` (Xcode CLT) and a real `lua5.4` via the Homebrew keg, but `luastatic`
      itself is entirely absent (`command -v luastatic` fails) and so is `luarocks`, the tool that
      installs it (`luastatic` ships as a Lua module distributed via luarocks, not a standalone
      Homebrew formula). Linux CI already installs it the same way (`tools/ci-setup-toolchain.sh`
      `install_linux()`: `luarocks install --local luastatic`) — this is the SAME already-used,
      already-locked project dependency (PROJECT.md D-02), just its FIRST macOS install.
    - Task 1's fixes (this plan) — BOTH the cflags/liblua restructure AND the have_luastatic() keg
      check — must already be in place before this task's build attempt: the cflags fix alone is not
      enough, since have_luastatic() gates whether build_with_luastatic() is even reached at all.
  </read_first>
  <files>tools/build.sh</files>
  <action>
    Install `luarocks` via Homebrew (`brew install luarocks`) if not already present, then install
    `luastatic` into the user's local luarocks tree (`luarocks install --local luastatic`), mirroring
    the exact command `tools/ci-setup-toolchain.sh install_linux()` already runs for Linux (same
    tool, same install verb, different OS's package manager providing the luarocks binary itself).
    Ensure the local luarocks bin directory (typically `~/.luarocks/bin` on macOS, matching
    `ci-setup-toolchain.sh`'s existing `${HOME}/.luarocks/bin` reference) is on PATH for this
    session. Then run `./tools/build.sh` from the repo root and confirm it takes the
    `build_with_luastatic()` path (its log line reads "luastatic toolchain present -> static
    single-binary build", not the dev-launcher line). Inspect the produced `dist/wez` with `file
    dist/wez` and confirm it reports a Mach-O executable (not a shebang-script dev launcher), then
    run `./dist/wez version` and confirm it prints a real version string and exits 0.
  </action>
  <acceptance_criteria>
    - `luastatic` resolves on PATH after installation (`command -v luastatic` succeeds).
    - `./tools/build.sh` on this Mac takes the luastatic path and produces `dist/wez` as a genuine
      Mach-O binary (confirmed via `file dist/wez`), not the dev source-launcher shim.
    - `./dist/wez version` runs successfully and prints non-empty output.
  </acceptance_criteria>
  <verify>
    <automated>command -v luastatic >/dev/null 2>&1 && ./tools/build.sh 2>&1 | tee /tmp/wez-build-07-02.log | grep -q "built static binary" && file dist/wez | grep -qi "Mach-O" && ./dist/wez version</automated>
    <fails_when>luastatic does not resolve on PATH after installation, OR tools/build.sh's own log reports the dev-launcher path instead of the static-binary path, OR `file dist/wez` does not report a Mach-O binary, OR `./dist/wez version` fails or prints nothing.</fails_when>
  </verify>
  <done>luastatic is installed on this Mac, tools/build.sh's static-binary path succeeds end-to-end, and dist/wez is confirmed to be a real, runnable Mach-O binary — closing MACOS-PARITY section C-1's luastatic Mach-O item with recorded evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Homebrew formula install (luarocks) → local toolchain | installing a build-time tool onto this developer machine; not a runtime dependency shipped to end users |
| luarocks module install (luastatic) → local toolchain | the SAME already-locked, already-used project build dependency (PROJECT.md D-02), previously only installed via apt+luarocks on Linux CI; this is its first macOS install, not a new dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | build_with_luastatic()'s macOS header/lib resolution silently picking up a wrong/stale liblua | low | mitigate | the new candidates are gated strictly behind `brew --prefix lua@5.4` (a specific, named keg) and an explicit file-existence test before use, mirroring the existing Linux candidate-list pattern exactly; no wildcard glob against arbitrary system paths is introduced. The keg cflags probe is ALSO ordered strictly BEFORE the generic `pkg-config --cflags lua` probe on macOS specifically, so a stock Homebrew Mac's now-5.5.x generic `lua.pc` (if pkg-config happens to be installed) cannot silently win over the project's actually-targeted `lua@5.4` keg headers |
| T-07-05 | Tampering | npm/pip/cargo-style unaudited package install | n/a | accept | out of scope for the Package Legitimacy Gate, which is explicitly scoped to npm/pip/cargo only; `luastatic`/`luarocks` are the project's pre-existing, already-locked (D-02) build dependency, unchanged in identity from the Linux CI install this project already runs |
</threat_model>

<verification>
- `bash -n tools/build.sh` is clean.
- `luastatic` is installed and `./tools/build.sh` takes the static-binary path on this Mac.
- `file dist/wez` reports a Mach-O executable; `./dist/wez version` runs successfully.
</verification>

<success_criteria>
The project's actual shipping build mechanism (luastatic single binary) is proven to work on real
Mac hardware for the first time, closing MACOS-PARITY-AND-FOLLOWUPS.md section C-1's Mach-O item,
and every later plan in this phase can build on a real static binary instead of the dev launcher.
</success_criteria>

<output>
Create `.planning/phases/07-macos-parity/07-02-SUMMARY.md` when done
</output>
