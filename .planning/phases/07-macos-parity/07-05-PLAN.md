---
phase: 07-macos-parity
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - docs/macos-verification.md
autonomous: false

requirements: [INST-01, INST-06, INST-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "tools/install.sh's real fetch/trap/hand-off glue is driven end-to-end on this Mac against THIS checkout's tools/setup.sh (via WEZ_BOOTSTRAP_CMD) — not the published curl|bash one-liner defaulting to whatever origin/main currently is, which a cross-AI review of this phase's plans found could silently exercise main's stub install_macos() instead of Plan 07-03's real one — and this is asserted from a real driven run, not read-the-script inference (D-02: agent-driven verification on the Mac)."
    - "make install / tools/setup.sh injects exactly one managed block into this Mac's real ~/.config/wezterm/wezterm.lua, with a timestamped backup, matching INST-01/INST-02's Linux-verified behavior."
    - "The full install -> doctor -> granular uninstall -> full uninstall -> reinstall cycle (docs/macos-verification.md Section 2) is driven for real and every observed divergence from the Linux baseline is recorded in the runbook's macOS Deviations table (D-03's evidence bar)."
    - "wez doctor is green on the resulting healthy install (DIAG-01's macOS half)."
  artifacts:
    - path: "docs/macos-verification.md"
      provides: "Section 2's Results-table rows and any newly-discovered macOS Deviations, filled in from a REAL run on this Mac, citing the exact section that proved each row"
  key_links:
    - from: "tools/install.sh"
      to: "tools/setup.sh"
      via: "the real one-liner (or its local WEZ_BOOTSTRAP_CMD-equivalent invocation) hands off to setup.sh exactly as it does on Linux, now exercised on macOS for the first time with a real install_macos() behind it (Plan 07-03)"
      pattern: "WEZ_REMOTE_BOOTSTRAP=1"
---

<objective>
Drive the real, install-mutating half of the macOS verification runbook
(`docs/macos-verification.md` Section 2 — Install/uninstall) on this actual Mac, against the real
`~/.config/wezterm/wezterm.lua` and the real `~/Applications`/`~/.local/bin`. This is the first task
in the phase that touches the developer's real system state rather than a scratch directory, and it
is the precondition every later live-session plan in this phase depends on (there must be a real,
healthy install in place before `wez doctor`, live WezTerm sessions, or scene launches make sense).

Purpose: closes INST-01/02/03/04/05/06's macOS-deferred status with real, recorded evidence (D-03's
evidence bar), and is the first genuine exercise of the real `install_macos()` (Plan 07-03) and the
build-time-codesigned real binary (Plan 07-02/07-04) together, end-to-end, on a real machine.

Output: a healthy, real install on this Mac for the rest of the phase to build on, plus the runbook's
Section 2 Results/Deviations rows filled in from real observation.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-macos-parity/07-CONTEXT.md
@docs/macos-verification.md
@tools/install.sh
@tools/setup.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Pre-flight backup + drive the real one-liner/setup.sh install on this Mac</name>
  <read_first>
    - `docs/macos-verification.md` Section 2 in full — the exact commands and expected stdout lines
      (`[setup] platform: os=macos arch=...`, `installed wez -> ~/.local/bin/wez`, `placing managed
      config -> ~/.config/wezterm/wezterm-setup`, seed-scenes lines, OSC 7 + completions registration
      lines) to compare against real output.
    - `tools/install.sh` in full (already read during planning) — the real one-liner's mechanics:
      `mktemp -d` + `trap ... EXIT` cleanup, codeload tarball fetch, hand-off to `setup.sh` with
      `WEZ_REMOTE_BOOTSTRAP=1`, `/dev/tty` revival for interactive prompts.
    - `tools/install.sh` line 76 — `ref="${WEZ_REF:-main}"` — a cross-AI review of this phase's plans
      flagged this as a HIGH-severity gap for THIS specific checkpoint: the published one-liner
      (`curl .../main/tools/install.sh | bash`) fetches a codeload tarball of `WEZ_REF` (default
      `main`) and hands off to THAT fetched checkout's `tools/setup.sh` — NOT this local working
      tree's `tools/setup.sh` — unless `WEZ_BOOTSTRAP_CMD` is set to override which script actually
      executes (see line 113, `bootstrap_cmd="${WEZ_BOOTSTRAP_CMD:-$setup}"`). Plan 07-03's real
      `install_macos()` may only exist on this local working tree's commits, not yet on whatever is
      currently published at `origin/main` (there is no guarantee the earlier Wave-1/Wave-2 plans'
      commits have been pushed by the time this checkpoint runs). Running the bare published
      one-liner as written could therefore silently exercise `main`'s STUB `install_macos()` instead
      of this phase's real implementation, passing the checkpoint while proving nothing about 07-03's
      actual code. This is why Task 1 below makes the LOCAL-CHECKOUT invocation (with
      `WEZ_BOOTSTRAP_CMD` pointed at THIS checkout's `tools/setup.sh`) the PRIMARY, always-correct
      method — not a fallback "if this work is not yet on main" — regardless of push state.
  </read_first>
  <precondition>Plan 07-02's codesigned real luastatic dist/wez and Plan 07-03's real install_macos() are both landed and committed on the branch this checkout is on.</precondition>
  <what-built>
    Nothing destructive has run yet. Before touching this Mac's real `~/.config/wezterm/wezterm.lua`,
    I will back it up myself (`cp ~/.config/wezterm/wezterm.lua /tmp/wezterm.lua.pre` if it exists),
    then drive the REAL installer via THIS checkout, not the published one-liner as a blind default
    (per the HIGH-severity gap recorded in `<read_first>` above — the published `curl .../main/...`
    form could silently fetch and run `main`'s STUB `install_macos()` instead of this phase's real
    one if the working tree's commits are not yet pushed): `WEZ_BOOTSTRAP_CMD=$(pwd)/tools/setup.sh
    bash tools/install.sh` run from this repo root. This still exercises install.sh's REAL fetch/
    trap/hand-off glue end-to-end (not a bypass of install.sh itself), but pins the actual EXECUTED
    setup.sh — the part that matters for testing 07-03's code — to THIS checkout via
    `WEZ_BOOTSTRAP_CMD`, which is unconditionally correct regardless of what is or is not currently
    published at `origin/main`. I will explicitly record in the SUMMARY that this checkpoint used the
    local-checkout form and WHY (citing this HIGH-severity finding), rather than silently defaulting
    to the published one-liner. (The published `curl .../main/tools/install.sh | bash` one-liner
    remains the correct form for a POST-MERGE sanity check once this phase's commits are actually on
    published `main` — that is explicitly OUT OF SCOPE for this checkpoint, not this task's job.)
    Either way, this is the REAL end-to-end installer, not a scratch-only simulation, and it now
    exercises the real `install_macos()` (WezTerm.app placement to `~/Applications`) for the first
    time on this machine, since WezTerm was confirmed absent here at the start of this phase.
  </what-built>
  <how-to-verify>
    1. I will run the install for real (via the local-checkout `WEZ_BOOTSTRAP_CMD` form above,
       exercising THIS checkout's `tools/setup.sh` — not a fetched `main` copy) and report, line by
       line against Section 2's expected output: whether WezTerm.app was placed under
       `~/Applications` (or reused if you already installed it yourself outside this flow), whether
       `wez` landed at `~/.local/bin/wez`, whether exactly one managed block was injected into
       `~/.config/wezterm/wezterm.lua` with a timestamped backup, and whether the
       seed-scenes/completions/OSC-7 registration lines appeared.
    2. I will check for a Gatekeeper/quarantine prompt or block during this real run (the D-07
       verify-then-decide question, using the REAL system Applications folder this time, not the
       scratch one Plan 07-03 already checked) and report exactly what happened.
    3. Review the reported outcome against Section 2's expected behavior. If anything diverges from
       the Linux baseline, tell me and I will log it in the runbook's macOS Deviations table before
       continuing — an honest divergence is a valid, useful finding, not a failure of this checkpoint.
  </how-to-verify>
  <resume-signal>Reply "approved" once you have reviewed the real install outcome and any Gatekeeper/quarantine observation, or describe what diverged.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Drive wez doctor + the full uninstall/reinstall cycle, then fill Section 2's Results and Deviations rows</name>
  <read_first>
    - `docs/macos-verification.md` Section 2's remaining bullets (re-install prompt TTY/no-TTY abort,
      `--force` idempotence, granular `KEEP_CONFIG=1` uninstall, full uninstall byte-identical to the
      pre-state backup, reinstall for the rest of the runbook) and Section 3's `wez doctor` bullets
      (green on a healthy install; fails loudly and names the failed gate after `make uninstall`).
    - Task 1's real install result (this task builds directly on it — do not re-run the initial
      install from scratch).
  </read_first>
  <what-built>
    Task 1 landed a real, healthy macOS install. I will now drive: `wez doctor` (expect exit 0, all
    four core gates passing); the no-TTY re-install abort (`./tools/setup.sh < /dev/null`, expect
    non-zero with explicit `--force`/`--restore`/`--skip` guidance); `--force` idempotence (still
    exactly one managed block); `make uninstall KEEP_CONFIG=1` (block + binary removed, config tree
    preserved); `make uninstall` (full — block excised, user lines byte-identical to your Task-1
    backup, binary gone); then `./tools/setup.sh --force` to leave you with a working install for the
    rest of this phase's live-session plans. Safety net: if the full-uninstall excision regresses on
    BSD `sed`/`grep` in a way that leaves your real `~/.config/wezterm/wezterm.lua` worse off than
    before, the immediate restore command is `cp /tmp/wezterm.lua.pre ~/.config/wezterm/wezterm.lua`
    (Task 1's backup path) — I will spell this out to you explicitly if any uninstall step diverges
    from the expected byte-identical outcome, not just note it in the runbook after the fact.
  </what-built>
  <how-to-verify>
    1. I will report each step's real exit code and observed behavior against the expectations above,
       including the BSD `cp -R` trailing-dot check (`ls ~/.config/wezterm/wezterm-setup/` shows no
       stray `.` entry) and `wez doctor`'s exact PASS/FAIL gate names.
    2. I will fill in `docs/macos-verification.md`'s Results table rows for INST-01 through INST-06
       and DIAG-01, and log any real divergence in the Deviations table with enough detail for a later
       fix, citing this session's evidence.
    3. Review the filled-in rows and the final `wez doctor` state.
  </how-to-verify>
  <resume-signal>Reply "approved" once you have reviewed the doctor/uninstall-cycle results and the runbook's filled-in rows, or describe what should change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Real one-liner install -> this Mac's actual dotfiles and ~/Applications | the first non-scratch, real-system-mutating action in this phase; a bug here affects the developer's real machine, not a disposable scratch dir |
| Real uninstall cycle -> the pre-existing wezterm.lua backed up in Task 1 | an uninstall bug could leave the user's real config in a worse state than before this phase started |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Tampering | real install mutating ~/.config/wezterm/wezterm.lua incorrectly | high | mitigate | Task 1 backs up the real file BEFORE any install step runs, and the existing sentinel-block excision logic (already Linux-verified, INST-01/04) is exercised, not reimplemented, on macOS here |
| T-07-14 | Repudiation | claiming Gatekeeper/quarantine behavior without observing the real system Applications folder | medium | mitigate | Task 1 explicitly checks for a real Gatekeeper prompt during THIS real install, distinct from and in addition to Plan 07-03's scratch-directory quarantine measurement |
</threat_model>

<verification>
- A real WezTerm.app + wez CLI is installed on this Mac via the real installer path.
- `wez doctor` exits 0 on the resulting healthy install.
- The full uninstall cycle leaves the pre-existing wezterm.lua user lines byte-identical to the Task-1 backup.
- docs/macos-verification.md's Section 2 Results/Deviations rows are filled in from this real run.
</verification>

<success_criteria>
INST-01/02/03/04/05/06 and DIAG-01's macOS-deferred status has real, recorded evidence from a genuine
end-to-end install/uninstall/reinstall cycle on this Mac, and the machine is left with a healthy
install for the remaining live-session plans in this phase.
</success_criteria>

<output>
Create `.planning/phases/07-macos-parity/07-05-SUMMARY.md` when done
</output>
