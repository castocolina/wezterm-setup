---
phase: 07-macos-parity
plan: 05
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - docs/macos-verification.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/MACOS-PARITY-AND-FOLLOWUPS.md
autonomous: true
requirements: [INST-01, INST-06, INST-07, FOUND-01, DIAG-05, PANE-01, PANE-02, PANE-03, PANE-04, SCEN-03, SCEN-04, SCEN-05, SCEN-06]
must_haves:
  truths:
    - "bash tools/verify-macos.sh passes its auto gate on this Intel Mac (FAIL=0, exit 0): build, suite, all __complete contexts, completion -n, scene-launch exit codes, copy-if-absent seeding"
    - "The runbook is driven AGENT-DRIVEN via a real harness (D-12): tmux + wezterm cli drive the keystroke/feature flows (panes, tabs, scenes, recipes, wez keys, __complete contexts), screencapture captures the live WezTerm window, and the agent-ui-ux-designer subagent judges the visuals from those screenshots — NO human drives the runbook and NO human visual sign-off"
    - "docs/macos-verification.md is driven top-to-bottom: every capability section has a result, the macOS deviations table is filled, and visual sections carry agent-ui-ux-designer notes fed by screencapture evidence"
    - "Every macOS-deferred D-18 status in REQUIREMENTS.md / ROADMAP coverage is flipped to Done autonomously on the green harness evidence, each flip citing the specific runbook section that proved it"
  artifacts:
    - path: "docs/macos-verification.md"
      provides: "Fully agent-driven runbook: Results table + macOS deviations table filled + ui-ux notes (from screencapture) for visual sections"
      contains: "Results table"
    - path: ".planning/REQUIREMENTS.md"
      provides: "D-18 statuses flipped to Done with runbook-section citations"
      contains: "macOS verified"
    - path: ".planning/ROADMAP.md"
      provides: "Coverage table flipped to match REQUIREMENTS"
      contains: "macOS verified"
  key_links:
    - from: "bash tools/verify-macos.sh"
      to: "PASS=N FAIL=0 exit 0"
      via: "auto parity gate on this Mac"
      pattern: "FAIL=0"
    - from: "tmux + wezterm cli + screencapture harness"
      to: "agent-ui-ux-designer notes in docs/macos-verification.md"
      via: "captured screenshots + transcript fed to the subagent (D-12, no human sign-off)"
      pattern: "screencapture"
    - from: "docs/macos-verification.md deviations table"
      to: ".planning/REQUIREMENTS.md / ROADMAP coverage"
      via: "each Done flip cites a runbook section (D-03 evidence bar)"
      pattern: "macOS verified.*§"
---

<objective>
Close the Phase 7 gate (autonomy item #5, D-09/D-12): drive the auto gate (`tools/verify-macos.sh`) and the full runbook (`docs/macos-verification.md`) top-to-bottom on this Intel Mac AGENT-DRIVEN via a real harness, and flip every "macOS deferred D-18" status to Done autonomously on the green evidence. Per D-12 the runbook is driven by a real feature-test harness — `tmux` + `wezterm cli` drive the keystroke/feature flows, `screencapture` captures the live WezTerm window, and the `agent-ui-ux-designer` subagent judges the visuals from those screenshots — NO human drives the runbook and NO human visual sign-off. Per the D-03 evidence bar, a flip requires: `verify-macos.sh` PASS for auto-checkable items + the runbook's macOS deviations table filled + `agent-ui-ux-designer` notes (fed by screencapture) for visual sections, each flip citing the runbook section that proved it. Systematically check macOS↔Linux parity for: keyboard shortcuts / `wez keys` (Cmd vs Super), every `wez` subcommand + options (`__complete` contexts, completion `-n`), recipes/scenes (`scene new --layout/--color` completion in zsh — the A-1 confirmation, scene-launch exit codes), and colors/glyphs/tab-bar accents/pane bg/emoji cell-width (runbook visual sections + ui-ux notes from screencapture). All evidence is Intel-runnable per D-01/D-03; arm64 first-launch on Apple Silicon is explicitly OUT OF SCOPE (non-gating Phase 7.1).

Purpose: declare macOS parity Done with recorded, agent-generated evidence — the final gate before v1.
Output: a fully agent-driven runbook (Results + deviations + ui-ux notes from screencapture), flipped REQUIREMENTS/ROADMAP/tracker statuses, and a green auto gate — all unattended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MACOS-PARITY-AND-FOLLOWUPS.md
@.planning/phases/07-macos-parity/07-CONTEXT.md
@.planning/phases/07-macos-parity/07-RESEARCH.md
@tools/verify-macos.sh
@docs/macos-verification.md
</context>

<artifacts_this_phase_produces>
- docs/macos-verification.md — Results table (every section: PASS/deviation), the macOS deviations table filled, and `agent-ui-ux-designer` notes (fed by screencapture screenshots of the live WezTerm window) appended to the visual sections (§5 pane/tab identity, §6 ad-hoc scenes glyphs/colors, tab-bar accents, emoji cell-width).
- .planning/REQUIREMENTS.md — the v1 checkbox list + Traceability table: each platform-sensitive ID's "macOS deferred D-18" qualifier replaced with "macOS verified D-18 (§<runbook section>)".
- .planning/ROADMAP.md — the Coverage table flipped to match REQUIREMENTS byte-for-byte (per ID).
- .planning/MACOS-PARITY-AND-FOLLOWUPS.md — §C C-1..C-8 checklist boxes ticked as the runbook sections pass; the A-1 zsh `--layout`/`--color` confirmation noted.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run verify-macos.sh green + drive the runbook AGENT-DRIVEN via the tmux/wezterm-cli/screencapture harness + agent-ui-ux-designer visual review</name>
  <files>docs/macos-verification.md, .planning/MACOS-PARITY-AND-FOLLOWUPS.md</files>
  <read_first>
    - tools/verify-macos.sh (the whole gate — §0 environment toolchain, §1 build+test, §2 __complete contexts, §3 completion scripts + the A-1 scene-layouts note, §4 scene-launch exit-code contract, §5 seed copy-if-absent, §6 skipped live/install checks; exit non-zero on any FAIL)
    - docs/macos-verification.md (drive top-to-bottom: §1 build/test line 104, §2 install/uninstall line 122, §3 foundation + doctor + CWD headline line 166/177, §4 diagnostics/keys line 197, §5 pane & tab identity line 234, §6 ad-hoc scenes line 268, §7 named scenes line 301; the Results table line 403; the macOS deviations table line 438 with the pre-identified candidate deviations line 449)
    - .planning/MACOS-PARITY-AND-FOLLOWUPS.md (§C C-1..C-8 by capability — the checklist to tick; A-1 zsh confirmation note line 30)
    - .planning/phases/07-macos-parity/07-RESEARCH.md (Phase Requirements → Test Map; Pitfall 6 BSD cp trailing-dot; Pitfall 8 zsh compinit insecure-dir; D-02 agent-ui-ux-designer review; D-03 evidence bar)
    - .planning/phases/07-macos-parity/07-CONTEXT.md (D-02/D-03 visual review model; D-08 BSD cp -R semantics to verify; D-09 full autonomy; D-12 the tmux/wezterm-cli/screencapture harness)
  </read_first>
  <action>
    Run `bash tools/verify-macos.sh` on this Intel Mac and confirm it exits 0 with FAIL=0 (build + suite + all 7 `__complete` contexts non-empty + completion `bash -n`/`zsh -n` + scene-launch exit-code contract + seed copy-if-absent). If any hard check FAILs, capture it, return the specifics for a gap-closure replan (do NOT mark green). Then drive docs/macos-verification.md top-to-bottom AGENT-DRIVEN via the harness (D-12 — no human drives this): start a live WezTerm session (the one placed by Plan 04 install_macos) and drive its keystroke/feature flows with `tmux` + `wezterm cli` (spawn/split panes, new tabs, launch scenes/recipes, run `wez keys`, exercise the `__complete` contexts) — capturing each step's terminal transcript. Cover: §2 install/uninstall (INST-01 sentinel pair; watch the BSD `cp -R config/wezterm-setup/. dst/` trailing-dot semantics — Pitfall 6/C-2, no stray `.` entry); §3 FOUND-01 CWD inheritance via OSC 7 (the headline macOS item — split panes/new tabs inherit cwd via `wezterm cli split-pane`/`spawn`), Cmd+K clear+scrollback (FOUND-02 maps to Command not Control), `wez doctor` green; §4 `wez keys` grouping/`--json`/source classification + completion install (note any zsh `compinit` insecure-dir warning under Homebrew fpath — Pitfall 8) + confirm A-1 `wez scene new --layout <Tab>`/`--color <Tab>` value completion in zsh runtime; §5 pane color/reset/title/persistence (PANE-01..04) + OSC 1337 SetUserVar; tab color/combined/priority/active-distinct (TAB-*); §6 all 4 layouts + per-pane commands + windowing; §7 scene launch happy path + error/exit codes + seeding + dynamic completion (SCEN-03..06).

    VISUAL-REVIEW PROTOCOL (D-02/D-12 — agent-driven on the Mac, fed by screencapture): for the VISUAL sections (§5 pane bg / tab-bar accents / emoji cell-width, §6 scene glyphs/colors), capture the live WezTerm window with `screencapture` (the macOS BSD-userland tool, zero installs — e.g. `screencapture -o <out.png>` of the live WezTerm window with the capability on screen) and invoke the `agent-ui-ux-designer` subagent. INPUT to the subagent: the specific runbook visual sub-sections under review (§5 pane & tab identity, §6 ad-hoc scenes) — name which capability is on screen (pane bg color/reset, tab-bar accent priority/active-distinct, emoji cell-width, scene glyph/color rendering) and pass the captured `screencapture` screenshot(s) + the harness transcript for each, one review pass per visual section. OUTPUT handling: append the subagent's notes verbatim INTO docs/macos-verification.md under the corresponding visual section's result row (an `agent-ui-ux-designer notes:` sub-block beneath §5 and beneath §6), so each visual section carries its own ui-ux note inline — these notes are the agent's evidence and the D-03 evidence-bar requirement for the PANE-* / SCEN-* visual flips in Task 2. There is NO human visual sign-off; the subagent's notes ARE the visual judgment. Do NOT summarize the notes away or relocate them to a separate file; they live with the section they reviewed.

    Fill the runbook Results table (one row per section: PASS or deviation) and the macOS deviations table (confirm or clear each pre-identified candidate). Tick the §C C-1..C-8 boxes in MACOS-PARITY-AND-FOLLOWUPS.md as sections pass. English only; verify-before-declaring-done (recorded transcript/screencapture output, no "should work").
  </action>
  <verify>
    <automated>bash tools/verify-macos.sh; echo "gate_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `bash tools/verify-macos.sh` prints `FAIL=0` in its summary and exits 0 on this Intel Mac.
    - BEHAVIOR (D-12): the runbook flows were driven by the `tmux` + `wezterm cli` harness (recorded transcripts) and the visuals captured with `screencapture` — NO human drove the runbook.
    - EVIDENCE: docs/macos-verification.md Results table has a row (PASS or explicit deviation) for §1-§7; the macOS deviations table is filled; the visual sections (§5, §6) each carry an inline `agent-ui-ux-designer notes:` block fed by the `screencapture` screenshots (D-02/D-12 protocol above) — no human visual sign-off.
    - BEHAVIOR: §3 FOUND-01 CWD inheritance is demonstrated live (via `wezterm cli` split/spawn) and recorded; `wez doctor` exits 0 in the live install; A-1 zsh `--layout`/`--color` value completion is confirmed and noted.
    - SOURCE: MACOS-PARITY-AND-FOLLOWUPS.md §C boxes are ticked for the sections that passed; `grep -c '\[x\]' .planning/MACOS-PARITY-AND-FOLLOWUPS.md` increased over its pre-plan value.
    - GAP-GATE: any verify-macos FAIL or runbook section failure is captured and returned for replan — no premature green.
  </acceptance_criteria>
  <done>verify-macos.sh auto gate is green (FAIL=0); the full runbook is driven AGENT-DRIVEN via the tmux/wezterm-cli/screencapture harness with Results + deviations + per-section ui-ux notes (D-02/D-12 protocol, no human sign-off); C-1..C-8 ticked.</done>
</task>

<task type="auto">
  <name>Task 2: Autonomously flip the macOS-deferred D-18 statuses to Done with runbook citations</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the v1 checkbox list lines 9-66 + the Traceability table lines 85-121 — every row carrying "macOS deferred D-18": INST-01, INST-06, INST-07, FOUND-01, DIAG-05, PANE-01..04, SCEN-03..06; the reconciliation note at the bottom)
    - .planning/ROADMAP.md (the Coverage table lines 375-413 — must agree byte-for-byte with REQUIREMENTS per ID; the Phase 7 success criteria lines 341-347; the Progress table Phase 7 row line 369)
    - docs/macos-verification.md (the section results from Task 1 — the citation source for each flip)
    - .planning/phases/07-macos-parity/07-CONTEXT.md (D-03 evidence bar — each flip cites the specific runbook section; D-01 — arm64 flips on the shared-build + codesign + Intel-proven parity contract, NO Silicon hardware run; D-09 — the flips happen autonomously on the green harness evidence, no manual approval)
    - .planning/phases/07-macos-parity/07-RESEARCH.md (Phase Requirements table mapping each ID → runbook section)
  </read_first>
  <action>
    On the green harness evidence from Task 1, flip the statuses AUTONOMOUSLY (D-09 — no manual approval gate). For each platform-sensitive requirement ID whose runbook section PASSED in Task 1, replace the "macOS deferred D-18" qualifier with "macOS verified D-18 (§<runbook section>)" in BOTH .planning/REQUIREMENTS.md (the v1 checkbox list AND the Traceability table) and .planning/ROADMAP.md (the Coverage table) — keeping the two tables byte-for-byte agreeing per ID (the reconciliation invariant). IDs to flip: INST-01 (§2), INST-06 (§2/prereq — install_macos `.app` placement proven in Plan 04), INST-07 (Plan 04 E2E + §2), FOUND-01 (§3 CWD), DIAG-05 (§4 completions + verify-macos §2/§3), PANE-01..04 (§5 pane identity + ui-ux notes), SCEN-03..06 (§7 named scenes + verify-macos §4/§5; SCEN-05 incl. the A-1 zsh `--layout`/`--color` confirmation). For arm64-specific concerns, cite the shared-build + codesign evidence (Plan 03/04 macos-14 smoke + dispatch dry-run) per D-01 — do NOT claim a Silicon hardware run. Update the Phase 7 row in the ROADMAP Progress table and the success-criteria checkboxes. Do NOT touch the Phase 7.1 (arm64 end-user) scope. If any single ID's runbook section did NOT pass, leave that ID deferred and surface it (do NOT flip on partial evidence). English only.
  </action>
  <verify>
    <automated>bash tools/check-d18-flip.sh 2>/dev/null || for id in INST-01 INST-06 INST-07 FOUND-01 DIAG-05 PANE-01 PANE-02 PANE-03 PANE-04 SCEN-03 SCEN-04 SCEN-05 SCEN-06; do echo "$id deferred=$(grep -F "$id" .planning/REQUIREMENTS.md | grep -c 'macOS deferred D-18')"; done</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE (ID-LEVEL flip gate — not a raw line count): for EACH of the 13 IDs (INST-01, INST-06, INST-07, FOUND-01, DIAG-05, PANE-01, PANE-02, PANE-03, PANE-04, SCEN-03, SCEN-04, SCEN-05, SCEN-06), the per-ID loop in `<verify>` reports `deferred=0` in .planning/REQUIREMENTS.md — i.e. NO row mentioning that ID still carries the "macOS deferred D-18" qualifier (an ID may appear in multiple rows — the checkbox list AND the Traceability table — so the gate is "this ID has zero remaining deferred rows", per-ID, NOT a single `grep -c >= 13` count which a multi-row ID could satisfy while another ID stays deferred). Run the same per-ID loop against .planning/ROADMAP.md and confirm each ID also reports `deferred=0` there.
    - AUTONOMY (D-09): the flips happened on the green harness evidence with NO manual approval gate.
    - SOURCE: every flipped ID reads "macOS verified D-18 (§...)" with a specific runbook-section citation (D-03 bar) in BOTH files; the checkbox list + Traceability table agree per ID.
    - SOURCE: the ROADMAP Phase 7 Progress row + success-criteria checkboxes reflect completion; the Phase 7.1 entry is untouched (no arm64 hardware claim).
    - CONSISTENCY: `diff <(grep -oE '(INST|FOUND|DIAG|PANE|SCEN)-[0-9]+ \| Phase [0-9.]+ \| [^|]+' .planning/REQUIREMENTS.md | sort) <(grep -oE '(INST|FOUND|DIAG|PANE|SCEN)-[0-9]+ \| Phase [0-9.]+ \| [^|]+' .planning/ROADMAP.md | sort)` shows no per-ID status divergence (or any divergence is explained).
    - EXCEPTION: if any single ID's runbook section did NOT pass in Task 1, that ID legitimately still reports `deferred=1` and MUST be surfaced (not silently flipped) — the gate then expects exactly that ID deferred and all others flipped.
  </acceptance_criteria>
  <done>Each of the 13 D-18 IDs reports zero remaining "macOS deferred D-18" rows (per-ID gate) and reads "macOS verified D-18 (§...)" in REQUIREMENTS + ROADMAP, flipped autonomously on the green harness evidence, tables agree per ID, arm64 cited via the shared-build contract, Phase 7.1 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live WezTerm session → verifier | The runbook executes real CLI behavior; verify-macos.sh uses scratch dirs only (non-destructive). |
| runbook §2 → ~/.config/wezterm | The install/uninstall steps mutate the real config (backed up first per the runbook). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18 | Tampering | runbook §2 mutates ~/.config/wezterm | mitigate | The runbook's documented pre-state backup (`cp ~/.config/wezterm/wezterm.lua /tmp/...pre`) runs before install; uninstall is reversible (make uninstall). |
| T-07-19 | Repudiation | a Done flip with insufficient evidence | mitigate | D-03 bar enforced: each flip cites a runbook section + verify-macos PASS + agent-ui-ux-designer notes (fed by screencapture); partial-evidence IDs stay deferred and are surfaced by the per-ID gate. |
| T-07-20 | Information disclosure | none — no secrets in scope | accept | verify-macos.sh is scratch-only; no PII/secrets; CI token already scoped (Plan 03). |
| T-07-SC | Tampering | no package installs in this plan | accept | This plan installs nothing (verification + doc flips only); the Wave-0/1 audits cover all installs. |
</threat_model>

<verification>
- `bash tools/verify-macos.sh` exits 0 with FAIL=0 on this Intel Mac.
- docs/macos-verification.md fully driven AGENT-DRIVEN via the tmux/wezterm-cli/screencapture harness (D-12): Results table + deviations table filled + agent-ui-ux-designer notes (from screencapture) on visual sections, no human sign-off; C-1..C-8 ticked.
- REQUIREMENTS + ROADMAP flips done autonomously (D-09): each of the 13 D-18 IDs reports zero remaining deferred rows (per-ID gate) and reads "macOS verified D-18 (§...)"; tables agree per ID; arm64 cited via shared-build; Phase 7.1 untouched.
- Any non-passing section/ID surfaced for replan, not flipped.
</verification>

<success_criteria>
- The auto gate is green (FAIL=0) and the full runbook is driven AGENT-DRIVEN (tmux/wezterm-cli/screencapture harness) with recorded evidence + ui-ux notes from screencapture (D-03 bar met, all Intel-runnable, no human sign-off).
- Keyboard/`wez keys`, every subcommand + completion contexts, scenes/recipes (incl. A-1 zsh completion + exit codes), and colors/glyphs parity are all checked via the harness.
- Every passing macOS-deferred D-18 status flipped to Done autonomously (D-09) with a runbook-section citation in REQUIREMENTS + ROADMAP; the per-ID gate confirms each of the 13 IDs has zero remaining deferred rows (tables agree per ID).
- arm64 flips rest on the shared CI/CD build + codesign + Intel-proven parity (D-01); Phase 7.1 remains separate + non-gating.
</success_criteria>

<output>
Create `.planning/phases/07-macos-parity/07-05-SUMMARY.md` when done
</output>
