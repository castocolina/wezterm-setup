---
phase: 07-macos-parity
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/run-tests.sh
  - tools/setup-dev.sh
  - Makefile
  - tools/build.sh
  - tests/cli/setup_dev_test.lua
autonomous: true
requirements: [DIAG-05, SCEN-03, SCEN-04, SCEN-05, SCEN-06]
user_setup:
  - service: xcode-clt
    why: "Apple clang for the luastatic link step (Mach-O wez). Already present on this Mac per RESEARCH; the setup target detects-and-instructs, never auto-sudos."
    dashboard_config:
      - task: "If `xcode-select -p` fails, run `xcode-select --install` and accept the GUI prompt (one-time, interactive)."
        location: "macOS system dialog"

must_haves:
  truths:
    - "On this Intel Mac, `make setup` (or tools/setup-dev.sh) installs lua@5.4 + luarocks + luastatic sudo-free and exposes lua5.4 on PATH"
    - "`bash tools/build.sh` produces a real luastatic dist/wez (not the dev source-launcher) after the setup target runs"
    - "`bash tools/run-tests.sh` discovers and runs every *_test.lua under stock bash 3.2 (no mapfile)"
  artifacts:
    - path: "tools/setup-dev.sh"
      provides: "Sudo-free macOS compile-toolchain setup (autonomy item #1): lua@5.4 keg + luarocks + luastatic; keg PATH export"
      contains: "lua@5.4"
    - path: "tools/run-tests.sh"
      provides: "bash-3.2-safe test discovery"
      contains: "while IFS= read -r"
    - path: "tests/cli/setup_dev_test.lua"
      provides: "Source-no-run TEXT + behavior gate for the new setup target"
  key_links:
    - from: "tools/setup-dev.sh"
      to: "brew --prefix lua@5.4"
      via: "keg-only PATH resolution"
      pattern: "brew --prefix lua@5\\.4"
    - from: "tools/run-tests.sh"
      to: "ALL_TESTS array"
      via: "while-read fill (replaces mapfile)"
      pattern: "while IFS= read -r f; do ALL_TESTS"
---

<objective>
Close the Wave-0 toolchain + harness-portability gaps that block every other Phase 7 step (CONTEXT D-08 "harness must run on stock macOS"; RESEARCH §C-1 "blocks everything"). This Mac has stock bash 3.2.57 and NO lua5.4/luastatic — so the suite cannot run and the CLI cannot build until both are fixed.

Two correlated fixes: (1) a sudo-free `tools/setup-dev.sh` compile-toolchain target (autonomy item #1) that installs `lua@5.4` (keg-only) + `luarocks` + `luastatic` and exposes `lua5.4` on PATH via `brew --prefix lua@5.4`; (2) replace the bash-4-only `mapfile` in `tools/run-tests.sh` with the repo's bash-3.2-safe `while IFS= read -r` idiom, and sweep any remaining bare `sha256sum` on a macOS-reachable build.sh path onto the existing `command -v sha256sum … else shasum -a 256` branch.

Purpose: make the harness runnable and the binary buildable on this Intel Mac so Waves 1-3 can execute.
Output: tools/setup-dev.sh (new), bash-3.2-safe tools/run-tests.sh, sha256 sweep on build.sh, a Makefile `setup` target, and tests/cli/setup_dev_test.lua.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-macos-parity/07-CONTEXT.md
@.planning/phases/07-macos-parity/07-RESEARCH.md
@.planning/phases/07-macos-parity/07-PATTERNS.md
@.planning/phases/07-macos-parity/07-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates/changes these symbols (downstream waves depend on them):
- `tools/setup-dev.sh` — new sudo-free dev-toolchain setup script: detects `xcode-select`, `brew install lua@5.4 luarocks`, `luarocks install luastatic`, exports `$(brew --prefix lua@5.4)/bin` onto PATH (and `$GITHUB_PATH` when set). Sourcing guard so it is unit-testable.
- `setup` target in `Makefile` (`@./tools/setup-dev.sh`).
- `tools/run-tests.sh` — `ALL_TESTS` filled by a `while IFS= read -r` loop (mapfile removed).
- `tools/build.sh` — every macOS-reachable checksum site uses the `command -v sha256sum … else shasum -a 256` branch (already true at lines 393-397; verify no bare `sha256sum` remains).
- `tests/cli/setup_dev_test.lua` — new auto-discovered unit test.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Make tools/run-tests.sh bash-3.2-safe (remove mapfile) + sweep build.sh sha256</name>
  <files>tools/run-tests.sh, tools/build.sh</files>
  <read_first>
    - tools/run-tests.sh (the file being modified — the TARGET to replace is the `mapfile -t ALL_TESTS` on L50; the ANALOG to copy is the `SHELL_SCRIPTS` `while IFS= read -r s` loop at L93-95; the downstream `ALL_TESTS` FILES filter at L52-65 and the second `while IFS= read -r f` integration read-loop at L70 are both LEAVE-UNTOUCHED — neither is the target)
    - tools/build.sh (lines 393-397 — the existing `command -v sha256sum … else shasum -a 256` branch to replicate; confirm no bare `sha256sum` remains on a macOS-reachable path)
    - .planning/phases/07-macos-parity/07-PATTERNS.md (§"`mapfile`→bash-3.2 loop"; §"Portable SHA-256 branch")
    - .planning/phases/07-macos-parity/07-RESEARCH.md (Pitfall 3 mapfile; Don't-Hand-Roll SHA-256 row)
  </read_first>
  <action>
    The ONLY line to replace in tools/run-tests.sh is the `mapfile -t ALL_TESTS < <(find "${TEST_ROOTS[@]}" -type f -name '*_test.lua' | sort)` at L50. Replace it with the repo's bash-3.2-safe array-fill idiom: initialize `ALL_TESTS=()` then `while IFS= read -r f; do ALL_TESTS+=("$f"); done < <(find "${TEST_ROOTS[@]}" -type f -name '*_test.lua' | sort)` — copying the SHAPE of the analog `SHELL_SCRIPTS` loop (`while IFS= read -r s; do … done`) already at L93-95 in the same file. Three constructs stay UNTOUCHED; do not confuse them with the target: (a) the shebang `#!/usr/bin/env bash` on line 1 and the separate `set -u` builtin around L20 are two DISTINCT constructs — leave both as-is (the shebang is not a `set` line; only the `mapfile` on L50 changes); (b) the downstream `ALL_TESTS` → FILES filter at L52-65 keeps consuming the array verbatim — do not alter its case/integration-skip logic; (c) the SECOND `while IFS= read -r f` read-loop at L70 (the `tests/integration` glob branch) is unrelated — it is neither the analog nor the target, leave it alone. In tools/build.sh, grep for any bare `sha256sum` invocation on a path reachable on macOS (the download_release/remote path); confirm each is already guarded by `command -v sha256sum >/dev/null 2>&1` with a `shasum -a 256` else-branch, and if any bare call remains, wrap it in that same branch (D-08, Claude's-discretion: branch on availability, not OS). Per CLAUDE.md, all comments in English.
  </action>
  <verify>
    <automated>/bin/bash tools/run-tests.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'mapfile' tools/run-tests.sh` returns 0 (mapfile fully removed).
    - SOURCE: `grep -c 'while IFS= read -r f; do ALL_TESTS' tools/run-tests.sh` returns 1.
    - SOURCE: in tools/build.sh, `grep -nE 'sha256sum' tools/build.sh | grep -v 'command -v sha256sum'` shows only lines inside a `command -v` guarded branch (no unguarded bare `sha256sum` on the macOS-reachable path).
    - BEHAVIOR: `/bin/bash tools/run-tests.sh` (stock bash 3.2 on this Mac) discovers and runs the suite — exit 0 once lua5.4 is present (Task 2). If lua5.4 is still absent it must fail with the explicit `'lua5.4' not found on PATH` message (exit 127), NOT a `mapfile: command not found` error.
  </acceptance_criteria>
  <done>run-tests.sh runs under stock bash 3.2 (no mapfile); build.sh has no unguarded sha256sum on the macOS path.</done>
</task>

<task type="auto">
  <name>Task 2: Create tools/setup-dev.sh sudo-free macOS compile-toolchain target + Makefile setup + test</name>
  <files>tools/setup-dev.sh, Makefile, tests/cli/setup_dev_test.lua</files>
  <read_first>
    - tools/ci-setup-toolchain.sh (the structural analog: `install_macos()` at lines 61-66 is the LATENT BUG — `brew install lua`=5.5; the `assert_and_capture()` fail-loud gate at lines 70-94; the platform_os dispatch + sourcing guard)
    - tools/build.sh (have_luastatic() at lines 92-96 — the predicate setup-dev must satisfy: luastatic + lua5.4 + cc/gcc all on PATH)
    - .planning/phases/07-macos-parity/07-RESEARCH.md (Pattern 2 keg-only lua@5.4 path; Installation block lines 116-130; Pitfall 1 + Pitfall 2)
    - .planning/phases/07-macos-parity/07-PATTERNS.md (§"`ci-setup-toolchain.sh` `install_macos()` lua@5.4 fix"; §"Sourcing guard")
    - tests/cli/bootstrap_update_test.lua (the source-no-run TEXT + behavior test pattern to mirror for tests/cli/setup_dev_test.lua)
    - Makefile (the thin-glue target convention — `setup: @./tools/setup-dev.sh`)
  </read_first>
  <action>
    Create tools/setup-dev.sh: a sudo-free DEV-toolchain provisioner for an end-developer's own Mac (autonomy item #1 — distinct from ci-setup-toolchain.sh which is the CI runner provisioner). Body: (1) detect Xcode CLT with `xcode-select -p >/dev/null 2>&1` and, if absent, log an instruction to run `xcode-select --install` then exit non-zero — DETECT-THEN-INSTRUCT, never auto-`sudo`, never auto-install Xcode (the GUI prompt is the user's one-time action; D-08 sudo-free). (2) On macOS (`platform_os`=macos, source tools/lib/platform.sh): `brew list lua@5.4 >/dev/null 2>&1 || brew install lua@5.4`; `brew list luarocks >/dev/null 2>&1 || brew install luarocks`; `command -v luastatic >/dev/null 2>&1 || luarocks install luastatic`. NEVER `brew install lua` (that is Lua 5.5 — the latent bug; Pitfall 1). (3) Resolve the keg-only prefix `PREFIX="$(brew --prefix lua@5.4)"`, `export PATH="${PREFIX}/bin:${PATH}"` (provides `lua5.4` for build.sh have_luastatic; Pitfall 2), and when `GITHUB_PATH` is set, append `${PREFIX}/bin` to it. (4) Reuse ci-setup-toolchain.sh's assert-and-capture spirit: fail loudly (non-zero) if `lua5.4` or `luastatic` is not on PATH after install, echoing `lua5.4 -v` and the luastatic version (via `luarocks show luastatic`). (5) On Linux, delegate to the existing `tools/ci-setup-toolchain.sh` (or print that the Linux dev path is apt-based) so the script is cross-platform-shaped but macOS is the verified path. Keep the `if [ "${BASH_SOURCE[0]}" = "${0}" ]; then main "$@"; fi` sourcing guard so the test can source it. Add a `setup` target to the Makefile `.PHONY` list and body (`@./tools/setup-dev.sh`) plus a help line. Author tests/cli/setup_dev_test.lua mirroring bootstrap_update_test.lua: TEXT asserts that the script installs `lua@5.4` (NOT bare `lua`), resolves `brew --prefix lua@5.4`, exports the keg bin onto PATH, echoes to `$GITHUB_PATH` when set, has the xcode-select detect-then-instruct branch, fail-loud asserts on missing lua5.4/luastatic, and has the sourcing guard; plus a `grep -v` non-comment assertion that no line invokes `brew install lua ` (bare, with trailing space) — the latent-bug regression guard. English only.
  </action>
  <verify>
    <automated>lua5.4 tests/cli/setup_dev_test.lua 2>/dev/null || lua tests/cli/setup_dev_test.lua</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c 'lua@5.4' tools/setup-dev.sh` >= 2 (install + prefix).
    - SOURCE: `grep -nE 'brew install lua ' tools/setup-dev.sh | grep -v '^[[:space:]]*#'` returns NOTHING (no bare `lua` formula — latent-bug guard).
    - SOURCE: `grep -c 'brew --prefix lua@5.4' tools/setup-dev.sh` >= 1 AND `grep -c 'GITHUB_PATH' tools/setup-dev.sh` >= 1.
    - SOURCE: `grep -c 'xcode-select' tools/setup-dev.sh` >= 1 (detect-then-instruct).
    - SOURCE: `grep -nE 'sudo ' tools/setup-dev.sh | grep -v '^[[:space:]]*#'` returns NOTHING (sudo-free invariant).
    - SOURCE: `grep -c 'setup' Makefile` shows the new target; `grep -c 'tools/setup-dev.sh' Makefile` >= 1.
    - BEHAVIOR: `bash -n tools/setup-dev.sh` exits 0; `lua5.4 tests/cli/setup_dev_test.lua` (or `lua`) reports `0 failed` and exits 0.
    - END-STATE (run on this Mac after the script): `make setup` completes; `command -v lua5.4` resolves and `lua5.4 -v` prints `Lua 5.4.x`; `command -v luastatic` resolves; `bash tools/build.sh` logs `luastatic toolchain present -> static single-binary build` and produces a runnable `dist/wez` (NOT the dev launcher).
  </acceptance_criteria>
  <done>tools/setup-dev.sh installs the sudo-free lua@5.4/luastatic toolchain, exposes lua5.4 on PATH, fails loud if absent; `make setup` then `make build` yields a real luastatic dist/wez; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Homebrew/LuaRocks → local toolchain | External formula/rock fetch crosses into the build toolchain (covered by RESEARCH Package Legitimacy Audit — all Approved). |
| network → none (harness) | run-tests.sh / setup-dev.sh perform no network beyond `brew`/`luarocks` (their own integrity). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `brew install lua@5.4` / `luarocks install luastatic` | accept | Homebrew core + project-locked LuaRocks rock; same packages the verified Linux CI leg installs (RESEARCH audit: Approved). No new registry. |
| T-07-02 | Elevation of Privilege | setup-dev.sh provisioning | mitigate | Sudo-free invariant enforced by the `grep -v '#' → no sudo` acceptance gate; Xcode CLT is detect-then-instruct, never auto-installed/sudoed. |
| T-07-03 | Tampering | wrong Lua formula (5.5) silently links the build | mitigate | The bare-`brew install lua` regression grep + the fail-loud `lua5.4`/`luastatic` assert prevent shipping against the wrong interpreter (Pitfall 1). |
| T-07-SC | Tampering | brew/luarocks installs | mitigate | slopcheck does not cover Homebrew/LuaRocks; all four externally-fetched items are `[VERIFIED]`/`[ASSUMED:project-locked]` in the RESEARCH audit — no `[SUS]`/`[SLOP]`. No blocking-human checkpoint required (no ASSUMED/SUS application packages). |
</threat_model>

<verification>
- `/bin/bash tools/run-tests.sh` runs under stock bash 3.2 (no `mapfile: command not found`).
- After `make setup` on this Mac: `lua5.4 -v` = 5.4.x, `luastatic` on PATH, `bash tools/build.sh` takes the luastatic path and ships a real `dist/wez`.
- `lua5.4 tests/cli/setup_dev_test.lua` exits 0.
- `bash -n tools/setup-dev.sh` and `bash -n tools/run-tests.sh` clean.
</verification>

<success_criteria>
- mapfile removed from run-tests.sh; suite discovers + runs on bash 3.2.
- tools/setup-dev.sh installs lua@5.4/luarocks/luastatic sudo-free and exposes lua5.4 on PATH; fails loud when absent.
- `make setup` then `make build` produces a luastatic dist/wez on this Intel Mac.
- No bare `sha256sum` remains on a macOS-reachable build.sh path.
- setup_dev_test.lua green; no bare `brew install lua` regression.
</success_criteria>

<output>
Create `.planning/phases/07-macos-parity/07-01-SUMMARY.md` when done
</output>
