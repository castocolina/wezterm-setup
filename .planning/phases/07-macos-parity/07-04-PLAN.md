---
phase: 07-macos-parity
plan: 04
type: execute
wave: 2
depends_on: ["07-02", "07-03"]
files_modified:
  - tools/install.sh
  - docs/macos-verification.md
autonomous: false
requirements: [INST-07, INST-06]
must_haves:
  truths:
    - "The agent dispatches the release workflow, then WAITS non-interactively (gh run watch --exit-status) for it to go green with both signed macOS assets + .sha256 uploaded"
    - "The real curl/install one-liner (or tools/install.sh) downloads the macOS wez binary, the integrity gate passes, and wez doctor exits 0 with a matching version on this Intel Mac"
    - "Quarantine is handled per D-07 verify-then-decide: probe com.apple.quarantine empirically; only add an xattr strip to install.sh if Gatekeeper actually blocks"
    - "The first real v* tag push is gated behind a human-verify checkpoint; the unattended dry-run stays fully autonomous"
  artifacts:
    - path: "docs/macos-verification.md"
      provides: "Filled prereq/§2 evidence: dispatch-dry-run run id, E2E install transcript, quarantine probe result"
      contains: "com.apple.quarantine"
    - path: "tools/install.sh"
      provides: "conditional xattr strip ONLY if the probe shows Gatekeeper blocking (else unchanged)"
      contains: "install.sh"
  key_links:
    - from: "agent (gh)"
      to: ".github/workflows/release.yml dispatch run"
      via: "gh workflow run + gh run watch --exit-status"
      pattern: "gh run watch"
    - from: "tools/install.sh (curl|bash)"
      to: "~/.local/bin/wez + wez doctor"
      via: "download → integrity gate → doctor exit 0"
      pattern: "wez doctor"
---

<objective>
Drive the unattended end-to-end supply→consume loop on this Intel Mac (autonomy item #4): dispatch the release workflow, WAIT non-interactively for it to go green with both signed macOS assets + `.sha256`, then run the real `curl|install.sh` one-liner (or tools/install.sh) and VERIFY the full chain — binary downloads, the per-asset SHA-256 integrity gate passes, Gatekeeper clears on first launch, `wez doctor` exits 0, version matches. Handle quarantine per D-07 verify-then-decide: PROBE `com.apple.quarantine` empirically (RESEARCH says curl downloads do NOT carry it) and ONLY add an `xattr -dr` strip to install.sh if Gatekeeper actually blocks — otherwise leave install.sh unchanged and keep the runbook's manual fallback note. Gate the first real `v*` tag push behind a blocking human-verify checkpoint (RESEARCH Open Q1) while keeping the unattended `workflow_dispatch` dry-run fully autonomous.

This wires existing primitives (gh run watch, the install.sh /dev/tty flow, the build.sh checksum gate) — the work is the unattended plumbing + the empirical D-07 decision, not new mechanism.

Purpose: prove the shipped macOS artifact installs and runs end-to-end (INST-07/INST-06 macOS) with recorded evidence.
Output: the dispatch-dry-run evidence + E2E transcript captured in docs/macos-verification.md, the D-07 quarantine decision (install.sh changed only if blocked), and a human-approved (or deferred) first real v* tag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-macos-parity/07-CONTEXT.md
@.planning/phases/07-macos-parity/07-RESEARCH.md
@.planning/phases/07-macos-parity/07-PATTERNS.md
@tools/install.sh
@tools/build.sh
@.github/workflows/release.yml
@docs/macos-verification.md
</context>

<artifacts_this_phase_produces>
- docs/macos-verification.md prereq + §2 + deviations rows filled with: the dispatched run id, `gh run watch --exit-status` exit 0, the macOS assets + `.sha256` list, the E2E `curl|install.sh` transcript, `xattr -p com.apple.quarantine ~/.local/bin/wez` probe result, `wez doctor` exit 0, and the version match.
- tools/install.sh — a guarded `xattr -dr com.apple.quarantine` strip ONLY if the probe shows Gatekeeper blocking (the DEFAULT outcome is NO code change to install.sh; D-07).
- The first real `v*` tag — pushed only after the human-verify checkpoint approves (or deferred to the maintainer with the dispatch dry-run standing as the macOS-leg proof).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch the release workflow (dry-run) and WAIT non-interactively for green macOS assets</name>
  <files>docs/macos-verification.md</files>
  <read_first>
    - .github/workflows/release.yml (the workflow_dispatch trigger + the D-07 channel-resolution step — a dispatch is a non-tag ref → CHANNEL=nightly → datestamped nightly-YYYYMMDD prerelease; the macOS legs added in Plan 03; the arm64 smoke). NOTE: `actionlint` static-lint on release.yml already ran clean in Plan 03 Task 2 (the static-lint owner) — this task does the LIVE dispatch run only and does NOT re-run actionlint (no double-lint; no skipped-lint gap).
    - .planning/phases/07-macos-parity/07-RESEARCH.md (Pattern 1 non-interactive CI wait; Code Examples "Resolve run-id and block on CI outcome" + "Verify a published asset downloads + checksum-matches"; Open Q3 arm64 evidence)
    - .planning/phases/07-macos-parity/07-PATTERNS.md (§"Run-the-binary evidence")
    - docs/macos-verification.md (§Prerequisites & environment lines 47-103; the Results table line 403; the deviations table line 438 — where evidence lands)
  </read_first>
  <action>
    Confirm `gh auth status` is authed on this Mac. Trigger the macOS-leg dry-run WITHOUT cutting a tag: `gh workflow run release.yml` (workflow_dispatch on the current branch → the D-07 step resolves CHANNEL=nightly). NOTE: do NOT re-run `actionlint` on release.yml — Plan 03 Task 2 already ran it clean (the static-lint pass is owned there); this task is the live-execution proof. Resolve the run id deterministically: `run_id="$(gh run list --workflow release.yml --limit 1 --json databaseId --jq '.[0].databaseId')"`. WAIT non-interactively: `gh run watch "$run_id" --exit-status --interval 10` (propagates pass/fail as exit code; on failure run `gh run view "$run_id" --log-failed` and stop). Once green, confirm BOTH macOS assets + their `.sha256` were uploaded to the resolved `nightly-YYYYMMDD` prerelease: `gh release view nightly-<date> --json assets --jq '.assets[].name'` should list `wez-macos-x86_64`, `wez-macos-x86_64.sha256`, `wez-macos-aarch64`, `wez-macos-aarch64.sha256` (plus the linux pair). Capture the arm64 in-build smoke log line (`codesign --verify` + `./dist/wez version` on the macos-14 leg) as the arm64 evidence (Open Q3 / D-06 — no local Silicon needed). Record all of this (run id, watch exit 0, asset list, arm64 smoke line) into docs/macos-verification.md (prereq/§2 evidence area + deviations table). If the dispatched run surfaces a real macOS-leg build issue (e.g. keg-only luastatic link flags — A2 risk), capture the failing log, return the specifics, and STOP for a gap-closure replan — do NOT silently mark green. English only.
  </action>
  <verify>
    <automated>gh run list --workflow release.yml --limit 1 --json databaseId,conclusion --jq '.[0] | "\(.databaseId) \(.conclusion)"'</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `gh run watch "$run_id" --exit-status` returned 0 (the dispatched dry-run is green).
    - BEHAVIOR: `gh release view nightly-<date> --json assets --jq '.assets[].name'` lists BOTH `wez-macos-x86_64`(+`.sha256`) AND `wez-macos-aarch64`(+`.sha256`).
    - EVIDENCE: docs/macos-verification.md records the run id, the watch exit 0, the macOS asset list, and the macos-14 arm64 smoke line (`codesign --verify` success + `dist/wez version` output).
    - SCOPE: this task does NOT re-run actionlint (Plan 03 owns the static lint); it records the live dispatch run only.
    - GAP-GATE: if the run fails, the failing-leg log is captured and returned for replan (no false green).
  </acceptance_criteria>
  <done>The workflow_dispatch dry-run is green; both signed macOS assets + .sha256 are published; arm64 smoke + run evidence recorded in the runbook; actionlint not re-run (owned by Plan 03).</done>
</task>

<task type="auto">
  <name>Task 2: Run the real E2E install on this Mac + D-07 quarantine verify-then-decide</name>
  <files>tools/install.sh, docs/macos-verification.md</files>
  <read_first>
    - tools/install.sh (the /dev/tty usable-probe idiom lines 116-130; the WEZ_REMOTE_BOOTSTRAP=1 handoff to bootstrap-wezterm.sh; main "$@" last line — the conditional xattr-strip site)
    - tools/build.sh (download_release() lines 340-409 — the channel resolve + per-asset .sha256 verify-before-chmod gate the E2E relies on)
    - tools/bootstrap-wezterm.sh (install_macos from Plan 02 — places ~/Applications/WezTerm.app)
    - .planning/phases/07-macos-parity/07-RESEARCH.md (Pitfall 5 curl≠quarantine; D-07 quarantine probe Code Example; A4 assumption; Don't-Hand-Roll "Interactive prompts under curl|bash" row)
    - .planning/phases/07-macos-parity/07-PATTERNS.md (§"conditional xattr strip — tools/install.sh (D-07, MAY BE NO-OP)" — the verify-then-act idiom; "The default outcome is no code change to install.sh")
    - docs/macos-verification.md (§Section 2 — Install / uninstall lines 122-165)
  </read_first>
  <action>
    Run the real consume side on this Intel Mac. Either the published one-liner (`curl -fsSL <raw install.sh url> | bash`) or `WEZ_CHANNEL=nightly bash tools/install.sh` (resolving the just-published nightly asset for `wez-macos-x86_64`). Verify the full chain end-to-end: (1) the binary downloads; (2) the per-asset SHA-256 integrity gate in build.sh download_release PASSES before chmod +x (a mismatch must abort — confirm it does not silently install); (3) `install_macos` places `~/Applications/WezTerm.app` (Plan 02) or an existing WezTerm is reused; (4) D-07 PROBE: `xattr -p com.apple.quarantine ~/.local/bin/wez` and `xattr -p com.apple.quarantine ~/Applications/WezTerm.app` — record present/absent. ONLY IF the probe shows quarantine present AND first launch is actually blocked by Gatekeeper, add a guarded `xattr -dr com.apple.quarantine` strip to install.sh (mirroring the usable-tty verify-then-act idiom) and document why; OTHERWISE make NO change to install.sh and keep the runbook's manual `xattr -d` / right-click-open fallback note (the expected default — Pitfall 5/A4). (5) `wez doctor` exits 0 on this Mac; (6) `wez version` matches the published asset's version. Capture the full transcript (download, checksum-verified line, quarantine probe result, doctor exit 0, version match) into docs/macos-verification.md §2 + the deviations table. English only.
  </action>
  <verify>
    <automated>wez doctor; echo "doctor_exit=$?"; xattr -p com.apple.quarantine "$(command -v wez)" 2>/dev/null && echo "QUARANTINED" || echo "no-quarantine (expected)"</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: the E2E install downloaded the macOS `wez` asset and the build.sh `checksum verified for wez-macos-x86_64` line appeared BEFORE chmod +x (integrity gate passed; never an unverified install).
    - BEHAVIOR: `wez doctor` exits 0 on this Mac; `wez version` matches the published nightly asset version.
    - DECISION (D-07): the quarantine probe result is recorded. If absent (expected): `grep -nE 'xattr -dr com.apple.quarantine' tools/install.sh | grep -v '^[[:space:]]*#'` returns NOTHING (install.sh unchanged). If present AND blocking: a guarded strip was added with a documented rationale.
    - EVIDENCE: docs/macos-verification.md §2 + deviations table contain the install transcript, the checksum-verified line, the quarantine probe result, the doctor exit 0, and the version match.
    - SOURCE: any install.sh change is sudo-free — `grep -nE 'sudo ' tools/install.sh | grep -v '^[[:space:]]*#'` returns NOTHING.
  </acceptance_criteria>
  <done>The macOS E2E install passes the integrity gate, Gatekeeper clears, wez doctor exits 0, version matches; D-07 decided empirically (install.sh changed only if Gatekeeper actually blocked).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Approve (or defer) the first real v* tag push</name>
  <files>(no source change — a maintainer git action: git tag/push only)</files>
  <action>
    PAUSE for the maintainer. Present the dispatch-dry-run + E2E evidence (run id, both signed macOS assets + .sha256, arm64 smoke line, E2E `wez doctor` exit 0) and ask whether to cut the first real `v*` tag now or defer it to the maintainer. Do NOT push a `v*` tag autonomously (RESEARCH Open Q1 — a tag push is a real public release). On "push": run `git tag vX.Y.Z && git push origin vX.Y.Z`, then `gh run watch <id> --exit-status` on the stable run and confirm the stable assets publish. On "defer": record that the dispatch dry-run stands as the macOS-leg proof and the stable tag is a later maintainer action (the nightly channel already auto-publishes). Either way, record the decision in docs/macos-verification.md.
  </action>
  <what-built>
    The macOS supply side (Plan 03) is correctness-verified AND live-proven via the workflow_dispatch dry-run (Task 1): both ad-hoc-signed macOS assets + `.sha256` published to a `nightly-YYYYMMDD` prerelease, arm64 smoke green, and the real E2E install (Task 2) passed the integrity gate with `wez doctor` exit 0 on this Intel Mac. Cutting a real `v*` tag triggers a PUBLIC stable release (RESEARCH Open Q1: a tag push is a real, irreversible-ish action) — so it is gated here per CONTEXT/RESEARCH rather than pushed autonomously.
  </what-built>
  <how-to-verify>
    1. Review the dispatch dry-run evidence in docs/macos-verification.md (run id, both signed macOS assets + .sha256, arm64 smoke line, E2E doctor exit 0).
    2. Decide: push the first real `v*` tag now, or defer to the maintainer.
       - To push: `git tag vX.Y.Z && git push origin vX.Y.Z`, then `gh run watch <id> --exit-status` on the stable run and confirm the stable assets publish.
       - To defer: the dispatch dry-run stands as the macOS-leg proof for the v1 close; the stable tag is a later maintainer action (the nightly channel already auto-publishes).
    3. Confirm the choice.
  </how-to-verify>
  <resume-signal>Type "pushed vX.Y.Z" (and the stable run is green) or "defer tag to maintainer"</resume-signal>
  <verify>
    <human-check>Maintainer confirms the choice: a pushed `v*` tag whose stable run is green (`gh run watch <id> --exit-status` returns 0), OR an explicit deferral to the maintainer with the dispatch dry-run standing as the macOS-leg proof.</human-check>
  </verify>
  <done>The first `v*` tag is either pushed with a green stable run, or explicitly deferred to the maintainer; the decision is recorded in docs/macos-verification.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub release (network) → local install | The published `wez` binary crosses into local execution via curl\|bash. |
| macOS Gatekeeper → first launch | The OS gates execution of a downloaded/self-built Mach-O. |
| tag push → public release | A `v*` tag triggers a public, hard-to-unpublish stable release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | MITM'd / wrong `wez` download | mitigate | Per-asset SHA-256 verified BEFORE chmod +x (build.sh download_release, portable shasum); a mismatch aborts non-zero — confirmed live in Task 2. |
| T-07-14 | Tampering | premature/unnecessary quarantine strip | mitigate | D-07 verify-then-decide: probe first; strip added ONLY if Gatekeeper actually blocks; default is no install.sh change (Pitfall 5). |
| T-07-15 | DoS | unsigned arm64 killed → user weakens Gatekeeper | mitigate | Ad-hoc codesign at build (Plan 03) clears the SIGKILL so users never disable Gatekeeper; arm64 evidence via the macos-14 smoke. |
| T-07-16 | Tampering | curl\|bash partial-stream execution | mitigate | install.sh invokes main() on the literal last line (existing) so a truncated stream can't run a half-command. |
| T-07-17 | Repudiation | premature public stable release | mitigate | First `v*` tag gated behind the blocking-human checkpoint (Open Q1); the autonomous path uses the dispatch dry-run only. |
</threat_model>

<verification>
- `gh run watch --exit-status` on the dispatched dry-run returns 0; both signed macOS assets + .sha256 published; arm64 smoke recorded (actionlint not re-run here — owned by Plan 03).
- E2E install on this Mac: integrity gate passes, Gatekeeper clears, `wez doctor` exit 0, version matches.
- D-07 decided empirically and recorded; install.sh changed only if Gatekeeper actually blocked (else unchanged, sudo-free).
- First `v*` tag pushed only after human approval, or explicitly deferred.
</verification>

<success_criteria>
- The unattended push(dispatch)→wait→install→verify loop runs green on this Intel Mac.
- Integrity gate proven (checksum-verified-before-chmod); wez doctor exit 0; version match.
- Quarantine handled per D-07 (probe → conditional strip only if blocked).
- Real v* tag gated behind the blocking-human checkpoint; dry-run autonomous.
- Evidence captured in docs/macos-verification.md.
</success_criteria>

<output>
Create `.planning/phases/07-macos-parity/07-04-SUMMARY.md` when done
</output>
