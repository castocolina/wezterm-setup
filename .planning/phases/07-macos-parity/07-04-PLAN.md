---
phase: 07-macos-parity
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - tools/build.sh
  - tools/ci-setup-toolchain.sh
  - .github/workflows/release.yml
autonomous: false

requirements: [INST-08]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "tools/build.sh ad-hoc-codesigns the built dist/wez binary at build time on macOS (D-06), so a self-built Mach-O never needs a manual codesign step before it will run. A codesign failure on macOS (when the codesign binary is present) is a FATAL build error, not a swallowed warning — per a cross-AI review finding that a non-fatal codesign failure would just defer the same failure to a less-informative point (the existing smoke-test step) while risking an unsigned binary shipping silently on a host where codesign fails for a real reason."
    - "tools/ci-setup-toolchain.sh's macOS leg installs the lua@5.4 keg this project actually targets, not the generic Homebrew lua formula (now a different major version, 5.5.x, than this project's Lua 5.4 target) - the exact CI toolchain drift release.yml's own comments already name."
    - "release.yml's build job is a real matrix over ubuntu-latest + macos-15-intel (x86_64) + macos-14 (arm64), each publishing its own codesigned wez-OS-ARCH asset - closing the macOS-legs-deferred-to-Phase-7 note the workflow's own header carries today."
    - "A real GitHub Actions run of the restored matrix is triggered and observed to complete successfully on all three legs, with real run evidence recorded - not just a syntax-correctness claim."
  artifacts:
    - path: "tools/build.sh"
      provides: "a codesign_macos_binary() step, gated on platform_os()==macos, run after build_with_luastatic() produces dist/wez"
    - path: "tools/ci-setup-toolchain.sh"
      provides: "install_macos() installing lua@5.4 (not the generic lua formula) and exporting its keg bin dir onto the job PATH/GITHUB_PATH"
    - path: ".github/workflows/release.yml"
      provides: "a strategy.matrix over the three OS/arch legs, each running the same ci-setup-toolchain.sh + build.sh (+ codesign) path, with the mapfile-based nightly-prune step scoped to run exactly once (ubuntu leg only) so it never executes under macOS's stock bash 3.2"
  key_links:
    - from: "tools/build.sh build_with_luastatic()"
      to: "tools/build.sh codesign_macos_binary()"
      via: "called immediately after the static binary is produced, before the existing smoke-test verify step"
      pattern: "codesign -s -"
    - from: ".github/workflows/release.yml build job"
      to: "tools/ci-setup-toolchain.sh"
      via: "each of the three matrix legs runs the same Install Lua toolchain step unchanged; only ci-setup-toolchain.sh's own OS branch differs"
      pattern: "strategy"
---

<objective>
Close two of ROADMAP Phase 7's named deferred gaps together, since they share the same file
(tools/build.sh) and the same downstream consumer (the release CI matrix): (1) D-06's auto ad-hoc
codesign of the built dist/wez at build time, and (2) restoring the macOS legs to
.github/workflows/release.yml's build matrix - deferred there explicitly ("the macOS legs are
DEFERRED TO PHASE 7 ... their toolchain needs on-Mac fixes that cannot be verified without a Mac:
Homebrew's lua is now 5.5 not 5.4"), a bug this very session independently rediscovered and fixed
in Plan 07-01/07-02's local build path. tools/ci-setup-toolchain.sh's macOS branch has the same
bug (brew install lua luarocks - the generic, now-5.5 formula) and must be fixed the same way
before the matrix can be safely restored.

Requirement note (per a cross-AI review of this phase's plans): this plan is tagged INST-08 (the
cross-platform CI matrix + codesigned per-OS/arch release assets requirement), not INST-07 (the
`curl|bash` one-liner, which is Plan 07-05's work). INST-08 is currently `Pending` in
REQUIREMENTS.md/ROADMAP.md's Coverage table (attributed to Phase 6, whose own `.github/workflows/
release.yml` header explicitly deferred the macOS legs to Phase 7) rather than a "macOS deferred
D-18" item like INST-01/06/07 — this plan is what actually closes it, on both platforms, not a
macOS-only status flip. ROADMAP.md's Phase 7 `**Requirements:**` line and Coverage table are updated
in Plan 07-07 to reflect INST-08 as a Phase-7-closed item, citing this plan.

Purpose: without this plan, macOS wez release assets are never built or published anywhere, and a
self-built Apple Silicon Mach-O needs a manual codesign -s - step the ROADMAP explicitly wants
automatic (D-06).

Output: build-time ad-hoc codesign on macOS, a fixed CI toolchain installer, a restored 3-leg matrix,
and a real, observed GitHub Actions run proving all three legs build and publish successfully.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-macos-parity/07-CONTEXT.md
@.planning/MACOS-PARITY-AND-FOLLOWUPS.md
@tools/build.sh
@tools/ci-setup-toolchain.sh
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add build-time ad-hoc codesign to tools/build.sh (D-06)</name>
  <read_first>
    - tools/build.sh lines 101-158 (build_with_luastatic) - the point right after
      chmod +x "${OUT}" / log "built static binary: ${OUT}" where a new codesign step belongs, and
      tools/lib/platform.sh's platform_os (already sourced at the top of build.sh) as the gate.
    - tools/verify-macos.sh lines 74-78 - the existing informational-only Apple-Silicon codesign
      probe ("$WEZ" version >/dev/null 2>&1 || note "... try: codesign -s - '$WEZ' ...") - this
      plan makes that manual suggestion unnecessary by signing automatically at build time; leave the
      probe itself untouched (it stays a harmless double-check for a binary built by some other path).
    - .planning/phases/07-macos-parity/07-CONTEXT.md D-06 - "Auto ad-hoc codesign at build time -
      tools/build.sh / CI runs codesign -s - dist/wez on the darwin-aarch64 (and darwin-x86_64) asset
      before publishing" (D-01: both Apple Silicon and Intel Macs are in scope for this decision, even
      though this specific execution session only has x86_64 hardware to run it on directly).
  </read_first>
  <files>tools/build.sh</files>
  <action>
    Add a new function codesign_macos_binary() to tools/build.sh that, when platform_os reports
    macos and command -v codesign succeeds, runs codesign -s - "${OUT}" (ad-hoc signing - no
    Developer ID, no notarization, per D-06's explicit scope) and logs the outcome. A cross-AI review
    of this phase's plans flagged the ORIGINAL draft's "log a warning, don't abort" framing as wrong:
    on Apple Silicon an unsigned binary is inert (SIGKILL on first run), so main()'s own existing
    smoke-test step (`"${OUT}" version`) would fail the build anyway a few lines later - just with a
    confusing "no output" message instead of a clear codesign error, AND without protecting an x86_64
    build from silently shipping an actually-unsigned binary if codesign failed for some other real
    reason. FIX: make a codesign failure FATAL when `platform_os` is macos AND `command -v codesign`
    succeeds (abort the build with a clear "codesign failed" error naming the exact command that
    failed) - codesign is only SKIPPED, non-fatally, when the `codesign` binary itself is absent
    (a defensive belt-and-braces case platform_os's own gate should already prevent, but still handled
    correctly). Call this function from build_with_luastatic() immediately after chmod +x "${OUT}",
    before its own log line, so every macOS build of the static binary is signed as part of the same
    build step CI will also run - do not add a second, separate invocation point. Do NOT apply this to
    build_dev_launcher()'s output (a bash script, not a Mach-O binary - codesign does not apply). After
    landing the change, run ./tools/build.sh on this Mac again and confirm codesign -v dist/wez exits 0
    (a valid, if ad-hoc, signature is present). Record explicitly in the SUMMARY that this machine is
    x86_64, so the Apple-Silicon SIGKILL-avoidance behavior this step specifically targets is exercised
    structurally (codesign runs and produces a valid signature, and is now a FATAL gate rather than a
    warning) but not runtime-verified on arm64 hardware BY THIS TASK (per D-01, both architectures are
    in scope for the decision; only x86_64 is physically available to this task) - do not claim arm64
    runtime verification that did not happen here. (Task 3's real GitHub Actions macos-14 leg, once it
    builds AND smoke-tests successfully, IS genuine arm64 runtime evidence for D-01/INST-08 - cite that
    leg's outcome, not this task's x86_64-only local run, as the Apple-Silicon evidence source; see
    Task 3 and Plan 07-07.)
  </action>
  <acceptance_criteria>
    - tools/build.sh gains a codesign_macos_binary() function, gated on platform_os()==macos
      and command -v codesign, called from inside build_with_luastatic() right after the binary
      is made executable.
    - A codesign failure on macOS, WHEN the codesign binary itself is present, is FATAL - it aborts
      the build with a clear error naming codesign as the cause. Codesign is skipped (non-fatally)
      ONLY when `command -v codesign` itself fails.
    - On this Mac, after the change, codesign -v dist/wez exits 0.
    - The SUMMARY explicitly states this task's own local verification was on x86_64, and that the
      real arm64/Apple-Silicon runtime evidence for D-01/INST-08 comes from Task 3's macos-14 CI leg
      (once observed to build and smoke-test successfully), not from this task's local run.
  </acceptance_criteria>
  <verify>
    <automated>bash -n tools/build.sh && grep -q "codesign_macos_binary" tools/build.sh && ./tools/build.sh 2>&1 | tail -5 && codesign -v dist/wez && echo CODESIGN_OK</automated>
    <fails_when>tools/build.sh has a syntax error, OR no codesign_macos_binary function/call exists, OR codesign -v dist/wez fails after a fresh build on this Mac.</fails_when>
  </verify>
  <done>tools/build.sh ad-hoc-signs dist/wez automatically on macOS builds, verified on this x86_64 Mac; Apple-Silicon runtime confirmation is explicitly flagged as pending real Silicon hardware, not falsely claimed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ci-setup-toolchain.sh's macOS lua@5.4 install and restore the macOS legs in release.yml's build matrix</name>
  <read_first>
    - tools/ci-setup-toolchain.sh lines 61-66 (install_macos) - currently brew install lua
      luarocks then luarocks install luastatic (WITHOUT --local, unlike install_linux() below). lua
      is the generic, now-Lua-5.5.x formula (the same root-cause bug Plans 07-01/07-02 found and fixed
      for the local build path); the subsequent assert_and_capture() (lines 70-94) calls lua5.4 -v and
      command -v lua5.4 - both of which will fail if only the generic lua was installed, since
      lua@5.4 is a separate, keg-only formula that Homebrew does not symlink onto PATH by default. A
      cross-AI review of this phase's plans flagged a SECOND, independent gap here beyond the wrong
      formula name: `luarocks install luastatic` (no `--local`) may install to a system-wide tree
      requiring sudo/write-perms this runner may not have, and even if it succeeds, nothing puts
      `~/.luarocks/bin` on PATH for the subsequent build step - install_linux() (below) does BOTH
      `--local` AND the PATH/GITHUB_PATH export; install_macos() currently does NEITHER.
    - tools/ci-setup-toolchain.sh lines 41-59 (install_linux) - the existing pattern to mirror for
      BOTH the luarocks install verb AND exporting a keg/local bin dir onto the job's PATH:
      `luarocks install --local luastatic`, then PATH="${HOME}/.luarocks/bin:${PATH}" + export PATH,
      then appending to "${GITHUB_PATH}" when running under Actions.
    - .github/workflows/release.yml lines 1-53 - the header comment explicitly defers the macOS
      matrix legs to Phase 7 and names the runner labels already vetted (macos-15-intel for x86_64,
      macos-14 for arm64, per "P6-D01 / State of the Art" - macos-15-intel supersedes the removed
      macos-13 label). Lines 44-53 (build: job) is currently a single hardcoded ubuntu-latest
      job, no strategy.matrix. Line 122's comment ("the Apple-Silicon ad-hoc codesign step lives
      only on the macOS arm64 leg, which is deferred to Phase 7") is now resolved by Task 1's
      codesign_macos_binary(), which is OS-gated internally (not per-leg YAML logic) - no separate
      YAML codesign step is needed; ./tools/build.sh already codesigns on any macOS leg by itself.
    - .github/workflows/release.yml lines 204-228 (Prune old nightly releases) - uses mapfile -t
      nightly_tags. Once this step runs under a matrix, it would execute on every leg unless scoped -
      the existing comment above it ("Distinct factored step so a Phase 7 matrix can wrap
      build/publish without touching prune (D-12)") signals the intended fix is to scope this step
      to run exactly once, not to bash-3.2-proof the mapfile line itself (pruning is a global, not
      per-asset, action - running it 3x is wasteful even where it would not break).
  </read_first>
  <files>tools/ci-setup-toolchain.sh, .github/workflows/release.yml</files>
  <action>
    In tools/ci-setup-toolchain.sh's install_macos(): change "brew install lua luarocks" to
    "brew install lua@5.4 luarocks" (the keg-only formula this project actually targets, matching
    Plan 07-02's local-machine finding). Since lua@5.4 is keg-only, add the same PATH-export
    pattern install_linux() already uses: resolve keg="$(brew --prefix lua@5.4)", prepend
    "${keg}/bin" onto PATH for the rest of this script's execution, and, when "${GITHUB_PATH}"
    is set, append "${keg}/bin" to it so the subsequent tools/build.sh step (a separate CI step)
    also sees it. ALSO change "luarocks install luastatic" to "luarocks install --local luastatic"
    (mirroring install_linux()'s exact verb) and, exactly as install_linux() already does, prepend
    "${HOME}/.luarocks/bin" onto PATH for this script's own execution and append it to
    "${GITHUB_PATH}" when set — install_macos() currently does neither the `--local` install nor
    the PATH export, a gap a cross-AI review of this phase's plans flagged independently of the
    wrong-formula-name bug: even with lua@5.4 correctly installed, `command -v luastatic` in
    assert_and_capture() (lines 70-94) could still fail on the macOS leg without this fix.

    In .github/workflows/release.yml's build job: convert it to a real strategy.matrix with
    three legs - one for ubuntu-latest, one for macos-15-intel (x86_64), one for macos-14
    (arm64) - selecting the runner image per leg via runs-on referencing the matrix. Every existing
    step (Checkout, Resolve channel, Skip if unchanged, Install Lua toolchain, Build dist/wez,
    Name + checksum asset, Ensure release exists, Upload release asset) stays unchanged and runs
    on every leg exactly as it already does for the single ubuntu leg today - tools/build.sh's
    OS-gated codesign_macos_binary() (Task 1) and tools/lib/platform.sh's platform_os/
    platform_arch naming already make each leg publish its own correctly-named
    wez-OS-ARCH/.sha256 asset with no per-leg YAML branching needed beyond runs-on. Add an
    if: guard to the "Prune old nightly releases" step scoping it to exactly one leg (combine a
    new leg-identity condition with the existing channel/unchanged guard, so it fires only on the
    ubuntu leg AND only when channel is nightly AND only when not already-unchanged) - this both
    avoids running the cleanup three times AND means its mapfile-based body never executes under a
    macOS runner's stock bash 3.2, closing the exact portability gap this project's D-08 already
    guards against everywhere else. Update the file's header/build-job comments to reflect that the
    macOS legs are now live, not deferred. Run bash -n over any inline multi-line run: blocks you
    touch, and if actionlint is available on this machine, run it over the whole file and resolve
    any findings; if it is not installed, note that in the SUMMARY rather than silently skipping the
    check.
  </action>
  <acceptance_criteria>
    - ci-setup-toolchain.sh install_macos() installs lua@5.4 (not the bare lua formula) and
      exports its keg bin dir onto PATH/GITHUB_PATH.
    - ci-setup-toolchain.sh install_macos() also installs luastatic via `luarocks install --local
      luastatic` (matching install_linux()'s verb) and exports `${HOME}/.luarocks/bin` onto
      PATH/GITHUB_PATH the same way install_linux() already does.
    - release.yml's build job is a real 3-leg matrix (ubuntu-latest, macos-15-intel, macos-14),
      with every existing build/publish step preserved unchanged across all legs.
    - The "Prune old nightly releases" step runs on exactly one leg (never on either macOS leg), so
      its mapfile usage never executes under macOS's stock bash.
    - actionlint (if available) reports no new findings against release.yml.
  </acceptance_criteria>
  <verify>
    <automated>bash -n tools/ci-setup-toolchain.sh && grep -q "lua@5.4" tools/ci-setup-toolchain.sh && sed -n '/^install_macos()/,/^}/p' tools/ci-setup-toolchain.sh | grep -q "luarocks install --local luastatic" && grep -q "strategy:" .github/workflows/release.yml && grep -q "macos-15-intel" .github/workflows/release.yml && grep -q "macos-14" .github/workflows/release.yml; command -v actionlint >/dev/null 2>&1 && actionlint .github/workflows/release.yml || echo "actionlint not installed - noted in SUMMARY"</automated>
    <fails_when>ci-setup-toolchain.sh has a syntax error, OR install_macos still installs the bare lua formula instead of lua@5.4, OR install_macos()'s own function body (isolated via sed) does not contain `luarocks install --local luastatic`, OR release.yml lacks a strategy.matrix with all three runner labels, OR the prune step lacks a leg-scoping guard, OR actionlint (when installed) reports a finding against release.yml.</fails_when>
  </verify>
  <done>ci-setup-toolchain.sh installs the correct Lua 5.4 keg AND luastatic (via --local + PATH export, mirroring install_linux()) on macOS, and release.yml's build job is a real 3-leg matrix with the nightly-prune step safely scoped to a single leg.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Trigger a real GitHub Actions run and confirm all three matrix legs build and publish successfully</name>
  <read_first>
    - .github/workflows/release.yml's workflow_dispatch trigger (already present, no inputs) - the
      way to fire this run on demand without waiting for the weekly cron or a tag push.
    - STATE.md's existing CI history ("RESOLVED + LIVE-VERIFIED (2026-06-15): CI run green (build
      linux-x86_64, 22s)") - the precedent this task extends to the two new macOS legs.
  </read_first>
  <precondition>gh CLI is authenticated against this repository (gh auth status succeeds) and the
  release.yml changes from Task 1/2 are committed to the branch the workflow will run against.</precondition>
  <what-built>
    Tasks 1-2 landed build-time ad-hoc codesign and restored the macOS legs to release.yml's build
    matrix, with the toolchain-install fix that makes them viable. This is the FIRST time this
    workflow has ever attempted a macOS build - it has only ever run its single ubuntu leg for real.
  </what-built>
  <how-to-verify>
    1. I will run `gh workflow run release.yml` (or push a no-op trigger) to fire a real, on-demand
       run of the restored matrix, then poll `gh run watch` / `gh run view` until all three legs
       (ubuntu-latest, macos-15-intel, macos-14) finish.
    2. I will report each leg's outcome, and for any macOS leg that publishes an asset, confirm via
       `gh release view <tag>` that a `wez-macos-x86_64` and/or `wez-macos-aarch64` asset (with its
       matching `.sha256`) actually landed on the resulting release.
    3. Review the reported per-leg outcomes and the run URL. A leg failure is an honest, valid signal
       to fix and re-run - not something to silently paper over as "close enough".
    4. IMPORTANT for Plan 07-07's later status-flip decision: I will explicitly record in this
       SUMMARY whether the `macos-14` (arm64) leg's own build step AND its `tools/build.sh` smoke-test
       (`"${OUT}" version`, which runs AFTER Task 1's now-FATAL codesign step) both succeeded. A green
       `macos-14` leg is genuine, real Apple-Silicon RUNTIME evidence for D-01/INST-08 (the ad-hoc
       signature let the arm64 Mach-O actually execute without SIGKILL) — Plan 07-07 gates its
       INST-08/darwin-aarch64 status flip on THIS specific leg's recorded outcome, not on Task 1's own
       x86_64-only local run. If `macos-14` failed or never ran, I will say so explicitly rather than
       letting a generic "matrix restored" summary imply arm64 was verified when it was not.
  </how-to-verify>
  <resume-signal>Reply "approved" once you have reviewed the real run URL and each leg's outcome, or describe what should change before re-running.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| release.yml build matrix -> public GitHub Releases | a broken macOS leg could publish a corrupt or non-functional asset to a real, public release that end users' installers/update mechanisms will fetch |
| ad-hoc codesign -> Gatekeeper | ad-hoc signing (codesign -s -) provides no developer-identity guarantee, only satisfies the OS's requirement that arm64 binaries carry SOME signature |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10 | Tampering | ad-hoc-signed dist/wez asserted as trustworthy beyond what ad-hoc signing actually provides | low | accept | ad-hoc signing's limited guarantee is already documented in this project's own CONTEXT.md D-06 discussion; this plan does not claim more than "runs without Gatekeeper/SIGKILL friction" |
| T-07-11 | Denial of Service | a broken macOS CI leg silently publishing a non-functional wez-macos-* asset | high | mitigate | Task 3 is a real, observed GitHub Actions run with per-leg outcome review before this plan is considered complete - a failing leg is reported, not silently accepted |
| T-07-12 | Tampering | the nightly-prune step's mapfile usage executing under macOS bash 3.2 if the leg-scoping guard is wrong | medium | mitigate | the guard combines the existing channel/unchanged condition with a new leg-identity condition, verified by this plan's own acceptance criteria before Task 3 ever runs a real workflow |
</threat_model>

<verification>
- codesign -v dist/wez exits 0 on this Mac after a fresh build.
- release.yml contains a 3-leg strategy.matrix with the exact runner labels macos-15-intel and macos-14, and actionlint (if available) is clean.
- A real, triggered GitHub Actions run completes with all three legs reported and their outcomes reviewed.
</verification>

<success_criteria>
Build-time ad-hoc codesign closes D-06; the CI toolchain's Lua-version bug is fixed before the macOS
matrix legs are restored; a real, observed GitHub Actions run proves the restored matrix actually
builds and publishes macOS assets, not just that the YAML parses.
</success_criteria>

<output>
Create `.planning/phases/07-macos-parity/07-04-SUMMARY.md` when done
</output>
