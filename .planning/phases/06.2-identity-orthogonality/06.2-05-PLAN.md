---
phase: 06.2-identity-orthogonality
plan: 05
type: execute
wave: 4
depends_on:
  - 06.2-03
  - 06.2-04
files_modified:
  - scenes/dev.toml
  - scenes/ai.toml
  - scenes/docker.toml
  - docs/repro/h-06.2-identity-orthogonality.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "The seed recipes use the explicit icon attribute (icon key) instead of an icon-name leading title word (D-03/D-05)"
    - "wez scene launch <seed> from a real WezTerm session renders icon + literal title, the split tab/pane colors with explicit-tab-color-wins, and a cwd-basename fallback (D-04/D-08/D-11/D-13)"
    - "The full co-located test suite stays green after all 6.2 changes"
  artifacts:
    - path: "scenes/dev.toml"
      provides: "explicit icon keys; literal {cwd} titles (D-03/D-05)"
      contains: "icon"
    - path: "scenes/ai.toml"
      provides: "explicit icon keys (D-03/D-05)"
      contains: "icon"
    - path: "scenes/docker.toml"
      provides: "explicit icon keys (D-03/D-05)"
      contains: "icon"
    - path: "docs/repro/h-06.2-identity-orthogonality.md"
      provides: "recorded live-session repro of icon render + color precedence + cwd fallback"
      contains: "WEZTERM_TAB_ICON"
  key_links:
    - from: "scenes/dev.toml"
      to: "cli/lib/recipe.lua + cli/commands/scene.lua"
      via: "the refreshed recipes load through the icon-aware schema and emit WEZTERM_TAB_ICON on launch (D-03)"
      pattern: "icon"
---

<objective>
Land the user-visible payoff of Phase 6.2: refresh the three seeded scene recipes to
use the explicit icon attribute (completing the G-1 migration in the shipped seeds),
run the full suite green across every 6.2 change, and verify the genuinely visual
behaviors against a LIVE WezTerm session — icon render, the split tab/pane color
precedence, and the empty-title cwd fallback — per the project hypothesis policy
(no "should work": a recorded repro or wez doctor output before declaring done).

This plan is NON-autonomous: a human must observe the terminal to confirm the visual
render. Automated tests (Plans 01-04) cover the logic; the live repro covers the
actual paint, which is not headlessly assertable.

Purpose: D-03/D-05 (seed scenes use explicit icon, not the dropped first-word swap),
D-04/D-08/D-11/D-13 (live confirmation of compose + precedence + fallback).
Output: refreshed seed recipes + a recorded repro doc; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-identity-orthogonality/06.2-CONTEXT.md
@.planning/phases/06.2-identity-orthogonality/06.2-PATTERNS.md
@scenes/dev.toml
@scenes/ai.toml
@scenes/docker.toml
</context>

<artifacts_this_phase_produces>
- Refreshed seeds scenes/dev.toml, scenes/ai.toml, scenes/docker.toml using explicit icon keys
- Recorded live repro docs/repro/h-06.2-identity-orthogonality.md
- This plan's SUMMARY records the full Phase 6.2 artifact inventory: subcommands wez tab/pane icon; flags --icon, --follow-pane-color; user vars WEZTERM_TAB_ICON, WEZTERM_PANE_COLOR, the follow carrier; recipe keys icon, follow_pane_color; lib functions title.resolve_icon, title.fallback_title, tab/pane run_icon, format-tab-title resolve_accent/compose_label_text/title_with_fallback/basename; new/extended tests across all 6 files
</artifacts_this_phase_produces>

<tasks>

<task type="execute">
  <name>Task 1: Refresh the three seed recipes to use the explicit icon attribute (D-03/D-05)</name>
  <files>scenes/dev.toml, scenes/ai.toml, scenes/docker.toml</files>
  <read_first>
    - scenes/dev.toml (the live reference — tab title "dev {cwd}" :23, per-pane "edit {cwd}"/"shell {cwd}"/"git {cwd}" :28,:33,:38 using icon-name leading words)
    - scenes/ai.toml (no titles today :15-23) and scenes/docker.toml (no titles :14-26)
    - cli/lib/recipe.lua (the icon + follow_pane_color schema added in Plan 04 — the keys the seeds now use)
    - .planning/phases/06.2-identity-orthogonality/06.2-PATTERNS.md (section "scenes/*.toml" :214-221)
    - .planning/phases/06.2-identity-orthogonality/06.2-CONTEXT.md (D-03, D-04, D-05, D-13; discretion: exact glyph for the seeds)
  </read_first>
  <action>
    Refresh the three seed recipes so identity is expressed via the explicit schema, not the
    removed first-word swap (D-05): in scenes/dev.toml replace the icon-name leading title
    words with an explicit per-[[pane]] icon key + a literal {cwd} title — the editor pane
    gets icon = "edit" + title = "{cwd}", the shell pane icon = "shell", the git pane
    icon = "git"; keep the per-pane color tints. Add a tab-level icon if it reads well
    (maintainer discretion on the exact glyph, D-03). For scenes/ai.toml and
    scenes/docker.toml add explicit per-pane icon keys (ai/claude pane icon, docker panes
    icon = "docker"). Update each file's header comment to describe the icon attribute and
    drop the now-false "leading icon name becomes its glyph" prose (D-05). Do NOT add
    follow_pane_color unless a seed clearly wants the opt-in (default OFF, D-09). Confirm each
    refreshed recipe LOADS + MAPS via recipe.load_and_map by running the recipe suite (Plan
    04 added the icon round-trip there) and a manual lua5.4 spot-check noted in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/user-zero/git/personal/wezterm-setup && grep -q "icon" scenes/dev.toml</automated>
    <automated>cd /home/user-zero/git/personal/wezterm-setup && grep -q "icon" scenes/ai.toml</automated>
    <automated>cd /home/user-zero/git/personal/wezterm-setup && grep -q "icon" scenes/docker.toml</automated>
    <automated>cd /home/user-zero/git/personal/wezterm-setup && lua5.4 cli/lib/recipe_test.lua</automated>
  </verify>
  <acceptance_criteria>
    - All three seed recipes contain an explicit icon key and no longer rely on an icon-name leading title word (no "edit {cwd}" / "git {cwd}" style icon-name-first titles remain)
    - Each header comment describes the icon attribute and no longer claims the first word becomes a glyph
    - recipe_test.lua (the suite that exercises load_and_map with icons) exits 0
  </acceptance_criteria>
  <done>The shipped seeds express icons explicitly; they load + map under the new schema.</done>
</task>

<task type="execute">
  <name>Task 2: Full-suite regression gate across all 6.2 changes</name>
  <files>tools/run-tests.sh (read-only gate; no source edits expected beyond stale-test fixes surfaced)</files>
  <read_first>
    - Makefile (the test target runs ./tools/run-tests.sh :37)
    - tools/run-tests.sh (discovers co-located *_test.lua under cli/ and config/)
    - .planning/STATE.md (the suite was green at phase start — this gate proves no regression)
  </read_first>
  <action>
    Run the full co-located test suite (make test / tools/run-tests.sh) and confirm it is
    green across every module 6.2 touched: title, color (unchanged), tab, pane, spec,
    scene (lib + command), recipe, and format-tab-title. Resolve any cross-module regression
    here (e.g. a stale assertion that still expected the first-word glyph swap, or a stale
    WEZTERM_TAB_COLOR expectation on the pane side). Re-run the purity grep on the pure
    modules (title.lua, color.lua, scene.lua lib, recipe.lua) to confirm zero io/os/wezterm.
  </action>
  <verify>
    <automated>cd /home/user-zero/git/personal/wezterm-setup && make test</automated>
  </verify>
  <acceptance_criteria>
    - make test exits 0 (the full co-located suite is green)
    - No test still expects the removed first-word glyph swap or the old shared WEZTERM_TAB_COLOR pane carrier
    - The pure modules keep the purity grep at 0 non-comment io/os/wezterm matches
  </acceptance_criteria>
  <done>Every 6.2 change is integrated with a green suite and no cross-module regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live-session repro — icon render + color precedence + cwd fallback (hypothesis policy)</name>
  <files>docs/repro/h-06.2-identity-orthogonality.md</files>
  <read_first>
    - docs/repro/h-tab-color.md and docs/repro/h-scene-tall.md (the recorded-repro doc format to mirror)
    - .planning/phases/06.2-identity-orthogonality/06.2-CONTEXT.md (D-04, D-08, D-09, D-10, D-11, D-13 — the behaviors to observe)
    - config/wezterm-setup/format-tab-title.lua (the render output being verified)
  </read_first>
  <action>
    Drive the live-session verification below against a real WezTerm session, observe each
    behavior, and RECORD the result in docs/repro/h-06.2-identity-orthogonality.md (mirroring
    the existing repro docs; name the carriers WEZTERM_TAB_ICON / WEZTERM_PANE_COLOR). This is
    the hypothesis-policy gate: a human confirms the actual terminal paint that automated
    tests cannot assert. Pause for the resume signal.
  </action>
  <what-built>
    The icon attribute, the split tab/pane color carriers with explicit-tab-color-wins
    precedence + the --follow-pane-color opt-in, and the empty-title cwd-basename fallback —
    all emitted by the CLI/scene path (Plans 02/04) and rendered by format-tab-title (Plan 03).
    Automated tests prove the logic; this checkpoint confirms the actual terminal paint.
  </what-built>
  <how-to-verify>
    In a live WezTerm session (against the source launcher or a fresh install of the 6.2
    branch):
    1. Icon as its own attr: run `wez tab icon node` then `wez tab title api` — the tab bar
       shows the node glyph LEFT of the literal text "api" (icon + space + title, D-04). Then
       `wez tab title build --icon test` — title is literal "build" with the test glyph.
    2. Color split / precedence: `wez tab color blue`, then in a pane `wez pane color red` —
       the tab keeps BLUE (explicit tab color wins, D-08), the pane background tints red.
       Clear the tab color (`wez tab color reset`) and set `wez tab color blue
       --follow-pane-color` with no explicit color elsewhere — confirm the opt-in follows the
       active pane; without the flag a colorless tab stays neutral (D-09/D-10).
    3. cwd fallback: from a repo dir, set an empty title (`wez tab title reset`) on a tab with
       an icon — the tab self-labels as icon + the repo basename (D-13); with no icon it shows
       just the basename (D-11).
    4. Seed scenes: `wez scene launch dev` from a repo — panes show their explicit icons +
       {cwd}-basename titles, per-pane color tints, and the tab accent.
    Record the observed result (screenshot description or copied user-var values via `wezterm
    cli list --format json`) in docs/repro/h-06.2-identity-orthogonality.md.
  </how-to-verify>
  <acceptance_criteria>
    - docs/repro/h-06.2-identity-orthogonality.md exists and records the observed render for all four checks above, naming WEZTERM_TAB_ICON and WEZTERM_PANE_COLOR
    - The recorded result confirms icon-left-of-literal-title (D-04), explicit-tab-color-wins + opt-in follow (D-08/D-09), and the cwd fallback (D-11/D-13)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live render matches, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed recipe TOML -> OSC + send-text on launch | the shipped recipes are loaded and their icon/title/color values emitted to panes (same surface as Plan 04, exercised here with real files) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-11 | Tampering | OSC injection via a seed recipe value | mitigate | The seeds are authored in-repo with named icons / {cwd} tokens only (no raw control bytes); on launch every value still flows through the base64 build_osc1337 + octal printf path (scene.lua, Plan 04, T-06.2-08). The seeds add no new emit path. |
| T-06.2-12 | Tampering | copy-if-absent never overwrites user edits | accept | The seeder is copy-if-absent (6.1 D-15); refreshing the in-repo seeds only affects FRESH installs — an existing user's edited recipes are never clobbered (no migration of installed files, consistent with D-06 relaxed stance). |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No new packages. |
</threat_model>

<verification>
- All three seeds contain explicit icon keys and load via recipe.load_and_map.
- make test green (full co-located suite, no regression).
- A live-session repro is recorded in docs/repro/ confirming the visual render of D-04/D-08/D-09/D-11/D-13.
</verification>

<success_criteria>
- Seed recipes use the explicit icon attribute (D-03/D-05) and still load+map.
- Full suite green across every 6.2 change.
- Live repro confirms icon render, color precedence (explicit tab wins + opt-in follow), and cwd fallback.
</success_criteria>

<output>
Create `.planning/phases/06.2-identity-orthogonality/06.2-05-SUMMARY.md` when done — include the full Phase 6.2 artifact inventory listed in artifacts_this_phase_produces.
</output>
