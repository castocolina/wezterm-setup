---
phase: 06.2-identity-orthogonality
plan: 03
type: execute
wave: 2
depends_on:
  - 06.2-01
files_modified:
  - config/wezterm-setup/format-tab-title.lua
  - config/wezterm-setup/format-tab-title_test.lua
autonomous: true
requirements: []
must_haves:
  truths:
    - "The tab label composes icon + space + literal title (icon left), reading WEZTERM_TAB_ICON (D-04)"
    - "Color precedence: tab WEZTERM_TAB_COLOR wins; else if WEZTERM_TAB_FOLLOW_PANE is \"1\" the active pane WEZTERM_PANE_COLOR; else neutral WezTerm default (D-08/D-10)"
    - "An empty resolved title falls back to the launch-dir basename; with an icon present yields icon + basename (D-11/D-13)"
    - "The legacy color:title parse is fully removed from the render path (D-05/D-06)"
  artifacts:
    - path: "config/wezterm-setup/format-tab-title.lua"
      provides: "icon composition + 3-step color precedence + cwd fallback; legacy parse removed (D-04/D-08/D-10/D-11/D-13)"
      contains: "WEZTERM_TAB_ICON"
    - path: "config/wezterm-setup/format-tab-title_test.lua"
      provides: "render e2e: representative user-var combinations yield expected label + accent"
      contains: "WEZTERM_PANE_COLOR"
  key_links:
    - from: "config/wezterm-setup/format-tab-title.lua"
      to: "active pane user_vars (WEZTERM_TAB_COLOR, WEZTERM_PANE_COLOR, WEZTERM_TAB_ICON, WEZTERM_TAB_TITLE, WEZTERM_TAB_FOLLOW_PANE)"
      via: "the format-tab-title event handler reads the user vars the CLI/scene path emits (D-04/D-08)"
      pattern: "WEZTERM_TAB_ICON"
---

<objective>
Make the render layer agree with the split carriers and the explicit icon attribute —
the composition + precedence site for G-1 render and G-2a/G-2b precedence. The
format-tab-title handler draws icon-left-of-title, resolves the tab accent by the D-08
three-step precedence, and self-labels empty-title tabs by cwd basename.

format-tab-title.lua is a STANDALONE config bundle with NO cli/lib require (it loads
inside WezTerm's Lua, not the luastatic CLI), so the small basename helper is
duplicated here deliberately — this plan calls that out and keeps it minimal and
lockstep-commented, per the reuse-first invariant's one justified exception.

LOCKED CROSS-PLAN CONTRACT (W3): the follow-pane opt-in carrier user var is the literal
**WEZTERM_TAB_FOLLOW_PANE** (payload "1" when ON, unset/empty when OFF). This plan reads
that exact literal directly — it does NOT depend on a comment Plan 02 writes at runtime,
so Plans 02 and 03 stay parallel in wave 2.

Purpose: D-04 (icon + title compose), D-08 (tab color > follow-pane color > neutral),
D-10 (neutral = WezTerm default), D-11/D-13 (empty-title cwd fallback, icon-aware),
D-05/D-06 (drop the legacy parse_tab_title from the render path).
Output: extended format-tab-title.lua + its co-located test, suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-identity-orthogonality/06.2-CONTEXT.md
@.planning/phases/06.2-identity-orthogonality/06.2-PATTERNS.md
@config/wezterm-setup/format-tab-title.lua
</context>

<artifacts_this_phase_produces>
- Render reads of NEW user vars: WEZTERM_TAB_ICON (compose), WEZTERM_PANE_COLOR (follow-pane accent), the follow-pane opt-in carrier WEZTERM_TAB_FOLLOW_PANE (LOCKED literal, payload "1"/unset — same literal Plans 02 and 04 emit)
- New pure helpers: local basename (duplicated, lockstep-commented), accent-precedence resolver, icon+title composer, empty-title cwd fallback
- BEHAVIOR CHANGE: parse_tab_title removed from the render path (D-05/D-06)
- Test file: extended config/wezterm-setup/format-tab-title_test.lua (render e2e)
</artifacts_this_phase_produces>

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: Compose icon + literal title and apply the empty-title cwd fallback (D-04/D-11/D-13)</name>
  <files>config/wezterm-setup/format-tab-title.lua, config/wezterm-setup/format-tab-title_test.lua</files>
  <read_first>
    - config/wezterm-setup/format-tab-title.lua (handler :128-163 — uv read :135, title chain :149-155, format_label :157; resolve_profile :46-61; DEFAULT_PROFILE :35; legacy parse_tab_title :63-89 to remove)
    - config/wezterm-setup/format-tab-title_test.lua (pure-helper harness, package.path preamble :7-10; resolve_profile/format_label assertions :22-40)
    - cli/lib/title.lua (basename :63-68 — the contract to DUPLICATE; not requireable from this bundle)
    - .planning/phases/06.2-identity-orthogonality/06.2-PATTERNS.md (section "format-tab-title.lua" :156-174 — lockstep-duplication caveat + lines to change)
    - .planning/phases/06.2-identity-orthogonality/06.2-CONTEXT.md (D-04, D-05, D-06, D-11, D-12, D-13)
  </read_first>
  <behavior>
    - M.compose_label_text(icon, title) returns icon + " " + title when icon non-empty, else title (D-04 icon left, single space; literal title)
    - local pure M.basename(path) duplicating cli/lib/title.lua:63-68 ("/x/myrepo" yields "myrepo", "/" yields "/", "" yields "/")
    - M.title_with_fallback(title, icon, launch_dir): non-empty title wins (D-11); empty + no icon yields basename; empty + icon yields icon + " " + basename (D-13)
    - parse_tab_title GONE: a stored "cyan:api" tab_title is NOT split — renders literally (D-05/D-06)
  </behavior>
  <action>
    In config/wezterm-setup/format-tab-title.lua: add a local pure M.basename(path)
    duplicating cli/lib/title.lua:63-68 with a comment marking it the deliberate
    standalone-bundle duplicate (lockstep with cli/lib/title.lua — change both together),
    matching the INVARIANT comment style at :73-75. Add pure M.compose_label_text(icon,
    title) (D-04) and M.title_with_fallback(title, icon, launch_dir) (D-11/D-13). DELETE
    M.parse_tab_title (:63-89), its handler call (:141), and the migratedTitle branch
    (:148-155) — title comes from uv.WEZTERM_TAB_TITLE, falling back via title_with_fallback
    to the active pane title / cwd basename. In the handler read uv.WEZTERM_TAB_ICON near
    :135, compute displayed text via title_with_fallback then compose with the icon, and pass
    to format_label (:157). Source launch_dir from the active pane cwd if available, else the
    pane title. Add render-e2e assertions to format-tab-title_test.lua: icon+title compose,
    empty title yields basename, empty + icon yields icon + basename, and a legacy "cyan:api"
    string renders literally.
  </action>
  <verify>
    <automated>cd /home/user-zero/git/personal/wezterm-setup/config/wezterm-setup && lua5.4 format-tab-title_test.lua</automated>
    <automated>grep -q "WEZTERM_TAB_ICON" /home/user-zero/git/personal/wezterm-setup/config/wezterm-setup/format-tab-title.lua</automated>
    <automated>grep -q "function M.parse_tab_title" /home/user-zero/git/personal/wezterm-setup/config/wezterm-setup/format-tab-title.lua; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - format-tab-title.lua contains M.compose_label_text, M.title_with_fallback, a local M.basename, and reads uv.WEZTERM_TAB_ICON
    - M.parse_tab_title is removed (no `function M.parse_tab_title` remains)
    - format-tab-title_test.lua asserts icon+title compose, empty-title basename fallback, empty-title+icon fallback, and literal rendering of a legacy color:title string; the test exits 0
    - The local basename carries a lockstep-with-cli/lib/title.lua comment
  </acceptance_criteria>
  <done>Tab labels render icon + literal title with a cwd-basename fallback; the legacy parser is gone.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 2: Three-step accent precedence — tab color > follow-pane color > neutral (D-08/D-10)</name>
  <files>config/wezterm-setup/format-tab-title.lua, config/wezterm-setup/format-tab-title_test.lua</files>
  <read_first>
    - config/wezterm-setup/format-tab-title.lua (single accent read :145, resolve_profile :46-61, DEFAULT_PROFILE :35, build_runs :106-123)
    - config/wezterm-setup/format-tab-title_test.lua (resolve_profile assertions :22-37 — analog for accent assertions)
    - .planning/phases/06.2-identity-orthogonality/06.2-PATTERNS.md (section "format-tab-title.lua" :156-174, "Color precedence (D-08)")
    - .planning/phases/06.2-identity-orthogonality/06.2-CONTEXT.md (D-07, D-08, D-09, D-10)
  </read_first>
  <behavior>
    - M.resolve_accent(uv): tab WEZTERM_TAB_COLOR non-empty yields resolve_profile(WEZTERM_TAB_COLOR) (D-08 step 1, explicit tab color always wins)
    - else WEZTERM_TAB_FOLLOW_PANE == "1" and WEZTERM_PANE_COLOR non-empty yields resolve_profile(WEZTERM_PANE_COLOR) (D-08 step 2)
    - else yields M.DEFAULT_PROFILE (D-08 step 3 / D-10 neutral = WezTerm default; no sticky state)
    - WEZTERM_TAB_FOLLOW_PANE unset/empty/non-"1" + a pane color set yields the neutral default, NOT the pane color (D-09 default OFF)
    - resolve_profile is unchanged (reused for the chosen value)
  </behavior>
  <action>
    In config/wezterm-setup/format-tab-title.lua: replace the single accent_color read at
    :145 with pure M.resolve_accent(uv) implementing the D-08 three-step precedence over the
    active pane user_vars, returning a {bg,fg} via the unchanged resolve_profile. Read the
    follow-pane opt-in carrier by the LOCKED literal name uv.WEZTERM_TAB_FOLLOW_PANE (W3 — the
    same literal Plan 02 emits; do NOT chase a comment); treat the value "1" as ON and any
    other/absent value as OFF (D-09 default OFF). Call M.resolve_accent(uv) in the handler and
    pass its result to build_runs (:159). Add accent-precedence assertions to
    format-tab-title_test.lua: tab color wins over a pane color; follow == "1" + only pane
    color yields the pane color; follow unset + pane color yields the default; nothing set
    yields the default.
  </action>
  <verify>
    <automated>cd /home/user-zero/git/personal/wezterm-setup/config/wezterm-setup && lua5.4 format-tab-title_test.lua</automated>
    <automated>grep -q "function M.resolve_accent" /home/user-zero/git/personal/wezterm-setup/config/wezterm-setup/format-tab-title.lua</automated>
    <automated>grep -q "WEZTERM_TAB_FOLLOW_PANE" /home/user-zero/git/personal/wezterm-setup/config/wezterm-setup/format-tab-title.lua</automated>
  </verify>
  <acceptance_criteria>
    - format-tab-title.lua contains M.resolve_accent implementing the 3-step precedence and reads WEZTERM_TAB_COLOR, WEZTERM_PANE_COLOR, and WEZTERM_TAB_FOLLOW_PANE
    - format-tab-title_test.lua asserts all four precedence cases (tab wins, follow-on pane, follow-off neutral, nothing neutral); the test exits 0
    - resolve_profile is unchanged
    - The follow carrier var name is the LOCKED literal WEZTERM_TAB_FOLLOW_PANE, identical to the name emitted by Plans 02 and 04
  </acceptance_criteria>
  <done>The render layer resolves the accent by explicit precedence; an explicit tab color always wins, follow-pane is opt-in (WEZTERM_TAB_FOLLOW_PANE == "1"), neutral is WezTerm default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active pane user_vars -> rendered tab-bar runs | icon/title/color values stored as user vars are read by the render handler and turned into displayed text + colors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-06 | Tampering | malformed/over-long color in WEZTERM_TAB_COLOR or WEZTERM_PANE_COLOR | mitigate | resolve_profile (unchanged, :46-61) only honors a known name or a well-formed 3/6/8-digit hex; any other value falls back to DEFAULT_PROFILE (T-06.1-12 tamper safety carried forward). resolve_accent layers on top without weakening this. |
| T-06.2-07 | Information disclosure / Tampering | icon/title text rendered into the tab bar | accept | Values arrive base64-decoded by WezTerm from the SetUserVar frame and are placed as plain Text runs in build_runs; WezTerm renders them as text, not as escape sequences (no re-emit of raw control bytes). The render layer performs no shell eval and no os.execute. No further escaping needed at the display layer. |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; pure-Lua config bundle. |
</threat_model>

<verification>
- format-tab-title_test.lua green (render e2e for compose + fallback + 4 precedence cases).
- format-tab-title.lua has no `function M.parse_tab_title` (legacy parser removed).
- The follow carrier var name is the LOCKED literal WEZTERM_TAB_FOLLOW_PANE, identical between this render layer and cli/commands/tab.lua (Plan 02) + cli/commands/scene.lua (Plan 04).
- Live render verification of the visual result is owned by Plan 05 (recorded repro).
</verification>

<success_criteria>
- Tab label = icon + literal title, with empty-title cwd-basename fallback (D-04/D-11/D-13).
- Accent precedence: tab color > follow-pane color (WEZTERM_TAB_FOLLOW_PANE == "1") > neutral default (D-08/D-09/D-10).
- Legacy color:title parser removed from the render path (D-05/D-06).
- Render-e2e suite green.
</success_criteria>

<output>
Create `.planning/phases/06.2-identity-orthogonality/06.2-03-SUMMARY.md` when done.
</output>
