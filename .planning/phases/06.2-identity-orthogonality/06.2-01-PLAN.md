---
phase: 06.2-identity-orthogonality
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - cli/lib/title.lua
  - cli/lib/title_test.lua
autonomous: true
requirements: []
must_haves:
  truths:
    - "A known icon name (case-insensitive) resolves to its glyph; any other input passes through verbatim (D-02)"
    - "A title's first word is NO LONGER swapped for a glyph — title text is fully literal (D-05)"
    - "An empty resolved title falls back to the basename of the launch dir (D-11)"
    - "fallback_title is the SINGLE shared helper applied by BOTH the tab-side (render, Plan 03) and the pane-side (scene loop, Plan 04) empty-title cwd fallback (D-12)"
    - "An empty title WITH an icon present yields icon + space + basename (D-13)"
  artifacts:
    - path: "cli/lib/title.lua"
      provides: "resolve_icon + cwd-empty title fallback helper; first-word swap removed (D-02/D-05/D-11/D-12/D-13)"
      contains: "function M.resolve_icon"
    - path: "cli/lib/title_test.lua"
      provides: "RED->GREEN pure-core assertions for resolve_icon, literal-title, and the cwd fallback"
      contains: "resolve_icon"
  key_links:
    - from: "cli/lib/title.lua"
      to: "cli/commands/tab.lua, cli/commands/pane.lua, cli/commands/scene.lua, config/wezterm-setup/format-tab-title.lua"
      via: "shared pure-core helpers re-exported / mirrored by every entry point (D-01); fallback_title reused tab-side and pane-side (D-12)"
      pattern: "M%.resolve_icon"
---

<objective>
Establish the pure-core identity helpers Phase 6.2 builds on — the SINGLE shared
source (6.1 D-01 invariant) for icon resolution and the cwd-empty title fallback,
and remove the legacy first-word icon swap from steady state.

This is a dedicated TDD plan (RED -> GREEN -> REFACTOR) because every helper here is
pure business logic with defined I/O, the prime TDD candidate per the phase TDD
policy. Every downstream entry point (tab/pane CLI, scene loop, render layer)
consumes these helpers — getting the contracts right and tested first prevents the
scavenger-hunt anti-pattern downstream. In particular, M.fallback_title is the ONE
helper both the tab-side (Plan 03 render) and the pane-side (Plan 04 scene loop) call
for the D-12 "applies to both panes AND tabs" fallback — defining it once here is what
makes D-12 a single shared rule rather than two divergent copies.

Purpose: D-02 (icon input = named key OR literal glyph), D-05 (drop the first-word
glyph swap; titles are literal), and the D-11/D-12/D-13 cwd-empty fallback composition
— all in cli/lib/title.lua, the one module the whole phase reuses.
Output: extended cli/lib/title.lua + cli/lib/title_test.lua, suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-identity-orthogonality/06.2-CONTEXT.md
@.planning/phases/06.2-identity-orthogonality/06.2-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan contributes the following NEW symbols (full phase artifact list lives in
06.2-05's summary):
- cli/lib/title.lua M.resolve_icon(input) — named-key OR literal-glyph resolver (D-02)
- cli/lib/title.lua M.fallback_title(title, icon, launch_dir) — empty-title -> cwd basename, icon-aware; the SHARED helper for both tabs and panes (D-11/D-12/D-13)
- BEHAVIOR CHANGE: M.resolve_title / M.resolve_title_str no longer swap the first word for a glyph (D-05)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — author title_test.lua expectations for resolve_icon + literal titles + cwd fallback</name>
  <files>cli/lib/title_test.lua</files>
  <read_first>
    - cli/lib/title.lua (the module being extended — M.ICONS :11-16, resolve_title :21-39, resolve_title_str :45-59, basename :63-68, expand_cwd :76-86)
    - cli/lib/title_test.lua (the canonical *_test.lua shape: check/eq counter, os.exit(fail==0 and 0 or 1); PATTERNS.md Pattern D :87-110)
    - .planning/phases/06.2-identity-orthogonality/06.2-CONTEXT.md (D-02, D-05, D-11, D-12, D-13)
    - .planning/phases/06.2-identity-orthogonality/06.2-PATTERNS.md (section "cli/lib/title.lua" :114-127 — exact analog line refs)
  </read_first>
  <behavior>
    - resolve_icon("node") returns the node glyph (known name -> glyph, D-02; mirror resolve_title's :lower() lookup at title.lua:29)
    - resolve_icon("NODE") returns the same glyph (case-insensitive)
    - resolve_icon(any literal glyph) returns the literal verbatim (D-02 — no reject)
    - resolve_icon("anything-else") returns "anything-else" (unknown input taken literally)
    - resolve_icon("") returns "" and resolve_icon(nil) returns "" (empty/nil -> empty, no error)
    - resolve_title({"node","api"}) returns "node api" (D-05: first word NOT swapped — was glyph pre-6.2; update existing expectation at title_test.lua:25-41)
    - resolve_title({"reset"}) returns "" and resolve_title({}) returns "" (KEEP reset/empty -> "")
    - resolve_title_str("node api") returns "node api" (D-05: literal; was glyph pre-6.2)
    - resolve_title_str("reset") returns "" (KEEP)
    - {cwd} token UNCHANGED: expand_cwd("{cwd}", "/x/myrepo") returns "myrepo" (regression guard)
    - fallback_title("", nil, "/x/myrepo") returns "myrepo" (D-11/D-12: empty title -> launch-dir basename; same helper used by tabs AND panes)
    - fallback_title("api", nil, "/x/myrepo") returns "api" (D-11: explicit non-empty title always wins)
    - fallback_title("", node-glyph, "/x/myrepo") returns "<glyph> myrepo" (D-13: icon + space + basename)
    - fallback_title("", node-glyph, "/") uses basename("/")=="/" (reuse title.lua:63-68 edge contract, no crash)
  </behavior>
  <action>
    Extend cli/lib/title_test.lua (do NOT create a parallel file) with the assertions in
    behavior, using the existing check/eq harness and counter. CHANGE the pre-6.2
    first-word-swap expectations at lines 25-41 to the new literal-title results per D-05
    (these currently assert the glyph swap and MUST flip to literal). Add one block
    exercising M.resolve_icon and one block exercising M.fallback_title. Run the suite to
    confirm it FAILS (RED): resolve_icon/fallback_title do not exist yet and the
    literal-title assertions contradict the current swap behavior. Commit
    test(06.2-01): RED title resolve_icon + literal titles + cwd fallback.
    Do NOT touch cli/lib/title.lua in this task.
  </action>
  <verify>
    <automated>cd /home/user-zero/git/personal/wezterm-setup && lua5.4 cli/lib/title_test.lua; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - cli/lib/title_test.lua contains the literal token resolve_icon and the literal token fallback_title
    - lua5.4 cli/lib/title_test.lua exits NON-ZERO (RED) before Task 2
    - The pre-6.2 glyph-swap expectations (title_test.lua:25-41) are replaced with literal-title expectations (no remaining assertion expecting a glyph from resolve_title on a known-name first word)
    - A RED commit exists with subject prefix "test(06.2-01): RED"
  </acceptance_criteria>
  <done>title_test.lua encodes every D-02/D-05/D-11/D-12/D-13 behavior and fails because the implementation is absent.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement resolve_icon, fallback_title, and drop the first-word swap</name>
  <files>cli/lib/title.lua</files>
  <read_first>
    - cli/lib/title.lua (resolve_title :21-39, resolve_title_str :45-59 — the swap branches to remove; basename :63-68, expand_cwd :76-86 — reuse for fallback_title)
    - cli/lib/title_test.lua (the RED expectations authored in Task 1 — the contract)
    - .planning/phases/06.2-identity-orthogonality/06.2-PATTERNS.md (section "cli/lib/title.lua" :114-127, Pattern A/C for the reuse invariant)
  </read_first>
  <action>
    In cli/lib/title.lua: (1) add pure M.resolve_icon(input) — lowercase the input, return
    M.ICONS[name] if it is a known key, else return the original input verbatim (no reject,
    D-02); nil/empty -> "". Mirror the :lower() lookup idiom at line 29. (2) Remove the
    first-word glyph swap from M.resolve_title (delete the M.ICONS lookup branch at :29-37
    so words join verbatim) and from M.resolve_title_str (delete the glyph branch at
    :54-57). KEEP the reset/empty -> "" guards and KEEP expand_cwd untouched (D-05 only
    removes the swap, not the token). (3) Add pure M.fallback_title(title, icon, launch_dir)
    — if title is a non-empty string return it as-is (D-11 explicit wins); else compute
    base = M.basename(launch_dir); if icon is a non-empty string return icon .. " " .. base
    (D-13) else return base. This ONE helper is what both the render layer (Plan 03, tabs)
    and the scene loop (Plan 04, panes) call, making the D-12 "panes AND tabs" fallback a
    single shared rule. Keep the module PURE (no wezterm, no io.*, no os.*). Update the
    module doc-comment header to note the swap removal (D-05). Run the suite to GREEN. Commit
    feat(06.2-01): GREEN resolve_icon + literal titles + cwd fallback (D-02/D-05/D-11/D-12/D-13).
    If GREEN is already clean, no REFACTOR commit; otherwise add a refactor(06.2-01): commit
    keeping the suite green.
  </action>
  <verify>
    <automated>cd /home/user-zero/git/personal/wezterm-setup && lua5.4 cli/lib/title_test.lua</automated>
    <automated>grep -q "function M.resolve_icon" /home/user-zero/git/personal/wezterm-setup/cli/lib/title.lua</automated>
    <automated>grep -q "function M.fallback_title" /home/user-zero/git/personal/wezterm-setup/cli/lib/title.lua</automated>
  </verify>
  <acceptance_criteria>
    - lua5.4 cli/lib/title_test.lua exits 0 (GREEN)
    - cli/lib/title.lua contains function M.resolve_icon and function M.fallback_title
    - Purity: zero non-comment lines in cli/lib/title.lua match wezterm|io.|os. (grep -vE comment-filter returns 0)
    - No code path in resolve_title / resolve_title_str swaps a known icon-name first word for a glyph (the M.ICONS lookup is gone from both)
    - A GREEN commit exists with subject prefix "feat(06.2-01): GREEN"
  </acceptance_criteria>
  <done>resolve_icon + fallback_title implemented (the shared tab+pane fallback), the first-word swap removed, module pure, full title_test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe/CLI value -> title.lua helpers | icon/title text (later emitted via OSC) is normalized here; first hop where untrusted glyph/title text is handled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-01 | Tampering | resolve_icon literal passthrough | accept | This module returns the value as a Lua string only; it performs NO terminal write. OSC/control-byte neutralization happens at the emit seam (color.build_osc1337 base64-encodes the value, cli/lib/color.lua:110-112) consumed by Plan 02/03/04. resolve_icon must NOT strip or interpret bytes — escaping is the emitter's job. The module stays pure (verified by the purity grep). |
| T-06.2-02 | Denial of Service | fallback_title basename on odd paths | mitigate | Reuse the existing basename edge contract (title.lua:63-68: "/" -> "/", "" -> "/") so no input crashes; covered by a behavior assertion. |
</threat_model>

<verification>
- RED then GREEN: title_test transitions from non-zero to zero across the two tasks.
- Purity grep on cli/lib/title.lua returns 0 non-comment matches for wezterm|io.|os.
- fallback_title is the single shared helper consumed tab-side (Plan 03) and pane-side (Plan 04) — D-12 has one implementation.
- No regression: the broader suite still passes (run-tests picks up the co-located test).
</verification>

<success_criteria>
- resolve_icon resolves named keys to glyphs and passes literals through (D-02).
- resolve_title / resolve_title_str never swap the first word for a glyph (D-05).
- fallback_title composes empty-title -> basename, icon-aware, shared by tabs and panes (D-11/D-12/D-13).
- cli/lib/title_test.lua green; module pure.
</success_criteria>

<output>
Create `.planning/phases/06.2-identity-orthogonality/06.2-01-SUMMARY.md` when done.
</output>
