---
phase: 06.5-keybinding-clarity-wez-keys-output-curation-inserted
plan: 02
type: tdd
wave: 2
depends_on: ["06.5-01"]
files_modified:
  - cli/commands/keys.lua
  - cli/commands/keys_test.lua
  - cli/spec.lua
  - tests/fixtures/showkeys_effective.lua
  - tests/fixtures/showkeys_baseline.lua
autonomous: true
requirements:
  - "keybinding/CLI UX clarity"
user_setup: []

must_haves:
  truths:
    - "A single uppercase letter with no SHIFT mod renders lowercased with explicit SHIFT (ALT+X -> ALT+SHIFT+x)"
    - "Digits and named keys (1, PageUp) NEVER fold — they pass through unchanged"
    - "Modifiers always render in fixed order SUPER>CTRL>ALT>SHIFT joined by +"
    - "Two records that canonicalize identically collapse to ONE rendered row (D-08)"
    - "An action reachable by multiple distinct chords renders as ONE row, reliable-chord-first (D-07)"
    - "The wezterm-setup (managed) section is printed BEFORE the WezTerm defaults section (D-05)"
    - "SUPER renders as Super on linux, Cmd on macos, inline-annotated under --platform all (D-03/D-04)"
    - "--json output carries native mods, NO SGR color, and stays jq-valid"
    - "Conflict rows carry ! prefix + [CONFLICT] token AND red SGR only on a TTY (D-06)"
  artifacts:
    - path: "cli/commands/keys.lua"
      provides: "Canonicalize + fixed mod-order + action-grouping + platform-label pure transforms; curated-first sectioned renderer with TTY/NO_COLOR-gated red conflicts"
      exports: ["classify", "build_json", "run"]
      contains: "canonicalize_key"
    - path: "cli/commands/keys_test.lua"
      provides: "Pure fixtures for canonicalize/mod-order/dedupe/grouping/sectioning/platform-label/color-gate"
      min_lines: 80
    - path: "cli/spec.lua"
      provides: "--platform option on the keys subcommand"
      contains: "platform"
    - path: "tests/fixtures/showkeys_effective.lua"
      provides: "Committed real show-keys --lua effective sample for headless renderer fixtures"
    - path: "tests/fixtures/showkeys_baseline.lua"
      provides: "Committed real -n show-keys --lua baseline sample"
  key_links:
    - from: "cli/commands/keys.lua"
      to: "cli/lib/ansi.lua"
      via: "require for conflict red SGR"
      pattern: "require\\(\"cli\\.lib\\.ansi\"\\)"
    - from: "cli/commands/keys_test.lua"
      to: "tests/fixtures/showkeys_effective.lua"
      via: "fixture text driven through showkeys.parse"
      pattern: "showkeys_effective"
    - from: "cli/spec.lua"
      to: "keys subcommand"
      via: "keys:option(\"--platform\", ...)"
      pattern: "platform"
---

<objective>
Rewrite the `wez keys` renderer so output is unambiguous, de-noised, and
curated-first — the heart of the phase. ALL new logic stays in the PURE layer of
`cli/commands/keys.lua` (fixture-testable headless, the autonomous gate); only the
host-OS default, the TTY/NO_COLOR color gate, and the actual `io.write` sectioning
live in `run()` (IO shell). Register the new `--platform` option in `cli/spec.lua`.

Implements:
- D-01 letter-only canonicalize (`ALT+X` → `ALT+SHIFT+x`; digits/named keys pass through).
- D-02 fixed mod order SUPER>CTRL>ALT>SHIFT joined with `+` for the HUMAN form
  (keep `id_of` on the pipe form so cross-source classify matching is undisturbed).
- D-07 action-centric grouping (one row per action, distinct chords grouped, reliable-chord-first).
- D-08 case-variant collapse (automatic once D-01 lowercases).
- D-05 curated-first sectioning (managed first, defaults after, nothing hidden, no filter flag).
- D-06 ANSI-red conflicts (inline `!`+`[CONFLICT]` + bold-red summary header), gated on
  `not --json AND not NO_COLOR AND tty`; never colorize `--json`; never SGR-wrap the action string.
- D-03/D-04 platform label (SUPER→Super/Cmd, `--platform linux|macos|all`, inline annotation for all)
  + the Linux Super footnote.

Purpose: Make declared==displayed unambiguous and collapse the near-duplicate noise.
Output: rewritten `keys.lua`, new `keys_test.lua`, committed `show-keys` fixtures, `--platform` flag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-CONTEXT.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-RESEARCH.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-UI-SPEC.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-PATTERNS.md

# The file being modified (existing pure/IO split + the render block to rewrite):
@cli/commands/keys.lua
# The pure SGR helper this consumes (from Plan 01):
@cli/lib/ansi.lua
# The untrusted-text parser feeding the renderer (touch only if canonicalize must move earlier):
@cli/lib/showkeys.lua
# Command-test harness + capture() idiom to copy:
@cli/commands/pane_test.lua
# The sibling --json flag the new --platform option mirrors:
@cli/spec.lua
</context>

## Artifacts this phase produces (Plan 02 slice)

- NEW pure functions in `cli/commands/keys.lua`: `canonicalize_key` (letter-only fold),
  a NEW human-form mod renderer (fixed-order `+`-joined; SEPARATE from `id_of`/`norm_mods`),
  `group_by_action` (D-07 action-centric dedupe), `platform_label` (D-03/D-04 SUPER map).
- REWRITTEN `M.run` render block (curated-first sectioning, color gate, footnote).
- NEW CLI option `--platform` on the `keys` subcommand in `cli/spec.lua`.
- NEW test `cli/commands/keys_test.lua`.
- NEW directory + fixtures `tests/fixtures/showkeys_effective.lua` + `tests/fixtures/showkeys_baseline.lua`.

These are newly-created symbols/files — exclude from source-drift/grounding verification.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — pure-renderer fixtures (canonicalize / mod-order / dedupe / grouping / sectioning / platform-label / color-gate) + committed show-keys samples</name>
  <files>cli/commands/keys_test.lua, tests/fixtures/showkeys_effective.lua, tests/fixtures/showkeys_baseline.lua</files>
  <read_first>
    - cli/commands/keys.lua — the existing pure functions (`classify`, `build_json`, `norm_mods`
      at lines 44-53, `id_of` at 56-58, the render block at 268-285). The new transforms wrap/replace
      these; the test pins the NEW exports.
    - cli/commands/pane_test.lua lines 1-15 (header + `check`/`eq`) and lines 55-63 (the `capture(fn)`
      stdout/stderr stub) — copy both verbatim; lines 164-165 (the pass/fail footer + `os.exit`).
    - cli/lib/showkeys.lua lines 47-88 — the `parse`/`parse_baseline` record shape `{key,mods,action}`
      the fixtures must produce so they drive the renderer with no live session.
    - 06.5-UI-SPEC.md — Typography (canonicalize rule, `^%a$` guard, mod order, `+` separator,
      `, ` multi-chord join), Layout & Section Ordering (managed-first, Tabs→Panes→Navigation→Font→Other
      inner order, reliable-chord-first), Platform Label (Super/Cmd/all-inline), Color (gate
      `not --json AND not NO_COLOR AND tty`; `!`+`[CONFLICT]`), Copywriting (exact header literals),
      Spacing (row format `"  %-26s %-30s %s\n"`).
    - 06.5-RESEARCH.md Pitfall 2 (digits/named keys do NOT fold) + the empirical fold rule
      (`ALT+X` → `ALT+SHIFT+x`).
  </read_first>
  <behavior>
    - canonicalize_key: `("X","ALT")` → `("x","ALT|SHIFT")` (letter fold, SHIFT re-added);
      `("1","ALT|SHIFT")` → unchanged (digit); `("PageUp","CTRL|SHIFT")` → unchanged (named key);
      `("z","CTRL|SHIFT")` → unchanged (already lowercase + SHIFT present).
    - human mod-render (NEW fn, fixed order + separator): `"SHIFT|CTRL"` → `"CTRL+SHIFT"`;
      `"ALT|SHIFT"` → `"ALT+SHIFT"`; `"SHIFT|CTRL|SUPER"` → `"SUPER+CTRL+SHIFT"`;
      `"NONE"`/empty → `""` (no leading `+`).
    - id_of / norm_mods UNCHANGED: still pipe-joined, still the cross-source identity key
      (a regression test asserts `norm_mods("SHIFT|CTRL")` still yields a pipe form so classify
      matching is not disturbed).
    - D-08 collapse: two effective records that canonicalize to the same (key,mods) → ONE rendered row.
    - D-07 group_by_action: one action with chords `[CTRL+SHIFT+k, SUPER+k]` → a single row whose
      chord-list string is `"CTRL+SHIFT+k, SUPER+k"` (reliable CTRL+SHIFT chord first on linux).
    - D-05 sectioning: given mixed `setup`/`default`/`user` entries, the rendered output places the
      `== wezterm-setup (managed) ==` block strictly before `== WezTerm defaults ==`; nothing dropped.
    - platform_label: `"SUPER"` → `"Super"` (linux), `"Cmd"` (macos); under `all`, a Super chord
      renders the inline annotation form `"Cmd+w (macOS) / Super+w (Linux)"`.
    - color gate (pure decision, stub os.getenv/TTY like pane_test stubs io): `--json` ⇒ false;
      `NO_COLOR` set ⇒ false; non-TTY ⇒ false; plain TTY + no NO_COLOR + not json ⇒ true.
    - run()-level (via capture()): the managed header appears at a LOWER byte offset than the
      defaults header; with conflicts present and color allowed, the conflict row contains `\27[31m`,
      the `! ` prefix, and `[CONFLICT]`; with `--json`, the output contains NO `\27[` byte.
    - Linux Super footnote: when platform is linux/all, the rendered output contains the exact
      3-line `Note: On Linux the Super key ...` block once, after the managed section; absent under macos.
  </behavior>
  <action>
    First capture the two fixtures from the live machine and COMMIT them as text (create the
    `tests/fixtures/` directory — it does not exist yet):
    `wezterm show-keys --lua > tests/fixtures/showkeys_effective.lua` and
    `wezterm -n show-keys --lua > tests/fixtures/showkeys_baseline.lua`. (These are committed sample
    TEXT, NEVER eval'd — the renderer drives them through `showkeys.parse`.) If the live capture is
    unavailable at author time, hand-write minimal fixtures matching the documented record shape
    (`{ key = '...', mods = '...', action = act.Foo ... },` lines inside a `keys = { ... }` block plus
    a `key_tables = {` terminator) covering: a letter-fold case (`key='X' mods='ALT'`), a digit,
    a named key (`PageUp`), a SUPER chord, and a dual-chord action.
    Then author `cli/commands/keys_test.lua` requiring `cli.commands.keys`, copying the pane_test
    harness (`check`/`eq` + `capture(fn)` + footer), and encoding ALL behavior cases above as
    assertions against the NEW exports. For run()-level cases, load the committed fixtures, stub the
    color gate (inject the env/TTY decision the way pane_test stubs `io`), and assert section ordering
    via byte-offset comparison and conflict/json color presence/absence. The file MUST fail RED
    (the new functions do not exist yet; the render is still the old 18/9-width plain form).
  </action>
  <verify>
    <automated>lua5.4 cli/commands/keys_test.lua; test $? -ne 0 && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/showkeys_effective.lua` and `tests/fixtures/showkeys_baseline.lua` exist and
      parse via `showkeys.parse` into non-empty `{key,mods,action}` record lists.
    - `lua5.4 cli/commands/keys_test.lua` exits NON-ZERO (new functions absent / render not yet rewritten).
    - Assertions cite the EXACT UI-SPEC literals: row format `"  %-26s %-30s %s"`, headers
      `== wezterm-setup (managed) ==` / `== WezTerm defaults ==` / `== Conflicts (who-wins) ==`,
      `\27[31m` red + `\27[1;31m` bold-red, `! `+`[CONFLICT]`, the 3-line Linux Super `Note:` footnote,
      fixed mod order SUPER>CTRL>ALT>SHIFT, `, ` chord join.
    - A regression assertion confirms `norm_mods`/`id_of` keep the PIPE form (classify matching intact).
    - The canonicalize cases include the `^%a$` letter-only guard (digit + named-key pass-through).
  </acceptance_criteria>
  <done>keys_test.lua + both fixtures exist; every D-01/D-02/D-05/D-06/D-07/D-08 + platform-label case is asserted against UI-SPEC literals; suite fails RED on the absent transforms.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement the pure transforms + curated-first sectioned renderer; register --platform</name>
  <files>cli/commands/keys.lua, cli/spec.lua</files>
  <read_first>
    - cli/commands/keys.lua — the whole file: keep `classify` (107-162) and the 3-way `label`
      (118-125) AS-IS (D-05 builds on the existing label, no reclassification); keep `id_of`/`norm_mods`
      on the PIPE form; rewrite ONLY the render block (268-285) and ADD the new pure transforms.
    - cli/lib/ansi.lua (Plan 01) — `M.red`/`M.bold_red` for the conflict color path.
    - cli/lib/color.lua lines 1-26 — the PURITY banner style for the new pure functions.
    - cli/spec.lua lines 106-108 — the sibling `keys:flag("--json", ...)`. For a VALUE option use
      argparse `:option(...)` not `:flag(...)`.
    - 06.5-UI-SPEC.md — every locked literal (Spacing column widths, Typography canonicalize/mod-order,
      Layout section ordering, Platform Label inline-annotation, Color gate + reserved-fields rule,
      Copywriting exact headers + footnote + empty-state OMIT rules).
    - 06.5-RESEARCH.md Pattern 1/2/3 (canonicalize, fixed mod order caveat about `id_of`, action grouping)
      + Pitfall 2 (letter-only guard) + the Code Examples (TTY+NO_COLOR gate via `io.popen("test -t 1")`).
    - config/wezterm-setup/keybindings.lua line 48 (`M.super_mod`) — context for the SUPER label intent.
  </read_first>
  <action>
    In `cli/commands/keys.lua` add module-level PURE functions (no io/os/wezterm), each with a
    one-line `---` doc comment:
    - `canonicalize_key(key, mods)`: if `key:match("^%a$")` AND `key:upper()==key` AND mods has no
      SHIFT → return `key:lower()` and mods with `SHIFT` added; else return unchanged. Letter-only
      (digits/symbols/named keys pass through — RESEARCH Pitfall 2).
    - a NEW human mod renderer (e.g. `render_mods`): split on `|`, drop NONE/empty, sort by fixed
      rank `{SUPER=1,CTRL=2,ALT=3,SHIFT=4}` (unknown→99), join with `+`. Keep `norm_mods`/`id_of`
      UNCHANGED on the pipe form (RESEARCH Pattern 2 caveat — id_of stays the cross-source key).
    - `platform_label(mods_or_chord, platform)`: map the `SUPER` token to `Super` (linux) / `Cmd`
      (macos); under `all` produce the inline annotation `"Cmd+… (macOS) / Super+… (Linux)"` for
      Super-bearing chords, plain otherwise (non-Super chords are platform-invariant).
    - `group_by_action(entries)`: collapse entries to one row per action, building the `, `-joined
      chord-list with the reliable (non-SUPER `CTRL+SHIFT+…`) chord FIRST on linux; D-08 case-variant
      twins already collapse because canonicalize lowercases before grouping.
    Apply canonicalize to each parsed record before grouping/rendering. Keep the existing category
    grouping (Tabs/Panes/Navigation/Font/Other) as the INNER unit (RESEARCH Open Q2: nest, don't flatten).
    Rewrite `M.run`'s human render block to: detect host OS (IO) + honor `args.platform`; gate color
    with `not args.json AND not os.getenv("NO_COLOR") AND tty` (TTY via the existing `capture`/`io.popen
    "test -t 1"` idiom); print `== wezterm-setup (managed) ==` first then `== WezTerm defaults ==`
    (omit a zero-row section), each row via `string.format("  %-26s %-30s %s\n", action, chord_list,
    "["..label.."]")`; emit the 3-line Linux Super `Note:` footnote (verbatim UI-SPEC text) once after
    the managed section when platform is linux/all; render conflicts ONLY when `#conflicts>0` with the
    `! `+`[CONFLICT]` markers and, when color allowed, `ansi.red` on the row + `ansi.bold_red` on the
    `== Conflicts (who-wins) ==` header — wrapping ONLY our fixed columns, NEVER the opaque `action`
    string (UI-SPEC ANSI-injection-safety). The `--json` path stays EXACTLY as-is (native mods, no
    color, dkjson) — only ensure no SGR enters it.
    In `cli/spec.lua` register `keys:option("--platform", "Render platform labels: linux | macos | all
    (default: host OS)")` on the existing `keys` command handle (the only NEW selector flag, D-04).
  </action>
  <verify>
    <automated>lua5.4 cli/commands/keys_test.lua && lua5.4 cli/spec_test.lua && make test</automated>
  </verify>
  <acceptance_criteria>
    - `lua5.4 cli/commands/keys_test.lua` exits 0 (all Task-1 assertions GREEN).
    - `grep -nE 'require\("wezterm"\)|io\.|os\.' cli/commands/keys.lua | grep -v '^[0-9]*:--'`
      shows io/os ONLY inside `run()`/`capture()`/`load_our_bindings`/`keybindings_path` (the IO shell);
      the NEW transforms (`canonicalize_key`, `render_mods`, `platform_label`, `group_by_action`) contain
      NONE.
    - Rendered managed header byte-offset < defaults header byte-offset (D-05 ordering).
    - A shifted letter chord renders lowercase with explicit SHIFT (e.g. `ALT+SHIFT+x`), never `ALT+X`.
    - `--json` output contains zero `\27[` bytes and stays jq-valid (`wez keys --json | jq .` exits 0
      where a live session exists; otherwise the pure `build_json` shape is unchanged).
    - `cli/spec.lua` exposes `--platform` on `keys` (spec_test asserts it); `id_of`/`norm_mods` still
      pipe-form (classify regression assertion green).
    - `make test` full suite green (no regression).
  </acceptance_criteria>
  <done>The pure transforms + curated-first sectioned renderer are implemented, `--platform` is registered, conflicts render red on a TTY only and never in JSON, the action string is never SGR-wrapped, and the full headless suite is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `wezterm show-keys --lua` → renderer | UNTRUSTED process text parsed line-by-line by `cli/lib/showkeys.lua`; `action` kept opaque, never eval'd |
| renderer → terminal | SGR escape bytes written to stdout for conflict rows |
| `--json` document → consumer | machine-readable; MUST be free of SGR bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-01 | Tampering/Elevation | `show-keys` text parse | mitigate | Preserve `showkeys.parse` line-by-line pattern parsing; NEVER `load()`/`eval` the text; keep `action` an opaque verbatim string (T-05-01 already in place). This plan does NOT introduce eval for the renderer. |
| T-06.5-02 | Tampering | ANSI injection via `action` text | mitigate | `ansi.red`/`bold_red` wrap ONLY our fixed columns (action/chord/label cells we format); the opaque `action` payload is NEVER passed through an SGR builder that could interpret embedded escapes (UI-SPEC ANSI-injection-safety; RESEARCH Security Domain). Conflict reasons are our own fixed tokens (`overridden`/`action-mismatch`). |
| T-06.5-03 | Information disclosure | SGR bytes leaking into `--json` | mitigate | Color gate is `not args.json AND not NO_COLOR AND tty`; the `--json` branch never calls `ansi.*`. A test asserts zero `\27[` in JSON output. |
| T-06.5-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH: zero external deps; pure Lua 5.4 + already-vendored argparse/dkjson). No legitimacy gate applies. |
</threat_model>

<verification>
- `lua5.4 cli/commands/keys_test.lua` → exit 0 (canonicalize/mod-order/dedupe/grouping/sectioning/platform/color-gate).
- `lua5.4 cli/spec_test.lua` → `--platform` registered on `keys`.
- Purity grep: the new transforms carry no io/os/wezterm; only `run()`/`capture()` do IO.
- `--json` carries no `\27[` byte; `id_of`/`norm_mods` keep the pipe form (classify intact).
- `make test` full suite green.
</verification>

<success_criteria>
- Every chord renders unambiguously (lowercase base + all mods explicit, fixed order, `+` join).
- Twin/dup rows collapse; actions render one row with grouped chords reliable-first.
- Managed section prints before defaults; nothing hidden; Linux Super footnote present on linux/all.
- Conflicts red on a TTY only, never in JSON, action string never SGR-wrapped.
- `--platform linux|macos|all` works; host OS is the default.
- Full headless suite green; one RED commit + one GREEN commit (REFACTOR only if needed).
</success_criteria>

<output>
Create `.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-02-SUMMARY.md` when done
</output>
