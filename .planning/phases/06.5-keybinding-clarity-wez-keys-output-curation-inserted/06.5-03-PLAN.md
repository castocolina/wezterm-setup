---
phase: 06.5-keybinding-clarity-wez-keys-output-curation-inserted
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - config/wezterm-setup/keybindings.lua
autonomous: true
requirements:
  - "keybinding/CLI UX clarity"
user_setup: []

must_haves:
  truths:
    - "Each reconciled action (ClearScreenAndScrollback, CloseCurrentTab) has ONE Ctrl|Shift entry, not a lower+upper twin pair (D-10)"
    - "The canonical declaration form is single lowercase base key + explicit SHIFT in mods, mirroring the working close-pane mapped('x') ALT|SHIFT entry"
    - "No action.type changes, so init.lua resolve_action stays in lockstep (no config-load crash, Pitfall 3)"
    - "M.disabled_defaults and M.key_map_preference = Mapped stay consistent with the reconciled keys"
  artifacts:
    - path: "config/wezterm-setup/keybindings.lua"
      provides: "Reconciled curated key table — single-entry per binding, no lower+upper twins"
      contains: "key_map_preference"
  key_links:
    - from: "config/wezterm-setup/keybindings.lua"
      to: "config/wezterm-setup/init.lua resolve_action"
      via: "action.type unchanged -> existing arm covers it"
      pattern: "RotatePanes|ClearScreenAndScrollback|CloseCurrentTab"
---

<objective>
Reconcile `config/wezterm-setup/keybindings.lua` to the D-10 canonical declaration
form: drop the redundant lower+upper `Ctrl|Shift` TWINS for `ClearScreenAndScrollback`
and `CloseCurrentTab`, leaving ONE entry per binding — single lowercase base key +
explicit `SHIFT` in mods — exactly mirroring the already-working single-entry close-pane
row (`mapped("x")` `ALT|SHIFT`). RESEARCH empirically verified the twins collapse to ONE
effective row anyway, so the second entry was always dead weight (D-08/D-10).

This is a DATA edit (the curated key table), not pure I/O logic — hence `type: execute`,
not `tdd`. It changes FIRING behavior, so per project rule "verify before declaring done"
the live confirmation that every reconciled chord still fires is owned by Plan 04's
`WEZTERM_INTEGRATION=1` e2e + `.tmp/h<NN>-*/` recorded repro. This plan makes the edit and
the headless safety checks (suite + parse + lockstep); it does NOT mark the firing
behavior verified — Plan 04 does.

Purpose: Make the declared form match the D-01 display convention so declared == displayed == pressed.
Output: a reconciled `keybindings.lua` with no twin rows, ready for Plan 04's live firing probe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-CONTEXT.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-RESEARCH.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-PATTERNS.md

# The file being modified (the twins to collapse + the canonical single-entry analog):
@config/wezterm-setup/keybindings.lua
# Lockstep target — the closed resolve_action switch that must still cover every action.type:
@config/wezterm-setup/init.lua
</context>

## Artifacts this phase produces (Plan 03 slice)

- MODIFIED `config/wezterm-setup/keybindings.lua` — twin rows removed; comments updated to
  describe the single-entry D-10 form instead of the dual lower+upper pattern.

No new symbols. The firing-behavior VERIFICATION artifact (`.tmp/h<NN>-*/` repro + the
integration test) is owned by Plan 04.

<tasks>

<task type="auto">
  <name>Task 1: Collapse the lower+upper twins to single D-10 entries; keep init.lua in lockstep</name>
  <files>config/wezterm-setup/keybindings.lua</files>
  <read_first>
    - config/wezterm-setup/keybindings.lua — the WHOLE file. The twins to drop:
      line 81 the `mapped("K")` `CTRL|SHIFT` ClearScreenAndScrollback row and line 92 the
      `mapped("W")` `CTRL|SHIFT` CloseCurrentTab row. The keepers: line 80 `mapped("k")`
      `CTRL|SHIFT` and line 91 `mapped("w")` `CTRL|SHIFT`. The Super-chord siblings (line 79
      `mapped("k")` `SUPER`, line 85 `mapped("w")` `SUPER`) STAY — they are the genuine second
      chord per action, NOT case twins. The canonical single-entry form to mirror: lines 100-103
      (the close-pane `mapped("x")` `ALT|SHIFT` family).
    - config/wezterm-setup/keybindings.lua lines 64-92 — the explanatory comments that currently
      justify declaring "both the lowercase k AND uppercase K" / "lower + upper produced char";
      these MUST be updated to the D-10 rationale (single entry; WezTerm folds Shift into the
      uppercase char and collapses the twins to one effective row, so the upper twin is redundant).
    - config/wezterm-setup/keybindings.lua lines 135-150 (`M.disabled_defaults`) + line 154
      (`M.key_map_preference = "Mapped"`) — confirm neither needs a change (the disabled defaults
      key on bare `k`/`w` `SUPER`, not the twins; preference stays Mapped).
    - config/wezterm-setup/init.lua resolve_action — a closed switch that `error()`s on an unknown
      `action.type` (RESEARCH Pitfall 3). D-10 changes only `key`/`mods`, never `action.type`, so
      resolve_action stays untouched — but VERIFY no type drifts.
    - 06.5-PATTERNS.md "config/wezterm-setup/keybindings.lua" section — the exact twins-to-collapse
      and canonical-form excerpts.
  </read_first>
  <action>
    Remove the two upper-case TWIN entries: the `mapped("K")` `CTRL|SHIFT` ClearScreenAndScrollback
    row (line ~81) and the `mapped("W")` `CTRL|SHIFT` CloseCurrentTab row (line ~92). Keep exactly
    ONE `CTRL|SHIFT` entry per action — the lowercase `mapped("k")` / `mapped("w")` form — alongside
    their unchanged `SUPER` siblings (a real distinct chord, not a case twin). Do NOT touch any
    `action.type` value, so `init.lua resolve_action` needs no change (lockstep preserved, Pitfall 3).
    Rewrite the now-stale comments (lines ~64-92) that describe declaring "both lowercase and
    uppercase" / "lower + upper produced char": replace with the D-10 rationale — a single
    lowercase-base + explicit-SHIFT entry is canonical; WezTerm folds Shift into the produced
    uppercase char and dedupes the twins to one effective row, so the second entry was redundant;
    this mirrors the already-working single-entry close-pane binding. Leave `M.disabled_defaults`
    and `M.key_map_preference = "Mapped"` unchanged (they stay consistent with the reconciled keys).
    Default to the D-10 PRIMARY lowercase form. The documented fallback (probe-driven only, decided
    in Plan 04 if Mapped mode requires the produced uppercase char to fire) is a single `mapped("K")`
    `CTRL|SHIFT` entry — STILL one entry, no twin — but do NOT pre-emptively use it here (RESEARCH
    verified both produce the identical effective row, so the lowercase primary is correct).
  </action>
  <verify>
    <automated>lua5.4 config/wezterm-setup/keybindings_count_check.lua 2>/dev/null || lua5.4 -e 'local m=dofile("config/wezterm-setup/keybindings.lua"); local n=0; for _,k in ipairs(m.keys) do if tostring(k.key)=="mapped:K" or tostring(k.key)=="mapped:W" then n=n+1 end end; assert(n==0,"upper twin still present"); assert(m.key_map_preference=="Mapped"); print("twins-collapsed-ok")'</automated>
  </verify>
  <acceptance_criteria>
    - The reconciled `config/wezterm-setup/keybindings.lua` contains ZERO `mapped("K")` and ZERO
      `mapped("W")` entries (the uppercase twins are gone); exactly one `mapped("k")` `CTRL|SHIFT`
      ClearScreenAndScrollback and one `mapped("w")` `CTRL|SHIFT` CloseCurrentTab remain, plus their
      unchanged `SUPER` siblings.
    - `dofile("config/wezterm-setup/keybindings.lua")` returns a table whose `keys` parses cleanly and
      whose `key_map_preference == "Mapped"` (unchanged).
    - No `action.type` value changed anywhere in the file (diff touches only `key`/`mods` rows + the
      comment block) — `init.lua resolve_action` needs no edit.
    - The clear-screen / close-tab comment block no longer claims "both lowercase AND uppercase"; it
      states the D-10 single-entry rationale.
    - `make test` full suite green (the data-only edit must not break any co-located test that loads
      keybindings.lua).
  </acceptance_criteria>
  <done>The lower+upper twins are collapsed to single D-10 entries, comments reflect the single-entry rationale, no action.type drifts (init.lua untouched), the module still loads with key_map_preference=Mapped, and the full headless suite is green. (Live firing verification is Plan 04.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config → installed config | The installed `keybindings.lua` is a COPY, not a symlink — repo edits do not take effect until re-synced (Pitfall 4; the sync + live firing test are Plan 04) |
| keybindings data → init.lua apply | `init.lua resolve_action` is a closed switch that crashes config load on an unknown `action.type` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-04 | Denial of Service | config-load crash from a drifted `action.type` (Pitfall 3) | mitigate | D-10 changes ONLY `key`/`mods`, never `action.type`; the acceptance criteria assert no type drift; the diff is restricted to the twin rows + comments. `init.lua resolve_action` stays in lockstep untouched. Plan 04's live reload would surface any config-error window immediately. |
| T-06.5-05 | Tampering | reconciled binding fails to fire on the live session | transfer | The firing verification is deliberately deferred to Plan 04's `WEZTERM_INTEGRATION=1` e2e + `.tmp/h<NN>-*/` recorded repro (per project "verify before declaring done"); this plan does NOT declare firing verified, only that the data edit + headless checks pass. |
| T-06.5-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH: zero external deps; pure Lua 5.4). No legitimacy gate applies. |
</threat_model>

<verification>
- `dofile` loads the reconciled `keybindings.lua`; `keys` parses; `key_map_preference == "Mapped"`.
- Zero `mapped("K")` / `mapped("W")` entries; the lowercase singles + `SUPER` siblings remain.
- No `action.type` drift (init.lua resolve_action untouched, lockstep preserved).
- `make test` full suite green.
- (Firing verification owned by Plan 04 — NOT asserted here.)
</verification>

<success_criteria>
- The dual lower+upper twins are reconciled to single D-10 entries.
- Comments describe the single-entry canonical form, not the dual pattern.
- `init.lua` stays in lockstep (no action.type change), module loads, suite green.
</success_criteria>

<output>
Create `.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-03-SUMMARY.md` when done
</output>
