# Phase 06.5 — Plan Review Feedback

**Source:** `/review-spec` (reviewing-specs skill, gsd · plan archetype) — fresh-context reviewer, grounded against the live codebase + `06.5-CONTEXT.md` + `06.5-RESEARCH.md` + approved `06.5-UI-SPEC.md` + `06.5-PATTERNS.md`.
**Date:** 2026-06-18
**Verdict:** Issues Found — replan to address HIGH + MEDIUM below.
**Scope:** Surgical edits only. The plans are otherwise verified coherent (all 10 CONTEXT decisions D-01..D-10 covered, requirement tag `keybinding/CLI UX clarity` present in all 4 plans, waves acyclic 01/03→02→04, threat models present, grounding accurate, TDD/execute split correct). Do NOT restructure the 4-plan / 3-wave decomposition; fix only the findings below.

---

## HIGH-1 — Plan 02 verify references a `--platform` assertion that `cli/spec_test.lua` does not contain, and the file is not in `files_modified` (affects 06.5-02)

**Where:**
- `06.5-02-PLAN.md` `<verification>` block — line `lua5.4 cli/spec_test.lua → '--platform' registered on 'keys'`; echoed in `<success_criteria>` item 5.
- `06.5-02-PLAN.md` frontmatter `files_modified` — `cli/spec_test.lua` is absent.

**Problem:** `cli/spec.lua` is modified by this plan to register the new `--platform` option, and the verify step asserts that registration via `cli/spec_test.lua`. But the existing `cli/spec_test.lua` (64 lines) only tests the Phase 6.2 surface (`pane icon`, `tab icon`, `--icon`, `--follow-pane-color`) — it has zero `--platform` coverage, and the plan never instructs the executor to add it or list `cli/spec_test.lua` as a modified file. As written the verify step either fails (executor runs the suite and finds no `--platform` assertion) or silently passes against a test that asserts nothing about the new flag — a false-positive verification.

**Required fix (06.5-02):** Either
(a) add `cli/spec_test.lua` to `files_modified` AND add a concrete Task 2 action to extend it with a `--platform`-registration assertion on the `keys` subcommand (mirroring the existing spec_test assertion style — grep/parse the registered options for `keys` and assert `--platform` is present with its `linux|macos|all` values), keeping the `<verify>` command as-is; OR
(b) replace the `<verify>` assertion with one that does not require `cli/spec_test.lua` to carry `--platform` coverage (e.g. assert directly against `cli/spec.lua` registration, or via a `wez keys --platform ...` behavioral check).
Option (a) is preferred — it keeps the new flag regression-guarded, consistent with the phase's "regression tests" scope item. Update `<read_first>`, the Task 2 `<action>`/`<acceptance_criteria>`, and `<success_criteria>` item 5 to match whichever option is chosen.

---

## MEDIUM-1 — Plan 02 never specifies the conflict-row format string (affects 06.5-02)

**Where:** `06.5-02-PLAN.md` Task 2 implementation actions + the `<must_haves><truths>` entry for conflict rows; cross-ref `06.5-UI-SPEC.md` "Spacing" section.

**Problem:** The plan specifies the main row format precisely (`"  %-26s %-30s %s\n"`) but leaves the conflict row format implicit. UI-SPEC says conflict rows use "a distinct format" from the main row but never gives the conflict-row format string. The current renderer (`cli/commands/keys.lua:281`) uses `("  ! %-16s %s\n")` (16-cell field). With no authoritative string, the `keys_test.lua` fixture and the renderer implementation can diverge on the conflict-row layout.

**Required fix (06.5-02):** Specify the exact conflict-row format string (column widths + field order, including the `! ` prefix and the `[CONFLICT]`/reason field) in Task 2's `<action>` and pin it in the `<acceptance_criteria>` so the fixture and renderer target one literal. If the intended conflict-row format equals the current `"  ! %-16s %s\n"` extended with the `[CONFLICT]` token + reason, state that literal explicitly. (Note for the human: UI-SPEC's "Spacing" section also omits the conflict-row format string — worth pinning at source separately so it stops propagating; not the planner's job in this pass.)

---

## MEDIUM-2 — Plan 01 has a duplicate `</output>` closing tag (affects 06.5-01)

**Where:** `06.5-01-PLAN.md`, end of file (~lines 187-189).

**Problem:** A trailing duplicate `</output>` tag leaves the document with a malformed `<output>` section. Harmless to a human executor but a structural anomaly that can confuse XML-aware tooling (e.g. `gsd-plan-checker`, automated plan parsers).

**Required fix (06.5-01):** Remove the duplicate trailing `</output>` so there is exactly one `<output>…</output>` section.

---

## Verified CLEAN — do NOT change (recorded so the replan does not "fix" non-issues)

- Wave ordering: 01 + 03 (Wave 1, no deps) → 02 depends on 01 (Wave 2) → 04 depends on 02 + 03 (Wave 3). Acyclic, consistent.
- `norm_mods`/`id_of` pipe-form preservation for cross-source matching, with a new `render_mods` producing the human `+`-joined form — consistent across Plans 02 and 04.
- Main row format `"  %-26s %-30s %s\n"` — consistent between Plan 02 and UI-SPEC.
- D-10 lockstep (keybindings.lua edits must not alter `action.type`; `init.lua resolve_action` excluded from modification) — correctly scoped to Plan 03, cross-referenced in Plan 04 Task 2.
- Plan 04 `SKIP-on-display-capable-host = FAILURE` (exit non-zero) divergence from `scene_cwd_integration_test.lua`, correctly called out in `read_first`.
- Color gate (TTY ∧ ¬--json ∧ ¬NO_COLOR) — consistent across Plan 02 actions, the `<threat_model>` T-06.5-03, and Plan 04's integration gate.
- All 10 CONTEXT decisions D-01..D-10 mapped; requirement tag `keybinding/CLI UX clarity` present in all 4 plans; threat models present on every plan; deferred items (6.4 doc audit, Phase 7 macOS, new keybindings) correctly excluded.
