---
phase: 06.5-keybinding-clarity-wez-keys-output-curation-inserted
plan: 04
type: execute
wave: 3
depends_on: ["06.5-02", "06.5-03"]
files_modified:
  - tests/integration/keys_integration_test.lua
autonomous: false
requirements:
  - "keybinding/CLI UX clarity"
user_setup: []

must_haves:
  truths:
    - "wez keys against a REAL running WezTerm shows the managed section before defaults (D-05)"
    - "Twin rows are collapsed to one and every chord renders lowercase base + explicit SHIFT (D-01/D-08)"
    - "An action with two chords renders one row, reliable-chord-first (D-07)"
    - "wez keys --json carries no ANSI bytes; wez keys --platform all inline-annotates Super chords"
    - "Red conflict output appears on a TTY and degrades to plain !/[CONFLICT] under NO_COLOR / non-TTY"
    - "After re-syncing the reconciled keybindings.lua (it is a COPY, Pitfall 4), every reconciled chord actually FIRES on the live session — proven by a concrete observable EFFECT (sentinel-gone screenshot for clear; pane/tab COUNT delta for close), not merely that wezterm cli list answers"
    - "On THIS display-capable host (DISPLAY set AND wezterm cli list --format json returns a pane_id, per RESEARCH Environment Availability) the e2e prints a LIVE-ASSERTED marker and the live assertions actually EXECUTE — a SKIPPED run here is a FAILURE, not a green pass"
  artifacts:
    - path: "tests/integration/keys_integration_test.lua"
      provides: "WEZTERM_INTEGRATION=1-guarded live e2e for ordering/dedupe/render/json/platform/red"
      min_lines: 60
  key_links:
    - from: "tests/integration/keys_integration_test.lua"
      to: "wez keys (live wezterm show-keys)"
      via: "spawn wez keys against the live mux, assert rendered stdout"
      pattern: "wez keys"
    - from: "reconciled keybindings.lua"
      to: "~/.config/wezterm/wezterm-setup/keybindings.lua"
      via: "cp re-sync before the live firing probe (Pitfall 4)"
      pattern: "wezterm-setup/keybindings.lua"
---

<objective>
Add the end-to-end test lane the operator explicitly requested ("use e2e testing
with all") and run the D-09 live firing hypothesis probe. The pure TDD fixtures
(Plan 02) prove the renderer logic headlessly; THIS plan proves the whole thing
against a REAL running WezTerm session per RESEARCH's Validation Architecture
(Lane B), and confirms the Plan 03 reconcile actually fires.

Two deliverables:
- An autonomous-where-possible integration test `tests/integration/keys_integration_test.lua`,
  guarded by `WEZTERM_INTEGRATION=1`. It self-skips ONLY where no display/mux exists
  (e.g. CI without WezTerm, mirroring `tests/integration/scene_cwd_integration_test.lua`'s
  no-mux gate); on a display-capable host (DISPLAY set AND `wezterm cli list --format json`
  returns a `pane_id`) the live assertions MUST execute and the run MUST print a `LIVE-ASSERTED`
  marker — a `SKIPPED` run on a display-capable host is a FAILURE, so a hollow/skipped pass is
  detectable. It asserts, end-to-end against
  the live session: curated-first ordering (D-05), twin/action dedupe (D-07/D-08),
  unambiguous lowercase render of a real shifted chord (D-01), `wez keys --json` (no
  ANSI) + `wez keys --platform all` (inline platform annotation), and red-conflict
  output behavior + its NO_COLOR / non-TTY degradation (D-06).
- The D-09 firing probe (a HYPOTHESIS under `.tmp/h<NN>-keys-reconcile/` per
  `docs/agent-iteration.md`): re-sync the COPIED installed config (Pitfall 4), then
  ACTUALLY press the reconciled chords in a live WezTerm window and confirm each action
  fires by its CONCRETE OBSERVABLE EFFECT (HIGH-1) — NOT merely that `wezterm cli list`
  answers. For ClearScreenAndScrollback: send a sentinel, press the chord, screenshot-confirm
  the sentinel is GONE. For CloseCurrentTab/close-pane: assert the tab/pane COUNT DELTA before
  vs after the press. This is a `checkpoint:human-verify` — the probe needs a live GUI session
  and a recorded manual repro; it is NOT a headless unit test, and it is NEVER auto-approved.

Purpose: Verify-before-done — the reconcile and the new output are confirmed on a real
session, not grep-verified. This is the phase's acceptance gate.
Output: the guarded e2e test + a recorded `.tmp/h<NN>-*/` firing repro (deleted after promotion).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-CONTEXT.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-RESEARCH.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-UI-SPEC.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-PATTERNS.md

# The live-e2e analog to copy (WEZTERM_INTEGRATION=1 guard + self-skip + package.path bootstrap):
@tests/integration/scene_cwd_integration_test.lua
# The renderer + run() being exercised end-to-end (from Plan 02):
@cli/commands/keys.lua
# The reconciled data being firing-tested (from Plan 03):
@config/wezterm-setup/keybindings.lua
# The hypothesis playbook for the .tmp/h<NN>-*/ firing probe:
@docs/agent-iteration.md
</context>

## Artifacts this phase produces (Plan 04 slice)

- NEW `tests/integration/keys_integration_test.lua` (guarded live e2e).
- A throwaway `.tmp/h<NN>-keys-reconcile/` hypothesis dir with a recorded firing repro
  (gitignored, deleted after manual promotion per project rule — NOT a committed artifact).

These are newly-created — exclude from source-drift/grounding verification. Note that the
integration test asserts against `wez keys` output produced by Plan 02 and the reconciled
chords from Plan 03 (cross-plan dependency is why this is Wave 3).

<tasks>

<task type="auto">
  <name>Task 1: Guarded live e2e — ordering / dedupe / render / json / platform / red against a real session</name>
  <files>tests/integration/keys_integration_test.lua</files>
  <read_first>
    - tests/integration/scene_cwd_integration_test.lua lines 1-45 — copy the header, the
      `package.path` bootstrap (repo-root resolution from `arg[0]`), the tiny `check` harness, the
      `shquote` helper, AND the `WEZTERM_INTEGRATION=1` guard + self-skip-when-no-live-mux pattern
      (exit 0 with an explicit logged skip, never a silent pass). BUT diverge from it on one point:
      that file's SKIP GATE 2 exits 0 whenever no mux answers; HERE the skip is only legitimate when
      the host is NOT display-capable. DISPLAY-CAPABLE PREDICATE (MEDIUM-2, matches RESEARCH
      §Environment Availability): `os.getenv("DISPLAY")` is non-empty AND `wezterm cli list --format
      json` parses to a list containing at least one `pane_id`. On a display-capable host a skip is a
      FAILURE — see the action below for the LIVE-ASSERTED-vs-SKIPPED branch recording requirement.
    - cli/commands/keys.lua (Plan 02) — `M.run`'s rendered output shape: the section headers, the
      `"  %-26s %-30s %s\n"` row format, the `--json`/`--platform` branches, the conflict block.
    - 06.5-UI-SPEC.md — the exact literals to assert: `== wezterm-setup (managed) ==`,
      `== WezTerm defaults ==`, `== Conflicts (who-wins) ==`, the 3-line Linux Super `Note:` footnote,
      `\27[31m`/`\27[1;31m`, `! `+`[CONFLICT]`, the `, ` chord join, fixed mod order.
    - 06.5-RESEARCH.md Validation Architecture "Lane B" + §Environment Availability (the display-capable
      predicate) + the `wez` dev-launcher invocation idiom (the e2e drives the real `wez keys` binary/
      launcher against the live `wezterm show-keys`).
  </read_first>
  <action>
    Create `tests/integration/keys_integration_test.lua` mirroring
    `scene_cwd_integration_test.lua`'s guard/self-skip/bootstrap. When the host is display-capable
    (the MEDIUM-2 predicate: DISPLAY non-empty AND `wezterm cli list --format json` returns a
    `pane_id`), it: (1) runs `wez keys` and captures stdout, asserting the
    `== wezterm-setup (managed) ==` header appears at a LOWER byte offset than
    `== WezTerm defaults ==` (D-05), that no uppercase-letter-implies-Shift chord appears (every
    letter chord is lowercase with explicit `SHIFT`, D-01), that an action reachable by two chords
    renders ONE row with a `, `-joined reliable-first chord-list (D-07) and that the old
    `CTRL+SHIFT+z`/`CTRL+SHIFT+Z` twin shows as a single row (D-08); (2) runs `wez keys --json`,
    asserting the output contains NO `\27[` byte and stays jq-valid (parse via the vendored dkjson or
    `jq .` exit 0); (3) runs `wez keys --platform all`, asserting a Super-bearing chord carries the
    inline annotation form (`(macOS)` AND `(Linux)` substrings on that row); (4) exercises the
    red-conflict path BOTH ways — with color allowed (TTY, no NO_COLOR) a conflict row (if any) carries
    `\27[31m` + `! ` + `[CONFLICT]`, and with `NO_COLOR=1` (or piped/non-TTY) the SAME row carries the
    `! `/`[CONFLICT]` text markers but ZERO `\27[` bytes (D-06 degradation). Each assertion self-skips
    gracefully (logged) if the live session lacks the data to exercise it (e.g. no conflicts present),
    never a hard fail on a clean host. Keep the file under the `WEZTERM_INTEGRATION=1` gate so the
    default `make test` run is unaffected.
    CRITICAL — record WHICH branch ran so a hollow pass is detectable: evaluate the MEDIUM-2
    display-capable predicate (DISPLAY non-empty AND `wezterm cli list --format json` answers with a
    `pane_id`). When display-capable, the live assertions MUST EXECUTE and the run MUST print an
    explicit `LIVE-ASSERTED` marker line (e.g. `keys_integration_test: LIVE-ASSERTED (DISPLAY=:1, mux
    reachable)`); print a `SKIPPED` marker line ONLY when the predicate is false (no display/mux). On a
    display-capable host a SKIPPED outcome is a FAILURE (exit non-zero) — DO NOT let the no-mux gate
    swallow a display-present run into a green skip. The per-assertion "no data to exercise this"
    graceful skip (e.g. no conflicts present) is still allowed and is NOT the same as skipping the whole
    live lane; the LIVE-ASSERTED marker must still print whenever the host is display-capable.
  </action>
  <verify>
    <automated>WEZTERM_INTEGRATION=1 lua5.4 tests/integration/keys_integration_test.lua 2>&1 | tee /tmp/keys_e2e.out; grep -q 'LIVE-ASSERTED' /tmp/keys_e2e.out</automated>
  </verify>
  <acceptance_criteria>
    - On a display-capable host (the MEDIUM-2 predicate: DISPLAY non-empty AND `wezterm cli list
      --format json` returns a `pane_id`, per RESEARCH §Environment Availability) the run prints a
      `LIVE-ASSERTED` marker (NOT `SKIPPED`) and the live assertions actually EXECUTE; a `SKIPPED`
      outcome here is a FAILURE (exit non-zero). The skip path is legitimate ONLY when the predicate is
      false (no display/mux — e.g. CI without WezTerm). The run records WHICH branch executed
      (live-asserted vs skipped) and the plan SUMMARY MUST report it, so a hollow/skipped pass is caught.
    - `WEZTERM_INTEGRATION=1 ./tools/run-tests.sh` runs the new file (it is discovered under
      `tests/integration/`) and exits 0 — with the live assertions PASSING (and a `LIVE-ASSERTED` marker)
      on a display-capable host, or with an explicit logged `SKIPPED` marker only when the predicate is
      false (never a silent pass, never a flaky hard fail).
    - The default `make test` (no `WEZTERM_INTEGRATION`) does NOT run this file (gated) and stays green.
    - When a live session IS present, the test asserts the UI-SPEC literals verbatim: managed header
      before defaults header; lowercase+explicit-SHIFT chords; one row per dual-chord action with `, `
      join (reliable CTRL|SHIFT chord first); `--json` free of `\27[`; `--platform all` inline
      `(macOS)`/`(Linux)` annotation; conflict red present on a TTY and absent (text markers only) under
      `NO_COLOR`.
    - The file copies the `scene_cwd_integration_test.lua` guard/self-skip/`package.path` structure
      (not a bespoke harness).
  </acceptance_criteria>
  <done>The guarded e2e exists, runs under WEZTERM_INTEGRATION=1, prints a LIVE-ASSERTED marker and actually executes the live assertions on a display-capable host (the MEDIUM-2 predicate; SKIPPED only when the predicate is false; a skip on a display-capable host is a FAILURE), and asserts curated-first ordering, dedupe, unambiguous render, json-no-ansi, platform-all annotation, and red-conflict TTY/NO_COLOR degradation against a real session; default make test is unaffected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: D-09 firing probe — re-sync the copied config, confirm every reconciled chord FIRES live by its concrete observable effect (recorded repro)</name>
  <files>.tmp/h&lt;NN&gt;-keys-reconcile/ (throwaway hypothesis repro; gitignored, deleted after promotion)</files>
  <action>
    HUMAN-VERIFY checkpoint — follows the GSD executor `checkpoint:human-verify` protocol (the
    executor BLOCKS here and pauses execution until the human records the repro and types the
    resume-signal; it is NEVER auto-approved) (MEDIUM-3). Full protocol in &lt;what-built&gt; /
    &lt;how-to-verify&gt; below. In brief: re-sync the COPIED installed `keybindings.lua` (Pitfall 4),
    reload WezTerm and confirm NO config-error window (Pitfall 3), then ACTUALLY press each reconciled
    chord on the live session and confirm each action FIRES by its CONCRETE OBSERVABLE EFFECT
    (HIGH-1) — NOT merely that `wezterm cli list` answers: for `Ctrl+Shift+K`
    (ClearScreenAndScrollback) send a known sentinel string first, press the chord, then screenshot-
    confirm the sentinel/scrollback is GONE; for `Ctrl+Shift+W` (CloseCurrentTab) and the `Alt+Shift+X`
    (close-pane) spot-check, capture the tab/pane COUNT from `wezterm cli list --format json` BEFORE the
    press and again AFTER, and confirm the count DROPPED by one (a before/after delta, not a standalone
    fire-detector). Record the repro under `.tmp/h&lt;NN&gt;-keys-reconcile/`, fold the verdict + evidence
    into the SUMMARY, then delete the scratch dir. If a chord does not fire, switch that single entry to
    the documented `mapped("K")`/`mapped("W")` uppercase-char fallback (still ONE entry, no twin) and
    re-verify.
  </action>
  <what-built>
    Plan 03 reconciled `config/wezterm-setup/keybindings.lua` to single D-10 entries (no lower+upper
    twins) for ClearScreenAndScrollback and CloseCurrentTab. Plan 02 rewrote the `wez keys` renderer.
    Because the INSTALLED config is a COPY (not a symlink — Pitfall 4), the repo edit does not take
    effect until re-synced; and because the reconcile changes FIRING behavior, project rule
    "verify before declaring done" requires a live confirmation that every reconciled chord still
    fires — a recorded hypothesis repro proving a concrete observable EFFECT, not a grep and not merely
    that `wezterm cli list` answers.
  </what-built>
  <how-to-verify>
    1. Create a throwaway hypothesis dir `.tmp/h<NN>-keys-reconcile/` (gitignored; pick the next free
       `h<NN>` per `docs/agent-iteration.md`).
    2. Re-sync the COPIED installed config (Pitfall 4):
       `cp config/wezterm-setup/keybindings.lua ~/.config/wezterm/wezterm-setup/keybindings.lua`
       (or re-run the installer's config copy). Log the exact command in the repro. WezTerm hot-reloads
       on file change — confirm NO config-error window appears (Pitfall 3: a drifted action.type would
       crash config load here).
    3. In a live WezTerm window, ACTUALLY press each reconciled chord and confirm the action fires by
       its CONCRETE OBSERVABLE EFFECT (HIGH-1 — `wezterm cli list` answering is NOT proof a chord
       fired; you need the before/after effect):
       - `Ctrl+Shift+K` → ClearScreenAndScrollback. FIRST `wezterm cli send-text` a known sentinel into
         the focused pane (e.g. `printf 'SENTINEL_06_5_K\n'`) so the scrollback visibly contains it;
         confirm the sentinel is present (`import -window <id>` screenshot → Read the PNG, sentinel
         visible). THEN press `Ctrl+Shift+K`; take a SECOND screenshot and confirm the sentinel /
         scrollback is GONE (cleared). Sentinel-present-before → sentinel-gone-after IS the firing proof.
       - `Ctrl+Shift+W` → CloseCurrentTab. Capture the tab COUNT from `wezterm cli list --format json`
         BEFORE the press (count of tab objects), press the chord, capture the tab count AFTER, and
         confirm it DROPPED by exactly one (the before/after delta is the firing proof — `cli list` is a
         legitimate delta source here, not a standalone fire-detector). (Open a throwaway tab first so
         closing one is safe.)
       - Spot-check an unchanged sibling (`Alt+Shift+X` close-pane) the same way: pane COUNT in
         `wezterm cli list --format json` BEFORE vs AFTER drops by one. (Split a throwaway pane first.)
       On Pop!_OS the `Super+*` chords may be grabbed by the WM (expected — that is exactly why the
       reliable `Ctrl+Shift+*` chord exists and is listed first); confirm the Ctrl+Shift chord fires by
       its observable effect.
    4. Record the repro (commands + the before/after effect evidence: the two clear screenshots and the
       tab/pane count deltas + verdict) in the `.tmp/h<NN>-*/` dir, then run `wez keys` once more and
       eyeball that the output matches the UI-SPEC (managed-first, lowercase+explicit-SHIFT, dual-chord
       rows collapsed, Linux Super footnote present).
    5. If a reconciled chord does NOT fire (the documented fallback case): switch that single entry to
       the `mapped("K")`/`mapped("W")` uppercase-char form (STILL one entry, no twin), re-sync, and
       re-verify (same observable-effect method). Note the deviation in the SUMMARY and in STATE Decisions.
    6. Delete the `.tmp/h<NN>-*/` dir after recording the outcome in the SUMMARY (manual promotion per
       project rule — the repro evidence lives in the SUMMARY, the scratch dir is ephemeral).
  </how-to-verify>
  <verify>
    <human-check>Every reconciled chord confirmed FIRING on the live session by a concrete observable effect — ClearScreenAndScrollback (Ctrl+Shift+K): sentinel present before / GONE after, by screenshot; CloseCurrentTab (Ctrl+Shift+W) and close-pane (Alt+Shift+X): tab/pane COUNT in `wezterm cli list --format json` dropped by one BEFORE vs AFTER the press. Installed config re-synced byte-identical; no WezTerm config-error window; wez keys live output matches the UI-SPEC.</human-check>
  </verify>
  <acceptance_criteria>
    - The installed `~/.config/wezterm/wezterm-setup/keybindings.lua` is byte-identical to the
      reconciled repo file (re-sync confirmed), and WezTerm reloaded with NO config-error window.
    - Each reconciled chord is confirmed to FIRE on the live session by its CONCRETE OBSERVABLE EFFECT
      (HIGH-1), recorded in the repro: `Ctrl+Shift+K` → sentinel present-before / GONE-after via
      screenshot; `Ctrl+Shift+W` and `Alt+Shift+X` → tab/pane COUNT delta of −1 in `wezterm cli list
      --format json` BEFORE vs AFTER the press. (`cli list` answering alone is NOT accepted as firing
      proof.)
    - The SUMMARY records the firing verdict + the observable-effect evidence (screenshots + count
      deltas); any fallback-to-uppercase deviation is noted.
    - `wez keys` live output matches the UI-SPEC contract (managed-first, unambiguous render, collapsed
      dual-chord rows, Linux Super footnote).
  </acceptance_criteria>
  <done>Every reconciled chord is confirmed firing on the live session by a concrete observable effect (sentinel-gone screenshot for clear; tab/pane count delta for close), config re-synced, no config-error window, output matches UI-SPEC; any uppercase-char fallback is noted in the SUMMARY.</done>
  <resume-signal>Type "approved" once every reconciled chord is confirmed firing on the live session by its observable effect (clear: sentinel gone; close: count dropped by one) with recorded evidence, or describe which chord failed (triggers the documented uppercase-char fallback).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config → installed config | Installed `keybindings.lua` is a COPY; the live test exercises the OLD config unless re-synced (Pitfall 4) |
| keybindings data → WezTerm config load | `init.lua resolve_action` crashes config load on an unknown `action.type` (Pitfall 3) — surfaces as a config-error window on reload |
| `wezterm show-keys --lua` → renderer | untrusted text; effective table lists SUPER chords even when the WM grabs them (not truthful about reachability — the footnote is the honest fallback) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-04 | Denial of Service | config-load crash from a drifted action.type (Pitfall 3) | mitigate | The firing probe re-syncs then reloads WezTerm and explicitly checks for NO config-error window before pressing chords — a drift is caught immediately, not shipped. (Plan 03's `tests/config/apply_test.lua` lockstep guard is the headless first line.) |
| T-06.5-05 | Tampering | reconciled binding silently fails to fire | mitigate | This plan IS the live firing verification (the deferred-from-Plan-03 gate): each chord is proven by a CONCRETE OBSERVABLE EFFECT (sentinel-gone screenshot for clear; tab/pane count delta for close — HIGH-1), not by `cli list` answering, with a documented single-entry uppercase fallback if the lowercase form does not fire. |
| T-06.5-06 | Spoofing/Information disclosure | a Super chord rendered as reliable while the WM grabs it (D-03 truthfulness) | accept | No reliable runtime grab-detection exists (Pitfall 1); the renderer orders the reliable Ctrl+Shift chord first and prints the static Linux Super footnote — the probe confirms the reliable chord fires even when Super is grabbed. |
| T-06.5-02 | Tampering | ANSI injection via action text in live output | mitigate | The e2e asserts conflict color wraps only fixed fields and that `--json` carries no `\27[`; the renderer (Plan 02) never SGR-wraps the opaque action string. |
| T-06.5-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH: zero external deps; pure Lua 5.4 + live wezterm tool already audited). No legitimacy gate applies. |
</threat_model>

<verification>
- `WEZTERM_INTEGRATION=1 ./tools/run-tests.sh` → on a display-capable host (DISPLAY non-empty AND `wezterm cli list --format json` returns a `pane_id`) the new e2e prints `LIVE-ASSERTED` and the live assertions EXECUTE (a `SKIPPED` here is a FAILURE); the explicit-skip path is legitimate only when the predicate is false. Exit 0 on a passing live run.
- Default `make test` → unaffected (e2e gated), green.
- Recorded `.tmp/h<NN>-*/` repro confirms every reconciled chord FIRES live by its concrete observable effect (sentinel-gone screenshot for clear; tab/pane count delta for close), no config-error window.
- `wez keys` live output matches the UI-SPEC (managed-first, unambiguous, dedup'd, footnote).
</verification>

<success_criteria>
- The guarded live e2e covers ordering / dedupe / render / json / platform / red + degradation, and records WHICH branch ran (LIVE-ASSERTED vs SKIPPED, by the display-capable predicate) so a hollow/skipped pass on a display-capable host is detectable and treated as a FAILURE.
- The D-09 reconcile is confirmed firing on a real Linux session by a concrete observable effect (sentinel-gone for clear; count delta for close) with a recorded repro (verify-before-done satisfied).
- Default headless suite stays green; the integration file is gated behind WEZTERM_INTEGRATION=1.
</success_criteria>

<output>
Create `.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-04-SUMMARY.md` when done
</output>
