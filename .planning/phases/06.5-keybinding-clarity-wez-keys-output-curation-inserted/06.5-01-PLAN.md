---
phase: 06.5-keybinding-clarity-wez-keys-output-curation-inserted
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - cli/lib/ansi.lua
  - cli/lib/ansi_test.lua
autonomous: true
requirements:
  - "keybinding/CLI UX clarity"
user_setup: []

must_haves:
  truths:
    - "M.red(s) wraps s in SGR 31 + reset 0 exactly (\\27[31m s \\27[0m)"
    - "M.bold_red(s) wraps s in SGR 1;31 + reset 0 exactly (\\27[1;31m s \\27[0m)"
    - "The module loads under plain lua5.4 with zero io/os/wezterm references (pure)"
  artifacts:
    - path: "cli/lib/ansi.lua"
      provides: "Pure ANSI SGR red text-color helper (D-06)"
      exports: ["red", "bold_red"]
      contains: "return M"
    - path: "cli/lib/ansi_test.lua"
      provides: "Byte-exact RED fixtures for the SGR helper"
      min_lines: 20
  key_links:
    - from: "cli/lib/ansi_test.lua"
      to: "cli/lib/ansi.lua"
      via: "require(\"cli.lib.ansi\")"
      pattern: "require\\(\"cli\\.lib\\.ansi\"\\)"
---

<objective>
Create `cli/lib/ansi.lua`: a pure ANSI SGR text-color helper that emits the EXACT
red sequences the conflict renderer (Plan 02) and the e2e lane (Plan 04) assert
against. This is the smallest, most independent unit in the phase — it has no
dependencies and unblocks the conflict-color path of the renderer.

Per D-06 and the UI-SPEC Color contract, conflict rows are colored red
(`\27[31m`…`\27[0m`) and the conflict summary header is bold-red
(`\27[1;31m`…`\27[0m`). `cli/lib/color.lua` is an OSC *background* emitter and MUST
NOT be overloaded for this text-color concern (RESEARCH Anti-Patterns) — a new,
separate pure module is the locked decision.

Purpose: Give the renderer a fixture-testable, side-effect-free SGR builder so
ANSI emission stays pure and the *whether-to-emit* gate stays in the IO shell.
Output: `cli/lib/ansi.lua` (+ its RED-first test) producing byte-exact SGR.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-CONTEXT.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-UI-SPEC.md
@.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-PATTERNS.md

# Module-shape + PURITY banner analog (do NOT reuse color.lua for text color):
@cli/lib/color.lua
# Pure-lib test header + byte-exact assertion style:
@cli/lib/title_test.lua
</context>

## Artifacts this phase produces (Plan 01 slice)

- NEW module `cli/lib/ansi.lua` exporting `M.red(s)` and `M.bold_red(s)` (pure).
- NEW test `cli/lib/ansi_test.lua`.

These are newly-created symbols — exclude them from any source-drift/grounding pass
(they did not exist before this phase).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — byte-exact SGR fixtures for cli/lib/ansi.lua</name>
  <files>cli/lib/ansi_test.lua</files>
  <read_first>
    - cli/lib/title_test.lua — copy the pure-lib test header (require + `check`/`eq` helpers + the
      `lua5.4 cli/lib/ansi_test.lua` run comment + the `pass/fail` footer that `os.exit`s non-zero).
    - cli/commands/pane_test.lua lines 43-52 — the byte-exact OSC-sequence assertion STYLE to mirror
      for SGR bytes (assert the literal escape string, e.g. `"\27]11;…"`).
    - 06.5-UI-SPEC.md "Color" section — the locked SGR literals: conflict rows `\27[31m`…`\27[0m`
      (SGR 31 / reset 0); conflict summary header `\27[1;31m`…`\27[0m` (bold red).
  </read_first>
  <behavior>
    - Test: `M.red("x")` == `"\27[31mx\27[0m"` (red foreground + reset).
    - Test: `M.bold_red("x")` == `"\27[1;31mx\27[0m"` (bold red + reset).
    - Test: empty input round-trips — `M.red("")` == `"\27[31m\27[0m"` (no crash, no extra bytes).
    - Test: the input string is wrapped VERBATIM with no interior transform — `M.red("a b!")`
      == `"\27[31ma b!\27[0m"` (the helper never inspects/escapes its argument; ANSI-injection
      safety is the CALLER's job — it only ever passes our own fixed fields, never the untrusted
      action string).
  </behavior>
  <action>
    Author `cli/lib/ansi_test.lua` requiring the not-yet-existing `cli.lib.ansi`, mirroring the
    `cli/lib/title_test.lua` header (`local M = require("cli.lib.ansi")`, `check`/`eq` helpers,
    the run-from-repo-root comment, and the `ansi_test: N passed, M failed` footer ending in
    `os.exit(fail == 0 and 0 or 1)`). Encode the four behavior cases above as byte-exact `eq`
    assertions on the literal escape strings. The file MUST fail when run (module absent),
    proving RED.
  </action>
  <verify>
    <automated>lua5.4 cli/lib/ansi_test.lua; test $? -ne 0 && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `lua5.4 cli/lib/ansi_test.lua` exits NON-ZERO (module `cli.lib.ansi` does not exist yet).
    - The four assertions reference the EXACT literals `\27[31m`, `\27[1;31m`, `\27[0m` from the
      UI-SPEC Color section — no other SGR codes appear.
    - Test header matches the `cli/lib/title_test.lua` shape (require + check/eq + exit footer).
  </acceptance_criteria>
  <done>Test file exists, requires `cli.lib.ansi`, asserts the byte-exact red/bold-red sequences, and fails RED because the module is absent.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement the pure cli/lib/ansi.lua SGR helper</name>
  <files>cli/lib/ansi.lua</files>
  <read_first>
    - cli/lib/ansi_test.lua (from Task 1) — the byte-exact contract this must satisfy.
    - cli/lib/color.lua lines 1-26 + line 125 — copy the module skeleton AND the PURITY banner
      verbatim-in-spirit (`local M = {}` … `return M`; a header stating: no require("wezterm"),
      no io.*, no os.execute, no os.getenv — loads under plain lua5.4, unit-testable in isolation).
      Do NOT require or reuse color.lua (it emits OSC background color; this is a distinct
      text-SGR concern — overloading it is a locked anti-pattern).
    - 06.5-UI-SPEC.md "Color" section — the locked SGR sequences.
  </read_first>
  <action>
    Create `cli/lib/ansi.lua` as a pure module: `local M = {}` with a PURITY banner mirroring
    color.lua's (explicitly: no wezterm/io/os). Implement `M.red(s)` returning
    `"\27[31m" .. tostring(s) .. "\27[0m"` and `M.bold_red(s)` returning
    `"\27[1;31m" .. tostring(s) .. "\27[0m"`, each with a one-line `---` doc comment naming the
    SGR code (31 = red, 1;31 = bold red, 0 = reset). The helper wraps its argument VERBATIM and
    never inspects/escapes it. `return M`. No other functions — the exact sequences and module
    placement are the only discretion the CONTEXT grants, and they are now fixed by the UI-SPEC.
  </action>
  <verify>
    <automated>lua5.4 cli/lib/ansi_test.lua</automated>
  </verify>
  <acceptance_criteria>
    - `lua5.4 cli/lib/ansi_test.lua` exits 0 (all assertions pass).
    - `grep -nE 'require\("wezterm"\)|io\.|os\.execute|os\.getenv' cli/lib/ansi.lua | grep -v '^[0-9]*:--'`
      returns ZERO lines (purity — comment lines excluded from the count).
    - `M.red("x")` byte-equals `"\27[31mx\27[0m"` and `M.bold_red("x")` byte-equals
      `"\27[1;31mx\27[0m"` (the UI-SPEC literals).
    - Module returns a table and is `require`-able as `cli.lib.ansi`.
  </acceptance_criteria>
  <done>`cli/lib/ansi.lua` exists, is pure (purity grep 0), exports `red`/`bold_red` emitting the exact UI-SPEC SGR bytes, and the Task-1 test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process output → CLI render | `wez keys` parses untrusted `wezterm show-keys --lua` text (handled in Plan 02/showkeys, NOT here) |
| renderer → terminal | SGR escape bytes are written to the user's terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.5-01 | Tampering | `ansi.red`/`bold_red` wrapping untrusted text in SGR | mitigate | The helper wraps its argument VERBATIM and is documented "fixed fields only". Callers (Plan 02) MUST pass only our own fixed columns, NEVER the opaque `action` string — so embedded escapes in `action` are never re-emitted through an SGR builder (UI-SPEC ANSI-injection-safety). This module adds no eval and no parsing. |
| T-06.5-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH: zero external deps; pure Lua 5.4). No legitimacy gate applies. |
</threat_model>

<verification>
- `lua5.4 cli/lib/ansi_test.lua` → exit 0, all RED/bold-red byte assertions pass.
- Purity grep on `cli/lib/ansi.lua` (comment-filtered) → 0 io/os/wezterm references.
- `make test` (full suite) stays green — the new file is auto-discovered by `tools/run-tests.sh`.
</verification>

<success_criteria>
- `cli/lib/ansi.lua` exists, pure, exporting `red` + `bold_red` with the exact UI-SPEC SGR bytes.
- `cli/lib/ansi_test.lua` green; one RED commit + one GREEN commit.
- Full suite green (no regression).
</success_criteria>

<output>
Create `.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-01-SUMMARY.md` when done
</output>
