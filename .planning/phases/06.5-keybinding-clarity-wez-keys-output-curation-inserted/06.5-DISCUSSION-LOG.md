# Phase 6.5: Keybinding Clarity & `wez keys` Output Curation (INSERTED) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-18
**Phase:** 6.5-Keybinding Clarity & `wez keys` Output Curation (INSERTED)
**Areas discussed:** Chord render format, Default verbosity + flags, Dedupe boundary, keybindings.lua reconcile

---

## Chord render format — key token

| Option | Description | Selected |
|--------|-------------|----------|
| Canonical lowercase base key | `ALT+SHIFT+x` — token is the physical/base key; all mods (incl. Shift) explicit; case never carries meaning | ✓ |
| Produced-uppercase char | `ALT+SHIFT+X` — roadmap's literal example; uppercase produced char + Shift also listed (visually redundant) | |

**User's choice:** Canonical lowercase base key.
**Notes:** Eliminates the "uppercase implies Shift" ambiguity entirely. Renderer must canonicalize WezTerm's `ALT+X` produced-char form back to `ALT+SHIFT+x`.

## Chord render format — separator

| Option | Description | Selected |
|--------|-------------|----------|
| Plus + fixed order | `CTRL+SHIFT+x`; mods sorted to a fixed canonical order | ✓ |
| Pipe \| wezterm-native | `SHIFT\|CTRL+x` — mirror WezTerm's native mods string (the noisy form being cleaned up) | |

**User's choice:** Plus + fixed order.
**Notes:** Canonical order SUPER > CTRL > ALT > SHIFT.

## Chord render format — SUPER label

| Option | Description | Selected |
|--------|-------------|----------|
| Platform-aware label | `Cmd+k` on macOS, `Super+k` on Linux | |
| Literal SUPER token | always `SUPER+k` | |
| You decide | defer to planning | |

**User's choice:** (free text, no option selected) — "could the keys command accept arguments to show all or specific platform, be careful of super for linux. We need to research more about it."
**Notes:** Resolved into two items: (1) a `--platform linux|macos|all` flag, confirmed IN SCOPE; (2) the SUPER label render decision is tagged RESEARCH NEEDED, with the Linux WM Super-grab truthfulness problem as a first-class research concern (a friendly `Super+k` row may imply a chord works when the compositor grabs it).

---

## Default verbosity + flags — default view

| Option | Description | Selected |
|--------|-------------|----------|
| Curated-only by default | bare `wez keys` shows only managed bindings; defaults behind a flag | |
| Curated-first, all shown | managed section first, defaults after; nothing hidden | ✓ |

**User's choice:** Curated-first, all shown.

## Default verbosity + flags — filter flag

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — add curated-only flag | `wez keys --managed` shows just managed bindings | |
| No flag — default view only | always full curated-first view | |

**User's choice:** (free text, no option selected) — "The flag filter I asked was for platform and I dont want to dismiss default, only see first my curated, then the defaults marking conflicts also in red if there are present."
**Notes:** No curated-only filter flag. `--platform` is the only new selector flag. Defaults never hidden. NEW requirement surfaced: mark conflicts in red.

## Default verbosity + flags — conflict presentation

| Option | Description | Selected |
|--------|-------------|----------|
| Red inline + red summary | conflicting rows red in their section AND a red summary block | ✓ |
| Red summary section only | only the bottom summary block is red | |
| Red inline only | color rows in place; drop the summary | |

**User's choice:** Red inline + red summary.
**Notes:** Adds an ANSI SGR text-color path (new — `cli/lib/color.lua` is OSC background only). Color-safe: TTY-only, respect `NO_COLOR`, no color in `--json`.

---

## Dedupe boundary — multi-chord actions

| Option | Description | Selected |
|--------|-------------|----------|
| One row per action, chords grouped | collapse to one logical line listing all real chords | ✓ |
| Separate row per distinct chord | keep today's behavior; two truthful rows per dual-chord action | |

**User's choice:** One row per action, chords grouped.
**Notes:** Action-centric layout. Case-variant twins (`CTRL+SHIFT+z`/`CTRL+SHIFT+Z`) already collapse via the lowercase render. Chord ordering within a row ties to the Super-grab research (reliable chord first).

---

## keybindings.lua reconcile — scope

| Option | Description | Selected |
|--------|-------------|----------|
| Normalize declarations + live-verify | change the declarations; gate behind a real-WezTerm probe | ✓ |
| Display-only reconcile | leave firing untouched; canonicalize in the display layer only | |

**User's choice:** Normalize declarations + live-verify.

## keybindings.lua reconcile — canonical form

| Option | Description | Selected |
|--------|-------------|----------|
| Lowercase base + explicit SHIFT | single entry `mapped('w')` `CTRL\|SHIFT`; mirrors working close-pane | ✓ |
| Let the probe decide the form | defer the exact token to empirical evidence | |

**User's choice:** Lowercase base + explicit SHIFT.
**Notes:** One entry per binding, Shift explicit in mods, twins dropped. Mirrors the live-verified single-entry close-pane binding. Probe-confirmed; fallback to a single uppercase entry only if Mapped mode requires it.

---

## Claude's Discretion

- Internal mod-ordering constant; `--platform all` layout (columns vs annotations).
- ANSI helper placement (new `cli/lib/ansi.lua` vs inline) and exact red SGR sequence.
- Whether the action-centric regrouping reshapes the existing category grouping or layers on top.
- The live probe's harness shape (reuse the live-GUI/headless callback recipe).

## Deferred Ideas

- Doc audit of the final keybinding reality → Phase 6.4 (runs last; consumes `--platform all`).
- macOS on-hardware re-verification of the reconciled bindings → Phase 7.
- New keybindings / re-binding which actions exist → out of scope (6.5 changes declaration/display, not what is bound).
