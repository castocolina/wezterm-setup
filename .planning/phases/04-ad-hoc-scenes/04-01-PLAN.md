---
phase: 04-ad-hoc-scenes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - cli/lib/scene.lua
  - cli/lib/scene_test.lua
autonomous: true
requirements: [SCEN-02]

must_haves:
  truths:
    - "Given a layout name and pane count N, the split-sequence planner returns the exact ordered list of wezterm cli split-pane arguments needed to reach N panes for tall, tall:mirrored, grid, and horizontal layouts"
    - "Given a --pane spec string, the parser returns a table with cmd/color/title/shell fields correctly populated for both bare-command and key=value forms, in either order"
    - "Given an unknown layout name, unknown color name, or malformed --pane key, validation rejects with the exact UI-SPEC error message before any split/spawn plan is built (validate-before-emit)"
    - "Given a parsed `wezterm cli list --format json` pane table plus N, the materialization decider returns a plan describing reuse-current-tab (1 pane) or new-tab (>=2 panes), and the resulting final pane count is always exactly N"
    - "Pane IDs and tab IDs read from list output are validated as integers before being used downstream, rejecting non-numeric or out-of-range values"
  artifacts:
    - path: "cli/lib/scene.lua"
      provides: "Pure scene-building helpers: split-sequence planner (4 layouts), --pane spec parser, layout/color validators, materialization decider, pane/tab id validator"
      exports: ["M.plan_splits", "M.parse_pane_spec", "M.validate_layout", "M.validate_color", "M.decide_materialization", "M.validate_pane_id"]
    - path: "cli/lib/scene_test.lua"
      provides: "Fixture-style unit tests for every exported function in cli/lib/scene.lua, runnable under plain lua5.4"
  key_links:
    - from: "cli/lib/scene.lua"
      to: "cli/lib/title.lua"
      via: "M.resolve_title / M.resolve_title_str reused for the title= key and bare-command auto-title (D-07)"
      pattern: "require\\(\"cli\\.lib\\.title\"\\)"
    - from: "cli/lib/scene_test.lua"
      to: "cli/lib/scene.lua"
      via: "require(\"cli.lib.scene\") fixture tests, mirroring cli/lib/title_test.lua structure"
      pattern: "require\\(\"cli\\.lib\\.scene\"\\)"
---

<objective>
Build the pure-logic core for `wez scene new`: a layout-to-split-sequence planner for all 4 layouts (tall, tall:mirrored, grid, horizontal) using the equal-share `100/(remaining)`% rule, a `--pane key=value` spec parser, layout/color validators, a materialization decider (reuse-current-tab vs new-tab), and a pane/tab-id integer validator.

This plan produces ONLY pure functions in plain Lua 5.4 — no `wezterm` global, no I/O, no `cli/spec.lua` wiring (reserved for 04-03), no live `wezterm cli` invocation (reserved for 04-02). Every function is unit-testable in isolation and mirrors the `cli/lib/title.lua` module style (`local M = {}` / `return M`, `---` doc comments, guard-clause-first bodies).

Purpose: De-risk the hardest algorithmic surface of Phase 4 (split percent math per layout, parser grammar, materialization invariants) with a full RED/GREEN/REFACTOR TDD cycle before any live wiring exists, so 04-02/04-03 consume already-correct, already-tested pure helpers.

Output: `cli/lib/scene.lua` (new pure module) + `cli/lib/scene_test.lua` (new fixture test suite).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ad-hoc-scenes/04-CONTEXT.md
@.planning/phases/04-ad-hoc-scenes/04-RESEARCH.md
@.planning/phases/04-ad-hoc-scenes/04-UI-SPEC.md
@cli/lib/title.lua
@cli/lib/title_test.lua
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing fixture tests for split-sequence planner, validators, and id validator</name>
  <files>cli/lib/scene_test.lua</files>
  <read_first>
    - .planning/phases/04-ad-hoc-scenes/04-UI-SPEC.md (Layout Geometry Contract table — authoritative split rules and percent formulas for tall, tall:mirrored, grid, horizontal; N=1 special case; Copywriting Contract error strings; 10-color list)
    - .planning/phases/04-ad-hoc-scenes/04-RESEARCH.md (Pattern 1 — equal-share split sequencing per layout, the percent-drift pitfall: always compute `100/(remaining)` relative to the CURRENT target pane, not the original N)
    - cli/lib/title_test.lua (the `check`/`eq` harness pattern, `io.write` summary line, `os.exit(fail == 0 and 0 or 1)` exit-code convention to mirror exactly)
  </read_first>
  <behavior>
    Create `cli/lib/scene_test.lua` requiring a not-yet-existing `cli/lib/scene.lua` via `require("cli.lib.scene")`. Use the same `check`/`eq` local helper pattern as `cli/lib/title_test.lua`. Write fixture assertions (all MUST FAIL initially — module does not exist yet, so the run errors/exits non-zero) for:

    - `M.plan_splits(layout, n)` returning an ordered array of split-step tables. Each step table has fields: `direction` (one of "left"/"right"/"top"/"bottom"), `percent` (integer), and `target` (an opaque index/tag identifying which existing pane to split — use a 0-based or 1-based pane index consistent across all 4 layouts).
      - N=1, all 4 layouts -> `plan_splits` returns an empty table `{}` (no split).
      - `tall`, N=3 -> first step `{direction="left", percent=50, target=<pane 0>}`; second step splits the pane created by step 1 (the right-side pane) with `direction="bottom", percent=50` (100/(remaining=2)).
      - `tall:mirrored`, N=3 -> first step `{direction="right", percent=50, target=<pane 0>}` (mirrored from tall); second step identical shape to tall's second step (`bottom`, `percent=50`, targeting the pane created by step 1).
      - `horizontal`, N=4 -> exactly 3 steps, percents in order `[25, 33, 50]` (i.e. `100/4`, `100/3`, `100/2` rounded), each step `direction="right"`, each `target` referring to the ORIGINAL un-split remainder pane (pane 0) per the research Pattern 1 horizontal algorithm.
      - `grid`, N=5 -> `cols=3, rows=2` (ceil(sqrt(5))=3, ceil(5/3)=2). Assert `#plan_splits("grid", 5)` equals the total step count for: 1 horizontal band split (`bottom`, `percent=50`, splitting the original pane into 2 row-bands) + column splits within each row-band (row 1 has 3 cols -> 2 splits at percents `[33,50]`; row 2 has `5 - 3*(2-1) = 2` cols -> 1 split at percent `50`). Total = 1 + 2 + 1 = 4 steps. Assert the exact total count is 4 and assert the first step is the row-band split (`direction="bottom", percent=50`).
      - `grid`, N=1 -> `{}` (covered by the N=1 case above, but add an explicit grid-specific assertion too).
    - `M.parse_pane_spec(spec_string)` returning a table `{cmd=<string|nil>, color=<string|nil>, title=<string|nil>, shell=<boolean>}`.
      - `"htop"` (bare, no `=`) -> `{cmd="htop", color=nil, title=nil, shell=false}`.
      - `"shell"` (literal keyword, D-04) -> `{cmd=nil, color=nil, title=nil, shell=true}`.
      - `"cmd=htop,color=red,title=Monitor"` -> `{cmd="htop", color="red", title="Monitor", shell=false}`.
      - `"color=blue,cmd=htop"` (order-independent) -> same result as above minus title: `{cmd="htop", color="blue", title=nil, shell=false}`.
      - `"title=Logs,cmd=tail -f app.log"` -> `cmd="tail -f app.log"`, `title="Logs"` (value may itself contain spaces; only the FIRST `=` per `key=value` segment splits key from value, segments separated by top-level commas).
    - `M.validate_layout(name)` returns `true, nil` for each of `"tall"`, `"tall:mirrored"`, `"grid"`, `"horizontal"`; returns `false, "error: unknown layout 'bogus' — expected one of: tall, tall:mirrored, grid, horizontal"` for `"bogus"` (exact string match per UI-SPEC Copywriting Contract).
    - `M.validate_color(name)` returns `true, nil` for each of the 10 colors (`red, orange, yellow, green, teal, cyan, blue, navy, purple, pink`), case-insensitively (`"RED"` -> `true, nil`); returns `false, "error: unknown color 'mauve' — expected one of: red, orange, yellow, green, teal, cyan, blue, navy, purple, pink"` for `"mauve"`.
    - `M.parse_pane_spec` with an unrecognized key, e.g. `"cmd=htop,foo=bar"` -> returns `nil, "error: invalid --pane value 'cmd=htop,foo=bar' — unknown key 'foo' (expected cmd, color, title)"` (exact string per UI-SPEC, including the full original spec value echoed back).
    - `M.validate_pane_id(value)` / `M.validate_tab_id(value)` (or a single `M.validate_id(value)` used for both — your call, document which in scene.lua):
      - `42` or `"42"` -> `true, 42` (returns the coerced integer).
      - `"abc"`, `3.5`, `nil`, `""` -> `false, nil` (or `false, <error string>` — pick one and assert it consistently).
  </behavior>
  <action>
    Write `cli/lib/scene_test.lua` from scratch following the exact structural pattern of `cli/lib/title_test.lua`: top `require("cli.lib.scene")` as `M`, local `pass`/`fail` counters, local `check(name, cond)` and `eq(name, got, want)` helpers, numbered/lettered test names matching the behavior list above (group by function: `plan_splits` cases as `1*`, `parse_pane_spec` as `2*`, `validate_layout` as `3*`, `validate_color` as `4*`, parser-key-error as `5*`, id validator as `6*`). For table-returning functions (`plan_splits`, `parse_pane_spec`), write a small local `deep_eq(a, b)` helper (recursive table comparison) since `==` does not compare table contents — model it after the `eq` helper's structure but recurse into nested tables and arrays. End with the same `io.write(string.format("\nscene_test: %d passed, %d failed\n", pass, fail))` and `os.exit(fail == 0 and 0 or 1)` lines, renamed for `scene_test`. Do NOT create `cli/lib/scene.lua` in this task — the require MUST fail, proving RED.
  </action>
  <acceptance_criteria>
    - Running `lua5.4 cli/lib/scene_test.lua` from the repo root exits non-zero (module `cli.lib.scene` does not exist yet — `require` raises an error, or the process errors before reaching `os.exit`).
    - `cli/lib/scene_test.lua` contains at least one test case per behavior bullet above (minimum: 2 N=1 cases, 1 tall N=3, 1 tall:mirrored N=3, 1 horizontal N=4, 2 grid N=5 cases, 5 parse_pane_spec cases, 1 unknown-key error case, 4 validate_layout cases (3 valid + 1 invalid), 2 validate_color cases (case-insensitive valid + 1 invalid), and 4+ id validator cases).
    - File requires `cli.lib.scene` (relative module path matching luastatic convention, identical require style to `cli/lib/title_test.lua`'s `require("cli.lib.title")`).
  </acceptance_criteria>
  <verify>
    <automated>lua5.4 cli/lib/scene_test.lua; test $? -ne 0</automated>
  </verify>
  <done>cli/lib/scene_test.lua exists, covers all 6 function groups with the exact I/O examples specified, and fails (non-zero exit) because cli/lib/scene.lua does not yet exist.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement cli/lib/scene.lua to pass all fixture tests</name>
  <files>cli/lib/scene.lua</files>
  <read_first>
    - cli/lib/scene_test.lua (the file written in Task 1 — the exact contract this implementation must satisfy, including exact error-message strings and table shapes)
    - cli/lib/title.lua (module skeleton to mirror: `local M = {}`, `---` doc comments above each public function, guard-clause-first bodies, `tostring()`/`tonumber()` defensive coercion; reuse `require("cli.lib.title")` for `M.resolve_title_str` to compute auto-titles from `cmd` when `title=` is omitted, per D-07)
    - .planning/phases/04-ad-hoc-scenes/04-UI-SPEC.md (Layout Geometry Contract table — re-confirm the exact percent formulas while implementing `plan_splits`; Materialization Contract table for `decide_materialization`)
    - .planning/phases/04-ad-hoc-scenes/04-RESEARCH.md (Pattern 2 inventory/decision code sketch — the `current_pane_id`/`current_tab_id`/`tab_pane_count` lookup shape to reimplement as a pure function taking a pre-parsed pane list + current pane id as arguments, NOT reading `os.getenv` directly — 04-02 supplies those values)
  </read_first>
  <behavior>
    Implement every function exercised by `cli/lib/scene_test.lua` so the full suite passes:

    - `M.plan_splits(layout, n)`:
      - Guard: `n <= 1` -> return `{}` for all layouts.
      - `tall`: step 1 = `{direction="left", percent=50, target=0}` (splits original pane 0, creating pane 1 in the right 50%). For j = 1 to n-2 (0-indexed remaining count `r = n - j`), step (j+1) = `{direction="bottom", percent=round(100/r), target=<most-recently-created pane index>}`.
      - `tall:mirrored`: identical to `tall` but step 1's `direction="right"` (mirrored main pane). All subsequent steps identical shape/targets to `tall`.
      - `horizontal`: for j = 1 to n-1, `r = n - j + 1` (remaining count including the piece about to be split off), step j = `{direction="right", percent=round(100/r), target=0}` (always targets the original un-split remainder, pane 0, per Pattern 1).
      - `grid`: `cols = ceil(sqrt(n))`, `rows = ceil(n/cols)`. Step 1..(rows-1) = horizontal-style row-band splits applied recursively to pane 0 with `direction="bottom"` and percent = `round(100/remaining_rows)` (mirrors the `horizontal` recursion but with `rows` substituted for `n` and `direction="bottom"`). Then for each row-band (in creation order), apply column splits: row's column count = `cols` for all rows except the last, where it is `n - cols*(rows-1)`; column splits use `direction="right"`, percent = `round(100/remaining_cols)`, targeting that row-band's pane index, mirroring the `horizontal` recursion per-row. Append all column-split steps after all row-band steps, grouped by row in creation order.
      - Use Lua's `math.ceil`, `math.floor`, and round-half-up via `math.floor(x + 0.5)` for all percent calculations (matches the `[25,33,50]` horizontal N=4 example: `100/4=25`, `100/3=33.33->33`, `100/2=50`).
    - `M.parse_pane_spec(spec)`:
      - Guard: `spec == "shell"` (exact match, D-04) -> return `{cmd=nil, color=nil, title=nil, shell=true}`.
      - Guard: `spec` contains no `=` at all -> bare command form, return `{cmd=spec, color=nil, title=nil, shell=false}`.
      - Otherwise: split `spec` on top-level commas (commas not inside a `key=value` value — since values themselves may contain commas is NOT a documented case, split on ALL commas, then split each segment on the FIRST `=` into key/value). For each segment, key must be one of `cmd`, `color`, `title` (case-sensitive lowercase keys per UI-SPEC examples); on first unrecognized key, return `nil, "error: invalid --pane value '<original spec>' — unknown key '<key>' (expected cmd, color, title)"` (interpolate the ORIGINAL full `spec` string and the offending `key`).
      - Assemble `{cmd=<from cmd= or nil>, color=<from color= or nil>, title=<from title= or nil>, shell=false}`.
    - `M.validate_layout(name)`: table-lookup against `{tall=true, ["tall:mirrored"]=true, grid=true, horizontal=true}`. Valid -> `true, nil`. Invalid -> `false, "error: unknown layout '<name>' — expected one of: tall, tall:mirrored, grid, horizontal"`.
    - `M.validate_color(name)`: lowercase the input via `tostring(name):lower()`, table-lookup against the 10-color set (`red, orange, yellow, green, teal, cyan, blue, navy, purple, pink`). Valid -> `true, nil`. Invalid -> `false, "error: unknown color '<original name as given>' — expected one of: red, orange, yellow, green, teal, cyan, blue, navy, purple, pink"` (echo the ORIGINAL case in the error message, not the lowercased form — match `cli/lib/scene_test.lua`'s exact assertion).
    - `M.validate_pane_id(value)` (and `M.validate_tab_id` as an alias or identical second function, per your scene_test.lua naming choice): coerce via `tonumber(value)`; reject if result is `nil`, non-integer (`x ~= math.floor(x)`), or negative. Valid -> `true, <integer>`. Invalid -> `false, nil` (or the error-string variant chosen in Task 1 — must match exactly).
    - `M.decide_materialization(panes, current_pane_id, n)`: pure function. `panes` is an array of `{pane_id=<int>, tab_id=<int>, ...}` tables (shape supplied by 04-02 from parsed `wezterm cli list --format json`). Reimplement the Pattern 2 lookup purely: find `current_tab_id` by scanning `panes` for `pane_id == current_pane_id`; count `tab_pane_count` = panes sharing that `tab_id`. If `tab_pane_count == 1` (D-10, regardless of N per D-12 "exactly N final, never N+1" — reuse applies whenever the CURRENT tab has exactly 1 pane, i.e. N==1 in THIS pane-count's context per D-01/D-10) -> return `{mode="reuse", target_tab_id=current_tab_id, first_pane_id=current_pane_id}`. Else (D-11) -> return `{mode="new-tab", target_tab_id=nil, first_pane_id=nil}` (the live wrapper fills in the new tab/pane ids after spawning). Document in a `---` comment above the function that `decide_materialization` does not itself decide based on `n` beyond what `plan_splits(layout, n)` already encodes — `n` is accepted for signature symmetry / future validation but the mode decision per D-10/D-11 is driven by `tab_pane_count == 1`.
    - Reuse `require("cli.lib.title")` and call `M_title.resolve_title_str(cmd)` (or build the title from `cmd` per D-07) where `parse_pane_spec` needs to supply an auto-title — actually, per D-07 "auto pane title from command when title= omitted" is a LIVE-WRAPPER concern (the title gets SET via `wez pane title` after spawn, 04-02's job) — so `parse_pane_spec` itself does NOT need to call title.lua; only require it if `scene_test.lua`'s fixtures actually assert a title-resolution behavior in `parse_pane_spec`. If Task 1's tests don't assert this, OMIT the `cli.lib.title` require entirely to keep this module's dependency surface minimal — note the omission in a comment for 04-02's benefit.
  </behavior>
  <action>
    Create `cli/lib/scene.lua` with `local M = {}` at the top and `return M` at the bottom, mirroring `cli/lib/title.lua`'s doc-comment style (`---` lines above each public function explaining the decision/spec reference it implements, e.g. "-- D-02 / UI-SPEC Layout Geometry Contract: tall layout"). Implement `plan_splits`, `parse_pane_spec`, `validate_layout`, `validate_color`, `validate_pane_id` (+ `validate_tab_id` if separate), and `decide_materialization` per the behavior spec above, until `lua5.4 cli/lib/scene_test.lua` passes with 0 failures. Iterate: run the test file, read failures, adjust implementation — do not modify the test file's assertions to fit the implementation (tests are the contract). If a genuine ambiguity in Task 1's tests blocks progress (e.g. an underspecified grid step ordering), resolve it by re-reading 04-UI-SPEC.md's Layout Geometry Contract table and 04-RESEARCH.md Pattern 1 as the tie-breaker, adjusting the TEST to match the spec-derived correct behavior (not the other way around), and noting the correction in a comment.
  </action>
  <acceptance_criteria>
    - `lua5.4 cli/lib/scene_test.lua` exits 0 with output ending `scene_test: N passed, 0 failed` where N equals the total assertion count from Task 1.
    - `cli/lib/scene.lua` contains no `require("wezterm")`, no `io.popen`, no `os.execute` — verified by `rg -c 'wezterm|io.popen|os.execute' cli/lib/scene.lua` returning `0` (or the grep producing no matches).
    - `M.plan_splits("grid", 5)` returns a 4-element array whose first element is `{direction="bottom", percent=50, target=0}` (or equivalent target-0 row-band split) — spot-checked by the grid N=5 test from Task 1.
    - `M.decide_materialization({{pane_id=1, tab_id=10}}, 1, 1)` returns `{mode="reuse", target_tab_id=10, first_pane_id=1}`; `M.decide_materialization({{pane_id=1, tab_id=10},{pane_id=2,tab_id=10}}, 1, 3)` returns `{mode="new-tab", target_tab_id=nil, first_pane_id=nil}`.
  </acceptance_criteria>
  <verify>
    <automated>lua5.4 cli/lib/scene_test.lua</automated>
  </verify>
  <done>cli/lib/scene.lua exists, exports plan_splits/parse_pane_spec/validate_layout/validate_color/validate_pane_id(/validate_tab_id)/decide_materialization, all are pure (no wezterm/IO), and `lua5.4 cli/lib/scene_test.lua` exits 0 with all assertions passing.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3 (REFACTOR): Clean up duplication and add edge-case coverage</name>
  <files>cli/lib/scene.lua, cli/lib/scene_test.lua</files>
  <read_first>
    - cli/lib/scene.lua (the GREEN implementation from Task 2 — look for duplicated percent-rounding logic across plan_splits' tall/tall:mirrored/horizontal/grid branches, and duplicated comma/equals-splitting logic if any helper was inlined repeatedly)
    - cli/lib/scene_test.lua (current coverage — identify any layout/N combination not yet exercised, e.g. tall N=2, tall:mirrored N=2, grid N=4 perfect square, horizontal N=2)
  </read_first>
  <behavior>
    No new public functions. Refactor internal structure only:
    - Extract a private local helper `local function round_pct(remaining)` returning `math.floor(100/remaining + 0.5)`, used by all 4 layout branches in `plan_splits` (removes ad-hoc rounding duplication from Task 2).
    - If `parse_pane_spec`'s comma/equals-splitting logic was duplicated inline, extract a private local helper `local function split_kv_segments(spec)` returning an array of `{key, value}` pairs.
    - Add edge-case fixture tests to `cli/lib/scene_test.lua`: `tall` N=2 (1 step: `{direction="left", percent=50, target=0}` only — no second split since `r=1` after step 1 means no further split needed), `grid` N=4 (perfect square: `cols=2, rows=2`, total steps = 1 row-band split + 1 col split per row = 3 steps), `horizontal` N=2 (1 step: `{direction="right", percent=50, target=0}`), and `M.parse_pane_spec("")` (empty string -> bare command form with `cmd=""`, or document as an explicit rejected case if 04-UI-SPEC implies empty `--pane` values are invalid — re-check the Copywriting Contract's "empty state" error which is about ZERO `--pane` flags total, a 04-02/04-03 concern, not a single empty spec string; treat single empty spec as `cmd=""` for this module).
  </behavior>
  <action>
    Apply the `round_pct` and (if applicable) `split_kv_segments` extractions inside `cli/lib/scene.lua`, replacing inline duplicated arithmetic/parsing with calls to the new private locals. Add the new edge-case assertions to `cli/lib/scene_test.lua` (numbered `1g`/`1h`/`1i`/`2f` following the existing numbering scheme from Task 1). Re-run the full suite after each change; all assertions (original + new) must pass with 0 failures throughout the refactor — if a refactor step causes a regression, revert that step before proceeding.
  </action>
  <acceptance_criteria>
    - `lua5.4 cli/lib/scene_test.lua` exits 0 with `scene_test: M passed, 0 failed` where `M` > `N` from Task 2 (strictly more assertions due to added edge cases).
    - `rg -c 'function round_pct' cli/lib/scene.lua` returns `1` (helper extracted exactly once).
    - `rg -c 'math.floor\(100' cli/lib/scene.lua` returns `1` (only inside `round_pct` — no remaining inline duplication of the rounding formula).
  </acceptance_criteria>
  <verify>
    <automated>lua5.4 cli/lib/scene_test.lua && test "$(rg -c 'function round_pct' cli/lib/scene.lua)" = "1" && test "$(rg -c 'math.floor\(100' cli/lib/scene.lua)" = "1" && echo "refactor OK"</automated>
  </verify>
  <done>`round_pct` is extracted exactly once and is the sole site of the `math.floor(100/...)` rounding formula, the comma/equals splitting is consolidated where it was duplicated, the added edge-case fixtures (tall N=2, grid N=4, horizontal N=2, empty-spec) are present, and `lua5.4 cli/lib/scene_test.lua` exits 0 with strictly more passing assertions than Task 2 and zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| function arguments -> cli/lib/scene.lua | The only inputs are Lua values passed by the caller (04-02's live orchestrator): a layout name, pane-count integer, raw `--pane` spec strings, color names, and a pre-parsed topology array. No I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00 | (all categories) | cli/lib/scene.lua | N/A | This is a PURE-LOGIC module with NO independent attack surface: no `wezterm` runtime, no `io.popen`, no `os.execute`, no filesystem or network access (enforced by acceptance criterion `rg -c 'wezterm|io.popen|os.execute' cli/lib/scene.lua` == 0). Its only inputs are function arguments supplied by the calling plan (04-02), which owns the trust boundary where user CLI strings and `wezterm cli list` JSON enter the system and is responsible for validating pane/tab IDs (via this module's `validate_pane_id`/`validate_tab_id`) before any shell-out. Spoofing/Tampering/Repudiation/Info-Disclosure/DoS/EoP all apply at 04-02's I/O boundary, not here. |
| T-04-SC | Tampering | npm/pip/cargo installs | N/A | No new package-manager dependencies — stdlib Lua only (`math.ceil`/`math.floor`/`string`/`table`). No Package Legitimacy Audit required. |
</threat_model>

<verification>
- `lua5.4 cli/lib/scene_test.lua` exits 0 with all assertions passing (RED tests from Task 1 now green; Task 3 edge-case fixtures included).
- `rg -c 'wezterm|io.popen|os.execute' cli/lib/scene.lua` returns 0 — the module is pure, with no runtime or I/O dependency (the load-bearing property that lets 04-02 reuse it deterministically and lets these tests run without a live WezTerm session).
- `rg -c 'function round_pct' cli/lib/scene.lua` returns 1 — rounding formula consolidated to a single private helper after the REFACTOR.
- `M.plan_splits("grid", 5)` returns a 4-step array; `M.decide_materialization` returns `mode="reuse"` for a 1-pane current tab and `mode="new-tab"` otherwise (D-10/D-11).
</verification>

<success_criteria>
- `cli/lib/scene.lua` exports the pure planner/parser/validator/materialization API (`plan_splits`, `parse_pane_spec`, `validate_layout`, `validate_color`, `validate_pane_id`/`validate_tab_id`, `decide_materialization`) that 04-02 consumes — satisfying the logic half of SCEN-02.
- Every layout (tall, tall:mirrored, grid, horizontal) produces the correct equal-share split sequence at arbitrary N, including the N=1 special case (empty split list) and non-square grids, per the UI-SPEC Layout Geometry Contract.
- The module is provably pure (no wezterm/IO), so its full behavior is covered by `lua5.4 cli/lib/scene_test.lua` with zero failures and no live session required.
- No threat surface exists at this layer (T-04-00 N/A); all I/O-boundary threats are owned and mitigated by 04-02.
</success_criteria>

<output>
Create `.planning/phases/04-ad-hoc-scenes/04-01-SUMMARY.md` when done
</output>