---
phase: 04-ad-hoc-scenes
plan: 03
type: tdd
wave: 3
depends_on: [04-02]
files_modified:
  - cli/spec.lua
  - cli/commands/complete.lua
  - cli/commands/complete_test.lua
  - cli/lib/scene.lua
  - cli/lib/scene_test.lua
autonomous: true
requirements: [SCEN-01, SCEN-02]

must_haves:
  truths:
    - "`wez scene new --layout tall --pane a --pane b --color blue --title x` parses into result.command='scene', result.scene_cmd='new', with --layout, repeated --pane (order preserved), --color, and --title all populated"
    - "`wez __complete scene-layouts` prints exactly the 4 layout names (tall, tall:mirrored, grid, horizontal), one per line, exit 0"
    - "the 4 layout names exist in exactly one place (cli/lib/scene.lua's M.LAYOUTS) — complete.lua's scene-layouts provider and validate_layout both read from it, no second literal copy"
    - "flag-name completion for `wez scene new --<Tab>` (--layout/--pane/--color/--title) works automatically via the spec walker (D-16), requiring no changes to cli/commands/complete.lua beyond the scene-layouts context"
    - "the cli/wez.lua dispatcher is unchanged — scene routes via the existing allow-list to cli/commands/scene.lua exactly as pane/tab do"
  artifacts:
    - path: "cli/spec.lua"
      provides: "scene namespace argspec: `scene new --layout <value> --pane <spec> [--pane <spec> ...] [--color <value>] [--title <value>]`, mirroring pane/tab command_target blocks"
      exports: ["scene (parser:command), scene:command_target(\"scene_cmd\"), scene_new (parser:command)"]
    - path: "cli/lib/scene.lua"
      provides: "M.LAYOUTS ordered array of the 4 closed layout names, consumed by both M.validate_layout and complete.lua's scene-layouts provider"
      exports: ["M.LAYOUTS", "M.plan_splits", "M.parse_pane_spec", "M.validate_layout", "M.validate_color", "M.decide_materialization", "M.validate_pane_id"]
    - path: "cli/commands/complete.lua"
      provides: "scene-layouts completion context added to CONTEXTS, deriving candidates from cli/lib/scene.lua's M.LAYOUTS"
      exports: ["M.run (unchanged signature)"]
    - path: "cli/lib/scene_test.lua"
      provides: "test coverage for M.LAYOUTS (contents, order, and that validate_layout accepts every name in it and rejects names not in it)"
    - path: "cli/commands/complete_test.lua"
      provides: "test coverage for the scene-layouts context: exact 4 names, newline-separated, derived from cli.lib.scene"
  key_links:
    - from: "cli/spec.lua"
      to: "cli/commands/scene.lua"
      via: "dispatcher allow-list routes result.command == \"scene\" (unchanged dispatcher, T-01-02)"
      pattern: "scene:command_target\\(\"scene_cmd\"\\)"
    - from: "cli/commands/complete.lua"
      to: "cli/lib/scene.lua"
      via: "require(\"cli.lib.scene\").LAYOUTS"
      pattern: "require\\(\"cli\\.lib\\.scene\"\\)"
    - from: "cli/lib/scene.lua validate_layout"
      to: "cli/lib/scene.lua M.LAYOUTS"
      via: "validate_layout built from / checked against the same LAYOUTS array (no duplicate literal set)"
      pattern: "M\\.LAYOUTS"
---

<objective>
Complete the `scene new` command surface so it is fully spec-driven (D-16): replace the placeholder dispatch entry in `cli/spec.lua` with the full argspec (`--layout`, repeated `--pane`, `--color`, `--title`), mirroring the existing `pane`/`tab` blocks, and wire `--layout` tab-completion through the `wez __complete scene-layouts` hook backed by a single source of truth in `cli/lib/scene.lua`.

Purpose: 04-02 delivered the live orchestration logic (`cli/commands/scene.lua`) but `cli/spec.lua` does not yet declare the `scene` namespace at all — `wez scene new ...` currently has no registered argspec, so it cannot be dispatched, cannot appear in `wez keys`, and has no shell completion. This plan closes that gap, satisfying success criterion #3 (D-16: completion + `wez keys` auto-generated from `cli/spec.lua`).

Output: `cli/spec.lua` gains a `scene` / `scene new` command_target block; `cli/lib/scene.lua` gains an exported `M.LAYOUTS` array (refactoring `validate_layout`'s inline set so there is exactly one definition of the 4 layout names); `cli/commands/complete.lua` gains a `scene-layouts` context deriving from `cli.lib.scene.LAYOUTS`. Full TDD cycle with test coverage in `cli/lib/scene_test.lua` and `cli/commands/complete_test.lua`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ad-hoc-scenes/04-CONTEXT.md
@.planning/phases/04-ad-hoc-scenes/04-UI-SPEC.md
@cli/spec.lua
@cli/lib/scene.lua
@cli/lib/scene_test.lua
@cli/commands/complete.lua
@cli/commands/complete_test.lua
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Failing tests for the scene argspec and scene-layouts completion</name>
  <files>cli/lib/scene_test.lua, cli/commands/complete_test.lua</files>
  <read_first>
    - cli/lib/scene.lua — confirm validate_layout's current inline literal set `{tall=true, ["tall:mirrored"]=true, grid=true, horizontal=true}` (the set to refactor into M.LAYOUTS)
    - cli/lib/scene_test.lua — existing test harness pattern (check/eq/deep_eq helpers, io.write summary, os.exit)
    - cli/commands/complete.lua lines 1-100 — CONTEXTS table shape and existing provider functions (pane_colors, tab_colors) as the template for scene_layouts
    - cli/commands/complete_test.lua — existing test pattern for a CONTEXTS provider (e.g. how pane-colors / tab-colors are asserted)
    - cli/spec.lua lines 100-160 — the pane/tab command_target blocks (structure to mirror for the new scene block); note there is currently NO scene block in spec.lua to replace — this is a net-new addition inserted after the tab block and before __complete
  </read_first>
  <behavior>
    - cli/lib/scene_test.lua: new test asserting M.LAYOUTS is a table/array containing exactly {"tall", "tall:mirrored", "grid", "horizontal"} in that order (deep_eq against the literal array)
    - cli/lib/scene_test.lua: new test asserting validate_layout(name) returns true/ok for every name in M.LAYOUTS and returns false/err for a name not in M.LAYOUTS (e.g. "diagonal"), proving validate_layout is driven by M.LAYOUTS and not an independent copy
    - cli/commands/complete_test.lua: new test calling M.run({context = "scene-layouts"}) (or the equivalent existing call convention used for pane-colors/tab-colors in this file) and asserting the captured output is exactly the 4 names from cli.lib.scene.LAYOUTS, newline-separated, in M.LAYOUTS order, with return/exit value 0
    - cli/commands/complete_test.lua: new test asserting cli.commands.complete's scene-layouts output is identical to (derived from) require("cli.lib.scene").LAYOUTS — e.g. build the expected string from the required module's LAYOUTS table rather than a hardcoded literal, so the test itself enforces single-source-of-truth
  </behavior>
  <acceptance_criteria>
    - Running the test files now FAILS: M.LAYOUTS is nil (attempt to index/iterate nil), and CONTEXTS["scene-layouts"] does not exist (M.run returns 0/no output for an unrecognized context per complete.lua's "unknown contexts emit nothing but exit 0" contract) — both failures are expected and demonstrate the tests exercise not-yet-built functionality
    - No source files under cli/lib/scene.lua or cli/commands/complete.lua are modified in this task — tests only
  </acceptance_criteria>
  <action>
    Add the test cases described in &lt;behavior&gt; to the two existing test files, following each file's established check/assertion idiom exactly (same helper names, same io.write/exit summary pattern as the rest of the file — do not introduce a new harness). Do not write any non-test source in this task. Run both test files with plain lua5.4 and confirm the new assertions fail for the expected reason (missing M.LAYOUTS / missing scene-layouts context), not for an unrelated error (e.g. syntax error or require failure) — a require failure on cli.lib.scene itself would indicate a mistake in the read step, not the intended RED state.
  </action>
  <verify>
    <automated>lua5.4 cli/lib/scene_test.lua; lua5.4 cli/commands/complete_test.lua</automated>
  </verify>
  <done>Both test files run under lua5.4, the new assertions fail with errors attributable to missing M.LAYOUTS / missing scene-layouts context (not syntax/require errors), and all pre-existing assertions in both files still pass.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement M.LAYOUTS, the scene-layouts completion context, and the scene argspec</name>
  <files>cli/lib/scene.lua, cli/commands/complete.lua, cli/spec.lua</files>
  <read_first>
    - cli/lib/scene.lua — full source of validate_layout (exact line(s) containing the inline literal set) and the module's M = {} / return M structure
    - cli/commands/complete.lua lines 1-120 — full CONTEXTS table, the pane_colors/tab_colors provider functions (exact require + iteration + table.sort idiom), and M.run's dispatch loop (how it looks up CONTEXTS[context], calls it, and prints results)
    - cli/spec.lua lines 95-160 — the pane and tab command_target blocks verbatim, including their header comments, command/argument/flag/option call signatures, and the position of the __complete block immediately after tab (insertion point for the new scene block)
    - .planning/phases/04-ad-hoc-scenes/04-UI-SPEC.md — the exact invocation grammar: `wez scene new --layout <tall|tall:mirrored|grid|horizontal> --pane '&lt;spec&gt;' [--pane '&lt;spec&gt;' ...] [--color &lt;name&gt;] [--title "&lt;text&gt;"]`, --layout and at least one --pane required
  </read_first>
  <behavior>
    Implementation must satisfy Task 1's tests exactly:
    - cli.lib.scene.M.LAYOUTS == {"tall", "tall:mirrored", "grid", "horizontal"} (ordered array)
    - validate_layout(name) is reimplemented to check membership in M.LAYOUTS (e.g. build a lookup set from M.LAYOUTS at module load, or iterate M.LAYOUTS) instead of its own inline literal table — net result: exactly one definition of the 4 names in the file
    - cli.commands.complete CONTEXTS gains ["scene-layouts"] = scene_layouts, where local function scene_layouts() requires cli.lib.scene and returns a copy/iteration of scene.LAYOUTS (mirroring pane_colors/tab_colors's structure — no table.sort needed since M.LAYOUTS is already in the desired display order, do not alphabetize and break the tall/tall:mirrored/grid/horizontal ordering)
    - cli.spec scene block: a new `local scene = parser:command("scene", "Ad-hoc scene layouts: spawn panes from a layout template")` with `scene:command_target("scene_cmd")`, then `local scene_new = scene:command("new", "Create a new scene by splitting the current tab into a layout")` carrying:
        - `scene_new:option("--layout", "Layout name (tall, tall:mirrored, grid, horizontal)")` — required option
        - `scene_new:option("--pane", "...")` configured as repeatable/accumulating (use the same option API the parser library provides for multi-value options — check the parser library's existing usage in this codebase, e.g. argparse-style `:count("*")` or equivalent, for any other repeated-option precedent before choosing the call; if none exists, use the minimal API that makes repeated `--pane` accumulate into a list in the parsed result under result.pane)
        - `scene_new:option("--color", "Tab accent color for the new scene (color name, hex, or 'reset')")` — optional
        - `scene_new:option("--title", "Tab title for the new scene")` — optional
    - The new scene block is inserted after the tab block and before the `-- __complete (hidden, Plan 07)` block, with a header comment in the same style as the pane/tab blocks (phase reference, dispatch-routing note: result.command stays "scene", result.scene_cmd carries "new", routes to cli/commands/scene.lua via the unchanged dispatcher per T-01-02)
  </behavior>
  <action>
    Implement in this order: (1) cli/lib/scene.lua — add `M.LAYOUTS = {"tall", "tall:mirrored", "grid", "horizontal"}` near the top of the module (alongside any other module-level constants), then rewrite validate_layout to derive its membership check from M.LAYOUTS rather than a separate literal table, preserving its existing return signature and error message format exactly. (2) cli/commands/complete.lua — add `local scene = require("cli.lib.scene")` to the require block, add a `scene_layouts()` local function mirroring `tab_colors()`'s structure (iterate scene.LAYOUTS, return the list — do not append "reset", these are layout names not colors), and register it as `CONTEXTS["scene-layouts"] = scene_layouts`. (3) cli/spec.lua — insert the new scene/scene-new command_target block as described in &lt;behavior&gt;, matching the pane/tab blocks' exact call style (parser:command, command_target, option/argument/flag signatures) so `wez keys` and the completion generator pick it up with zero additional changes. Do not touch cli/wez.lua (the dispatcher) — confirm via `rg -n "scene" cli/wez.lua` before and after that the dispatcher file is unmodified (T-01-02 allow-list already covers any command registered in spec.lua's SUBCOMMANDS/parser tree). Run both test files from Task 1 and iterate until green; then run the full existing test suites for cli/lib/scene_test.lua and cli/commands/complete_test.lua to confirm zero regressions.
  </action>
  <verify>
    <automated>lua5.4 cli/lib/scene_test.lua && lua5.4 cli/commands/complete_test.lua && lua5.4 -e 'package.path = "./?.lua;" .. package.path; local spec = require("cli.spec"); local parser = spec.build_parser(); local r = parser:parse({"scene","new","--layout","tall","--pane","a","--pane","b","--color","blue","--title","x"}); assert(r.command=="scene" and r.scene_cmd=="new" and r.layout=="tall", "scene argspec wiring"); print("scene argspec OK")'</automated>
  </verify>
  <done>Both test files exit 0 (new assertions plus all pre-existing ones); `cli/lib/scene.lua` defines `M.LAYOUTS` exactly once and `validate_layout` derives from it; `cli/commands/complete.lua` resolves `scene-layouts` from `cli.lib.scene.LAYOUTS`; `cli/spec.lua`'s `build_parser()` parses `scene new --layout ... --pane ... --pane ... --color ... --title ...` into `result.command="scene"`, `result.scene_cmd="new"`, and the per-option fields; `cli/wez.lua` is byte-for-byte unchanged.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3 (REFACTOR): Consolidate single-source-of-truth and confirm D-16 auto-wiring</name>
  <files>cli/lib/scene.lua, cli/commands/complete.lua, cli/spec.lua</files>
  <read_first>
    - cli/lib/scene.lua — the just-added M.LAYOUTS and the rewritten validate_layout, to confirm no second literal copy of the 4 names survived the GREEN step
    - cli/commands/complete.lua — the scene_layouts provider and the require block, to confirm it mirrors the pane_colors/tab_colors idiom (no stray table.sort, no hardcoded list)
    - cli/commands/complete.lua header comment (lines 1-21) — verify the "teaching __complete a NEW context makes that completion work WITHOUT regenerating or editing the static completion scripts" claim holds: cli/commands/completions.lua must NOT have been touched
  </read_first>
  <behavior>
    - Exactly one definition of the layout-name set exists in the codebase (cli/lib/scene.lua M.LAYOUTS); `rg -n '"tall:mirrored"' cli/` returns at most the M.LAYOUTS definition line and the test files' expected-value literals (tests are allowed to assert against a literal — that is the point of the RED test), never a second production definition
    - The scene_layouts provider is structurally identical to tab_colors/pane_colors minus the "reset" append, keeping the completion module uniform and easy to extend
    - No change to cli/commands/completions.lua (the generated bash/zsh scripts) — flag-name and layout-value completion both flow from spec.lua + the __complete hook (D-16)
    - All other module-level constants in cli/lib/scene.lua remain untouched; the refactor is scoped to the layout set only
  </behavior>
  <acceptance_criteria>
    - `rg -n 'tall.*mirrored|horizontal' cli/lib/scene.lua` shows the layout names in M.LAYOUTS only (not also in a separate validate_layout literal)
    - `git diff --stat cli/commands/completions.lua` shows ZERO changes
    - Both test files still exit 0; the spec smoke-parse from Task 2 still passes
  </acceptance_criteria>
  <action>
    Review the GREEN implementation for duplication and idiom drift. If validate_layout still carries its original inline `{tall=true, ...}` literal alongside M.LAYOUTS, collapse it so the membership check is built from M.LAYOUTS (e.g. a module-load lookup set derived by iterating M.LAYOUTS once). Confirm the scene_layouts provider matches the tab_colors shape. Run `rg -n '"tall:mirrored"' cli/` and confirm the only production hit is the M.LAYOUTS line (test-file literals are expected and acceptable). Run `git diff --stat cli/commands/completions.lua` and confirm it is unmodified. Re-run both test files and the Task 2 spec smoke-parse; all must pass.
  </action>
  <verify>
    <automated>lua5.4 cli/lib/scene_test.lua && lua5.4 cli/commands/complete_test.lua && test -z "$(git diff --name-only cli/commands/completions.lua)" && echo "completions.lua untouched"</automated>
  </verify>
  <done>Single-source-of-truth holds (one M.LAYOUTS definition, validate_layout + scene-layouts completion both derive from it), cli/commands/completions.lua is unmodified, and all tests remain green — D-16 (spec-driven completion + `wez keys`) is satisfied with no duplicated layout list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CLI argv -> spec.lua parser | User-supplied `scene new --layout/--pane/--color/--title` tokens are parsed into a structured result by the argparse tree |
| shell Tab -> `wez __complete scene-layouts` | The generated completion script shells out to the hidden hook at Tab time; the hook prints candidate tokens consumed by the shell |
| cli/lib/scene.lua M.LAYOUTS -> complete.lua | The closed layout-name set crosses from the lib module into the completion provider's output stream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Injection (Tab-time code execution) | `wez __complete scene-layouts` output consumed by the shell completion function | mitigate | Candidates are the 4 fixed layout names from the closed `M.LAYOUTS` array — `[%w:]`-only identifiers with no shell metacharacters. complete.lua emits bare token + `\n` and nothing else (existing T-07-02 contract). The shell treats the output strictly as data during expansion, never as code. No user-derived string ever reaches the candidate stream for this context. |
| T-04-07 | Elevation of Privilege (dispatch bypass) | `scene` namespace registration in spec.lua routing to cli/commands/scene.lua | mitigate | The dispatcher (cli/wez.lua) is NOT edited (T-01-02): `scene` is dispatched through the SAME closed allow-list / parser tree that already gates pane/tab. Registering the command in spec.lua's parser is the only surface; an unregistered or misspelled subcommand falls through to the existing unknown-command path. result.command stays "scene" — no new dispatch branch is introduced. |
| T-04-08 | Tampering (drift / dual source) | layout-name set duplicated between validate_layout and the completion provider | mitigate | Refactor (Task 3) enforces a single `M.LAYOUTS` definition; both `validate_layout` and the `scene-layouts` completion derive from it, and the GREEN test asserts the completion output is built from `require("cli.lib.scene").LAYOUTS` (not a literal). Drift between "what validates" and "what completes" is structurally impossible. |
| T-04-SC | Tampering | npm/pip/cargo installs | N/A | This plan introduces no new package-manager dependencies — stdlib Lua and existing in-repo modules only. No Package Legitimacy Audit required. |

This is a LOW-surface plan: it adds declarative argspec data and a closed-set completion context. No live `wezterm cli` shell-outs, no user-string interpolation, no filesystem or network I/O are introduced.
</threat_model>

<verification>
- `lua5.4 cli/lib/scene_test.lua` exits 0 (M.LAYOUTS contents/order + validate_layout-derives-from-LAYOUTS assertions pass alongside 04-01's suite).
- `lua5.4 cli/commands/complete_test.lua` exits 0 (`scene-layouts` emits exactly the 4 names, newline-separated, derived from `cli.lib.scene.LAYOUTS`).
- `lua5.4 -e '...spec.build_parser():parse({"scene","new","--layout","tall","--pane","a","--pane","b","--color","blue","--title","x"})...'` yields `result.command=="scene"`, `result.scene_cmd=="new"`, `result.layout=="tall"`.
- `rg -n '"tall:mirrored"' cli/` shows the only PRODUCTION definition is `cli/lib/scene.lua`'s M.LAYOUTS line (test-file expected-value literals are acceptable).
- `git diff --name-only cli/commands/completions.lua cli/wez.lua` is EMPTY — neither the generated completion scripts nor the dispatcher were touched (D-16 + T-01-02).
- `lua5.4 -e 'require("cli.spec"); require("cli.commands.complete"); require("cli.lib.scene")'` loads all three modules without error.
</verification>

<success_criteria>
- `wez scene new` is fully registered in `cli/spec.lua` (`--layout` required, repeated `--pane`, `--color`, `--title`), so it dispatches to cli/commands/scene.lua and appears in `wez keys` and shell completion with zero dispatcher edits — satisfying D-16 / success criterion #3.
- `wez scene new --layout <Tab>` completes the 4 layout names via `wez __complete scene-layouts`, and `wez scene new --<Tab>` completes the flag names via the spec walker — neither requires editing the generated completion scripts.
- The 4 layout names exist in exactly one place (`cli/lib/scene.lua` M.LAYOUTS); validate_layout and the completion provider both derive from it, enforced by test.
- No threat in the STRIDE register is unmitigated — T-04-06/07/08 mitigated, no accepted residual risk (LOW-surface, declarative-only plan).
</success_criteria>

<output>
Create `.planning/phases/04-ad-hoc-scenes/04-03-SUMMARY.md` when done
</output>