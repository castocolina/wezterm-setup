# 06.9-04 SUMMARY — Tier 4 visual capture / RMSE diff / exact-byte approve

Shipped the deterministic half of Tier 4 as a genuine `*_e2e_test.lua` file
auto-discovered by `tools/run-e2e.sh`. `make e2e` runs it (self-skip without
`WEZ_E2E_VISUAL=1`). `make e2e-visual` captures four window-scoped scenarios
into a stable manifest; `APPROVE=1` promotes the exact prior-capture bytes.
No committed baseline PNGs (`.gitkeep` only) — Plan 05 owns the first reviewed
set. Did not call `gui.probe` (it fires a canary this host cannot deliver);
the visual driver never injects keystrokes.

R6 probes (deleted after encoding):

- ImageMagick 7.1.2-27: `compare -metric RMSE` writes `<raw> (<normalized>)`
  to STDERR with no trailing newline; identical `0 (0)` exit 0; solid-color
  mismatch `53509.1 (0.816497)` exit 1. Parse the parenthesized value; ignore
  compare's own exit 1.
- `spectacle -b -a -n -d 400 -o` is window-scoped (1150x873) — not full
  desktop (1920x1080) and not pane-pixel-exact (800x480, chrome/tab-bar).
- `mux.spawn_pane` opens a new tab whose pane is `is_active=true` (per-tab);
  still `activate_tab(tab1)` so tab 1 is selected for the tabbar capture.

Window cleanup: `xdotool search --class "wez-e2e"` empty; no leftover
`wez-e2e-fire` GUI process. `tests/e2e/baselines/linux/` contains only
`.gitkeep`.

## Task 1 verify (actual)

`VISUAL_DIFF_VERIFY_OK` exit 0.

```
identical_ec=0 different_ec=1 dim_ec=1 dim_msg_ec=0 bad_ec=1
=== identical ===
rmse=0 threshold=0.05 dimensions=100x100
=== different ===
rmse=0.816497 threshold=0.05 dimensions=100x100
=== dim ===
dimension mismatch: fresh=100x100 baseline=50x50
=== bad ===
cannot decode: /tmp/wez_visual_bad.txt.png
VISUAL_DIFF_VERIFY_OK
```

`bash -n` and `shellcheck -x` both passed.

## Task 2 verify (actual)

No-flag:

```
tier4 visuals: SKIP (WEZ_E2E_VISUAL != 1)
```

exit 0.

`WEZ_BIN=./dist/wez ./tools/run-e2e.sh` discovered and PASS'd
`tests/e2e/tier4/visual_diff_e2e_test.lua` (skip path). Makefile has
`$(if $(APPROVE)` and `WEZ_BIN:-./dist/wez`. `.gitkeep` is git-tracked.

`make e2e-visual APPROVE=1` with no prior capture:

```
tier4 visuals: LIVE-ASSERTED (needs xdotool/forced-XWayland + a screenshot tool + ImageMagick compare/identify)
no capture manifest found — run `make e2e-visual` first (without APPROVE), review the output, then approve.
make: *** [Makefile:50: e2e-visual] Error 1
```

Live `make e2e-visual` capture: exit 0, 24 passed / 0 failed. Manifest:

```
RUN_ID=1788986188
dev|/tmp/wez-e2e-visual-latest/dev.png|1150x873|NO BASELINE|NONE
ai|/tmp/wez-e2e-visual-latest/ai.png|1150x873|NO BASELINE|NONE
docker|/tmp/wez-e2e-visual-latest/docker.png|1150x873|NO BASELINE|NONE
tabbar|/tmp/wez-e2e-visual-latest/tabbar.png|1150x873|NO BASELINE|NONE
```

Log contains `tabbar: 2 tabs (same-title + active/inactive)`.

`make e2e-visual APPROVE=1 SCENARIOS=dev`: exit 0. Checksums matched
`e515c11e29e049413ce9f10ef3e6abb77cf11e1fe36d42a9f9b0840c3df93196`
(byte-for-byte, no recapture). Test-promoted `dev.png` deleted afterward.

Stale `RUN_ID=1` then `APPROVE=1 SCENARIOS=dev`:

```
stale capture manifest (captured 1788986215 seconds ago, max age is 3600) — re-run `make e2e-visual` first, then approve
```

exit non-zero; nothing promoted.

## Task 3 verify (actual)

```
README_VISUAL_DOCS_OK
```

exit 0.

## Commit

`512938a` — `test(06.9): ship Tier 4 visual capture/diff/approve driver`
