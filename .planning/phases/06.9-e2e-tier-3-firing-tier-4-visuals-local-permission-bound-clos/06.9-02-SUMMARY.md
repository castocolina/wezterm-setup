# 06.9-02 SUMMARY — e2e-setup provisioning (D-06/D-07/D-08/D-09)

Replaced the 06.6-era `tools/run-e2e.sh --setup` placeholder with
`tools/e2e-setup.sh`. `make e2e-setup` now runs that script.

On this box every tool was already present, `/dev/uinput` was already
writable via the systemd-udev `uaccess` ACL, and `sudo -n true` failed
(no passwordless ticket). The script skipped installs, skipped group/udev
changes, deferred nothing that needed mutation, and started one `ydotoold`
on the first run. The second run did not start a duplicate.

R6 probes (deleted after encoding into call-site comments): uaccess
writability holds; 70-*.rules GROUP=/MODE= would survive 71 TAG-only;
rpm-ostree `--idempotent` is the ostree native fallback and needs a reboot;
Homebrew-on-Linux packages only `xdotool` and `imagemagick` among this
set; `sudo -n true` is the hang-prevention gate.

## Task 1 verify

`bash -n` and `shellcheck -x` passed. Two consecutive runs exited 0.
`pgrep -c ydotoold` = 1. usermod group args: `input`, `uinput` only.
No bare `sudo` sites outside `sudo -n true` / `sudo -n "$@"`.

First live run (ydotoold started, pid 703464):

```
[e2e-setup] no passwordless sudo (sudo -n true failed) — privileged steps will be deferred, not blocked
[e2e-setup] os=linux
[e2e-setup] xdotool already present, skipping
[e2e-setup] ydotool already present, skipping
[e2e-setup] convert already present, skipping
[e2e-setup] already accessible via the active seat's systemd-udev uaccess ACL — no group/udev change needed
[e2e-setup] started ydotoold in background (pid 703464)
[e2e-setup] session desktop=KDE type=wayland
[e2e-setup] KDE Plasma detected; spectacle has no Homebrew-on-Linux formula — native/rpm-ostree path
[e2e-setup] spectacle already present, skipping
```

exit 0.

Official combined verify:

```
ec1=0 ec2=0 ydotoold=1 BAD_USERMOD=0 USERMOD_LINES=[input
uinput] SUDO_SITES=[]
E2E_SETUP_VERIFY_OK
```

`git ls-files -s tools/e2e-setup.sh` shows mode `100755`.

## Task 2 verify

`--setup` now `exec`s `${SCRIPT_DIR}/e2e-setup.sh`. Placeholder text is gone.
Plain `./tools/run-e2e.sh` still prints `run-e2e: 11 file(s)`.

```
make_ec=0 battery_ec=0
RUN_E2E_SETUP_WIRING_OK
```

`make e2e-setup` output matched the Task 1 second-run shape (tools present,
uaccess skip, `ydotoold already running`).

## Commit

`c8ef3fb3dcf40250908655100e77f42d8e51853c` — `feat(06.9): ship make e2e-setup provisioning script`
