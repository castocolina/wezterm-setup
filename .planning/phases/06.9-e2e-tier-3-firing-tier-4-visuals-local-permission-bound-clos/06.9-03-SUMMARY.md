# 06.9-03 SUMMARY — Full fire(B) chord matrix (ESCALATION CLAUSE)

Expanded Plan 01's tracer from 2 fire(B) cases to the full PRD Appendix A.3
set (12 actions), reused `gui.lua` unchanged, extended `mux.lua` /
`platform.lua fire_chords`, and filled the README Tier 3 Firing placeholders.
On this Wayland/KWin host the live matrix **cannot fire**, so
`WEZ_E2E_INPUT=1` **loud-SKIPS** rather than printing a false `LIVE-ASSERTED`.

## ESCALATION CLAUSE (triggered — same class as Plan 01)

`gui.probe` still fails the canary pane-text-mutation check on this host
(XTEST and ydotool/uinput both produce no mux effect). The file therefore
self-skips **before any of the 12 cases run**. That is expected and correct:
the official `<verify>` one-liner greps the live log for `LIVE-ASSERTED` and
per-action `h.check` labels, so it reports `FIRE_MATRIX_VERIFY_FAILED`. Do
not weaken the gate; the cases are structurally complete for a capable host.

R6 probes (deleted after encoding):

- `01-cli-list-tab-order`: JSON array order **holds** as left-to-right
  creation/UI order. `is_active` is **per-tab**, not window-level focus
  (every 1-pane tab reports `is_active=true`). Workaround: MoveTabRelative
  asserts ordered-tab-id permutation; ActivateTabRelative observes focus via
  `get_text` after a probe keystroke. `mux.active_pane` remains valid inside
  a single multi-pane tab (ActivatePaneDirection, TogglePaneZoomState).
- `02-xdotool-keysyms`: `Prior`/`Next`/`Tab`/`equal`/`minus`/`0`/`Left`/
  `Right`/`Up`/`Down` are valid xdotool names. `PageUp`/`PageDown` are **not**
  (`No such key name`). Encoded in `fire_chords.linux` and call-site comments.

## Task 1 verify (actual)

No-flag (`WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier3/firing_e2e_test.lua`):

```
tier3 firing: SKIP (WEZ_E2E_INPUT != 1)
```

exit 0.

`WEZ_E2E_INPUT=1` live run:

```
  retry - gui probe canary (attempt 1/8)
  ...
  retry - gui probe canary (attempt 8/8)
tier3 firing — focus-verified OS input injection: SKIPPED (reason=OS-level key injection cannot deliver a keystroke into a focus-verified forced-XWayland WezTerm window on this Wayland/KWin host (xdotool XTEST and ydotool/uinput both produce no mux effect))
```

exit 0. Official combined verify printed `FIRE_MATRIX_VERIFY_FAILED
missing=[ ClearScreenAndScrollback SpawnTab CloseCurrentTab
ActivateTabRelative MoveTabRelative SplitHorizontal SplitVertical
CloseCurrentPane TogglePaneZoomState RotatePanes Left Right Up Down
FontSize]` solely because it greps for `LIVE-ASSERTED` and per-action labels
in a skip log — the escalation-allowed outcome here.

Tier 2 regression (`WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier2/scene_reuse_e2e_test.lua`):

```
24 passed, 0 failed
```

exit 0. Additive `split_pane(opts)` did not break existing callers.

Structural confirmation (syntax-load + code review, since live cases never
run here): `lua5.4 loadfile` SYNTAX_OK for `mux.lua`, `platform.lua`,
`firing_e2e_test.lua`. `fire_chords.linux` and `.mac` both have all 12
fire(B) keys (FontSize as three string keys; ActivatePaneDirection has
left/right/up/down). Every required action name appears in an `h.check`
label. WordNav/SendString has no case (reg(M)-only, out of scope).

Window cleanup: `xdotool search --class "wez-e2e"` empty; no leftover
`wez-e2e-fire` GUI process.

## Task 2 verify (actual)

```
README_FIRING_DOCS_OK
```

exit 0. Firing row Phase is `06.9 (landed)`; tools table names `xdotool`,
`ydotool`/`ydotoold`, uaccess/allowlist; heading placeholder gone; Screenshot
cell is `_TBD (06.9-04)_`.

## Commit

`cad23bf` — `test(06.9): expand Tier 3 Firing matrix to all 12 fire(B) actions`
