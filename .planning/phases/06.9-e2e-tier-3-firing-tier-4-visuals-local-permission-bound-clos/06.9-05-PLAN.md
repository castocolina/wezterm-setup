---
phase: 06.9-e2e-tier-3-firing-tier-4-visuals
plan: 05
type: execute
wave: 4
depends_on: ["06.9-04"]
files_modified:
  - .claude/skills/e2e-visual-review/SKILL.md
  - tests/e2e/baselines/linux
autonomous: false
cross_ai: true

requirements: [PRD-T4-skill]

estimate:
  tokens: 90000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `e2e-visual-review` skill NEVER silently picks a vision-capable model — it enumerates candidates (current-runtime models + opencode's configured model via workflow.cross_ai_command when opencode is not the active runtime), explicitly flags which candidates are ONLY catalog-listed vs confirmed reachable/authenticated, and explicitly asks the user, with a suggested preference (D-02)."
    - "The skill instructs agent-PERFORMED visual judgment (reading a captured PNG and scoring the PRD Appendix A.4 checklist item-by-item, pass/fail, no percentage threshold) — never a scripted/API vision call (D-01)."
    - "The skill's SKILL.md scores >=90% when evaluated by the existing global skill-judge skill, via a genuinely separate/clean-context evaluation pass rather than self-scoring inline in the authoring context (D-03) — the ONLY numeric acceptance bar anywhere in this phase."
    - "This plan actually INVOKES the finished skill, once, end-to-end, against REAL screenshots captured by Plan 04's `make e2e-visual` — reading Plan 04's manifest, dispatching to a real, user-selected model (with a concrete, grounded adapter contract for at least the opencode CLI path), scoring the real A.4 checklist per scenario, and — for scenarios that pass every applicable item — running `make e2e-visual APPROVE=1 SCENARIOS=<names>` (Plan 04's REAL, valid invocation contract, never the prior plan's incompatible `--approve` form) to produce the FIRST genuinely-reviewed committed baseline PNGs. This is a real, human-in-the-loop checkpoint task, not something asserted only in the skill's own prose."
  artifacts:
    - path: ".claude/skills/e2e-visual-review/SKILL.md"
      provides: "the D-02 model-enumeration-and-ask workflow + the D-01 agent-performed A.4 checklist judgment procedure + Plan 04's real manifest/approve contract, scoring >=90% under a clean-context skill-judge evaluation"
      min_lines: 40
    - path: "tests/e2e/baselines/linux"
      provides: "at least one real, genuinely-reviewed committed baseline PNG (from a scenario that passed every applicable A.4 item on this box), produced by this plan's Task 3 checkpoint — the first real baseline this phase commits, per the Plan 04 Review Dispositions Ledger's explicit deferral"
  key_links:
    - from: ".claude/skills/e2e-visual-review/SKILL.md"
      to: "tests/e2e/tier4/visual_diff_e2e_test.lua"
      via: "step 1 of the skill's workflow runs `WEZ_E2E_VISUAL=1 make e2e-visual` to produce the screenshots + manifest it judges, and reads that SAME manifest to locate exact scenario/path/dimension/verdict data rather than guessing a printed path"
      pattern: "make e2e-visual"
    - from: ".claude/skills/e2e-visual-review/SKILL.md"
      to: "Makefile"
      via: "MEDIUM finding fix (Round 2) — the approval step invokes `make e2e-visual APPROVE=1 SCENARIOS=<names>`, Plan 04's REAL `e2e-visual` Makefile target (never `tools/run-e2e-visual.sh`, the Round-1 orchestrator script Plan 04 deleted outright — this link previously pointed at a file that no longer exists)"
      pattern: "APPROVE=1"
    - from: ".claude/skills/e2e-visual-review/SKILL.md"
      to: "workflow.cross_ai_command"
      via: "the model-enumeration step checks opencode's configured model per .planning/config.json when opencode is not the active runtime, and documents the CONCRETE opencode adapter invocation (`opencode run --model <id> --file <png> \"<prompt>\"`)"
      pattern: "cross_ai_command\\|opencode run"
---

<objective>
Author the `e2e-visual-review` project skill (D-02's working name, kept as the final name) — the
agent-performed half of Tier 4 Visuals — AND actually run it once, for real, against Plan 04's real
captures, producing the phase's first genuinely-reviewed committed baseline. It never silently
picks a vision model (D-02: enumerate candidates, distinguish catalog-presence from confirmed
reachability, ask the user, suggest a preference), it never calls a scripted vision API (D-01: the
judgment IS the agent reading a screenshot), its SKILL.md must score >=90% under a genuinely
separate skill-judge evaluation pass (D-03), and — resolving the Round-1 review's central complaint
that no plan in this phase ever actually exercised the finished mechanism — Task 3 below is a real,
interactive checkpoint that runs the finished skill end-to-end and commits the first real baseline.

## Round-1 review findings this revision fixes (see Review Dispositions Ledger at the bottom)
The prior version of this plan only wrote and self-scored `SKILL.md`; no task ever invoked it
against a real screenshot, so the semantic half of Tier 4 would have shipped completely unexercised.
Its approval instructions cited `make e2e-visual --approve` (Plan 04's OLD, invalid-Make-syntax,
global-recapture form) — now replaced with Plan 04's real `APPROVE=1 SCENARIOS=<names>` contract.
Its capture instruction told the skill to run bare `make e2e-visual` without the `WEZ_E2E_VISUAL=1`
gate that actually enables capture. Its model-dispatch step never defined HOW a selected external
model actually receives the screenshot + prompt — this revision grounds the opencode adapter
concretely (`opencode run --model <id> --file <png> "<message>"`, confirmed live via `opencode run
--help` during planning: `-f/--file` and `-m/--model` are real, documented flags). Its "available"/
"vision-capable" language was operationally undefined — this revision requires the skill to
distinguish CATALOG presence (`opencode models`) from CONFIRMED reachability, never asserting
confidence it cannot back up. Its self-scoring in Task 2 is now explicitly a SEPARATE, clean-context
evaluation. `autonomous: true` is corrected to `false` — Task 3 is a genuine, human-in-the-loop
checkpoint, not something the phase can complete unattended.
Depends on Plan 04 (`make e2e-visual` and its real manifest/approve contract must exist for both the
skill's own text and this plan's Task 3 to reference something real, not aspirational).
Output: `.claude/skills/e2e-visual-review/SKILL.md`, plus (via Task 3) the phase's first real
committed baseline PNG(s).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.9-e2e-tier-3-firing-tier-4-visuals-local-permission-bound-clos/06.9-CONTEXT.md
@docs/prds/e2e-testing-battery-v1.0-prd.md
@.planning/config.json
@tests/e2e/tier4/visual_diff_e2e_test.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .claude/skills/e2e-visual-review/SKILL.md (D-01/D-02/D-03, grounded in Plan 04's REAL contract)</name>
  <read_first>
    - `$HOME/.claude/skills/skill-judge/SKILL.md` in FULL — this is the exact rubric this new skill
      will be scored against (8 dimensions, 120 points, grade A = 90%+/108+ points). Pay particular
      attention to D1 Knowledge Delta ("expert-only knowledge minus what Claude already knows" —
      generic terminal-testing advice scores near-zero; the project-specific pitfalls below are the
      real content), D4 Specification Compliance (the `description` field must carry WHAT + WHEN +
      KEYWORDS, since it is the ONLY thing an agent sees before deciding to load the skill), and D6
      Freedom Calibration (this is a MEDIUM-freedom task — visual judgment needs principles, not a
      rigid script, but the model-selection step is LOW-freedom/non-negotiable per D-02).
    - `docs/prds/e2e-testing-battery-v1.0-prd.md` Appendix A.4 (the exact six checklist items this
      skill judges) and A.3's fire(B) framing (B = best-effort, local, non-blocking — context for why
      this skill is invoked manually, not from CI).
    - `06.9-CONTEXT.md` D-01/D-02/D-03 in full, including D-03's CORRECTION note (only the
      SKILL.md's own skill-judge score has a numeric bar; the A.4 checklist itself is pass/fail per
      item, NEVER a percentage — do not reintroduce a checklist-score threshold anywhere in this
      skill's text).
    - `tests/e2e/tier4/visual_diff_e2e_test.lua` (Plan 04's REAL driver) — the skill's Step 1 and
      Step 5 MUST cite the mechanism THIS file actually implements: `WEZ_E2E_VISUAL=1 make
      e2e-visual` to capture (the env gate is required — a bare `make e2e-visual` self-skips), the
      STABLE manifest path and its `<scenario>|<path>|<dims>|<verdict>|<baseline>` line format
      (read it to locate exact screenshot paths, never guess a printed path), and `make e2e-visual
      APPROVE=1 SCENARIOS=<comma-separated-names>` to promote — never the old, invalid `--approve`
      form.
    - `.planning/config.json`'s `workflow.cross_ai_command` (currently `/home/bazzite/.opencode/
      bin/opencode run --model xai/grok-4.6`) and, live during planning, `opencode run --help` —
      confirms `-m/--model <provider/model>` and `-f/--file <path>` (repeatable) are REAL flags:
      `opencode run --model <id> --file <screenshot.png> "<message>"` is a concrete, working
      adapter invocation, and `opencode models` lists the real model catalog for THIS installation
      — cite both verbatim rather than describing the dispatch mechanism vaguely.
    - `.planning/phases/06.5-keybinding-clarity-wez-keys-output-curation-inserted/06.5-04-PLAN.md`
      (~line 226) — the D-09 firing-verdict precedent this skill's "capture, then an agent reads and
      judges" pattern directly generalizes.
  </read_first>
  <files>.claude/skills/e2e-visual-review/SKILL.md</files>
  <action>
    Write `.claude/skills/e2e-visual-review/SKILL.md` with valid frontmatter:
    `name: e2e-visual-review`, and a `description` answering WHAT (agent-performed visual review of
    WezTerm E2E screenshots against the PRD A.4 checklist) + WHEN (use when reviewing/approving Tier
    4 visual baselines, after running `make e2e-visual`, or when asked to visually verify a WezTerm
    scene/tab-bar/pane-tint/emoji-icon rendering) + KEYWORDS (`make e2e-visual`, baseline, screenshot,
    tab bar, pane tint, emoji icon, visual regression) — per skill-judge D4, this is the field that
    determines whether the skill ever gets loaded at all, so front-load the trigger scenarios rather
    than burying them in the body.

    Body content (target ~180-260 lines, a Process-pattern skill per skill-judge D7 — phased
    workflow, checkpoints, medium freedom):

    1. **Step 1 — Capture.** Run `WEZ_E2E_VISUAL=1 make e2e-visual` (the env gate is REQUIRED — a
       bare `make e2e-visual` self-skips loudly per its own contract; report a skip honestly to the
       user rather than pretending a review happened). Read the manifest at the stable path the
       driver prints (`${TMPDIR:-/tmp}/wez-e2e-visual-latest/manifest.txt`) to get the EXACT
       scenario -> screenshot path -> dimensions -> diff-verdict mapping — never guess a path.

    2. **Step 2 — Enumerate candidates, distinguishing catalog presence from confirmed
       reachability (D-02, MANDATORY, never skip).** List the vision-capable models available in
       the CURRENT runtime. THEN check `.planning/config.json`'s `workflow.cross_ai_command` — if it
       names a DIFFERENT runtime (e.g. opencode) than the one currently active, run `opencode models`
       (if the opencode CLI is present) to get its REAL catalog, and ALSO surface that as a
       candidate. For every candidate, explicitly label it as either "catalog-listed, vision support
       inferred from the model name" or "confirmed reachable and vision-capable" — being cataloged is
       NOT the same as being usable (a model can appear in a provider's list without valid
       credentials or attachment support on this machine); never assert confidence beyond what was
       actually confirmed.

    3. **Step 3 — Ask the user (D-02, a genuine interactive checkpoint — never bypass this by
       picking the "obviously best" model).** Present the labeled candidates with ONE suggested
       preference and the reasoning for it — but the user picks, not the skill. Include a concrete
       DECISION-TREE-shaped table (skill-judge D2/D8): "IF the current runtime IS vision-capable AND
       no cross_ai_command is configured -> default suggestion is 'use this session'; IF a
       cross_ai_command IS configured -> surface it as an alternative, naming its real model id; IF
       the current runtime is NOT vision-capable -> the cross_ai_command (or another named
       vision-capable CLI) becomes the ONLY viable suggestion, state this plainly" — and instruct the
       user to confirm the chosen candidate is actually authenticated/reachable before proceeding
       (the reachability distinction from Step 2, carried through to the decision itself).

    4. **Step 4 — Dispatch to the chosen model (concrete adapter, not a vague description).** If the
       user chose "use this session," read each manifest-listed PNG directly. If the user chose
       opencode's configured model, dispatch via the REAL, grounded invocation: `opencode run
       --model <id> --file <screenshot.png> "<rendered prompt containing the verbatim A.4 checklist
       + the scenario name>"` (confirmed live: `-f/--file` and `-m/--model` are real `opencode run`
       flags) — parse its response for a per-item pass/fail verdict. Document that a DIFFERENT
       external CLI would need its own adapter line here, following the same shape, and that adding
       one is this skill's natural extension point (never silently assume every future runtime
       shares opencode's exact flags).

    5. **Step 5 — Judge (D-01: the judgment IS an agent reading pixels, never a scripted heuristic
       or API call).** For each captured scenario, score EVERY PRD Appendix A.4 item that applies to
       it, PASS or FAIL, with a one-line reason per item — reproduce the six A.4 items verbatim as a
       checklist table in the skill body so the judging agent has the EXACT wording, not a paraphrase
       that could drift from the PRD over time. Note which scenario(s) specifically exercise the
       same-title-tab-width and active-vs-inactive items (the `tabbar` scenario, per Plan 04). Emphasize
       (an anti-pattern/NEVER, per skill-judge D3): NEVER average the six items into a percentage or
       pass rate — D-03's correction note is explicit that this checklist carries NO numeric
       threshold, only per-item pass/fail. Include at least one non-obvious judging pitfall an
       inexperienced reviewer would miss (e.g.: "a screenshot captured mid-render, before the
       compositor's frame settles, can show a stale/blank pane that looks like a real rendering bug —
       if a tint/title looks WRONG, re-run `WEZ_E2E_VISUAL=1 make e2e-visual` once before failing the
       item, since a flaky capture is not the same class of bug as a real rendering defect").

    6. **Step 6 — Approve or reject, using Plan 04's REAL contract.** For scenarios that pass ALL
       applicable A.4 items AND (per the manifest's own diff verdict) either pass the RMSE diff or
       show `NO BASELINE`, run `make e2e-visual APPROVE=1 SCENARIOS=<exact comma-separated scenario
       names>` for EXACTLY those scenarios — the ONLY valid approval invocation is this `APPROVE=1`
       Make-variable form; never a blanket approve-everything, and never any dashed/double-hyphen
       flag passed directly on the `make` command line (HIGH finding fix — see the NEVER-list
       wording note below for why this anti-pattern must never be named using its own literal,
       superseded flag spelling). For a FAILING scenario, report the specific failing item(s) and
       STOP — do not approve a baseline the checklist rejected. State that approval promotes the
       manifest's EXACT, checksum-verified bytes — never a recapture.

    Add a concise NEVER list (skill-judge D3 — specific + reasoned, not generic): never silently pick
    a model (D-02); never call a scripted vision API (D-01); never average the A.4 checklist into a
    score (D-03's correction); never approve a scenario with a failing item; never treat a
    catalog-listed model as confirmed-reachable without the user's confirmation; never invoke `make
    e2e-visual` without `WEZ_E2E_VISUAL=1` and expect a real capture; never pass a dashed,
    double-hyphen Make flag on the command line to approve a baseline — `APPROVE=1` (a Make
    variable, never a `--`-prefixed flag) is the ONLY valid approval invocation. HIGH finding fix
    (Round 2, self-contradiction repair): describe this last anti-pattern WITHOUT ever spelling out
    the superseded flag's own literal two-dash text anywhere in the skill body — this file's own
    `<verify>` negative-greps for that exact literal string as proof the invalid form was never
    reintroduced, so naming the anti-pattern BY citing its own forbidden spelling would make the
    skill fail its own verification the moment it correctly documents what to avoid.

    Keep the file self-contained (no `references/` subdirectory needed at this size — skill-judge D5
    scores a simple, well-organized single file fine when under ~300 lines).
  </action>
  <acceptance_criteria>
    - Frontmatter is valid: `name: e2e-visual-review` (lowercase, hyphenated, matches the directory
      name); `description` contains an explicit "Use when..." trigger clause AND at least the
      keywords `make e2e-visual` and `baseline`.
    - The body contains a literal, verbatim reproduction of all six PRD Appendix A.4 checklist items.
    - The body's capture step names `WEZ_E2E_VISUAL=1 make e2e-visual` (with the env gate, not a bare
      invocation) and references the manifest file's stable path.
    - The body's approval step names `make e2e-visual APPROVE=1 SCENARIOS=` (the REAL Plan 04
      contract) and does NOT contain the string `--approve` anywhere (the invalid, superseded form).
    - The body's model-selection section contains an explicit instruction to ASK THE USER, references
      `.planning/config.json`'s `workflow.cross_ai_command` by name, AND contains a concrete
      `opencode run --model` + `--file` invocation line (not just a description of dispatching).
    - The body contains an explicit statement that the A.4 checklist has NO percentage/numeric
      threshold — only per-item pass/fail.
    - The body distinguishes "catalog-listed" from "confirmed reachable" candidates explicitly.
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/skills/e2e-visual-review/SKILL.md && grep -q "^name: e2e-visual-review" .claude/skills/e2e-visual-review/SKILL.md && grep -qi "ask the user\|user select\|user choos" .claude/skills/e2e-visual-review/SKILL.md && grep -q "cross_ai_command" .claude/skills/e2e-visual-review/SKILL.md && grep -q "opencode run --model" .claude/skills/e2e-visual-review/SKILL.md && grep -qi "no.*percentage\|no.*numeric threshold\|not a percentage\|no partial.credit" .claude/skills/e2e-visual-review/SKILL.md && grep -q "WEZ_E2E_VISUAL=1 make e2e-visual" .claude/skills/e2e-visual-review/SKILL.md && grep -q "APPROVE=1" .claude/skills/e2e-visual-review/SKILL.md && ! grep -q -- "--approve" .claude/skills/e2e-visual-review/SKILL.md && echo "SKILL_STRUCTURE_OK" || { echo "SKILL_STRUCTURE_FAILED"; exit 1; }</automated>
    <fails_when>the SKILL.md file is missing, the frontmatter `name` does not match the directory, "ask the user"-equivalent language is absent, `cross_ai_command` or a concrete `opencode run --model` line is not referenced, no explicit "no percentage/numeric threshold" statement is present, the capture step omits the `WEZ_E2E_VISUAL=1` gate, the approve step omits `APPROVE=1`, or the superseded `--approve` string is still present anywhere in the file.</fails_when>
  </verify>
  <done>`.claude/skills/e2e-visual-review/SKILL.md` exists with valid frontmatter, a mandatory ask-the-user model-enumeration step citing `workflow.cross_ai_command` and a concrete opencode adapter invocation, a verbatim PRD A.4 checklist with an explicit no-percentage statement, the REAL Plan 04 capture/approve contract, and a genuine expert-knowledge NEVER list.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the skill against skill-judge via a separate, clean-context evaluation — must score >=90% (D-03)</name>
  <read_first>
    - `$HOME/.claude/skills/skill-judge/SKILL.md` — the Evaluation Protocol section (Step 1-5) and
      the Report template.
    - `.claude/skills/e2e-visual-review/SKILL.md` (Task 1's output).
  </read_first>
  <files>.claude/skills/e2e-visual-review/SKILL.md</files>
  <action>
    Dispatch a SEPARATE, clean-context evaluation of `.claude/skills/e2e-visual-review/SKILL.md`
    against skill-judge's own rubric — do not self-score inline in the same context that authored
    the file (per the review's own finding that a same-context self-score is weak evidence). If this
    runtime supports subagent/Task dispatch, use it here exactly as GSD's own code-review pattern
    dispatches a clean-context reviewer; if no such mechanism is available in this execution
    environment, explicitly note that constraint in the SUMMARY (never silently substitute an
    inline score for a genuinely independent one without saying so). The evaluation scores all 8
    dimensions (Knowledge Delta, Mindset + Procedures, Anti-Pattern Quality, Specification
    Compliance, Progressive Disclosure, Freedom Calibration, Pattern Recognition, Practical
    Usability) with one-line evidence citations per dimension, sums to a total out of 120, and
    computes the percentage. If the score is BELOW 90% (108/120), this is a real gate failure per
    D-03 — revise the SKILL.md (strengthen the weakest dimensions per skill-judge's own "Top 3
    Improvements" guidance) and RE-SCORE (via a fresh clean-context pass again, not the same
    context that just revised it), repeating until >=90% is reached. Record the final score, its
    dimension breakdown, whether the evaluation was genuinely clean-context or an explicitly-flagged
    fallback, and (if any revision rounds were needed) what changed between rounds, in this plan's
    SUMMARY.
  </action>
  <acceptance_criteria>
    - A skill-judge evaluation was actually performed against the real file content and the final
      total score is >=90% (>=108/120).
    - The evaluation method (clean-context dispatch, or an explicitly-flagged same-context fallback
      with the constraint named) is recorded in the SUMMARY alongside the per-dimension breakdown.
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/skills/e2e-visual-review/SKILL.md && wc -l .claude/skills/e2e-visual-review/SKILL.md && echo "MANUAL_GATE: a skill-judge score >=90% (108/120) with a dimension-by-dimension breakdown, and the evaluation method used, must be recorded in the SUMMARY — this is a human-legible evaluation the executor performs against skill-judge's own rubric, not a bash-computable number."</automated>
    <fails_when>the SKILL.md file does not exist, OR the SUMMARY does not record a skill-judge score of at least 90%/108 points with a dimension-by-dimension breakdown AND a stated evaluation method as evidence.</fails_when>
  </verify>
  <done>The `e2e-visual-review` skill scores >=90% under skill-judge's own rubric via a documented evaluation method, with the evidence recorded in the SUMMARY — the phase's sole numeric acceptance bar is met and traceable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Run the finished skill for real against Plan 04's live captures — produce the first genuine committed baseline</name>
  <read_first>
    - `.claude/skills/e2e-visual-review/SKILL.md` (Task 1/2's validated output) — follow it exactly
      as written; this task IS its first real invocation, and any friction found here is itself a
      finding to fix in the skill before this task can complete (do not work around a broken skill
      step silently — fix the skill, per Task 1's own contract, then resume).
    - `tests/e2e/tier4/visual_diff_e2e_test.lua` (Plan 04) — the real capture/manifest/approve
      mechanism this task exercises live.
  </read_first>
  <files>tests/e2e/baselines/linux</files>
  <precondition>A local, permission-bound Linux GUI session is available on this box (the same
  session Plan 01/03/04 already proved capable of forced-XWayland capture) — this task cannot run
  headlessly or unattended.</precondition>
  <what-built>
    Tasks 1-2 produced and validated `.claude/skills/e2e-visual-review/SKILL.md` (skill-judge
    score >=90%). This checkpoint is that skill's FIRST real, end-to-end invocation — not an
    autonomous step. I will run `WEZ_E2E_VISUAL=1 make e2e-visual` to capture the four live
    scenarios (dev/ai/docker/tabbar) for real on this box, then follow the skill's own procedure to
    enumerate vision-model candidates (distinguishing catalog-listed from confirmed-reachable) and
    score every applicable PRD Appendix A.4 item, per scenario, pass/fail with a one-line reason —
    including the `tabbar` scenario's same-title-width and active-vs-inactive items, the two
    checklist items no other scenario can exercise.
  </what-built>
  <how-to-verify>
    1. I will present the enumerated model candidates with a suggested preference and STOP here —
       reply with your choice (e.g. "use this session", or an opencode model id such as
       `xai/grok-4.6`) before I dispatch the real judgment.
    2. After you choose, I will dispatch the judgment, record a per-item A.4 pass/fail + one-line
       reason for all four scenarios, and for every scenario that passes ALL applicable items run
       `make e2e-visual APPROVE=1 SCENARIOS=<those names>` (Plan 04's real, valid contract — never
       `--approve`) to promote the manifest's exact, checksum-verified bytes into
       `tests/e2e/baselines/linux/*.png` and `git add` them. Any scenario that fails an item will be
       reported plainly and NOT approved.
    3. Review the reported per-scenario A.4 scores, the model actually used, and (if any) the newly
       git-tracked baseline PNG path(s) under `tests/e2e/baselines/linux/`. A partial result (some
       scenarios approved, others not) or even a zero-baseline result (every scenario genuinely
       failed an item) is an honest, valid outcome of a real review, not a failure of this
       checkpoint — only skipping the real judgment entirely would be a failure.
  </how-to-verify>
  <resume-signal>Reply with your chosen model (or "use this session") to unblock the judgment step; then reply "approved" once you've reviewed the per-scenario A.4 results and any newly committed baseline PNG(s) — or describe what should change.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| e2e-visual-review skill -> model selection | a silently-chosen vision model could be the wrong one for a screen the user actually wanted judged by a specific, trusted model |
| agent-performed visual judgment -> committed baseline | a rubber-stamped "looks fine" judgment promotes a genuinely broken baseline into the trusted committed set |
| Task 3's real approval -> git history | this is the FIRST real write to `tests/e2e/baselines/linux/` — a mistaken approval here becomes the reference baseline every future diff compares against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.9-15 | Repudiation | a silently-picked vision model with no record of which model judged a baseline | medium | mitigate | D-02's mandatory ask-the-user step is a hard gate in the skill's own text AND in Task 3's live execution; the SUMMARY records which model was actually used |
| T-06.9-16 | Tampering | the skill's own quality regressing below the D-03 bar in a future edit without re-scoring | low | accept | this plan's Task 2 records the passing score as of this phase; a future edit to the skill is that future change's own responsibility to re-verify, not retroactively enforced here |
| T-06.9-17 | Tampering | Task 3 approving a baseline without a genuine per-item review having actually happened | high | mitigate | Task 3's `<how-to-verify>` requires the real per-item A.4 scores and the model actually used to be reported and reviewed BEFORE the human's `resume-signal` reply is treated as approval; a checkpoint that skips straight to approval without recording real scores has not satisfied its own resume-signal |
| T-06.9-SC | Tampering | npm/pip/cargo installs | accept | no package-manager installs in this plan — a Markdown skill file and PNG baseline commits only |
</threat_model>

<verification>
- `.claude/skills/e2e-visual-review/SKILL.md` exists with valid frontmatter and the required D-01/D-02/D-03 content (verbatim A.4 checklist, mandatory ask-the-user model step with a concrete opencode adapter, explicit no-percentage statement, Plan 04's REAL capture/approve contract, no stale `--approve` string).
- A skill-judge evaluation was performed via a documented method (clean-context or explicitly-flagged fallback) and scored >=90% (108/120), recorded with dimension-by-dimension evidence in the SUMMARY.
- Task 3 actually ran the finished skill once against real Plan 04 captures, with a real user model choice and real per-item A.4 scores recorded, and either produced the phase's first real committed baseline(s) or an honest account of why none were approved.
</verification>

<success_criteria>
- The phase's ONLY numeric acceptance bar (D-03: skill-judge >=90% on the SKILL.md, via a genuinely separate evaluation) is met and evidenced.
- The skill never silently picks a model (D-02) and never scripts the vision judgment itself (D-01).
- Tier 4's semantic-judgment half is not just specified but PROVEN via one real, human-in-the-loop invocation, closing out the phase goal alongside Plan 04's deterministic half.
</success_criteria>

<output>
Create `.planning/phases/06.9-e2e-tier-3-firing-tier-4-visuals-local-permission-bound-clos/06.9-05-SUMMARY.md` when done.
</output>

## Review Dispositions Ledger

### Round 1 — be2e08b

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|----------------|
| No task runs the completed skill against real screenshots (L211) | CRITICAL | new Task 3 — a real, interactive checkpoint (`autonomous: false`) that runs the finished skill end-to-end against Plan 04's live captures |
| Approval command incompatible with Plan 04 (L212) | CRITICAL | skill text and Task 3 now cite Plan 04's REAL `APPROVE=1 SCENARIOS=` contract; the superseded `--approve` string is grep-forbidden from the skill file |
| Capture invoked without enabling the visual gate (L213) | HIGH | skill Step 1 and Task 3 both require `WEZ_E2E_VISUAL=1 make e2e-visual`, never a bare invocation |
| External model dispatch unspecified (L214) | HIGH | skill Step 4 now names the concrete, live-confirmed `opencode run --model <id> --file <png> "<message>"` invocation |
| "Available"/"vision-capable" not operationally defined (L215) | HIGH | skill Step 2 requires explicitly labeling each candidate as catalog-listed vs confirmed-reachable, never asserting unconfirmed confidence |
| Skill has no machine-readable capture manifest (L216) | HIGH | skill Step 1 now reads Plan 04's stable manifest file directly, never a guessed/printed path |
| Validation self-scored and weakly evidenced (L217) | HIGH | Task 2 requires a genuinely separate/clean-context evaluation pass, with the method used recorded explicitly (never a silent same-context substitute) |
| Reachability not distinguished from catalog presence (L218) | MEDIUM | folded into the Step 2 fix above — every candidate is explicitly labeled |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none) | every actionable finding against this plan was addressed directly |

### Round 2 — 994cceb

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Task 1's action required a NEVER-list line that NAMES the old `--approve` form, but Task 1's own `<verify>` negative-greps the whole SKILL.md file for the literal substring `--approve` — an executor following the action as written cannot both document the anti-pattern and pass its own verify; two opposite outcomes from one file | HIGH | reworded Step 6 and the NEVER list so the anti-pattern (a dashed/double-hyphen `make` flag) is described WITHOUT ever spelling out its own literal two-dash text (`APPROVE=1`, a Make variable, is named as the ONLY valid form instead) — the self-contradiction is structurally impossible now, not just reworded to be luckier |
| `key_links` pointed `to: "tools/run-e2e-visual.sh"`, the Round-1 bash orchestrator Plan 04's revision deleted outright; an executor following key_links looks for a file that must not exist | MEDIUM | repointed `to:` to `Makefile` (the actual target the approval step invokes: `make e2e-visual APPROVE=1 SCENARIOS=<names>`), with the via-text explicitly noting the stale file was removed by Plan 04 |
| `make e2e-visual` lacking a `WEZ_BIN` export (HIGH, shared with Plan 04) | HIGH (cross-plan) | the fix lands entirely in Plan 04's Makefile target (`WEZ_BIN=$${WEZ_BIN:-./dist/wez}`); this plan's Task 3 invokes `make e2e-visual` as a caller and inherits that fix with no changes needed here — noted for traceability, not a defect in this plan's own text |
| Task 2's "genuinely separate/clean-context skill-judge evaluation" is unverifiable from inside this sandbox (`~/.claude/skills/skill-judge/SKILL.md` lives outside this repo) — risk that a same-context self-score gets silently substituted, making D-03 theater | MEDIUM | confirmed already-sound, no text change needed: Task 2's `<acceptance_criteria>`/`<verify>` already make "the evaluation method (clean-context, or an explicitly-flagged same-context fallback with the constraint named)" a HARD gate — `<fails_when>` already fails the task if the SUMMARY lacks a stated evaluation method alongside the score. Recorded here so this concern is not silently untouched: the reviewer's own suggestion ("keep that as a hard SUMMARY requirement") was already true of the shipped text before this Round |
| Task 3 (the first live capture) is only a valid checkpoint if Plan 04's live gaps (runtime dir, `first_pane_id`, `WEZ_BIN`) are fixed first | MEDIUM | all three cited gaps are now fixed in Plan 04's own Round 2 revision (isolation `runtime/` dir + `first_pane_id` land via Plan 01's Round 2; `WEZ_BIN` lands via Plan 04's Round 2 Makefile fix) — Task 3's dependency on Plan 04 actually capturing is satisfied, not merely assumed |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none — the one cross-plan-shared concern, `WEZ_BIN`, is fixed in Plan 04 and inherited here, not deferred) | n/a |
