# 06.9-01 SUMMARY — Tier 3 Firing tracer (ESCALATION CLAUSE)

Shipped the focus-verify-then-fire pipeline (`tests/e2e/lib/gui.lua`, public
`mux.env_prefix`, `platform.expectations.fire_chords`, `firing_e2e_test.lua`).
On this Wayland/KWin host the pipeline **cannot deliver a real OS keystroke
into WezTerm**, so the tracer **loud-SKIPS** rather than printing a false
`LIVE-ASSERTED`.

## ESCALATION CLAUSE (triggered)

`M.verify_focus` is stable and repeatable: `xdotool windowactivate --sync <id>
getactivewindow` matches the `--class`-tagged forced-XWayland test window every
time. The **fire** half is not.

Tried (this run + the prior attempt's XTEST battery, not repeated):

- Bare XTEST `xdotool key --clearmodifiers` (plain `a`, `--window` XSendEvent,
  `type`, click-then-key, `ctrl+shift+k`, folded `ctrl+k`) — pane text unchanged.
- `ydotoold` as the session user, no sudo: `/dev/uinput` is writable via ACL
  `user:bazzite:rw-`. Custom `--socket-path` works. KWin sees
  `ydotoold virtual device` (enabled, keyboard, isVirtual).
- `ydotool type` **does** reach a native Wayland kwrite document
  (`HELLOFROMYDOTOOL` saved via Ctrl+S).
- The same `ydotool type` / raw `key 30:1 30:0` does **not** change
  `wezterm cli get-text` for a focus-verified forced-XWayland WezTerm pane,
  even when fired immediately on map, after a uinput click, or with
  `enable_wayland = true`. Control: `wezterm cli send-text` **does** reach
  the pane (`SENDTEXT_OK`) — the CLI bypass the plan forbids as the real
  mechanism.

R6 probes (deleted after encoding): xdotool chaining holds for focus verify;
`--config-file` is top-level; scratch `require('wezterm-setup')` needs
`$XDG_CONFIG_HOME/wezterm/wezterm-setup` (plan path `cfg/wezterm-setup` alone
misses); wmctrl/xdotool see the XWayland test window; OS injection into
WezTerm fails.

`M.probe(repo_root)` therefore requires a canary pane-text mutation after
focus verify. Focus-only success would have printed a false `LIVE-ASSERTED`
on this box. Canary fails → loud `SKIPPED (reason=...)`.

## Task 1 verify

Structural checks passed (`gui.lua` loads; `M.env_prefix` public once; no
nil-binding alias; `windowactivate` count = 1; `M.check_focus_unchanged`
present; `M.probe(repo_root)`; `chmod 700`; `first_pane_id`).

No-flag (`WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier3/firing_e2e_test.lua`):

```
tier3 firing: SKIP (WEZ_E2E_INPUT != 1)
```

exit 0.

`WEZ_E2E_INPUT=1` live run:

```
  retry - gui probe canary (attempt 1/8)
  ...
  retry - gui probe canary (attempt 8/8)
tier3 firing — focus-verified OS input injection: SKIPPED (reason=OS-level key injection cannot deliver a keystroke into a focus-verified forced-XWayland WezTerm window on this Wayland/KWin host (xdotool XTEST and ydotool/uinput both produce no mux effect))
```

exit 0. Official combined verify printed `FIRING_TRACER_VERIFY_FAILED` solely
because it greps for `LIVE-ASSERTED` — the escalation-allowed outcome here.

## Task 2 — five consecutive runs

All five `WEZ_E2E_INPUT=1` fresh-session runs exited 0 with `SKIPPED` and
without `LIVE-ASSERTED` (same-verdict-class, not the LIVE-ASSERTED stability
the plan wanted). Escalation already triggered; this is the honest stable
result on this host class.

`fire_chords.note` names `ClearScreenAndScrollback` and `SpawnTab` as the
tracer population and states the remaining ten PRD Appendix A.3 fire(B)
actions are Plan 03's scope.

## Commit

`597460e` — `test(06.9): ship Tier 3 Firing tracer as honest self-skip`
