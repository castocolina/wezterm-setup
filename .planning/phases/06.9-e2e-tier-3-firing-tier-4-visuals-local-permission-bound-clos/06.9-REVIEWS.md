---
phase: 06.9
reviewers: [opencode]
reviewed_at: 2026-09-09T16:10:49Z
plans_reviewed: [06.9-01-PLAN.md, 06.9-02-PLAN.md, 06.9-03-PLAN.md, 06.9-04-PLAN.md, 06.9-05-PLAN.md]
models:
  opencode: "xai/grok-4.6 (reasoning=high)"
model_sources:
  opencode: "user-directed substitution for this cycle only: the lane's configured pin (openai/gpt-5.6-sol) again returned HTTP 429 'The usage limit has been reached' on a direct CLI probe before this cycle's review was invoked, so `review.models.opencode` was temporarily repointed to `xai/grok-4.6` (already configured for this project's `workflow.cross_ai_command`), the review invoked, and the config reverted to `openai/gpt-5.6-sol` immediately after (confirmed via `git diff .planning/config.json` showing no net change beyond an unrelated pre-existing `timeouts.opencode` addition) — this cycle's substitution is not a persisted config change"
cycles: 3
---

# Cross-AI Plan Review — Phase 06.9

<!-- gsd:plan-revision-conflicts:begin -->
## Plan-Revision Conflicts

(none — first review cycle for this phase)
<!-- gsd:plan-revision-conflicts:end -->

## Cycle 3 — OpenCode Review (2026-09-09T16:10:49Z)

Round 3 (final convergence cycle, MAX_CYCLES=3) of the plan-review convergence loop. Round 2 (commit b4121a8, below as Cycle 2) found 7 cross-plan HIGH + 3 cross-plan MEDIUM + several per-plan MEDIUM/LOW findings against the Round-1 fixes; the plan author then revised all five PLAN.md files again, adding "Round 2" ledger sections to each plan. This cycle re-verifies each Round-2 disposition against the current plan text and live source, and separately surfaces new/still-unresolved concerns. Reviewer ran under the sandbox-scope guardrail (repo-only reads; no attempt to reach absolute system paths outside the repo — none of this cycle's findings required such a path). Live source checked: `tests/e2e/lib/mux.lua`, `tests/e2e/lib/harness.lua`, `tests/e2e/lib/skip.lua`, `tests/e2e/platform.lua`, `tools/run-e2e.sh`, `Makefile`, `tests/e2e/README.md`, `config/wezterm-setup/init.lua`, `keybindings.lua`, `cwd.lua`, `tests/e2e/tier2/scene_reuse_e2e_test.lua`, `cli/commands/pane.lua`, `tools/ci-setup-toolchain.sh`, `.planning/config.json`.

## 06.9-01

### Summary

The tracer plan now matches live `mux.spin` isolation and handle shape. Forced-XWayland + chained `windowactivate --sync`/`getactivewindow`, real `apply()` via symlink, two mux-effect classes, and loud skip remain sound. Round-2 isolation, pane-id, env_prefix, and post-fire TOCTOU fixes are in the current text, not just the ledger.

### Round-2 Disposition Verification

1. **`gui.spin` omits HOME / XDG_RUNTIME_DIR 0700** — **FULLY RESOLVED.** Live `mux.env_prefix` still sets `HOME=dir/home` and `XDG_RUNTIME_DIR=dir/runtime` (`mux.lua:110-117`); `mux.spin` mkdirs those and `chmod 700` runtime (`mux.lua:152-155`). Plan action copies that verbatim (`06.9-01-PLAN.md:294-303`); must_haves (`:30`) and acceptance (`:467`) require `chmod 700` plus all four path fragments.
2. **`first_pane_id` missing on handle** — **FULLY RESOLVED** for the handle. Action sets `session.first_pane_id` from `mux.cli_list` after liveness (`:765-770`; live precedent `mux.lua:213-214`). Plan 04 can read `s.first_pane_id`. Residual: the tracer still says `mux.send_marker` / `mux.get_text` "into the pane" (`:432-437`) without naming `session.first_pane_id`, while those helpers require a pane id (`mux.lua:276-292`).
3. **`local env_prefix = M.env_prefix` nil-bind** — **FULLY RESOLVED.** Action forbids the alias and updates the one call site to `M.env_prefix(dir)` (`:254-263`). Verify: `! grep -q "local env_prefix = M\.env_prefix"` and `grep -c "M\.env_prefix(dir)"` (`:466`). Live call site is still `local env = env_prefix(dir)` (`mux.lua:188`) — correct pre-execution.
4. **Post-fire `verify_focus` vs getactivewindow-only** — **FULLY RESOLVED.** `M.check_focus_unchanged` is a bare `getactivewindow` (`:804-812`). Acceptance requires `windowactivate` exactly once (`:894`). Threat T-06.9-01 matches.
5. **`input_tools` ydotool-as-Wayland-firing** — **PARTIALLY RESOLVED.** Only `.note` is updated (`:265-272`, `:468`). Live row is still `linux = { "xdotool (X11)", "ydotool (Wayland)" }` (`platform.lua:105-110`). A skimmer of the array still sees ydotool as the Wayland firing tool.

### New/Still-Open Concerns

- **LOW** — Tracer never names `session.first_pane_id` at `send_marker`/`get_text` (`:432-437`). Handle has the field; `mux.lua` needs the argument. Executor should connect them; the plan should say it.
- **LOW** — `input_tools.linux` values still say `ydotool (Wayland)` (`platform.lua:107`). Note vs array conflict.

### Suggestions

- Write `mux.send_marker(session, session.first_pane_id, marker)` and the matching `get_text` in the tracer action.
- Keep both tools in the linux array as provisioned; change the Wayland label so it does not read as the firing mechanism.

### Risk Assessment

**LOW.** Cycle-2 HIGHs that could break the tracer or Tier 2 are actually designed out.

## 06.9-02

### Summary

D-06..D-09 design is still the strongest of the five: per-tool brew-then-native, uaccess-first uinput, allowlisted `usermod`, non-interactive `run_privileged`, ImageMagick on the list. Round-2 sudo-hang and usermod-grep fixes are in the task text and verify.

### New/Still-Open Concerns

- **MEDIUM** — Action says cite `ci-setup-toolchain.sh`'s SUDO SCOPE NOTE in the header (`:165-167`). That note contains lowercase `sudo` (`ci-setup-toolchain.sh:19-24`: `sudo apt-get`, `sudo-free`, `apt-sudo`). Verify is `grep '\bsudo\b' | grep -v 'sudo -n true\|sudo -n "\$@"'` and requires `SUDO_SITES` empty (`:298`). Copying the note fails verify. Paraphrase without lowercase `sudo` passes. Same class as Cycle 2's NEVER-vs-grep trap, milder because a paraphrase exists.
- **LOW** — Unquoted `usermod -aG input` evades the quoted grep. Safe if literal; not the allowlist audit the acceptance claims.

### Suggestions

- Cite the SUDO SCOPE NOTE by name only, or exclude comment lines from the sudo grep.
- Also reject any `usermod -aG` line whose group token is not the literal `input`/`uinput`, quoted or not.

### Risk Assessment

**LOW.** Privilege algorithm and non-interactive sudo gate are executable. Residual risk is verify false-fail on a required header, not `usermod -aG root`.

## 06.9-03

### Summary

Horizontal expansion is correctly sequenced on Plan 01's pipeline. Font-size overclaim, README grep, tracer names in the live log, and 12-vs-13 wording are fixed in the current text. Inherited isolation/`first_pane_id` now ride Plan 01's Round-2 handle.

### Round-2 Disposition Verification

1. **Inherited `gui.spin` dirs / `first_pane_id`** — **FULLY RESOLVED** (via Plan 01). This plan reuses `gui.spin` unchanged (`:26`, `:176`).
2. **README verify looser than action** — **FULLY RESOLVED.** Verify greps the real heading `"placeholder — wired in 06.9"`, requires `06.9 (landed)`, forbids bare `_TBD (06.9)_`, keeps `_TBD (06.9-04)_` (`:328`). Current README uses those strings (`README.md:44, 78-87`). `_TBD (06.9)_` is not a substring of `_TBD (06.9-04)_` (the `)_` sits after `06.9`, not `-04`).
3. **Verify loop omits tracer action names** — **FULLY RESOLVED.** Loop includes `ClearScreenAndScrollback` and `SpawnTab` (`:255`).
4. **"13 curated EXCEPT SendString" vs "12 fire(B)"** — **FULLY RESOLVED.** Acceptance spells 13 PRD bullets, WordNav is the 13th and fire-ineligible, font-size is one family / three keys (`:228-236`). Matches PRD A.3 (`e2e-testing-battery-v1.0-prd.md:300-312`).
5. **Font-size overclaim (Cycle-2 HIGH)** — **FULLY RESOLVED.** Must-have carves out CTRL defaults vs SUPER curated rows (`:26`). Matches `keybindings.lua:122-124` (SUPER zoom) and `platform.lua:167-169` (Linux CTRL catch-scope). RotatePanes remains the real-config proof (`keybindings.lua:147-149`).
6. **`input_tools` ydotool** — **FULLY RESOLVED for README.** Task 2 states xdotool+forced-XWayland fires, ydotool is provisioned only (`:283-295`). Code-table note stays Plan 01's.

### New/Still-Open Concerns

- **LOW** — `MoveTabRelative` chords are `ctrl+shift+Prior` / `ctrl+shift+Next` (`:166-167`). Live bindings are PageUp/PageDown (`keybindings.lua:98-99`). xdotool often aliases Prior/Next; this is an R6 probe, not a silent assume.
- **LOW** — Must-have asks for an explicit pre-fire `gui.verify_focus` plus `fire_chord`, which already calls `verify_focus` (`:24`, `:179`). Redundant activate, not a TOCTOU undo.

### Suggestions

- Probe xdotool keysyms for PageUp/PageDown before locking Prior/Next.
- Drop the extra pre-fire `verify_focus`; `fire_chord` already does it.

### Risk Assessment

**LOW.** Inventory, fields, and README greps match the PRD and live `mux.lua`.

## 06.9-04

### Summary

Tier 4 as a discovered `*_e2e_test.lua`, `APPROVE=1` Make syntax, `WEZ_ABS`, `first_pane_id`, isolation dirs, and a real live-capture verify chain are in the current text. One Round-2 HIGH is fixed in a way that breaks D-04: verify promotes an unreviewed `dev.png` into the committed baseline path and never deletes it.

### Round-2 Disposition Verification

1. **Task 2 verify has no live capture (L175)** — **FULLY RESOLVED as a missing-capture defect.** Verify block (`06.9-04-PLAN.md:412`) now runs `make e2e-visual`, asserts `RUN_ID=`, four scenario lines, `NO BASELINE`, `tabbar: 2 tabs`, then checksum-compares promote. Final gate includes `capture_ec`, `manifest_ec`, `runid_ec`, empty `MISSING`, `checksum_ec` — confirmed against the live `<verify>` block, which genuinely executes `make e2e-visual`, `make e2e-visual APPROVE=1`, and `make e2e-visual APPROVE=1 SCENARIOS=dev`, not a text/skip check.
2. **`make e2e-visual` does not export `WEZ_BIN`** — **FULLY RESOLVED.** Recipe is `WEZ_BIN=$${WEZ_BIN:-./dist/wez} WEZ_E2E_VISUAL=1 lua5.4 ...` (`:362-367`). Prefix assignment puts `WEZ_BIN` in lua's environment (`harness.lua:84-85`). Live `run-e2e.sh:79-82` is the convention being mirrored. `WEZ_ABS` copies `scene_reuse_e2e_test.lua:57-72` (`:288-297`).
3. **`first_pane_id` / isolation dirs inherited** — **FULLY RESOLVED** (via Plan 01). Capture step 3 uses `WEZTERM_PANE=<s.first_pane_id>` (`:303-305`). `read_first` requires Plan 01 to have shipped both (`:214-219`).
4. **`detect_screenshot_tool` vs data-table header** — **FULLY RESOLVED** as a contradiction. Header is scoped to `M.expectations`; the new function is named as a third detection helper (`:247-255`). Live header still says "no decision logic — this is a data table" (`platform.lua:8-12`) — correct pre-execution.
5. **`cli_list` missing pixel_width/pixel_height** — **FULLY RESOLVED** in this plan. Decode loop copies `size.pixel_width`/`pixel_height` (`:234-242`). Live `cli_list` still only copies `pane_id`/`tab_id`/`cwd`/`user_vars` (`mux.lua:259-266`). Plan 03 adds rows/cols first; this plan adds pixels. Sequential waves are consistent.
6. **Stale-manifest accept thin** — **FULLY RESOLVED as design.** `RUN_ID=<epoch>` + `WEZ_E2E_VISUAL_MAX_AGE` default 3600s, APPROVE refuses (`:336-344`, `:38`). **Not exercised in verify** (no planted old `RUN_ID`) — confirmed: the `<verify>` block at `:412` never plants a stale `RUN_ID` and asserts the refusal.

### New/Still-Open Concerns

- **HIGH — Task 2 `<verify>` promotes `tests/e2e/baselines/linux/dev.png` and never removes it.** Confirmed directly against the live plan text: `<verify>` (`06.9-04-PLAN.md:412`) runs `make e2e-visual APPROVE=1 SCENARIOS=dev` which "actually copies the manifest's EXACT file for `dev` to `tests/e2e/baselines/linux/dev.png`" (acceptance criteria, `:399-402`), asserts `[ -f tests/e2e/baselines/linux/dev.png ]` (`approved_exists_ec`) and a matching checksum, then the verify block ends — no `rm -f tests/e2e/baselines/linux/dev.png` or equivalent restore-to-`.gitkeep`-only step exists anywhere in this task or a later one in this plan. Must-have explicitly says this plan does not populate real baseline PNGs (`:34`, `:375`), and D-04 requires human review before promote. A leftover unreviewed PNG means Plan 05's first real capture (Task 3) diffs against this file instead of getting a clean `NO BASELINE` run, and a routine `git add -A` after running this plan's own verify would stage an unreviewed baseline for commit.
- **MEDIUM** — Stale-manifest refuse is specified but absent from the `:412` AND chain (see disposition 6 above).
- **LOW** — `fails_when` says the Makefile must not contain a literal `--approve` token (`:413`), while the recipe must pass `--approve` as lua argv (`:363`). Verify only greps `if $(APPROVE)`, so it will not false-fail; the `fails_when` sentence is imprecise but not a verify defect.
- **LOW** — APPROVE path still runs `gui.probe` (spins a GUI) before reading the manifest (`:266-270`). Wasteful; on a probe failure the missing-manifest run SKIPs (exit 0) instead of exit 1.
- **LOW** — `WEZ_BIN` defaults to `./dist/wez` even if that path is not executable; `run-e2e.sh:79` checks `-x`.

### Suggestions

- After the checksum comparison, `rm -f tests/e2e/baselines/linux/dev.png` (restore to `.gitkeep`-only) inside the same `<verify>` block, so a plan-author verify run leaves no unreviewed baseline artifact behind.
- Add a verify step: rewrite `RUN_ID` to `os.time()-7200`, run `APPROVE=1`, expect a "stale capture manifest" message and no copy.
- Split skip: capture needs `gui.probe`; approve-only needs a readable manifest, not a window.

### Risk Assessment

**MEDIUM.** Live capture and `WEZ_BIN` are real and verified. The unreviewed-baseline leftover is the one remaining HIGH that can poison Plan 05's first real capture and D-04's review-before-promote guarantee.

## 06.9-05

### Summary

Skill workflow matches D-01/D-02/D-03: ask-the-user, no A.4 percentage, `WEZ_E2E_VISUAL=1 make e2e-visual`, `APPROVE=1 SCENARIOS=`, Task 3 human checkpoint. Cycle-2 `--approve`/NEVER contradiction and stale `run-e2e-visual.sh` link are fixed in the current text.

### Round-2 Disposition Verification

1. **Skill NEVER list vs `! grep --approve`** — **FULLY RESOLVED.** Step 6 and the NEVER list describe a dashed Make flag without spelling `--approve` (`:196-218`). Verify still has `! grep -q -- "--approve"` on SKILL.md only (`:240`). `APPROVE=1` does not contain that substring. PLAN.md still names `--approve` in the ledger prose; that file is not grepped, so no self-contradiction.
2. **`key_links` names `tools/run-e2e-visual.sh`** — **FULLY RESOLVED.** `to:` is `Makefile`, pattern `APPROVE=1` (`:37-40`).
3. **`WEZ_BIN` export** — **FULLY RESOLVED** (inherited from Plan 04 `:362-367`). Task 3 invokes `make e2e-visual` (`:303`, `:394`).
4. **skill-judge UNCHECKABLE** — **UNRESOLVED / UNCHECKABLE** (unchanged, correctly labeled). Judge lives at `~/.claude/skills/skill-judge/SKILL.md`, outside this repo — genuinely unverifiable from inside the sandbox. Task 2 already makes method + dimension breakdown a SUMMARY gate (`:273-281`, `:395`), which is as far as a plan text can go toward mitigating this.
5. **Task 3 depends on Plan 04 actually capturing** — **PARTIALLY RESOLVED.** Plan 04 now has a genuine live-capture verify, so the dependency itself is sound. But Plan 04's leftover `dev.png` (see 06.9-04 above) still contaminates Task 3's first capture unless that HIGH is fixed first.

### New/Still-Open Concerns

- **MEDIUM** — Task 3's first `make e2e-visual` is only a clean `NO BASELINE` run if Plan 04 verify deleted `tests/e2e/baselines/linux/dev.png`. Directly inherited from the Plan 04 HIGH above; not a defect in this plan's own text.
- **LOW** — `min_lines: 40` vs body target 180-260 (Cycle 2 leftover, still present, still harmless against skill-judge D5's "one file under ~300 lines" preference).

### Suggestions

- Gate Task 3 on an empty `tests/e2e/baselines/linux/` except `.gitkeep`, or delete leftover PNGs before capture.
- Keep SUMMARY method recording as a hard fail.

### Risk Assessment

**LOW** in isolation; **MEDIUM** until Plan 04 stops leaving an unreviewed baseline. Skill text and checkpoint design match D-01/D-02/D-03.

## Cross-plan (Cycle 3)

| Cycle-2 issue | Plans | Disposition |
|---|---|---|
| `gui.spin` omits HOME / XDG_RUNTIME_DIR 0700 (`mux.lua:110-155`) | 01 → 03, 04 | **FULLY RESOLVED** |
| `first_pane_id` missing on Plan 01 handle | 01, 04 | **FULLY RESOLVED** (handle); tracer call sites still unnamed (LOW) |
| `local env_prefix = M.env_prefix` nil-bind | 01 | **FULLY RESOLVED** |
| Post-fire `verify_focus` vs getactivewindow-only | 01 | **FULLY RESOLVED** |
| Plan 04 Task 2 verify has no live capture | 04 | **FULLY RESOLVED** (capture is real; promote leftover is a NEW HIGH) |
| `make e2e-visual` does not export `WEZ_BIN` | 04, 05 | **FULLY RESOLVED** |
| Skill NEVER list vs `! grep --approve` | 05 | **FULLY RESOLVED** |
| `key_links` names `tools/run-e2e-visual.sh` | 05 | **FULLY RESOLVED** |
| `platform.lua` `input_tools` says ydotool fires on Wayland | 01, 02, 03 | **PARTIALLY RESOLVED** (note + README fixed; `linux` array unchanged — LOW) |
| usermod grep bypassable | 02 | **FULLY RESOLVED** for `$group`; unquoted literal is LOW |
| README verify looser than action | 03 | **FULLY RESOLVED** |
| Verify loop omits tracer names | 03 | **FULLY RESOLVED** |
| 13 vs 12 fire(B) wording | 03 | **FULLY RESOLVED** |
| `detect_screenshot_tool` in data module | 04 | **FULLY RESOLVED** (header scoped) |
| `cli_list` missing pixel_width/height | 04 | **FULLY RESOLVED** |
| Stale-manifest accept thin | 04 | **FULLY RESOLVED** as design; not exercised in verify (MEDIUM) |
| skill-judge UNCHECKABLE | 05 | **UNCHECKABLE** (already gated on SUMMARY method) |
| Task 3 depends on Plan 04 capturing | 05 | **PARTIALLY RESOLVED** (capture yes; leftover PNG contamination) |
| Wave/file ownership | all | **OK** (01∥02, 03 after both, 04 after 01+03, 05 after 04, README 03 then 04) |

Round-2 ledgers that match live plan text: isolation dirs, `M.env_prefix` without alias, `check_focus_unchanged`, live-capture AND-chain, `WEZ_BIN=$${WEZ_BIN:-./dist/wez}`, NEVER list without `--approve`, `key_links` → Makefile.

### New cross-plan HIGH

| Issue | Plans | Severity |
|---|---|---|
| Plan 04 Task 2 `<verify>` writes an unreviewed `tests/e2e/baselines/linux/dev.png` via `make e2e-visual APPROVE=1 SCENARIOS=dev` and never deletes it — contradicts D-04 (review before promote) and Plan 04's own must_have (`:34`, `:375`); Plan 05's Task 3 then diffs against that leftover file instead of a clean `NO BASELINE` run | 04 → 05 | HIGH |

### Overall risk: MEDIUM

Cycle-2's execution blockers (no mux socket, nil `env_prefix`, no pane id, TOCTOU undone, skip-only Tier 4 verify, stale-PATH `wez`, self-contradictory skill grep) are genuinely fixed in the current plan text — verified against live source, not just the Round-2 ledgers' own claims. Do not execute until Plan 04's `<verify>` cleans up the promoted `dev.png` (or promotes into a throwaway path instead of the committed baseline path). Plans 01/02/03/05 text is ready aside from that inherited residue and Plan 02's sudo-comment-vs-grep false-fail risk.

### Status: Issues Found — one HIGH remains (Plan 04 verify baseline leftover); fix and re-invoke, or accept/defer explicitly before execution

## Consensus Summary (Cycle 3)

Only one reviewer (OpenCode, source-grounded, running under the sandbox-scope guardrail with full repo access, no evidence-quality discount marker) ran this cycle. Its verdict stands at full weight per plan. Coverage: `## 06.9-01` … `## 06.9-05` headers all present, no missing ids.

Of Cycle 2's 7 cross-plan HIGHs, 6 are FULLY RESOLVED in the live plan text (confirmed against `mux.lua`, `platform.lua`, `harness.lua`, `run-e2e.sh`, README, and the plans' own `<verify>` blocks — not merely the Round-2 ledgers' claims). Plan 04's live-capture HIGH is resolved (a genuine capture now runs) but is replaced by a new HIGH: the same verify block that proves the capture leaves an unreviewed `dev.png` promoted into the committed baseline path with no cleanup step, which would contaminate Plan 05's first real approval. Remaining MEDIUM/LOW leftovers: `input_tools.linux` array still lists ydotool as Wayland-firing (note-only fix), Plan 02's SUDO SCOPE NOTE citation vs its own sudo-grep (false-fail risk), Plan 04's stale-manifest refusal not exercised in verify, and skill-judge path genuinely UNCHECKABLE from this sandbox (already mitigated as far as plan text can go).

### Agreed Strengths

Not applicable — single-reviewer cycle, no cross-reviewer agreement to synthesize. See per-plan sections above.

### Agreed Concerns

Not applicable — single-reviewer cycle. The one blocking concern is Plan 04's `<verify>` promoting `tests/e2e/baselines/linux/dev.png` without review or cleanup, inherited by Plan 05.

### Divergent Views

Not applicable — single-reviewer cycle.

---

## Cycle 2 — OpenCode Review (2026-09-09T15:18:12Z)

Round 2 of the plan-review convergence loop. Round 1 (commit `be2e08b`, below as Cycle 1) found 43 HIGH/CRITICAL plus 5 actionable non-HIGH findings; the plan author then revised all five PLAN.md files (commit `2b2f4a3`), each now carrying a "Review Dispositions Ledger — Round 1" section. This cycle re-verifies each Round-1 disposition against the current plan text and live source, and separately surfaces new/still-unresolved concerns in the current plans. Reviewer ran under the sandbox-scope guardrail (repo-only reads; no attempt to reach `/usr/lib/systemd/system`, `/usr/lib/udev/rules.d`, or other absolute system paths — none of this cycle's findings required such a path).

## 06.9-01

### Summary

The tracer architecture is right: forced-XWayland + `--class` + chained `windowactivate --sync`/`getactivewindow`, real `apply()` via symlink, two structurally different mux effects, loud skip. Round-1 CRITICAL items on product config, `mux.shquote`, and nested `fire_chords` are actually fixed in the current text. The plan still omits the isolation directories `mux.env_prefix` requires, prescribes a Lua alias that is nil if assigned "at the top", never puts a pane id on the session handle, and its acceptance criteria would re-activate the window after fire — undoing the TOCTOU fix.

### Strengths

- Product config load is now real: symlink `dir/cfg/wezterm-setup` → `config/wezterm-setup/` matches `init.lua:43-46` dotted `require` (no `debug`, no `package.path` mutation).
- `apply()` field claim holds: `init.lua:105-157` only sets `keys` / `key_map_preference` / `scrollback_lines` / tab-title hook; `enable_wayland` / geometry are safe to set first.
- `mux.shquote` does not exist; `harness.shquote` is public (`harness.lua:46-48`). Promoting `env_prefix` (`mux.lua:110-117`, currently `local`) is the right reuse.
- `skip.require_tool` really prints `LIVE-ASSERTED` (`skip.lua:51-58`); requiring it on this XWayland-capable box is the correct Round-1 fix.
- WM_CLASS from `harness.scratch_dir`'s `time-random` suffix (`harness.lua:73-75`) is invocation-unique.
- Chords match live Linux CTRL-family: ClearScreenAndScrollback is `CTRL|SHIFT+k` (`keybindings.lua:82`); SpawnTab CTRL fold is the documented Linux catch (`platform.lua:157`).

### Concerns

- **HIGH — `gui.spin` never creates the dirs `mux.env_prefix` points at.** `mux.env_prefix` sets `HOME=dir/home` and `XDG_RUNTIME_DIR=dir/runtime` (`mux.lua:110-117`). `mux.spin` mkdir's those and `chmod 700` runtime because "WezTerm refuses to use it for the socket" (`mux.lua:152-155`). Plan action only creates `dir/cfg`, `dir/launch`, and the symlink (`06.9-01-PLAN.md:270`). Without a 0700 runtime dir, `wezterm cli list` / `get-text` / `send-marker` against the GUI session have no mux socket — the tracer's mux-effect assertions cannot work.
- **HIGH — `local env_prefix = M.env_prefix` "at the top" is nil in Lua.** Prescribed at `06.9-01-PLAN.md:244`. `local M = {}` then `local env_prefix = M.env_prefix` captures nil; `mux.spin`'s `env_prefix(dir)` at `mux.lua:188` becomes a nil call and breaks **Tier 2 as well**. Alias must be assigned **after** `function M.env_prefix`, or call sites must use `M.env_prefix`.
- **HIGH — session handle has no pane id, but the tracer needs one.** Handle is `{ dir, launch_dir, env, class, window_id, pid, live }` (`06.9-01-PLAN.md:253`). `mux.send_marker(session, pane_id, marker)` and `mux.get_text` require a pane id (`mux.lua:276-292`). Action never says how to get it. `mux.spin` sets `first_pane_id` (`mux.lua:213-214`); `gui.spin` does not.
- **HIGH — acceptance vs action on post-fire check.** Action correctly does `getactivewindow` only after fire (`06.9-01-PLAN.md:340-342`) so it does not re-activate. Acceptance requires `M.verify_focus` in the post-fire path (`06.9-01-PLAN.md:413`). `verify_focus` runs `windowactivate --sync` (`06.9-01-PLAN.md:330-331`), which would **mask** a stolen-focus race and void T-06.9-01. Executor following acceptance ships a false-green integrity check.
- **HIGH — `M.probe` snippet still contradicts the wrapper.** Code block at `06.9-01-PLAN.md:308-317` shows zero-arg `M.probe()` with internal repo-root discovery; the next paragraph requires `M.probe(repo_root)` and a test-file closure. Two readings, opposite implementations.
- **MEDIUM — no R6 probe that `wezterm cli` talks to a GUI `wezterm start` (not `wezterm-mux-server`).** `mux.cli_list` is proven only against mux-server (`mux.lua:1-7, 253-254`). Four listed probes never cover this. If it fails, every mux-effect assert fails.
- **MEDIUM — `input_tools.linux` still says ydotool is the Wayland firing tool** (`platform.lua:107`). This plan does not update that row.

### Suggestions

- Copy `mux.spin`'s mkdir/`chmod 700` for `home` + `runtime` (and `cfg`/`launch`) into `gui.spin`.
- Define `function M.env_prefix` first, then `local env_prefix = M.env_prefix`, or just call `M.env_prefix` at `mux.lua:188`.
- Set `session.first_pane_id` from `cli_list` the same way `mux.spin` does.
- Change acceptance/grep to require post-fire `getactivewindow` **without** `windowactivate`; keep `verify_focus` pre-fire only.
- Delete the zero-arg `M.probe` snippet; keep only `M.probe(repo_root)` + test-file wrapper.
- Add an R6 probe: GUI `wezterm start` under `env_prefix` → `wezterm cli list --format json` returns ≥1 pane.

### Risk Assessment

**HIGH.** Round-1 product-config and skip-gate fixes are real, but isolation dirs and the env_prefix alias can make the tracer (and Tier 2) fail before a single key is injected.

## 06.9-02

### Summary

This is the strongest of the five. Round-1 CRITICAL uinput-root and Homebrew-formula falsehoods are actually designed out. Remaining risk is sudo/autonomous hang, a weak usermod grep, and `input_tools` still describing ydotool as the Wayland firing mechanism.

### Strengths

- Placeholder `--setup` arm is real (`run-e2e.sh:41-61`); replacing it with `exec "${SCRIPT_DIR}/e2e-setup.sh"` matches `SCRIPT_DIR` at `run-e2e.sh:31` and leaves `Makefile:48-49` untouched.
- `command -v` before brew is the right idempotency vs cellar-tracking.
- Empty brew formula for ydotool/spectacle/grim is the correct fix for "Homebrew-on-Linux does not package those."
- uaccess-first, then allowlist `input`/`uinput` only, never `stat` → `usermod`, cannot become `usermod -aG root`.
- ydotoold start gated on **current** writability, deferred on a fresh grant — matches D-08.
- ImageMagick on the install list unblocks Plan 04.
- SUDO SCOPE NOTE citation matches `ci-setup-toolchain.sh:19-25` (dev-tool vs end-user install).
- `platform_os` reuse of `tools/lib/platform.sh:19-27` is correct.

### Concerns

- **HIGH — `autonomous: true` plus live `sudo` (groupadd, udev, rpm-ostree).** Task 1 verify runs the script twice (`06.9-02-PLAN.md:254`). A password prompt hangs or fails the wave. Not a design bug; it is an execution-gate the plan does not name.
- **MEDIUM — usermod grep is bypassable.** `! grep -E 'usermod -aG "\$\(?stat'` does not catch `group=$(stat …); usermod -aG "$group"`. Acceptance prose is stricter than verify.
- **MEDIUM — `input_tools` still lists ydotool as the Wayland firing tool** (`platform.lua:104-110`). This plan installs both tools (good) but never updates the expectation row Plan 01 just falsified.
- **LOW — udev/rpm-ostree/uaccess claims are UNCHECKABLE here** (sandbox: no `/usr/lib/udev`). R6 probes are the right mitigation if the executor actually writes them.

### Suggestions

- Document a non-interactive sudo requirement (or a dry-run/`WEZ_E2E_SETUP_DRY=1` path) so autonomous execution cannot block on a password.
- Assert allowlist in verify: every `usermod -aG` argument is the literal `input` or `uinput`.
- Update `input_tools.note` to: provisioned = xdotool+ydotool; **firing** = xdotool + forced-XWayland (Plan 01).

### Risk Assessment

**MEDIUM.** Design of D-06..D-09 looks executable and the Round-1 privilege bug is gone. Residual risk is sudo interactivity and expectation-row drift, not the uinput algorithm.

## 06.9-03

### Summary

Horizontal expansion is correctly sequenced on Plan 01's pipeline and Plan 02's README ownership. Real `cli_list` fields, all four pane directions, nested `fire_chords` extension, and live-log grep are genuine Round-1 fixes. Remaining issues: font-size fires WezTerm defaults not curated SUPER chords (documented, slightly overclaimed), README verify is looser than the action, and `split_pane` extension must stay default-identical.

### Strengths

- `mux.spawn_pane` really opens a **new tab** (`mux.lua:305-312`), so CloseCurrentTab / ActivateTabRelative preconditions using `spawn_pane` are valid.
- `mux.split_pane` is hardcoded `--right --percent 50` (`mux.lua:320-323`); optional `opts` with those defaults is additive. Tier 2 regression via `scene_reuse_e2e_test.lua` is the right check.
- `cli_list` currently copies only `pane_id`/`tab_id`/`cwd`/`user_vars` (`mux.lua:248-266`). Extending with `is_active`/`is_zoomed`/`size` is required; zoom-via-real-field is the right Round-1 fix.
- Curated Linux chords match `keybindings.lua:95-118` (Ctrl+Shift+W, Ctrl+Tab, Alt+Shift+H/V/X/Z/R/E, Alt+arrows).
- Font-size firing CTRL `=/−/0` matches `chord_families.linux` (`platform.lua:167-169`): managed SUPER zoom is WM-shadowed.
- RotatePanes as proof of real config is valid: `keybindings.lua:147-149` / `platform.lua:165` — no WezTerm default.
- README split with Plan 04 (`_TBD (06.9-04)_` for Screenshot) avoids same-wave conflict. Wave deps (`depends_on: 06.9-01, 06.9-02`) are correct.
- Live-log grep (not source grep) is the correct L132 fix.

### Concerns

- **HIGH — must-have overclaims "every fired chord exercises curated `keybindings.lua` bindings."** Font-size cases fire WezTerm **defaults** (`CTRL+=/−/0`), not `keybindings.lua:122-124` SUPER chords. Fine as Linux catch-scope, but the must-have and "real product config ⇒ curated bindings" sentence are false for that case. DisableDefaultAssignment only disables SUPER zoom (`keybindings.lua:144-146`), so CTRL defaults still work even without product config — RotatePanes is the real self-check, font-size is not.
- **MEDIUM — Plan 01 `gui.spin` gaps (runtime dir, `first_pane_id`) are inherited.** This plan reuses `gui.spin` unchanged; if 01 ships without those dirs, the whole matrix fails the same way.
- **MEDIUM — README verify does not match the action.** Action says no "wired up in 06.9" **in sections this task owns**; verify is file-wide `! grep -q "wired up in 06.9"` (`06.9-03-PLAN.md:299`). Current README uses "wired in 06.9" / "placeholder — 06.9" (`README.md:78-92`), not that exact string — verify can pass without filling the table.
- **MEDIUM — verify loop omits tracer action names** (`CloseCurrentTab`…`FontSize` but not `ClearScreenAndScrollback` / `SpawnTab`). A Plan 03 edit that drops the tracer cases still passes.
- **MEDIUM — "13 curated EXCEPT SendString" vs "12 fire(B)".** A.3 has 13 bullets, last is WordNav-only (`e2e-testing-battery-v1.0-prd.md:297-312`). Harmless if 12 fire(B) line-items is the contract; the "13" wording will confuse an executor counting `Increase`/`Decrease`/`Reset` as three `fire_chords` keys (which the action correctly adds as three strings).
- **LOW — JSON list order = UI tab order is correctly an R6 probe**, not assumed. Keep it blocking for ActivateTabRelative/MoveTabRelative.

### Suggestions

- Narrow the must-have: curated chords where they register; font-size = "CTRL family that actually registers on Linux, which is the WezTerm default fold."
- Grep live log for `ClearScreenAndScrollback` and `SpawnTab` too.
- README verify should require `xdotool`, `enable_wayland` or `forced XWayland`, and `06.9 (landed)` **on the Firing row**, not a file-wide absent string.
- Deduplicate `tab_id` when building ordered tab lists (safe even with 1 pane/tab).

### Risk Assessment

**MEDIUM.** Inventory and field choices match the PRD and live `mux.lua`. Risk is inherited Plan 01 isolation failure plus overclaim on font-size as a product-binding proof.

## 06.9-04

### Summary

Moving Tier 4 into a discovered `*_e2e_test.lua`, Tier 2 scene seeding, `tabbar`, exact-byte approve, and `APPROVE=1` Make syntax are real Round-1 fixes. Two dispositions are **not** complete: live capture is still absent from Task 2 `<verify>`, and the driver depends on `s.first_pane_id` / `WEZ_BIN` that Plan 01 / the Makefile do not provide. That is enough to miss D-04/D-05 on the live path while verify still prints `TIER4_DRIVER_VERIFY_OK`.

### Strengths

- Auto-discovery is real: `run-e2e.sh:69` is `find tests/e2e -type f -name '*_e2e_test.lua'`. A `tests/e2e/tier4/visual_diff_e2e_test.lua` is picked up with no orchestrator change. D-10 (no CI edit) still holds because the file self-skips without `WEZ_E2E_VISUAL=1`.
- Seeding recipe is specified as a copy of `scene_reuse_e2e_test.lua:129-144` (`WEZTERM_SETUP_DIR` + `WEZTERM_PANE` + `cd` launch dir). That is the actual isolated-recipe contract.
- `wez pane title` exists (`cli/commands/pane.lua:120`); tabbar same-title setup is not a fictional CLI.
- `KEEP_CONFIG=1` convention is real (`Makefile:20-22, 51-52`); `APPROVE=1`/`SCENARIOS=` is the valid Make form.
- Comparator order (decode → dimensions → RMSE) is the right D-05 shape; Task 1 verify actually exercises identical/different/mismatch/corrupt.
- Deferring first real PNGs to Plan 05 matches D-04 (review before promote). `.gitkeep` only is honest.
- D-01 (no vision API from `make e2e`) is cleanly separated.

### Concerns

- **HIGH — Round-1 L175 disposition is false.** Ledger says Task 2 `<verify>` "runs a real live capture." Actual verify (`06.9-04-PLAN.md:318`) is: syntax load, no-flag skip, `run-e2e.sh` **name** grep, Makefile `$(if $(APPROVE)`, `.gitkeep`, APPROVE with no manifest. **No** `WEZ_E2E_VISUAL=1` capture, no four-scenario run, no `tabbar` tab-count, no checksum promote. Same class of defect Round 1 already called HIGH.
- **HIGH — `s.first_pane_id` is not on Plan 01's `gui.spin` handle** (`06.9-01-PLAN.md:253`), but capture step 3 uses `WEZTERM_PANE=<s.first_pane_id>` (`06.9-04-PLAN.md:245`). Without it, scene launch does not target the isolated GUI pane (the exact Round-1 "recipes will not resolve" failure, shifted from env vars to pane id).
- **HIGH — `make e2e-visual` does not set `WEZ_BIN`.** Proposed target is `@WEZ_E2E_VISUAL=1 lua5.4 tests/e2e/tier4/...` only. `run-e2e.sh:79-82` defaults `WEZ_BIN=./dist/wez`; `harness.lua:84-85` falls back to PATH `wez`. README calls that the "stale PATH wez" footgun (`README.md:32-34`). Scene launch and `pane title` would hit the wrong binary. `scene_reuse` also absolutizes `WEZ_ABS` after `cd` (`scene_reuse_e2e_test.lua:57-70`); the Makefile path does not.
- **HIGH — inherits Plan 01 missing `XDG_RUNTIME_DIR`.** Screenshot + `wezterm cli` settle checks need the GUI mux socket.
- **MEDIUM — `platform.detect_screenshot_tool()` puts compositor decision logic in a module whose header says it is a data table with no decision logic** (`platform.lua:8-12`). Duplication across bash/Lua is acknowledged; putting it in `gui.lua` would match that header.
- **MEDIUM — `cli_list` extension in Plan 03 copies `size.rows`/`cols` only, not `pixel_width`/`pixel_height`.** Task 2 R6 probe wants pixel compare vs spectacle capture. Driver cannot read pixels off `mux.cli_list` as specified.
- **MEDIUM — stale-manifest accept (T-06.9-14) is thin.** Stable `/tmp/wez-e2e-visual-latest` plus APPROVE-from-manifest means a leftover capture from yesterday can be promoted. Overwrite-on-capture helps only if someone captured first in **this** session.

### Suggestions

- Add to Task 2 verify: `WEZ_E2E_VISUAL=1` capture of all four scenarios, `NO BASELINE` (or RMSE) in the log, `tabbar` pane/tab count, then `APPROVE=1 SCENARIOS=dev` with matching checksums.
- Plan 01 must set `first_pane_id`; Plan 04 should also show the `WEZ_ABS` construction from `scene_reuse_e2e_test.lua:57-70`.
- Makefile: `WEZ_BIN ?= ./dist/wez` (or the same default `run-e2e.sh` uses) exported into the lua invocation.
- Stamp the manifest with a run id / mtime and refuse APPROVE if older than this capture session.

### Risk Assessment

**HIGH.** The integration shape is right (`*_e2e_test.lua` + VAR=1 + exact-byte promote), but verify still cannot catch a driver that never captures, and the live path is underspecified on pane id and `WEZ_BIN`.

## 06.9-05

### Summary

Task 3 as a blocking human checkpoint, `WEZ_E2E_VISUAL=1`, `APPROVE=1 SCENARIOS=`, catalog-vs-reachable, and "no A.4 percentage" are real Round-1 fixes. Two leftovers remain: `key_links` still points at deleted `tools/run-e2e-visual.sh`, and Task 1's NEVER list cannot name `--approve` without failing its own grep. Skill-judge ≥90% via clean-context is specified; the judge file is outside this repo (UNCHECKABLE here).

### Strengths

- `autonomous: false` + `checkpoint:human-verify` matches D-02 ask-the-user and D-04 review-before-promote.
- Capture/approve contract in the task body matches Plan 04 (`WEZ_E2E_VISUAL=1 make e2e-visual`, `${TMPDIR:-/tmp}/wez-e2e-visual-latest/manifest.txt`, `APPROVE=1 SCENARIOS=`).
- `workflow.cross_ai_command` is real: `.planning/config.json:47` is `/home/bazzite/.opencode/bin/opencode run --model xai/grok-4.6`.
- A.4 six items are the right verbatim checklist (`e2e-testing-battery-v1.0-prd.md:314-320`); no numeric bar matches D-03's correction.
- `tabbar` called out for same-title / active-vs-inactive — those two A.4 bullets cannot be judged from single-tab captures.
- Frontmatter `description` WHAT+WHEN+KEYWORDS requirement matches skill-judge D4 as described (judge SKILL.md itself not readable in this sandbox).

### Concerns

- **HIGH — Task 1 action vs verify on `--approve`.** Action requires a NEVER list that names the old `--approve` form (`06.9-05-PLAN.md:205-207`). Verify is `! grep -q -- "--approve"` on the whole file (`06.9-05-PLAN.md:234`). An executor cannot both document the anti-pattern and pass verify. Opposite outcomes from one SKILL.md.
- **MEDIUM — leftover Round-1 artifact in `key_links`.** `to: tools/run-e2e-visual.sh` (`06.9-05-PLAN.md:37-40`) is the orchestrator Plan 04 deleted. Via-text is correct (`APPROVE=1`); the `to:` path is not. An executor following key_links looks for a file that must not exist.
- **MEDIUM — Task 2 "clean-context skill-judge" is UNCHECKABLE here** (`~/.claude/skills/skill-judge/SKILL.md` is outside the repo). If the executor self-scores in the authoring context, D-03 is theater. The plan does tell them to record the method — keep that as a hard SUMMARY requirement.
- **MEDIUM — Task 3 depends on Plan 04 actually capturing.** If 04 verify stays skip-only, Task 3 is the first live capture. That is acceptable only if 04's live gaps (runtime dir, `first_pane_id`, `WEZ_BIN`) are fixed first.
- **LOW — `min_lines: 40` vs body target 180-260.** Not harmful; skill-judge D5 prefers one file under ~300 lines, which the action already states.

### Suggestions

- Verify: forbid `--approve` except in a NEVER/do-not-use sentence, **or** say "never pass a dashed Make flag; use `APPROVE=1`" without the substring `--approve`.
- Point `key_links.to` at `Makefile` / `visual_diff_e2e_test.lua`, not `tools/run-e2e-visual.sh`.
- Task 2 SUMMARY must include dimension scores + evaluation method; treat a missing method as gate failure.

### Risk Assessment

**MEDIUM.** Skill workflow and checkpoint design match D-01/D-02/D-03. The `--approve` grep contradiction is blocking for Task 1 verify; live success still rides on Plan 04 actually working.

## Cross-plan (Cycle 2)

| Issue | Plans | Severity |
|---|---|---|
| `gui.spin` omits `HOME` / `XDG_RUNTIME_DIR` 0700 that `mux.env_prefix` requires (`mux.lua:110-155`) | 01 → 03, 04 | HIGH |
| `first_pane_id` missing on Plan 01 handle, required by 01 tracer + 04 `WEZTERM_PANE` | 01, 04 | HIGH |
| `local env_prefix = M.env_prefix` "at the top" nil-binds and can break Tier 2 | 01 | HIGH |
| Post-fire `verify_focus` vs `getactivewindow`-only (acceptance undoes TOCTOU fix) | 01 | HIGH |
| Plan 04 Task 2 verify still has no live capture (L175 disposition unsound) | 04 | HIGH |
| `make e2e-visual` does not export `WEZ_BIN` (`run-e2e.sh:79-82` / `harness.lua:84-85`) | 04, 05 | HIGH |
| Skill NEVER list vs `! grep --approve` contradiction | 05 | HIGH |
| `key_links` still names `tools/run-e2e-visual.sh` | 05 | MEDIUM |
| `platform.lua` `input_tools` still says ydotool fires on Wayland | 01, 02, 03 | MEDIUM |
| Wave/file ownership (01∥02, 03 after both, 04 after 01+03, 05 after 04, README 03 then 04) | all | OK |

Round-1 ledgers that **are** sound: product-config symlink, nested `fire_chords`, uinput allowlist, brew-per-formula, `*_e2e_test.lua` discovery, exact-byte approve, Task 3 human checkpoint, `WEZ_E2E_VISUAL=1` in skill prose.

### Overall risk: HIGH

Do not execute until Plan 01 isolation dirs + env_prefix alias + pane id + post-fire acceptance are corrected, and Plan 04 grows a live-capture verify plus `WEZ_BIN`/`first_pane_id`. Plan 02 is ready aside from sudo interactivity. Plan 05 needs the `--approve` grep/NEVER contradiction and the stale `run-e2e-visual.sh` link fixed.

### Status: Issues Found — fix and re-invoke

## Consensus Summary (Cycle 2)

Only one reviewer (OpenCode, source-grounded, running under the sandbox-scope guardrail with full repo access, no evidence-quality discount marker) ran this cycle. Its verdict stands at full weight per plan. All 7 Round-1 HIGH/CRITICAL classes it re-checked that were genuinely fixed are confirmed fixed; 7 NEW HIGHs surfaced as a direct consequence of Round-1's own fixes (isolation-dir/env_prefix/pane-id plumbing in Plan 01, the still-incomplete live-capture verify in Plan 04, the `WEZ_BIN` gap, and the `--approve`/NEVER-list self-contradiction in Plan 05). The review cites concrete `file:line` evidence for every finding and reached all 5 plans in the coverage manifest (verified programmatically: `## 06.9-01` … `## 06.9-05` headers all present, no missing ids).

### Agreed Strengths

Not applicable — single-reviewer cycle, no cross-reviewer agreement to synthesize. See per-plan Strengths sections above.

### Agreed Concerns

Not applicable — single-reviewer cycle. See per-plan Concerns sections and the Cross-plan (Cycle 2) table above for the reviewer's own synthesis across plans.

### Divergent Views

Not applicable — single-reviewer cycle.

---

## Cycle 1 — OpenCode Review (2026-09-09T11:50:04Z, superseded — retained for history)

## OpenCode Review

Using `ai-kit-spec-review-checklist` to audit all five GSD plans against the repository, and `graphify` to trace referenced code relationships before judging the proposed mechanisms.
The repository has neither a CodeGraph index nor an existing Graphify graph, so the review will use direct source reads. I am splitting the five plan audits across independent reviewers, then I will reconcile shared-file and dependency issues across the phase.
The first pass found phase-blocking interface mismatches: the isolated GUI is not planned to load the product configuration, Plan 01 and Plan 03 disagree on `fire_chords`, and Plan 04 cannot safely promote the exact image the agent reviewed. I am now validating the setup and model-selection workflows, where the remaining risks are privilege handling, tool availability, and the autonomous-versus-interactive contract.
Cross-plan analysis confirms the phase cannot currently meet its done-bar: `make e2e` would still omit Tier 4, no task performs the real semantic review or commits approved PNG baselines, and the approval interface would recapture rather than promote the exact reviewed artifact. The setup plan also contains an unsafe `/dev/uinput` group-selection rule and assumes several nonexistent Homebrew formulas.
# Plan Review

**Document type:** GSD implementation plans
**Status:** Issues found. Revision is required before execution.

## 06.9-01

### Summary

The forced-XWayland approach is a reasonable response to Wayland's targeting limitations, and the two tracer cases provide useful structural coverage. However, the planned scratch session does not load the repository's keybindings, accepts a capability skip on the machine where live proof is required, and relies on private `mux.lua` internals. These issues can produce false confidence or test stock WezTerm behavior instead of the product.

### Strengths

- The environment and capability gates follow the established loud-skip contract in `tests/e2e/lib/skip.lua:31-58`; the plan applies both gates at `06.9-01-PLAN.md:214-220`.
- Focus is verified immediately before each injection rather than assumed once per session, matching the safety requirement in `06.9-CONTEXT.md:115-122` and the implementation contract in `06.9-01-PLAN.md:187-197`.
- Clear-screen marker removal and SpawnTab count growth directly correspond to the required effects in `docs/prds/e2e-testing-battery-v1.0-prd.md:297-302`.
- The proposed observation functions exist as public APIs: `cli_list`, `get_text`, `send_marker`, and `poll_until` are implemented in `tests/e2e/lib/mux.lua:231-293`.
- Cleanup through a guarded path is explicitly considered at `06.9-01-PLAN.md:198-201,228-230`.

### Concerns

- **CRITICAL: The isolated GUI does not load the product keybindings.** The scratch config only sets runtime/window properties at `06.9-01-PLAN.md:171-181`. The actual bindings are installed through `wezterm-setup.apply` in `config/wezterm-setup/init.lua:105-139`. The tracer would therefore test defaults or fail to exercise `ClearScreenAndScrollback`.
- **CRITICAL: A phase-blocking capability failure is accepted as success.** The context says unreliable targeting blocks the phase at `06.9-CONTEXT.md:115-122`, but acceptance permits either `LIVE-ASSERTED` or `SKIPPED` at `06.9-01-PLAN.md:250-256`, and five identical skips satisfy Task 2 at `06.9-01-PLAN.md:275-299`.
- **CRITICAL: The plan treats private helpers as reusable interfaces.** `06.9-01-PLAN.md:156-162` names `mux.shquote` and the environment-prefix machinery, but both are private locals in `tests/e2e/lib/mux.lua:34-39,104-117`.
- **HIGH: Verification does not enforce its claims.** The command at `06.9-01-PLAN.md:259` prints earlier failure codes without testing them. The stability command at `06.9-01-PLAN.md:296` runs `sort -u` but never compares verdict classes or skip reasons.
- **HIGH: `os.time()` is not invocation-unique.** The claim at `06.9-01-PLAN.md:178-180` fails for concurrent or same-second runs. The existing harness uses a random suffix for precisely this reason in `tests/e2e/lib/harness.lua:70-76`.
- **HIGH: Cleanup does not cover a partial failure inside `probe` or `spin`.** Cleanup is specified only after a session is returned at `06.9-01-PLAN.md:198-201,228-230`; a launch followed by an exception can leave a visible GUI process.
- **HIGH: The focus check still has a time-of-check/time-of-use race.** Activation and `getactivewindow` complete before a separate injection process starts at `06.9-01-PLAN.md:187-197`. Another desktop event can steal focus in between.
- **HIGH: The required hypothesis/probe workflow is absent.** New behavior may not land in `tests/` before a green hypothesis, and external CLI/window assumptions require probe artifacts, per `docs/agent-iteration.md:10-20,40-51,92-125`.
- **MEDIUM: Marker appearance and removal are not polled.** `06.9-01-PLAN.md:221-228` specifies direct checks even though marker delivery and rendering are asynchronous; existing scene tests poll such state at `tests/e2e/tier2/scene_reuse_e2e_test.lua:158-162`.

### Suggestions

- Make `gui.spin` load and apply the repository's actual `config/wezterm-setup` module.
- Export a shared environment builder or define a public `mux.session_env` API; use `harness.shquote`.
- Require five `LIVE-ASSERTED` passes on the designated capable host. Any capability skip there must trigger the checkpoint.
- Use `harness.scratch_dir`-grade uniqueness and exact class matching.
- Make `probe` and `spin` internally exception-safe.
- Replace verification commands with fail-fast assertions for exit code, exact runtime labels, verdict class, and skip reason.
- Add the required `.tmp/probes/` and hypothesis work before promotion.

### Risk Assessment

**HIGH.** The plan can complete without proving firing and can exercise the wrong configuration.

## 06.9-02

### Summary

The plan correctly preserves the existing `make e2e-setup` entry point and distinguishes local developer provisioning from end-user installation. Its package and uinput assumptions are unsafe on the current host, however. The proposed Homebrew path cannot install most Linux dependencies, and the `/dev/uinput` group algorithm would currently attempt to add the user to the `root` group.

### Strengths

- The existing target is correctly identified: `Makefile:48-49` already dispatches `tools/run-e2e.sh --setup`.
- Reusing `platform_os` is appropriate; the function supplies the required Linux/macOS mapping at `tools/lib/platform.sh:18-27`.
- Replacing only the setup arm preserves the existing battery discovery loop at `tools/run-e2e.sh:41-69`.
- The macOS manual permission flow matches the locked decision in `06.9-CONTEXT.md:93-98`.
- The plan explicitly reports re-login requirements rather than treating new group membership as immediately active at `06.9-02-PLAN.md:125-138`.

### Concerns

- **CRITICAL: The `/dev/uinput` group-selection algorithm is unsafe.** `06.9-02-PLAN.md:125-132` selects the device owner group and passes it to `sudo usermod -aG`. Live verification shows `/dev/uinput` is currently `root:root`, so this algorithm would attempt to add the developer to the privileged `root` group. The context only authorizes an `input`/`uinput` group at `06.9-CONTEXT.md:83-88`.
- **CRITICAL: The Homebrew Linux assumptions are false.** `06.9-02-PLAN.md:119-145` invokes `brew install` for `ydotool`, `spectacle`, `grim`, `slurp`, and `gnome-screenshot`. Live `brew info` reports no formula for any of them. Because native fallback is allowed only when Brew is absent, a Linux host with Brew present cannot be provisioned.
- **HIGH: Existing system tools do not make the Brew command idempotent.** The plan says system-installed `xdotool`/`ydotool` makes setup a no-op at `06.9-02-PLAN.md:100-106,166-170`, but Brew tracks its own cellar, not `command -v`; it reports the system `xdotool` as "not installed."
- **HIGH: Tier 4's ImageMagick dependency is omitted.** Plan 04 requires `compare` at `06.9-04-PLAN.md:159-166` and claims Plan 02 provisions it at `06.9-04-PLAN.md:286`, but Plan 02's tool list contains only input and screenshot utilities at `06.9-02-PLAN.md:118-151`.
- **HIGH: Daemon startup is attempted before new permissions can apply.** `06.9-02-PLAN.md:125-138` adds the group and immediately starts `ydotoold`, even though the same section recognizes that the current process lacks the new membership. No socket/readiness check is specified.
- **HIGH: Verification can pass after setup failure.** `06.9-02-PLAN.md:177` captures and prints `setup_exit` without asserting it. Task 2 similarly runs `make e2e-setup; echo ...` before unrelated checks at `06.9-02-PLAN.md:215`.
- **MEDIUM: The acceptance criteria require idempotency but verification runs setup only once.** Duplicate daemon prevention and repeated group-add behavior at `06.9-02-PLAN.md:164-170` are not tested.
- **MEDIUM: New executable mode is not an explicit task step.** The script is required to be executable at `06.9-02-PLAN.md:180`, but creation instructions at `06.9-02-PLAN.md:110-116` do not require setting the Git executable bit.

### Suggestions

- Never derive an authorization group directly from a `root`-owned device. Require an allowlisted `input` or `uinput` group and install an appropriate udev rule when neither safely owns the device.
- Install only missing commands. Probe Brew formula availability per package, then use the approved native fallback for unsupported Linux formulas.
- Add ImageMagick to `e2e-setup`.
- Defer daemon startup when a re-login is required, or start it through a verified system service with an explicit socket/readiness probe.
- Test setup twice and assert group operation count, daemon count, selected backend, and exact exit status.
- Include executable-mode setup and a non-privileged dry-run/probe path.

### Risk Assessment

**HIGH.** The current group logic creates an unacceptable privilege risk, while the package strategy fails on the target Linux environment.

## 06.9-03

### Summary

The plan has a good effect-oriented testing direction, but its observation and topology APIs are insufficient for the proposed cases. It also contradicts itself by promising every pane-direction variant while firing only one representative direction.

### Strengths

- Each case is intended to use a fresh GUI session, verified focus, bounded polling, and a named mux effect at `06.9-03-PLAN.md:110-150`.
- The selected Linux chords generally match the curated bindings in `config/wezterm-setup/keybindings.lua:81-129`.
- WordNav is correctly excluded because Appendix A.3 marks it registration-only at `docs/prds/e2e-testing-battery-v1.0-prd.md:297-312`.
- README ownership is sequenced after setup and before Plan 04's visual documentation, avoiding a same-wave file conflict at `06.9-03-PLAN.md:176-225`.

### Concerns

- **CRITICAL: The scratch GUI still does not load the bindings being tested.** Plan 03 fires product-specific chords at `06.9-03-PLAN.md:87-95`, but Plan 01's scratch config at `06.9-01-PLAN.md:171-181` never applies `config/wezterm-setup/init.lua:105-139`.
- **CRITICAL: Plans 01 and 03 define incompatible `fire_chords` schemas.** Plan 01 creates a flat action-keyed table at `06.9-01-PLAN.md:203-212`; Plan 03 expects `fire_chords.linux` and `.mac`, including actions with multiple variants, at `06.9-03-PLAN.md:102-108`.
- **HIGH: Required mux fields are discarded.** The plan relies on size, geometry, activity, and zoom at `06.9-03-PLAN.md:121-149`, but `mux.cli_list` returns only `pane_id`, `tab_id`, `cwd`, and `user_vars` in `tests/e2e/lib/mux.lua:247-269`. `mux.lua` is not listed for modification.
- **HIGH: Active-tab observation is not defined.** The "active-pane-of-active-tab convention" at `06.9-03-PLAN.md:121-124` is not provided by the current helper contract. A focused pane/client observer is needed.
- **HIGH: Existing topology helpers are too limited.** `spawn_pane` creates a new tab, and `split_pane` always makes a right-side 50% split at `tests/e2e/lib/mux.lua:304-324`. The plan needs selected-tab activation, selected-pane activation, vertical layouts, and unequal splits.
- **HIGH: The zoom fallback is invalid.** `06.9-03-PLAN.md:131-136` suggests that the other pane may become inaccessible through `get-text`; zoom does not remove the pane from the mux. Preserve and assert `is_zoomed` instead.
- **HIGH: Pane-direction coverage contradicts the must-have.** Appendix A.3 requires Left/Right/Up/Down at `docs/prds/e2e-testing-battery-v1.0-prd.md:309-311`; the plan explicitly fires only one direction at `06.9-03-PLAN.md:140-143`.
- **HIGH: Font-size observation is not stabilized.** The rows/columns assertion assumes fixed window pixels at `06.9-03-PLAN.md:144-149`, but the GUI config does not pin `adjust_window_size_when_changing_font_size = false`.
- **HIGH: Runtime verification is not enforced.** `06.9-03-PLAN.md:169-171` records `live_exit` without testing it and greps source text rather than proving each runtime assertion executed.
- **MEDIUM: `h.check` does not emit per-case `LIVE-ASSERTED`.** The plan claims it does at `06.9-03-PLAN.md:152-154`; the actual output is `ok` or `FAIL` in `tests/e2e/lib/harness.lua:92-113`.

### Suggestions

- Freeze a nested schema supporting OS and variants, such as `fire_chords.linux.ActivateTabRelative.next`.
- Extend `mux.cli_list` to retain geometry, `is_active`, and `is_zoomed`; add a focused-client helper.
- Add deterministic activation and configurable split helpers.
- Test all four pane directions and all listed chord variants.
- Pin window-resize behavior for font tests.
- Assert runtime labels and live exit status rather than grepping implementation text.
- Mark README completion as Linux-landed/macOS-deferred rather than globally landed.

### Risk Assessment

**HIGH.** Several assertions cannot be implemented with the current helpers, and partial variant coverage would overstate PRD completion.

## 06.9-04

### Summary

The deterministic visual-testing direction is appropriate, but the plan cannot currently launch isolated recipes, exercise the shipped tab renderer, approve the exact reviewed capture, or satisfy the required `make e2e` and committed-baseline deliverables.

### Strengths

- It correctly separates deterministic diffing from agent semantic review, matching `06.9-CONTEXT.md:23-32`.
- Fresh captures remain outside the committed baseline tree at `06.9-04-PLAN.md:168-170`.
- The privacy risk of full-desktop screenshots is explicitly recognized at `06.9-04-PLAN.md:180-185,271-284`.
- RMSE is a reasonable candidate metric, and the plan exposes threshold override through `WEZ_E2E_VISUAL_THRESHOLD` at `06.9-04-PLAN.md:108-123`.

### Concerns

- **CRITICAL: Tier 4 is not integrated into `make e2e`.** The PRD requires full local `make e2e` coverage at `docs/prds/e2e-testing-battery-v1.0-prd.md:249-255`. The current target runs only `tools/run-e2e.sh` at `Makefile:42-43`, which discovers only Lua files at `tools/run-e2e.sh:63-69`. Plan 04 adds a separate target but no integration.
- **CRITICAL: Isolated scene recipes will not resolve.** Recipe lookup follows scratch environment paths in `cli/commands/scene.lua:680-699`. Existing Tier 2 tests copy recipes and export `WEZTERM_SETUP_DIR` at `tests/e2e/tier2/scene_reuse_e2e_test.lua:129-145`; Plan 04 omits both steps at `06.9-04-PLAN.md:172-179`.
- **CRITICAL: Screenshots will not include the shipped fancy tab renderer.** Plan 04 reuses Plan 01's minimal config at `06.9-04-PLAN.md:142-146,172-177`, while the renderer is registered only by `wezterm-setup.apply` at `config/wezterm-setup/init.lua:151-154`.
- **CRITICAL: The scenarios do not cover same-title width or active/inactive distinction.** Those checks are required at `docs/prds/e2e-testing-battery-v1.0-prd.md:314-320`, but each scenario uses a fresh single-tab session at `06.9-04-PLAN.md:172-186`.
- **CRITICAL: Approval does not promote the exact reviewed image.** `06.9-04-PLAN.md:192-202` proposes rerunning capture during approval and provides only global approve-all behavior. A recapture may differ from the PNG judged by the agent, and there is no scenario selector.
- **CRITICAL: `make e2e-visual --approve` is not valid Make syntax.** The plan uses it at `06.9-04-PLAN.md:189-200,245-253`, while separately proposing `APPROVE=1` at `06.9-04-PLAN.md:204-207`.
- **CRITICAL: Committed baselines are promised but not planned.** The files list and verification include only `.gitkeep` at `06.9-04-PLAN.md:156,209-225`; the PRD requires committed baselines at `docs/prds/e2e-testing-battery-v1.0-prd.md:203-204,249-255`.
- **HIGH: The Bash/Lua lifecycle interface is unspecified.** `gui.spin` returns an in-memory Lua table, but the Bash driver needs that state across launch, focus, capture, and teardown. The optional companion driver at `06.9-04-PLAN.md:174-177` is neither defined nor listed as an artifact.
- **HIGH: Render readiness is reduced to pane count.** `06.9-04-PLAN.md:178-185` does not establish that user vars, tab formatting, fonts, or compositor frames have settled.
- **HIGH: The comparator does not reject dimension mismatches or malformed metrics.** `06.9-04-PLAN.md:108-123` treats RMSE as resolution-independent without first enforcing identical dimensions.
- **HIGH: The threshold is uncalibrated.** The tests compare identical images and opposite solid colors at `06.9-04-PLAN.md:125-134`; they do not prove repeat captures pass or localized tab/icon regressions fail.
- **HIGH: Live capture is absent from verification.** Acceptance requires three captures at `06.9-04-PLAN.md:213-218`, but verification only covers syntax, no-flag skip, Makefile text, and `.gitkeep` at `06.9-04-PLAN.md:223-225`.
- **HIGH: Screenshot provisioning and consumption disagree.** Plan 02 selects several compositor tools, while Plan 04 gates specifically on Spectacle at `06.9-04-PLAN.md:159-166`. No persisted backend-selection interface exists.

### Suggestions

- Add Tier 4's deterministic runner to `make e2e`, behind its opt-in skip gate.
- Create a dedicated Lua driver that owns the full `gui.spin` through `gui.teardown` lifecycle.
- Seed recipes and export `WEZTERM_SETUP_DIR` exactly as Tier 2 does.
- Load the managed config and create dedicated multi-tab visual fixtures.
- Split capture from approval. Approval must accept an immutable run directory and explicit scenario names without recapturing.
- Replace the invalid command with one contract, such as `make e2e-visual APPROVE_RUN=<dir> SCENARIOS=dev,ai`.
- Explicitly include approved Linux PNGs in the artifacts.
- Check PNG decoding and exact dimensions before RMSE.
- Calibrate the threshold from repeated real captures plus controlled localized regressions.
- Persist or expose the screenshot backend selected by setup.

### Risk Assessment

**HIGH.** The plan is likely to fail before meaningful capture, and its approval design can commit an image that was never reviewed.

## 06.9-05

### Summary

The skill content requirements correctly preserve user-selected model choice and item-by-item visual judgment. The execution mechanism is incomplete: it does not reliably enable capture, dispatch screenshots to an external chosen model, approve selected immutable artifacts, or perform a real phase-closing visual review.

### Strengths

- Model choice is explicitly reserved for the user at `06.9-05-PLAN.md:114-130`, matching `06.9-CONTEXT.md:33-45`.
- The plan correctly preserves the single numeric gate: only the skill artifact receives a percentage score, while A.4 remains item-level pass/fail at `06.9-05-PLAN.md:132-149`.
- The six source checklist items are grounded in `docs/prds/e2e-testing-battery-v1.0-prd.md:314-320`.
- The configured external model command is real and present at `.planning/config.json:46-48`.
- The proposed skill location follows the project's supported skill-directory convention described in `CLAUDE.md`.

### Concerns

- **CRITICAL: No task runs the completed skill against real screenshots.** Plan 05 only writes and scores `SKILL.md` at `06.9-05-PLAN.md:68-210`. The PRD requires actual AI-vision checks against committed baselines at `docs/prds/e2e-testing-battery-v1.0-prd.md:203-204,249-255`.
- **CRITICAL: The approval command is incompatible with Plan 04.** Plan 05 requires `make e2e-visual --approve` for selected scenarios at `06.9-05-PLAN.md:146-149`; Plan 04 proposes a global `APPROVE=1` interface and no scenario selector at `06.9-04-PLAN.md:192-207`.
- **HIGH: Capture is invoked without enabling the visual gate.** Step 1 says to run `make e2e-visual` at `06.9-05-PLAN.md:110-112`, while Plan 04 explicitly skips unless `WEZ_E2E_VISUAL=1` at `06.9-04-PLAN.md:158-166`.
- **HIGH: External model dispatch is unspecified.** The plan selects a model at `06.9-05-PLAN.md:114-130` but never defines how the selected runtime receives the screenshots and checklist prompt. The installed OpenCode CLI supports `opencode run --model <id> --file <png>`, but that adapter contract is absent.
- **HIGH: "Available" and "vision-capable" are not operationally defined.** `.planning/config.json:46-48` provides one command, not a complete model inventory. The skill should require runtime-specific enumeration and capability inspection rather than inferring vision support from model names.
- **HIGH: The skill has no machine-readable capture manifest.** It is expected to locate a printed scratch path at `06.9-05-PLAN.md:110-112`, but Plan 04 does not guarantee stable artifact retention or map scenarios to files, baselines, dimensions, and diff results.
- **HIGH: Validation is self-scored and weakly evidenced.** The same executor authors, revises, and scores the skill at `06.9-05-PLAN.md:180-208`. The actual rubric requires evidence per dimension at `/home/bazzite/.claude/skills/skill-judge/SKILL.md:494-582`, but verification only checks line count and trusts a summary claim.
- **MEDIUM: Reachability is not distinguished from catalog presence.** A model can appear in a runtime catalog without usable credentials or attachment support. No fallback behavior is specified at `06.9-05-PLAN.md:114-130`.

### Suggestions

- Add a real, non-autonomous review task with a user model-selection checkpoint.
- Run capture as `WEZ_E2E_VISUAL=1 make e2e-visual`.
- Define runtime adapters, including OpenCode's `run --model ... --file ...` command and a required structured per-item output.
- Generate a capture manifest and approve immutable file paths from that manifest.
- Use an independent clean-context reviewer for `skill-judge`, retaining the complete scoring report.
- Require one successful end-to-end invocation of the skill, semantic checklist result, and baseline promotion before phase completion.
- Distinguish model catalog entries from authenticated, image-capable candidates.

### Risk Assessment

**HIGH.** The skill may be well-written but remain operationally unusable, leaving the semantic half of Tier 4 unexecuted.

## Cross-Plan Assessment

### Blocking Inconsistencies

- **Product configuration omission:** Plans 01, 03, and 04 all rely on a scratch GUI that never loads the keybindings or tab renderer under test. Evidence: `06.9-01-PLAN.md:171-181` versus `config/wezterm-setup/init.lua:105-154`.
- **Firing interface conflict:** Plan 01 defines a flat `fire_chords` table; Plan 03 consumes per-OS nested tables. Evidence: `06.9-01-PLAN.md:203-212` versus `06.9-03-PLAN.md:102-108`.
- **Provisioning gap:** Plan 02 cannot install its proposed Linux Brew packages and omits ImageMagick, while Plan 04 assumes all required tools were provisioned. Evidence: `06.9-02-PLAN.md:118-151` and `06.9-04-PLAN.md:159-166,286`.
- **Approval contract conflict:** Plan 04 provides global approval with recapture; Plan 05 requires selected-scenario approval of already-reviewed images. Evidence: `06.9-04-PLAN.md:192-207` and `06.9-05-PLAN.md:146-149`.
- **Baseline sequencing gap:** Plan 04 creates only `.gitkeep`; Plan 05 neither owns PNG artifacts nor performs the review that would approve them. Evidence: `06.9-04-PLAN.md:156,209-225` and `06.9-05-PLAN.md:7-8`.
- **Battery integration gap:** No plan adds Tier 4 to the full local `make e2e`, contrary to `docs/prds/e2e-testing-battery-v1.0-prd.md:249-255`.
- **Process violation:** The implementation plans omit the mandatory probe/hypothesis-first flow required by `docs/agent-iteration.md:10-20,40-51,92-125`.

### Overall Risk

**HIGH.** The phase should not execute in its current form. The unsafe uinput group mutation is the most immediate security blocker. The configuration, approval, and baseline gaps would otherwise allow the phase to complete without testing the shipped UI or conducting the required semantic review.

---

## Consensus Summary (Cycle 1, superseded)

Only one reviewer (OpenCode, source-grounded with full repo access, no evidence-quality discount marker) ran this cycle. Its verdict stands at full weight per plan. The review cites concrete `file:line` evidence for every finding and reached all 5 plans in the coverage manifest (verified: `## 06.9-01` … `## 06.9-05` headers all present, no missing ids).

### Agreed Strengths

Not applicable — single-reviewer cycle, no cross-reviewer agreement to synthesize. See per-plan Strengths sections above.

### Agreed Concerns

Not applicable — single-reviewer cycle. See per-plan Concerns sections and the Cross-Plan Assessment above for the reviewer's own synthesis across plans.

### Divergent Views

Not applicable — single-reviewer cycle.
