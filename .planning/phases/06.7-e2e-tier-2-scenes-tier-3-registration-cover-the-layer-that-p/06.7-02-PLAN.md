---
phase: 06.7-e2e-tier-2-scenes-tier-3-registration
plan: 02
type: execute
wave: 2
depends_on: ["06.7-01"]
files_modified:
  - tests/e2e/tier2/scene_newtab_e2e_test.lua
  - tests/e2e/README.md
autonomous: true
requirements: [PRD-T2, PRD-A.2]
must_haves:
  truths:
    - "On a host with a mux, scene_newtab_e2e_test.lua LIVE-ASSERTS each new-tab-mode recipe (ai/dev/docker): launching from a >=2-pane tab spawns a NEW tab holding exactly the recipe pane count, while the originating tab is untouched (D-01/PRD A.2)."
    - "Each new-tab case asserts per-pane cwd, user-vars, no-escape-leak, responsiveness, and cleanup against a fresh isolated mux (D-02/D-04/D-05/D-08(cwd)/D-03)."
    - "The file loud-SKIPs via skip.require_tool when no mux is resolvable (D-03)."
    - "tests/e2e/README.md tier matrix marks Tier 2 (scenes) and Tier 3 (registration) as landed in 06.7, with the mux gate documented."
  artifacts:
    - path: "tests/e2e/tier2/scene_newtab_e2e_test.lua"
      provides: "Tier 2 new-tab-mode driver iterating ai/dev/docker with all six assertions per case"
      min_lines: 90
    - path: "tests/e2e/README.md"
      provides: "tier matrix + mux-gate + scene-tier rows updated for 06.7"
      contains: "Tier 2"
  key_links:
    - from: "tests/e2e/tier2/scene_newtab_e2e_test.lua"
      to: "tests/e2e/lib/mux.lua"
      via: "require('mux') — reuses the lifecycle helper from Plan 01"
      pattern: "require\\(['\"]mux['\"]\\)"
    - from: "tests/e2e/tier2/scene_newtab_e2e_test.lua"
      to: "cli/lib/scene.decide_materialization"
      via: ">=2-pane precondition -> new-tab; the materialized result is asserted"
      pattern: "scene[^\\n]*launch"
---

<objective>
Complete Tier 2 (D-01..D-05, D-08(cwd)) with the new-tab launch mode: launching a recipe from a tab
that already has >=2 panes must spawn a NEW tab carrying exactly the recipe's pane count, leaving
the originating tab untouched. Consumes `tests/e2e/lib/mux.lua` from Plan 01 (no new lifecycle
code) and mirrors the reuse driver's six assertions. Finishes by updating the `tests/e2e/README.md`
tier matrix to reflect Tier 2 + Tier 3 landing in 06.7.

Purpose: new-tab mode is the second half of the recipe x mode matrix (PRD A.2 = 6 cases); reuse
mode shipped in Plan 01, this plan ships new-tab so all 3 recipes x 2 modes are covered.
Output: `tests/e2e/tier2/scene_newtab_e2e_test.lua` + an updated `tests/e2e/README.md`, green
under `make e2e`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.7-e2e-tier-2-scenes-tier-3-registration-cover-the-layer-that-p/06.7-CONTEXT.md
@.planning/phases/06.7-e2e-tier-2-scenes-tier-3-registration-cover-the-layer-that-p/06.7-PATTERNS.md
@tests/e2e/lib/mux.lua
@tests/e2e/tier2/scene_reuse_e2e_test.lua
@tests/e2e/lib/harness.lua
@tests/e2e/lib/skip.lua
@cli/commands/scene.lua
@cli/lib/scene.lua
@tests/e2e/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: New-tab-mode scene driver — tests/e2e/tier2/scene_newtab_e2e_test.lua</name>
  <read_first>
    - tests/e2e/tier2/scene_reuse_e2e_test.lua (Plan 01 — the DIRECT analog: copy its skeleton, gate, run_case shape, and the six assertions; this file differs ONLY in the precondition + which tab is asserted)
    - tests/e2e/lib/mux.lua (Plan 01 — spin/spawn_pane/split_pane/poll_until/cli_list/get_text/send_marker/teardown)
    - cli/commands/scene.lua lines 332-353 (decide_materialization: a current tab with >=2 panes -> new-tab) and 372-399 + 515-543 (the new-tab spawn path + post-spawn tab-id resolution)
    - scenes/ai.toml / dev.toml / docker.toml (pane counts: ai=2, dev=3, docker=3)
  </read_first>
  <files>tests/e2e/tier2/scene_newtab_e2e_test.lua</files>
  <action>
    Create `tests/e2e/tier2/scene_newtab_e2e_test.lua` as the new-tab sibling of the reuse driver.
    Same package.path bootstrap, same `skip.require_tool(mux.probe, "tier2 new-tab-mode scene
    live-mux", "wezterm-mux-server unavailable (headless mux not resolvable)")` gate (D-03), same
    harness footer.

    Define `run_case(recipe, expected_panes)` that differs from the reuse driver ONLY in the
    precondition and the asserted tab:
    1. `mux.spin(...)` a fresh isolated mux, then BUILD a >=2-pane originating tab: take the first
       pane and `mux.split_pane` it once so the current tab has 2 panes (the new-tab precondition).
       Record the originating tab id and its pane count (2).
    2. Drive `WEZ_BIN scene launch <recipe>` with `WEZTERM_PANE` set to a pane IN that 2-pane tab and
       the session env prefix, FROM the scratch launch dir, with the recipe seeded into the scratch
       WEZTERM_SETUP_DIR/scenes (same seeding as the reuse driver).
    3. `mux.poll_until` a NEW tab (a tab id absent from the pre-launch topology) appears holding
       exactly `expected_panes` panes (D-02). Then assert:
       - PANE-COUNT parity (D-01/PRD A.2): the NEW tab has exactly `expected_panes` panes.
       - The ORIGINATING tab is UNTOUCHED: it still has exactly 2 panes (no residue / no accidental
         in-place build).
       - CWD (D-08(cwd)): every pane in the new tab has cwd == the scratch launch dir.
       - USER-VARS: the styled panes carry the recipe's WEZTERM_TAB_COLOR / WEZTERM_TAB_ICON /
         WEZTERM_TAB_TITLE (same mechanism choice as the reuse driver).
       - NO-ESCAPE-LEAK (D-05): get_text of each styled pane has no `printf '`, no raw octal run, no
         `quote>`.
       - RESPONSIVENESS (D-04): send a unique marker into the new tab's focused pane; poll get_text
         until it echoes within the bounded timeout.
    4. ALWAYS `mux.teardown` (D-03), even on assertion failure (pcall/finally).

    Call `run_case("ai", 2)`, `run_case("dev", 3)`, `run_case("docker", 3)`. End with
    `os.exit(h.footer())`. Reuse mux.lua wholesale — do NOT add lifecycle code here; do NOT
    reimplement materialization selection (the >=2-pane precondition drives new-tab; assert the
    result). No fenced shell logic beyond mux.lua's API.
  </action>
  <acceptance_criteria>
    - `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier2/scene_newtab_e2e_test.lua` exits 0; on this box it prints `LIVE-ASSERTED`, runs ai/dev/docker, footer reports `0 failed`.
    - Each case asserts BOTH the new tab's pane count (ai=2/dev=3/docker=3) AND that the originating 2-pane tab is unchanged (residue guard).
    - The three leak patterns are each asserted absent (grep file for `printf '`, `quote>`, octal-run check).
    - Every case tears down even on failure (grep shows mux.teardown in a pcall/finally or unconditionally post-assert).
    - The file requires `mux` and does NOT redefine spin/teardown/poll_until (grep: no `function .*spin`/`kill-server` literals — those live in mux.lua).
    - After the run the user's live WezTerm session is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier2/scene_newtab_e2e_test.lua; echo "exit=$?"</automated>
  </verify>
  <done>New-tab-mode driver runs ai/dev/docker each against a fresh isolated mux, asserts a new tab with the recipe pane count + the originating tab untouched + cwd/user-vars/no-leak/responsiveness/cleanup, tears down every case, exits 0 LIVE-ASSERTED on this box, and loud-SKIPs without a mux.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests/e2e/README.md tier matrix for 06.7</name>
  <read_first>
    - tests/e2e/README.md lines 35-57 (the tier matrix + layout tables to edit) and 44-48 (the M/B + LIVE-ASSERTED skip-contract prose to keep)
    - tests/e2e/tier2/scene_reuse_e2e_test.lua + scene_newtab_e2e_test.lua (the two files to reference)
    - tests/e2e/lib/mux.lua (the mux gate to describe)
  </read_first>
  <files>tests/e2e/README.md</files>
  <action>
    Update the README so it describes the now-landed Tier 2 (scenes) and Tier 3 (registration).
    In the Tier matrix table, change the Tier 2 (Scenes) and Tier 3 (Registration) rows' Phase column
    from "06.7" to "06.7 (this phase)" / landed, and keep them classed M (must-pass). Add a `Layout`
    table row for `tests/e2e/lib/mux.lua` (headless-mux lifecycle helper) and for `tests/e2e/tier2/`
    + `tests/e2e/tier3/` test files. Add a short paragraph documenting the Tier 2 mux gate: Tier 2
    self-skips loudly via `skip.require_tool` when `wezterm-mux-server` is not resolvable (resolved on
    PATH or as a sibling next to the `wezterm` binary), and each case spins a FRESH isolated mux
    (scratch XDG/HOME/config) so the user's live session is never touched. Do NOT add the
    break-harness / `make e2e-prove` docs here — that is Plan 04's surface. Keep English-only and the
    existing progressive-disclosure tone.
  </action>
  <acceptance_criteria>
    - The tier matrix marks Tier 2 (Scenes) and Tier 3 (Registration) as landed in 06.7 (no longer "pending/06.7" as a future phase).
    - A Layout-table row exists for `tests/e2e/lib/mux.lua` and the tier2/tier3 test dirs.
    - A paragraph documents the Tier 2 mux gate (sibling-resolution + fresh-isolated-mux-per-case + loud self-skip).
    - No `make e2e-prove` / break-harness wording is added (that is Plan 04).
    - `grep -n "Tier 2" tests/e2e/README.md` shows the updated row.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "mux\.lua|Tier 2|Tier 3" tests/e2e/README.md</automated>
  </verify>
  <done>README tier matrix + layout reflect the landed Tier 2 (scenes) and Tier 3 (registration), the mux gate is documented, and no break-harness wording leaks in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> wezterm-mux-server / wezterm cli | test-controlled scratch paths + recipe names cross into spawned process command lines |
| spawned mux -> user's real WezTerm GUI session | the launcher uses bare `wezterm cli`; isolation keeps it off the live session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.7-01 | Tampering | scene_newtab shell-outs via mux.lua | mitigate | reuse mux.lua's shquote-guarded command builders (inherited from Plan 01 T-06.7-01); no new unescaped interpolation |
| T-06.7-02 | Denial of Service | spawned headless mux (orphan / dirty tree) | mitigate | per-case mux.teardown in pcall/finally; bounded poll, never a bare sleep |
| T-06.7-03 | Information Disclosure / wrong-target | bare `wezterm cli` in scene.lua | mitigate | drive launch ONLY against the scratch mux env so the user's live session is never targeted |
| T-06.7-SC | Tampering | npm/pip/cargo installs | accept | no package-manager installs in this phase; wezterm binaries are pre-existing |
</threat_model>

<verification>
- `WEZ_BIN=./dist/wez lua5.4 tests/e2e/tier2/scene_newtab_e2e_test.lua` exits 0 (LIVE-ASSERTED on this box).
- `make e2e` exits 0 with BOTH tier2 files auto-discovered.
- README tier matrix updated; `grep "Tier 2" tests/e2e/README.md` shows landed status.
- User's live WezTerm session unchanged after the run.
</verification>

<success_criteria>
- All 3 recipes x new-tab mode assert pane-count (new tab) + originating-tab-untouched + cwd/user-vars/no-leak/responsiveness/cleanup (D-01..D-05, D-08(cwd)).
- Combined with Plan 01, the full PRD A.2 matrix (3 recipes x 2 modes = 6 cases) is covered.
- README reflects the landed scene + registration tiers.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
- `tests/e2e/tier2/scene_newtab_e2e_test.lua` — NEW Tier 2 new-tab-mode driver; function `run_case(recipe, expected_panes)`; cases ai=2, dev=3, docker=3.
- `tests/e2e/README.md` — UPDATED tier matrix + layout + mux-gate documentation (no new symbols).
</artifacts_produced>

<output>
Create `.planning/phases/06.7-e2e-tier-2-scenes-tier-3-registration-cover-the-layer-that-p/06.7-02-SUMMARY.md` when done.
</output>
