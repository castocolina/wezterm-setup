---
phase: 06.3-distribution-channels-inserted
verified: 2026-06-17T00:00:00Z
status: passed
score: 20/20 must-haves verified
overrides_applied: 0
---

# Phase 06.3: Distribution Channels (INSERTED) Verification Report

**Phase Goal:** Make releases self-renewing and reversible: a scheduled rolling nightly channel alongside pinned `v*` tags, a bootstrapper that lets the user choose which channel to install, and a first-class uninstall path that works even when only the binary was downloaded (no repo checkout).

**Verified:** 2026-06-17
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

The phase goal decomposes into three deliverables, each mapped to one plan. All three are
present, substantive, wired, behaviorally correct, and (for the CI deliverable) live-verified
against a real GitHub Actions run. Goal achieved.

### Observable Truths

#### Deliverable 1 — `wez uninstall` (reversible, binary-only) — Plan 01

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | `wez uninstall` removes managed block + config tree + backups + binary, no repo checkout | ✓ VERIFIED | `cli/commands/uninstall.lua` resolves cli path from `seams.cli_path`/`$WEZ_BIN`/`~/.local/bin/wez` (lines 44-54, all user paths, sudo-free); delegates removal to `uninstall_state` engine (line 113); `uninstall_test.lua` 43/43 pass |
| 2   | TTY without `--yes` prompts for confirmation before removing | ✓ VERIFIED | uninstall.lua lines 90-104: prints what-will-be-removed + `proceed? [y/N]`, reads answer, aborts non-zero unless affirmative; test "TTY decline aborts / TTY confirm proceeds" green |
| 3   | Non-TTY pipe without `--yes` aborts non-zero and removes nothing (D-11/D-03) | ✓ VERIFIED | uninstall.lua lines 79-85 return 1 BEFORE the engine call. Behavioral spot-check: `printf '' \| lua5.4 cli/wez.lua uninstall` → exit 1, "refusing to remove on a non-interactive pipe without --yes", nothing removed |
| 4   | `--yes` skips prompt; `--keep-config`/`--keep-cli`/`--keep-backup` each preserve their component | ✓ VERIFIED | engine_args passes keep_config/keep_backup straight through (lines 108-112); `--keep-cli` skips the final unlink (lines 125-130); spec registers all four flags (spec.lua 128-132); per-flag preservation tests green |
| 5   | The running binary is unlinked LAST, after block/config/backups (D-10) | ✓ VERIFIED | Engine called with forced `keep_cli=true` (line 110) → engine removes block+config+backups but NOT binary; `os.remove(cli_path)` runs only after engine returns 0 (lines 125-127); ordering test green |
| 6   | `make uninstall` works end-to-end through the new front door | ✓ VERIFIED | `tools/uninstall.sh` line 60 delegates to `"${WEZ}" uninstall "${FLAGS[@]}"` with `FLAGS=(--yes)` seed (line 54); decision-free glue, no `rm`; `bash -n` + `shellcheck -x` clean; Makefile unchanged (still threads KEEP_* env) |

#### Deliverable 2 — Rolling nightly CI channel — Plan 02 (live-verified)

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 7   | Weekly schedule + manual dispatch builds wez and publishes `nightly-YYYYMMDD` prerelease (D-04/D-05) | ✓ VERIFIED | release.yml `schedule: cron "17 6 * * 1"` (line 38) + `workflow_dispatch` (line 32); LIVE: dispatch run 27729836865 published `nightly-20260618`; `gh release list` confirms it exists as Pre-release |
| 8   | A pushed `v*` tag still publishes a STABLE (non-prerelease) release, unchanged (D-07) | ✓ VERIFIED | `push: tags: ["v*"]` preserved (lines 25-27); stable branch uses `--generate-notes` (non-prerelease) at lines 178-181; LIVE: `v0.1.0` remains "Latest", untouched |
| 9   | Channel derived from `github.ref`: tag→stable, schedule/dispatch→nightly; no manual flag (D-07) | ✓ VERIFIED | "Resolve channel" step branches `[[ "$GITHUB_REF" == refs/tags/* ]]` writing CHANNEL/TAG to `$GITHUB_ENV` (lines 71-82); no channel input declared |
| 10  | Nightly reuses per-asset `.sha256` + idempotent create + `--clobber` byte-identically (P6-D08) | ✓ VERIFIED | Same `platform_os/platform_arch` asset block (lines 133-144); `gh release upload "$TAG" "$WEZ_ASSET" "$WEZ_ASSET.sha256" --clobber` (lines 192-193); LIVE: nightly-20260618 has both `wez-linux-x86_64` + `.sha256` |
| 11  | A scheduled run whose HEAD == last nightly's recorded SHA is a no-op (D-05) | ✓ VERIFIED | "Skip if unchanged" guard reads `built-from:` marker via `gh release view --json body`, compares to `git rev-parse HEAD`, emits `unchanged` output (lines 93-114); publish/prune gated on it; LIVE: 2nd dispatch 27729873619 skipped upload+prune (16s no-op) |
| 12  | Only newest 5 `nightly-*` survive; older pruned; no `v*` ever deleted (D-06) | ✓ VERIFIED | Prune step filters `startswith("nightly-")`, slices `[@]:5`, re-asserts `case "$tag" in nightly-*)` before each `gh release delete` (lines 204-228); LIVE: exactly 1 nightly-*, `v0.1.0` intact |
| 13  | Steps stay factored for a Phase 7 macOS matrix; v1 single ubuntu-latest job (D-12) | ✓ VERIFIED | Single `runs-on: ubuntu-latest` job (line 53); build/checksum/publish/prune are distinct factored steps; inline comments document the deferred macOS matrix; no macOS legs present |

#### Deliverable 3 — Bootstrapper channel selector — Plan 03

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 14  | `WEZ_CHANNEL=nightly\|stable\|<vX.Y.Z>` selects the build, replacing `WEZ_RELEASE_TAG=v0.1.0` (D-08) | ✓ VERIFIED | `WEZ_CHANNEL="${WEZ_CHANNEL:-nightly}"` (build.sh line 76); `grep WEZ_RELEASE_TAG:-v0.1.0` == 0; `WEZ_RELEASE_TAG` demoted to escape hatch (lines 77-79) |
| 15  | A non-interactive pipe defaults to nightly deterministically, never hangs (D-02) | ✓ VERIFIED | `resolve_channel_tag()` `[ ! -t 0 ]` branch (line 227) resolves from WEZ_CHANNEL default nightly; spot-check under `timeout 10` returned (no hang) |
| 16  | Interactive TTY with no channel shows a picker (nightly/stable/recent) (D-08) | ✓ VERIFIED | TTY picker block (lines 248+) mirrors `select_release()`; numbered menu to stderr, tag to stdout |
| 17  | stable→/releases/latest; nightly→newest nightly-*; <vX.Y.Z>→literal (D-03) | ✓ VERIFIED | `resolve_stable_tag` GET /releases/latest (line 193); `resolve_nightly_tag`/`list_nightly_tags` parse newest `nightly-*` via tr/grep (lines 206-220); literal arm spot-check `WEZ_CHANNEL=v9.9.9` → `v9.9.9` exit 0 |
| 18  | Channel switch never bypasses verify-SHA-before-chmod (P6-D08) | ✓ VERIFIED | Only the URL tag segment is channel-resolved; `checksum verified` (line 363), `checksum mismatch` abort (lines 360-361), `chmod +x` AFTER verify (line 368) preserved; build_channel_test asserts the block survives |
| 19  | A degraded/empty resolution fails loud (non-zero), never installs an unverified asset | ✓ VERIFIED | Resolver `return 1` on empty/failed fetch (lines 172/194/202/220); download_release aborts before URL construction (lines 310-312); spot-check unreachable-API → exit 1 |

#### Cross-cutting

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 20  | Phase introduces NO new test regressions | ✓ VERIFIED | Full `./tools/run-tests.sh`: only 1 failing file `cli/lib/recipe_test.lua` (the known, documented, out-of-scope baseline from commit 723af62, touches scenes/ai.toml — no 06.3 file). New `uninstall_test.lua` (43/0) and `build_channel_test.lua` (23/0) both green |

**Score:** 20/20 truths verified

### Required Artifacts

| Artifact | Expected    | Status | Details |
| -------- | ----------- | ------ | ------- |
| `cli/commands/uninstall.lua` | front door over uninstall_state engine | ✓ VERIFIED | 135 lines; requires + delegates to `cli.commands.uninstall_state`; no duplicated removal logic (no `rm -rf`/`excise_block`) |
| `cli/spec.lua` | uninstall registered 3-place + flags | ✓ VERIFIED | CATEGORIES["uninstall"]="install" (line 45), SUBCOMMANDS (line 62), build_parser with --yes + 3 --keep-* (lines 128-132) |
| `tools/uninstall.sh` | decision-free glue → `wez uninstall --yes` | ✓ VERIFIED | line 60 delegation; FLAGS=(--yes); no live `uninstall-state` reference; shellcheck clean |
| `tests/cli/uninstall_test.lua` | front-door/ordering/no-TTY tests | ✓ VERIFIED | 43 assertions, all pass |
| `.github/workflows/release.yml` | schedule + routing + nightly publish + skip + prune | ✓ VERIFIED | actionlint exit 0; all D-04..D-07/D-12 steps present and live-exercised |
| `tools/build.sh` | WEZ_CHANNEL selector replacing v0.1.0 pin | ✓ VERIFIED | resolve_channel_tag + 3 resolution arms; checksum gate preserved; bash -n + shellcheck clean |
| `tests/cli/build_channel_test.lua` | regression-guard for selector | ✓ VERIFIED | 23 assertions, all pass |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `wez uninstall` dispatch | `cli/commands/uninstall.lua` | allow-list `name:gsub("-","_")`, no alias needed | ✓ WIRED | `lua5.4 cli/wez.lua uninstall` routes to the module (clean leaf), runs run(), no traceback |
| `uninstall.lua` | `uninstall_state` engine | `require` + `run(engine_args, seams)` | ✓ WIRED | line 113; engine called with forced keep_cli=true |
| `tools/uninstall.sh` | `wez uninstall --yes` | delegation with KEEP_* → --keep-* | ✓ WIRED | line 60; FLAGS seeded --yes; only nightly grep is a comment |
| release.yml schedule/dispatch | nightly publish | `github.ref` non-tag branch | ✓ WIRED | LIVE: dispatch resolved CHANNEL=nightly, published |
| nightly publish | GitHub Releases | `gh release create --prerelease` + `--clobber` | ✓ WIRED | LIVE: nightly-20260618 prerelease with both assets |
| prune | `gh release delete` (nightly-* only) | filter + slice + case guard | ✓ WIRED | LIVE: ≤5 nightly-*, v0.1.0 never deleted |
| build.sh download_release | GitHub release API | resolve_channel_tag → tag → checksum path | ✓ WIRED | resolver feeds url/sums_url; verify-before-chmod unchanged |
| `/releases/latest` exclusion | Plan 03 stable resolver | prerelease auto-filter | ✓ WIRED | LIVE: `/releases/latest` returns v0.1.0, excludes nightly-20260618 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| D-11 no-TTY abort | `printf '' \| lua5.4 cli/wez.lua uninstall` | exit 1, refusal message, nothing removed | ✓ PASS |
| D-11 no-TTY abort with flag | `printf '' \| lua5.4 cli/wez.lua uninstall --keep-cli` | exit 1, refusal | ✓ PASS |
| literal-arm resolution | `WEZ_CHANNEL=v9.9.9 ... resolve_channel_tag` | `v9.9.9`, exit 0 | ✓ PASS |
| fail-loud nightly | unreachable API, `resolve_channel_tag` | exit 1 (no tag) | ✓ PASS |
| uninstall test suite | `lua5.4 tests/cli/uninstall_test.lua` | 43 passed, 0 failed | ✓ PASS |
| channel test suite | `lua5.4 tests/cli/build_channel_test.lua` | 23 passed, 0 failed | ✓ PASS |
| workflow static | `actionlint release.yml` | exit 0 | ✓ PASS |
| shell lint | `shellcheck -x tools/uninstall.sh tools/build.sh` | clean | ✓ PASS |

### Live CI Verification (Plan 02 Task 4 — blocking gate, re-confirmed against the real repo)

| Check | Evidence | Status |
| ----- | -------- | ------ |
| Nightly prerelease published | `gh release list` → `nightly-20260618  Pre-release` | ✓ VERIFIED |
| Stable intact | `v0.1.0  Latest` present | ✓ VERIFIED |
| Prerelease excluded from latest | `gh api .../releases/latest` → `v0.1.0` (NOT the nightly) | ✓ VERIFIED |
| Prune scope ≤5 | exactly 1 `nightly-*` release | ✓ VERIFIED |
| Dispatch run success | run 27729836865 (success, 26s) | ✓ VERIFIED |
| Skip-if-unchanged no-op | run 27729873619 (success, 16s, upload+prune skipped) | ✓ VERIFIED |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| INST-07 | 01/02/03 | distribution behavior (extended, no new ID) | ✓ SATISFIED | uninstall front door + nightly channel + selector all shipped |
| INST-08 | 01/02/03 | release/supply distribution | ✓ SATISFIED | release.yml nightly publish live-verified; asset + .sha256 contract preserved |
| INST-09 | 01/02/03 | install/uninstall surface | ✓ SATISFIED | `wez uninstall` + `make uninstall` parity verified |

ROADMAP scope checklist (3 items): nightly rolling channel ✓, bootstrapper channel selector ✓, `wez uninstall` ✓ — all delivered.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | — | — | No TODO/FIXME/XXX/PLACEHOLDER in any 06.3-modified file; no stubs; no empty returns that flow to user output |

The only file-level `os.remove(cli_path)`/`return 1` patterns are intentional destructive/fail-loud
behavior, not stubs. No debt markers in scope.

### Deferred Items (informational — not gaps)

| Item | Status | Note |
| ---- | ------ | ---- |
| `cli/lib/recipe_test.lua → 2.9d ai.toml` failure | Out of scope | Pre-existing baseline (commit 723af62 edited scenes/ai.toml; test fixture drifted). Touches NO 06.3 file. Logged in `deferred-items.md`. Confirmed: reproduces independent of 06.3 changes; not a 06.3 regression |
| macOS build legs | Deferred to Phase 7 (D-18) | release.yml kept matrix-friendly (D-12); no macOS work in 06.3 by design |
| README build/release badges | Deferred to Phase 6.4 | CI infra that makes them meaningful exists; badges land in the README rewrite |

### Human Verification Required

None. The one path that cannot run locally (the `schedule` trigger / live nightly publish) was
the explicit subject of Plan 02's blocking `checkpoint:human-verify` Task 4, which was executed
against REAL GitHub Actions runs and independently re-confirmed here via `gh release list` /
`gh api .../releases/latest` against the live repo (nightly-20260618 prerelease present, v0.1.0
stable intact and still `/releases/latest`, prune scope ≤5). No outstanding human checks.

### Gaps Summary

No gaps. All three deliverables are present in the codebase, substantively implemented (not
stubs), correctly wired (dispatch → front door → engine; trigger → channel routing → publish →
prune; WEZ_CHANNEL → resolver → checksum gate), behaviorally correct under spot-checks, and the
CI deliverable is verified against a real running GitHub Actions session — satisfying the
project's "verify before declaring done / no should-work" rule. The single suite failure is the
known, documented, out-of-scope baseline that touches no 06.3 file and introduces no regression.

---

_Verified: 2026-06-17_
_Verifier: Claude (gsd-verifier)_
