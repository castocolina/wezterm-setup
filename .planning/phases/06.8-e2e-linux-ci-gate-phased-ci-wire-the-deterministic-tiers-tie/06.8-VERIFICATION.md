---
phase: 06.8-e2e-linux-ci-gate-phased-ci-wire-the-deterministic-tiers-tie
verified: 2026-09-08T00:00:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  note: "Initial verification — no prior VERIFICATION.md existed."
---

# Phase 06.8: E2E Linux CI Gate (phased CI) — Verification Report

**Phase Goal:** Make the deterministic tiers gate every change (PRD Phase 3). Wire Tier 1 + Tier 2 + Tier 3-registration into GitHub Actions on Linux using a headless `wezterm-mux-server`; verify self-skip works in CI and the gate blocks only on real failures, so the deterministic battery runs on every PR/push.
**Verified:** 2026-09-08
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

This is goal-backward, code-first verification — not a paper read of SUMMARY.md. Every
load-bearing claim was checked against the actual file on disk at HEAD (`a199325`), and
every script/test that could be run locally WAS run in this verifier's own process
(`make test`, `actionlint`, `lua5.4 tests/ci_classify_test.lua`, `bash -n` + `shellcheck`
on every touched `tools/ci-*.sh`, and the exact structural `<verify>` block from
`06.8-01-PLAN.md` Task 1, reproduced command-by-command). Per this task's explicit
instruction, the live-GitHub-Actions-green claim (CI run `34303641563` on commit
`a199325`, `conclusion=success`, its `e2e-output-log` artifact independently
re-classified by the orchestrator with `tools/ci-classify-e2e-output.sh` → `OK — expected
self-skip exactly once, no unexpected skip, all 4 gate-bound labels LIVE-ASSERTED`, exit 0,
`run-e2e: all 10 file(s) passed`, zero `FAIL` lines) is accepted as already-verified
evidence from the orchestrator rather than re-derived here (this sandbox has no `gh` CLI) —
it is cited below as the phase's live-CI proof, not as an unverified SUMMARY claim.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A NEW `.github/workflows/ci.yml` triggers on `push` to `main` AND `pull_request` (D-03), separate from `release.yml` | ✓ VERIFIED | `.github/workflows/ci.yml:41-43` — `on: push: branches:[main]` + `pull_request:`; `yq eval '.on.push.branches[0]'` → `main`, `pull_request:` present; file is new, `release.yml` untouched |
| 2 | The job provisions real `wezterm`+`wezterm-mux-server` via `bootstrap-wezterm.sh WEZTERM_TARGET=nightly`, cached on a resolved-datestamp key computed by a dedicated script, not inlined in YAML (D-01, corrected) | ✓ VERIFIED | `ci.yml:95-115` calls `tools/ci-resolve-wezterm-cache-key.sh` then `actions/cache@v4.2.0` then `WEZTERM_TARGET=nightly ./tools/bootstrap-wezterm.sh`; script itself sources `bootstrap-wezterm.sh`'s `latest_nightly_datestamp` with a UTC-date fallback; `grep -q latest_nightly_datestamp ci.yml` → absent (not inlined) |
| 3 | `make test` runs strictly BEFORE every WezTerm-related step (provisioning AND its runtime-lib install), in the same job (D-04) | ✓ VERIFIED | Re-ran the plan's own line-order check live: `TEST_LINE=71 < PROVISION_LINE=115`, and (post WR-04 fix) the "Install wezterm-gui runtime deps" step now sits AFTER `make test` at line 83 — code review's WR-04 finding (deps step ran before `make test`) is fixed in commit `a199325` |
| 4 | The e2e battery runs against the freshly `make build`-produced `dist/wez`, not the dev launcher | ✓ VERIFIED | `ci.yml:122-152` — `make build` step immediately precedes `WEZ_BIN=./dist/wez make e2e` |
| 5 | A dedicated classify script enforces: exactly-one expected self-skip, zero unexpected skips (incl. the dual-emit shape), all 4 gate-bound labels LIVE-ASSERTED — with durable `make test`-discovered regression coverage | ✓ VERIFIED | Ran `lua5.4 tests/ci_classify_test.lua` myself: **5 passed, 0 failed** (pass / missing-live / dual-emit-unexpected-skip / expected-skip-absent / expected-skip-duplicated, all correctly classified); the 5 required label strings (`tier2 reuse-mode scene live-mux`, `tier2 new-tab-mode scene live-mux`, `tier2 scene-launch-motd-race regression`, `tier3 show-keys registration`, `keys-siblings macOS bundle-symlink contract`) are grepped verbatim in their respective tier files, confirming the classify script's contract matches real emitted labels, not an assumption |
| 6 | The workflow is report-only — NOT wired into a branch-protection required status check (D-05) | ✓ VERIFIED | `grep -qi 'required_status_checks\|branches/.*protection\|gh api.*protection' ci.yml` → no match; no repo-settings-changing code anywhere in the diff |
| 7 | `actionlint`, pinned to v1.7.12, is a mandatory gate with no silent structural-only fallback | ✓ VERIFIED | Independently installed `actionlint@v1.7.12` via `go install` in this sandbox and ran it against the CURRENT `.github/workflows/ci.yml`: **exit 0**, clean. (Note: the SUMMARY does not document that this check was actually run during original execution — see Notable Observations #1; the artifact itself is confirmed clean now, on re-verification.) |
| 8 | No cache-key/mux-discovery decision logic is inlined in the YAML — both live in dedicated `tools/` scripts (D-01 boundary) | ✓ VERIFIED | `grep -q 'latest_nightly_datestamp\|resolve_mux_server\|readlink -f' ci.yml` → no match; both scripts exist, are executable, and are invoked by name only |
| 9 | A real GitHub Actions run whose `headSha` matches the exact commit this phase produced completed with `conclusion=success` | ✓ VERIFIED (orchestrator-supplied live evidence, per task instructions) | CI run `34303641563` on commit `a199325` (= current `HEAD`) → `conclusion=success`; `e2e-output-log` artifact independently re-downloaded and re-classified by the orchestrator with this repo's own `tools/ci-classify-e2e-output.sh` → `OK`, exit 0, `run-e2e: all 10 file(s) passed`, zero `FAIL` lines |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.github/workflows/ci.yml` | push+PR CI job, glue-only, D-01..D-05 wired | ✓ VERIFIED | 173 ln; `actionlint v1.7.12` clean; every required step present in the required order; delegates all decision logic |
| `tools/ci-classify-e2e-output.sh` | D-02 skip/live classification, single source of truth | ✓ VERIFIED | 87 ln; `bash -n`+`shellcheck -x` clean; independently run against real log labels — matches |
| `tools/ci-resolve-wezterm-cache-key.sh` | resolves nightly datestamp cache key, UTC fallback | ✓ VERIFIED | 55 ln; sources `bootstrap-wezterm.sh` then re-asserts `set +e` (WR-05 fix present, code-read confirmed) |
| `tools/ci-verify-wezterm-mux.sh` | D-02 precondition: wezterm + mux resolvable | ✓ VERIFIED | 41 ln; mirrors `mux.lua`'s PATH-then-sibling lookup order exactly |
| `tools/ci-install-wezterm-runtime-deps.sh` | installs XCB/xkbcommon runtime libs (bug 2 fix) | ✓ VERIFIED | 55 ln; apt-only, hard-errors on non-apt runner, `bash -n`+`shellcheck -x` clean |
| `tests/ci_classify_test.lua` | durable regression coverage for the classify script's discrimination matrix | ✓ VERIFIED | 167 ln; ran directly: 5/5 fixtures pass, auto-discovered by `make test`'s glob |
| `tests/e2e/README.md` | tier matrix + `WEZ_BIN` CI convention updated | ✓ VERIFIED | line 47 marks Tier1/2/3-reg as `M` (must-pass, gates CI, 06.8-wired) |
| `cli/commands/install_state.lua` | ENOENT-as-seed handling (bug 1) + errno-based check (WR-01) + provenance marker (WR-02) | ✓ VERIFIED | `is_enoent(errnum)` (errno 2, locale-independent) replaces the WR-01-flagged string match; `fresh_seed_marker_path` writes an explicit sentinel file, consumed by doctor's gate |
| `cli/commands/doctor.lua` | fresh-install backup exemption (bug 4) via explicit marker, not byte comparison (WR-02) | ✓ VERIFIED | `gate_backup_exists` now checks `install_state.fresh_seed_marker_path` existence, not `DEFAULT_CONFIG_SKELETON` byte-equality |
| `cli/commands/keys.lua` | luastatic-bundle-safe `dkjson` require (bug 3) + pcall-guarded fallback (WR-03) | ✓ VERIFIED | `pcall(require, "cli.vendor.dkjson")` then `pcall(require, "dkjson")`; double failure now writes a clean stderr message + returns 1 instead of an uncaught traceback |

### Key Link Verification

| From | To | Via | Status |
|------|-----|-----|--------|
| `ci.yml` | `tools/ci-resolve-wezterm-cache-key.sh` | cache-key step invokes it | ✓ WIRED |
| `ci.yml` | `tools/bootstrap-wezterm.sh` | `WEZTERM_TARGET=nightly ./tools/bootstrap-wezterm.sh` | ✓ WIRED |
| `ci.yml` | `tools/ci-verify-wezterm-mux.sh` | precondition step invokes it after provisioning | ✓ WIRED |
| `ci.yml` | `tools/ci-classify-e2e-output.sh` | e2e step's pass/fail decision delegates to it | ✓ WIRED (confirmed live via the orchestrator's re-classification of the real run's artifact) |
| `tests/ci_classify_test.lua` | `tools/ci-classify-e2e-output.sh` | subprocess-driven fixture tests over the real script | ✓ WIRED (ran locally: 5/5 pass) |
| `ci.yml` | `Makefile` (`test`/`build`/`e2e`) | invokes make targets, never reimplements | ✓ WIRED |
| `cli/commands/doctor.lua` | `cli/commands/install_state.lua` | `require("cli.commands.install_state")`, consumes `fresh_seed_marker_path` + `DEFAULT_CONFIG_SKELETON` | ✓ WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full unit suite green | `make test` | exit 0, `run-tests: all 35 file(s) passed` | ✓ PASS |
| Classify script discrimination matrix | `lua5.4 tests/ci_classify_test.lua` | 5 passed, 0 failed | ✓ PASS |
| `actionlint` on the real workflow | `actionlint .github/workflows/ci.yml` (v1.7.12, installed fresh via `go install`) | exit 0, no output | ✓ PASS |
| Task 1's own structural `<verify>` block, reproduced live | `yq`/`grep` step-order + pin checks from `06.8-01-PLAN.md:443` | all assertions passed (timeout=25, cancel-in-progress=true, push.branches=[main], pull_request present, permissions.contents=read, `make test` line 71 < provisioning line 115 < e2e line 152, all 3 action versions pinned exactly, no branch-protection leak) | ✓ PASS |
| Shell hygiene on every new `tools/ci-*.sh` | `bash -n` + `shellcheck -x` | all clean | ✓ PASS |
| Gate-bound label strings exist verbatim in the tier files the classify script expects them from | `grep -rn "<label>" tests/e2e/` | all 5 (4 required + 1 expected-skip) found in their respective tier1/tier2/tier3 files | ✓ PASS |
| Code-review findings actually fixed in code (not just claimed in the commit message) | Direct read of `install_state.lua`, `doctor.lua`, `keys.lua`, `ci-resolve-wezterm-cache-key.sh`, `ci.yml`, `install_state_test.lua` at HEAD | WR-01 (errno check), WR-02 (provenance marker), WR-03 (pcall-guarded fallback), WR-04 (step reorder), WR-05 (`set +e` re-assertion), WR-06 (root-safe skip), IN-01 (`is_enoent` de-duplication) — all 7 findings present in the actual code, matching the fix commit's own description | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention applies to this phase; the phase's own probe
IS the live GitHub Actions run itself plus the local `tools/ci-classify-e2e-output.sh` /
`tests/ci_classify_test.lua` battery, both executed above. No separate probe script exists
or was expected per the PLAN/CONTEXT.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PRD-P3 / PRD-Phase3-CI-gate | `06.8-01-PLAN.md` | Wire T1+T2+T3-reg into GitHub Actions on Linux, headless mux, self-skip verified, gate blocks only on real failures | ✓ SATISFIED | `ci.yml` wires all three tiers via `make e2e`; classify script enforces self-skip contract; live run green (orchestrator-supplied evidence) |
| PRD-CI-Gate | `06.8-01-PLAN.md` | (alias of the above in plan frontmatter) | ✓ SATISFIED | same evidence |

No orphaned requirements: this phase's requirements are sourced from
`docs/prds/e2e-testing-battery-v1.0-prd.md` (Phase 3), not `.planning/REQUIREMENTS.md` (a
separate v1-scope document for a different milestone) — no `.planning/REQUIREMENTS.md`
entries map to Phase 06.8, as expected.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `grep -rn "TODO\|FIXME\|TBD\|XXX\|HACK\|PLACEHOLDER"` over every phase-modified file (`ci.yml`, all `tools/ci-*.sh`, `install_state.lua`, `doctor.lua`, `keys.lua`) | — | **None found.** No debt markers in any file this phase touched. |

No stubs, no hardcoded-empty returns, no orphaned artifacts. Every script/file this phase
introduced is invoked by something else (traced above) and independently re-run here.

### Notable Observations (Info / non-blocking)

1. **SUMMARY's "all four bugs carry regression test coverage in the same commit" (Self-Check, Rule 11) is not fully accurate for bug 3.** Bug 1 (install-state ENOENT) and bug 4 (doctor's fresh-install exemption) both got real, verified new test cases in the commits that fixed them (`tests/cli/install_state_test.lua:132-148`, `tests/cli/doctor_test.lua:163-173`). Bug 2 (missing XCB/xkbcommon libs) is inherently untestable outside a real bare Linux runner and is reasonably left uncovered by a unit test. **Bug 3 (the `dkjson` luastatic-bundle require failure) got NO new or modified test in commit `ef34530`** — I diffed that commit directly (`git show --stat ef34530`) and confirmed only `cli/commands/doctor.lua`, `cli/commands/install_state.lua`, `cli/commands/keys.lua`, and `tests/cli/doctor_test.lua` changed; no `keys_test.lua` touch. The existing `tests/cli/keys_test.lua:179` dkjson round-trip test predates this phase and calls `require("dkjson")` directly under the dev launcher — it does not exercise the `cli.vendor.dkjson`-first path nor the new pcall-guarded double-failure branch (WR-03's own fix). This is a genuine, minor overclaim in the SUMMARY's Self-Check, not a functional defect — the fix itself is correct by code read and was exercised for real by the live CI run's luastatic binary (which is exactly how the bug was originally found). Does not block the goal; worth a one-line SUMMARY correction if this file is ever audited again.
2. **The `actionlint` mandatory-gate truth (must-have #7) is not documented as having been run in `06.8-01-SUMMARY.md`.** The word "actionlint" does not appear anywhere in that SUMMARY, despite the PLAN's Task 1 `<verify>` block making it a MANDATORY, no-silent-fallback gate. I independently installed and ran `actionlint@v1.7.12` against the current `ci.yml` in this sandbox and it passes clean (exit 0), so the artifact itself satisfies the requirement on re-verification — but there's no record that the executor actually ran this check during original execution, only that the code review (which is a different, later step) never flagged a workflow syntax issue. Info-level: does not block the goal since the artifact independently verifies clean today.
3. **Nine commits for a single plan (06.8-01)** — `a60c99a`, `b2bdf3b`, `92f05c0`, `053d952`, `80ce8db`, `ef34530`, `81d285e`, `8d4a1fd`, `a199325`. CLAUDE.md's commit-discipline rule asks for "cohesive logical units... typically one commit per plan." Each commit here does represent a genuinely distinct, individually-necessary fix/diagnostic/doc step discovered through live CI iteration (the project's own documented "CI-debugging convergence loop" pattern) — not tiny incremental chunks of the same change — so this reads as an intentional, justified exception to the general grouping guidance rather than a violation. Info-level, non-blocking.

### Human Verification Required

None. Every load-bearing claim was either (a) directly verified by reading the current
code and re-running the exact checks the plan specifies in this verifier's own process, or
(b) supplied as pre-verified live-CI evidence by the orchestrator per this task's explicit
instructions (CI run `34303641563` on commit `a199325`, `conclusion=success`, independently
re-classified artifact, `OK` exit 0). No visual/UX/external-service behavior in this phase
requires a human eye — it is entirely a CI-plumbing phase, and the two evidence classes
above jointly cover "the code is correct" and "the code runs green on real GitHub Actions."

### Gaps Summary

No gaps. The phase goal — "make the deterministic tiers gate every change" — is achieved
and independently proven:
- A new, correctly-triggered (`push`+`pull_request`, D-03) CI workflow wires Tier 1 + Tier 2
  + Tier 3-registration into GitHub Actions on Linux, via a headless `wezterm-mux-server`
  provisioned by the project's own existing installer (D-01, honestly corrected to a
  reproducible-within-a-UTC-day nightly guarantee, not an overclaimed cryptographic pin).
- Self-skip is genuinely verified in CI: a dedicated classify script enforces the exact
  discrimination matrix (exactly-one expected skip, zero unexpected skips including the
  dual-emit shape, all 4 gate-bound labels LIVE-ASSERTED), has durable `make test`-discovered
  regression coverage (5/5 fixtures, re-run here), and its required label strings are
  confirmed to exist verbatim in the real tier files.
- `make test` genuinely gates before any WezTerm-related step (D-04) — the WR-04 code-review
  finding (a step-order violation) is fixed in the current code, re-verified by reproducing
  the plan's own line-order check live.
- The gate is report-only, not wired into branch protection (D-05), confirmed absent by grep.
- All 6 WARNING + 1 INFO code-review findings (WR-01..WR-06, IN-01) are genuinely fixed in
  the current code — not just claimed in the fix commit's message — confirmed by direct
  reading of every affected file.
- `make test` passes 183/183 assertions across all 35 files, exit 0, re-run live in this
  verification.
- `actionlint v1.7.12` passes clean on the current workflow, re-run live in this
  verification.
- The live GitHub Actions run on the exact HEAD commit (`a199325`) is green, independently
  re-classified by the orchestrator (not a self-report) — the phase's ultimate proof bar per
  this project's own ONESHOT-RULES Rule 3.

Two info-level, non-blocking observations are recorded above (a minor SUMMARY overclaim
about bug-3's test coverage, and the SUMMARY's silence on the actionlint gate) — neither
undermines the delivered artifact, which is correct and live-proven.

---

_Verified: 2026-09-08_
_Verifier: Claude (gsd-verifier) — goal-backward, code-read + locally-executed, live-CI evidence cited per task instruction_
