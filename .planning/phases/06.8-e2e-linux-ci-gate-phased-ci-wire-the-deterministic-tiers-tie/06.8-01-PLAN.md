---
phase: 06.8-e2e-linux-ci-gate-phased-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - tools/ci-classify-e2e-output.sh
  - tools/ci-resolve-wezterm-cache-key.sh
  - tools/ci-verify-wezterm-mux.sh
  - tests/ci_classify_test.lua
  - tests/e2e/README.md
autonomous: true
requirements: [PRD-P3, PRD-CI-Gate]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A GitHub Actions workflow (`.github/workflows/ci.yml`) runs on every `push` to `main` and every `pull_request` (D-03) — a NEW workflow, separate from the tag-triggered `release.yml`."
    - "The job provisions a real `wezterm` + sibling `wezterm-mux-server` via `tools/bootstrap-wezterm.sh` with `WEZTERM_TARGET=nightly` (D-01, formally CORRECTED in `06.8-CONTEXT.md` this cycle — not just a plan-level deviation: live verification, independently reconfirmed by two review cycles, showed `WEZTERM_PINNED_RELEASE` has never been a real fetchable `wez/wezterm` release, with or without its short-SHA suffix, and no dated release has ever shipped the required `Ubuntu24.04` asset). Cache is keyed on the resolved nightly-asset datestamp, resolved by a dedicated script (`tools/ci-resolve-wezterm-cache-key.sh`, sourcing `bootstrap-wezterm.sh`'s own `latest_nightly_datestamp`, falling back to today's UTC date when that resolution is empty) — never inlined in YAML. The resulting guarantee is HONEST and EXPLICIT: reproducible-within-a-UTC-day (an unchanged day never re-downloads and always reuses byte-identical bits), NOT a cryptographic pin to an immutable artifact — no such artifact exists upstream for this asset."
    - "`make test` (unit suite) runs BEFORE both WezTerm provisioning AND `make e2e`, in the SAME job (D-04) — a unit failure stops the job before any mux-provisioning/e2e time is spent."
    - "The e2e battery runs against the freshly `make build`-produced `dist/wez` binary, never the dev launcher, per `tests/e2e/README.md`'s documented CI convention — this binary choice is CONTEXT.md's unnumbered 'Claude's Discretion' section, not D-05 (D-05 covers branch protection; citation corrected per `06.8-REVIEWS.md` cycle-2 LOW-6)."
    - "A dedicated classification script (`tools/ci-classify-e2e-output.sh`), with permanent regression coverage in `tests/ci_classify_test.lua` (auto-discovered by `make test`, per `06.8-REVIEWS.md` cycle-2 MEDIUM-2), distinguishes the ONE intentional platform self-skip (Tier 1's macOS-only `keys-siblings` sibling-symlink check, which self-skips loudly and correctly on Linux) from an UNEXPECTED self-skip of any gate-bound Tier 2/3 case. The expected self-skip label MUST appear EXACTLY ONCE (not merely be tolerated — cycle-2 MEDIUM-3: zero or >1 occurrences is a hard failure, evidencing that the self-skip gate itself was genuinely exercised). Any OTHER `SKIPPED (reason=` line — including one appearing ALONGSIDE an already-printed `LIVE-ASSERTED` for the same label, the real dual-emit shape of `tests/e2e/tier2/scene_motd_race_e2e_test.lua` when bash is unexpectedly absent (cycle-2 MEDIUM-1) — is a hard job failure. Each of the four gate-bound labels (`tier2 scene-launch-motd-race regression`, `tier2 new-tab-mode scene live-mux`, `tier2 reuse-mode scene live-mux`, `tier3 show-keys registration`) must print `LIVE-ASSERTED` at least once (D-02)."
    - "The workflow reports pass/fail visibly on the commit/PR but is NOT wired into a branch-protection required status check (D-05) — purely additive, run-and-report only."
    - "`actionlint`, pinned to a specific released version (not `@latest`), is a MANDATORY verification gate with no silent structural-only fallback when unavailable (cycle-2 MEDIUM-4) — its absence after a genuine install attempt is itself a verify failure."
    - "The workflow YAML contains NO nightly-fallback resolution policy or mux-discovery logic inlined in bash — both live in dedicated, reusable `tools/` scripts (`tools/ci-resolve-wezterm-cache-key.sh`, `tools/ci-verify-wezterm-mux.sh`) that the YAML only invokes, honoring the project's existing thin-glue boundary (`06.8-CONTEXT.md:149-153`; cycle-2 MEDIUM-5)."
    - "A real GitHub Actions run — whose `headSha` matches the exact SHA this plan's own commit pushed (never just 'the latest run on main') — completed with `conclusion=success`, giving live evidence (not just local simulation) that the gate works end-to-end."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "the push+PR-triggered CI job: toolchain install -> make test (fail-fast, D-04) -> tools/ci-resolve-wezterm-cache-key.sh + cache + WezTerm provisioning (D-01, corrected) -> tools/ci-verify-wezterm-mux.sh precondition check -> make build -> WEZ_BIN=./dist/wez make e2e, gated by tools/ci-classify-e2e-output.sh (D-02) — no inlined resolution/discovery logic"
      min_lines: 65
    - path: "tools/ci-classify-e2e-output.sh"
      provides: "the single source of truth for the expected-skip-vs-unexpected-skip classification (D-02), including the exactly-once required-skip check, reused identically by ci.yml's e2e step, Task 2's local dry-run, tests/ci_classify_test.lua, and Task 3's remote-log verification"
      min_lines: 30
    - path: "tools/ci-resolve-wezterm-cache-key.sh"
      provides: "sources bootstrap-wezterm.sh's latest_nightly_datestamp (never re-implemented), falls back to today's UTC date, prints the cache key to stdout — the ONLY place this resolution logic lives (cycle-2 MEDIUM-5)"
      min_lines: 12
    - path: "tools/ci-verify-wezterm-mux.sh"
      provides: "mirrors tests/e2e/lib/mux.lua's resolve_mux_server lookup order (PATH, then sibling of the resolved wezterm) as a standalone precondition check — the ONLY place this discovery logic lives (cycle-2 MEDIUM-5)"
      min_lines: 12
    - path: "tests/ci_classify_test.lua"
      provides: "durable make-test-discovered regression coverage for tools/ci-classify-e2e-output.sh's full discrimination matrix (cycle-2 MEDIUM-2/MEDIUM-1/MEDIUM-3): pass, missing-live, extra-unexpected-skip-alongside-all-lives-present (the real scene_motd_race dual-emit shape), expected-skip-absent, expected-skip-duplicated"
      min_lines: 40
    - path: "tests/e2e/README.md"
      provides: "WEZ_BIN CI-override example corrected to the real ./dist/wez build.sh output path + tier-matrix footnote updated to reflect 06.8 landing"
      contains: "wired in"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tools/ci-resolve-wezterm-cache-key.sh"
      via: "the cache-key resolution step delegates to this script (never sources bootstrap-wezterm.sh's latest_nightly_datestamp directly inline in YAML — cycle-2 MEDIUM-5)"
      pattern: "ci-resolve-wezterm-cache-key.sh"
    - from: ".github/workflows/ci.yml"
      to: "tools/bootstrap-wezterm.sh"
      via: "WEZTERM_TARGET=nightly invocation (D-01, corrected — the only genuinely fetchable target on the real wez/wezterm repo)"
      pattern: "WEZTERM_TARGET=nightly"
    - from: ".github/workflows/ci.yml"
      to: "tools/ci-verify-wezterm-mux.sh"
      via: "the mux-resolvable precondition step delegates to this script (never mirrors mux.lua's resolve_mux_server inline in YAML — cycle-2 MEDIUM-5)"
      pattern: "ci-verify-wezterm-mux.sh"
    - from: ".github/workflows/ci.yml"
      to: "tools/ci-classify-e2e-output.sh"
      via: "the e2e step's pass/fail decision delegates to this script, not inline YAML grep logic"
      pattern: "ci-classify-e2e-output.sh"
    - from: "tests/ci_classify_test.lua"
      to: "tools/ci-classify-e2e-output.sh"
      via: "durable make-test regression coverage over the classify script's real subprocess behavior, not just text assertions"
      pattern: "ci-classify-e2e-output.sh"
    - from: ".github/workflows/ci.yml"
      to: "Makefile"
      via: "invokes make test / make build / make e2e — never reimplements their logic (D-01 boundary)"
      pattern: "make (test|build|e2e)"
---

<objective>
Wire the deterministic E2E tiers — Tier 1 (subcommands), Tier 2 (scenes, live mux), Tier 3
registration (show-keys chord families) — into a new GitHub Actions workflow on Linux, so they
gate every `push` to `main` and every `pull_request` going forward (PRD Phase 3). WezTerm is
provisioned in the runner via `tools/bootstrap-wezterm.sh WEZTERM_TARGET=nightly` (D-01, now
formally CORRECTED in `06.8-CONTEXT.md` — not just a plan-level workaround: the literal `pinned`
mechanism was found, via live verification independently reconfirmed across TWO review cycles, to
reference a `wez/wezterm` release that has never existed and could never have shipped the needed
`Ubuntu24.04` asset), cached across runs keyed on the resolved nightly-asset datestamp, resolved by
a dedicated `tools/ci-resolve-wezterm-cache-key.sh` script rather than inlined in YAML. The
resulting guarantee is stated precisely, not oversold: reproducible-WITHIN-a-UTC-day (a same-day
cache hit reuses byte-identical bits), never a cryptographic pin to an immutable artifact, because
none exists upstream for this asset. `make test` runs before BOTH WezTerm provisioning and
`make e2e` in the same job (D-04); the e2e run targets the freshly `make build`-produced `dist/wez`,
never the dev launcher (Claude's Discretion per CONTEXT.md, not D-05 — D-05 covers branch
protection, citation corrected this cycle); a `tools/ci-verify-wezterm-mux.sh` script (not inline
YAML) confirms `wezterm`/`wezterm-mux-server` are genuinely resolvable before the battery runs; and
a dedicated classification script (`tools/ci-classify-e2e-output.sh`, now with permanent
`tests/ci_classify_test.lua` regression coverage under `make test`) treats any UNEXPECTED Tier 2/3
self-skip as a hard failure — including one that appears ALONGSIDE an already-`LIVE-ASSERTED` label,
the real dual-emit shape `scene_motd_race_e2e_test.lua` can produce — while REQUIRING the ONE
intentional macOS-only Tier 1 self-skip to appear EXACTLY ONCE (not merely tolerating it), so a
"green" run can never be a hollow one and the self-skip gate itself is evidenced as exercised (D-02).
The workflow reports pass/fail only — no branch-protection required-status-check wiring (D-05).
Tier 3 firing and Tier 4 visuals stay local/best-effort (06.9) and are never touched here.

This is REVIEW CYCLE 3 (the final allowed cycle before this convergence loop escalates to the
user) of a `--reviews` mode replan. Cycle 1's cross-AI review (OpenCode) found 5 HIGH + 2 MEDIUM
defects, all resolved by cycle 2. Cycle 2's review then found the cycle-1 fix for the unfetchable
pin (switching to `nightly`) itself contradicted the LOCKED `D-01` decision and the roadmap's
"pinned" wording — a genuine tension, not a defect to paper over again. This cycle resolves it
HONESTLY by formally correcting `D-01` in `06.8-CONTEXT.md` (not re-asserting "deterministic"
language a reviewer twice correctly rejected), plus incorporates cycle 2's six remaining
actionable findings (negative-fixture completeness, durable classifier regression test,
exactly-once self-skip assertion, mandatory pinned `actionlint`, moving inlined resolution/discovery
logic into `tools/` scripts, and a citation fix) — see `<review_dispositions>` Round 2.

Purpose: a green e2e battery is only trustworthy going forward if it runs automatically on every
change, not just when a human remembers to run `make e2e` locally — this closes that gap, honestly
(a gate that cold-fails on its first real run, that can't tell an expected skip from a real one, or
that overstates its own determinism guarantee, is worse than no gate at all).
Output: `.github/workflows/ci.yml` (new), `tools/ci-classify-e2e-output.sh` (new),
`tools/ci-resolve-wezterm-cache-key.sh` (new), `tools/ci-verify-wezterm-mux.sh` (new),
`tests/ci_classify_test.lua` (new, durable), a corrected `tests/e2e/README.md` WEZ_BIN example,
plus one real, live-verified GitHub Actions run — correlated to the exact pushed commit SHA —
proving the gate actually works end-to-end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.8-e2e-linux-ci-gate-phased-ci-wire-the-deterministic-tiers-tie/06.8-CONTEXT.md
@.planning/phases/06.8-e2e-linux-ci-gate-phased-ci-wire-the-deterministic-tiers-tie/06.8-REVIEWS.md
@.github/workflows/release.yml
@tools/bootstrap-wezterm.sh
@tools/lib/wezterm-release.sh
@tools/ci-setup-toolchain.sh
@tests/e2e/README.md
@tests/e2e/lib/skip.lua
@tests/e2e/lib/mux.lua
@tests/e2e/tier1/keys_siblings_e2e_test.lua
@tests/e2e/tier2/scene_motd_race_e2e_test.lua
@tests/cli/bootstrap_update_test.lua
@tools/run-tests.sh
@Makefile
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end CI gate — ci.yml + ci-classify-e2e-output.sh + ci-resolve-wezterm-cache-key.sh + ci-verify-wezterm-mux.sh + tests/ci_classify_test.lua (D-01..D-05)</name>
  <read_first>
    - `.github/workflows/release.yml` in full — the project's ONLY existing workflow: pinned action
      versions (`actions/checkout@v4.2.2`), least-privilege `permissions:`, the D-01 boundary header
      comment ("this workflow is PURE glue... no install/version/update decision in YAML"), single
      hardcoded `ubuntu-latest` job. Also note its own header (lines 55-56): "Hardening to a full
      commit SHA is a recommended follow-up once registry access exists" — this project's own
      precedent already accepts tag-pinning (not SHA-pinning) as the current posture; this new
      workflow follows the SAME convention, and the threat model below states the residual risk
      honestly rather than overstating tag-pin protection (MEDIUM-7 from cycle 1).
    - `06.8-CONTEXT.md`'s D-01 section IN FULL, including its **Correction note** — read this before
      writing a single line of YAML. D-01 was FORMALLY CORRECTED this cycle (not just patched at the
      plan level as cycle 1 did): the originally-locked `WEZTERM_TARGET=pinned` referenced a dated
      WezTerm release that has never existed on `wez/wezterm`, confirmed via live
      `api.github.com/repos/wez/wezterm/releases` / `.../releases/tags/nightly` queries in BOTH
      review cycle 1 and, independently, review cycle 2. The corrected decision is
      `WEZTERM_TARGET=nightly` with an EXPLICIT, HONEST determinism claim:
      reproducible-within-a-UTC-day (a same-day cache hit reuses byte-identical bits), never a
      cryptographic pin to an immutable artifact — no such artifact is fetchable for this asset
      upstream. Every place this task writes "deterministic"-adjacent prose must match this precise
      claim, not the looser "deterministic" language cycle 2's reviewer correctly flagged as HIGH.
    - `tools/bootstrap-wezterm.sh` lines 1-65 (header + `WEZTERM_TARGET`/`WEZTERM_MIN_RELEASE`
      handling — note line 60: `WEZTERM_TARGET` already DEFAULTS to `nightly`, including the
      non-interactive path; `pinned` is the opt-in exception, not the default), lines 82-147
      (`latest_nightly_datestamp` / `resolve_want_datestamp` — the EXACT dynamic resolution
      mechanism `tools/ci-resolve-wezterm-cache-key.sh` below reuses verbatim, sourced not
      reinvented), lines 170-243 (`detect_and_reuse` — a cache-restored `wezterm` on `PATH`
      short-circuits a re-fetch when it already meets the resolved nightly `want`), lines 499-517
      (the `BASH_SOURCE[0]`-vs-`$0` guard: `main` never runs when this script is SOURCED — the exact
      property `tests/cli/bootstrap_update_test.lua` already relies on, and the property
      `tools/ci-resolve-wezterm-cache-key.sh` depends on), and lines 403-419 (`install_linux` — the
      fetch this task's provisioning step drives).
    - `tools/lib/wezterm-release.sh` lines 24-36 — **THE BUG THIS TASK WORKS AROUND, NOW FORMALLY
      RECORDED AS A CORRECTED D-01**: `WEZTERM_PINNED_RELEASE:=20260604-145453`. Live verification
      (both review cycles' and this planner's own `api.github.com/repos/wez/wezterm/releases` /
      `releases/tags/nightly` queries) confirmed this dated tag has **never existed** as a real
      `wez/wezterm` release, with or without its documented short-SHA suffix
      (`.planning/decisions/wezterm-cli-surface.md:3`'s `-eeb80972`) — the real upstream's newest
      DATED release is `20240203-110809-5046fc22` (Feb 2024), and no dated tag has ever shipped an
      `Ubuntu24.04` asset (only the rolling `nightly` tag does; confirmed live: a HEAD request to
      `https://github.com/wez/wezterm/releases/download/nightly/wezterm-nightly.Ubuntu24.04.tar.xz`
      returns `200`). `tools/lib/wezterm-release.sh` and `tools/bootstrap-wezterm.sh` are NOT
      modified by this task — only driven with the argument that actually works, and now sourced
      (never re-implemented) by the two new focused scripts below.
    - `tests/e2e/tier1/keys_siblings_e2e_test.lua` lines 78-97 — the ONE intentional, expected
      self-skip: `skip.require_tool(function() return OS == "macos" end, "keys-siblings macOS
      bundle-symlink contract", ...)`. On Linux this ALWAYS prints
      `keys-siblings macOS bundle-symlink contract: SKIPPED (reason=...)` **exactly once** and exits
      0 — this is CORRECT and MUST NOT fail the gate (cycle-1 HIGH-2), but its presence must now be
      REQUIRED exactly once, not merely tolerated (cycle-2 MEDIUM-3) — a run where this line is
      absent, or appears more than once, is itself suspicious and must fail.
    - `tests/e2e/tier2/scene_reuse_e2e_test.lua:81-84`, `tests/e2e/tier2/scene_newtab_e2e_test.lua:88-91`,
      `tests/e2e/tier2/scene_motd_race_e2e_test.lua:84-96`, `tests/e2e/tier3/registration_e2e_test.lua:157-160`
      — the FOUR gate-bound `skip.require_tool` labels (`tier2 scene-launch-motd-race regression`,
      `tier2 new-tab-mode scene live-mux`, `tier2 reuse-mode scene live-mux`,
      `tier3 show-keys registration`) that MUST print `LIVE-ASSERTED` on this runner (WezTerm
      provisioned). Read `scene_motd_race_e2e_test.lua:84-96` CAREFULLY: it prints its mux gate's
      `LIVE-ASSERTED` via `skip.require_tool` at line 84-87, THEN — separately, at line 93-97 — can
      print a SECOND, manually-written `"tier2 scene-launch-motd-race regression: SKIPPED
      (reason=no bash on PATH)"` line if `bash` is absent, before `os.exit(0)`. A single test file
      can therefore legitimately emit BOTH a `LIVE-ASSERTED` AND an unexpected `SKIPPED` line for the
      SAME label in the SAME run (cycle-2 MEDIUM-1) — the classifier below, and its durable test,
      must treat that combination as a hard failure, not a pass-because-LIVE-ASSERTED-was-seen.
    - `tests/e2e/lib/skip.lua` in full — `require_tool`'s EXACT printed strings:
      `"%s: SKIPPED (reason=%s)"` and `"%s: LIVE-ASSERTED (%s)"`.
    - `tests/e2e/lib/mux.lua` `M.resolve_mux_server` (PATH first, then sibling of the `readlink -f`'d
      `wezterm` binary) — the EXACT lookup order `tools/ci-verify-wezterm-mux.sh` below mirrors, in
      ONE focused script rather than inline YAML (cycle-2 MEDIUM-5 — `06.8-CONTEXT.md:149-153`'s D-01
      thin-glue boundary rule: "no bash logic inlined in the YAML beyond invoking the existing
      `tools/`/`make` entry points" — confirmed this citation is accurate by reading that exact
      section).
    - `Makefile` lines 27-51 — the existing `build` / `test` / `e2e` / `e2e-setup` targets this
      workflow invokes and never reimplements.
    - `tests/cli/bootstrap_update_test.lua` in full — the established convention for testing a
      `tools/*.sh` script's real subprocess behavior from Lua (`bash_ok`/`bash_stdout` helpers
      writing a temp bash script that sources the target, then asserting real exit codes/output) —
      `tests/ci_classify_test.lua` below follows the SAME idiom (never invents a new one).
    - `tools/run-tests.sh` lines ~30-45 — confirms `tests/*_test.lua` (excluding `*_e2e_test.lua`) is
      auto-discovered by `make test` with NO registration step needed; `tests/ci_classify_test.lua`
      lands directly under `tests/` and is picked up for free.
    - `06.8-REVIEWS.md` cycle 1 AND cycle 2 in full (already loaded via `<context>`) — cycle-1
      HIGH-1/2/3/4/5 + MEDIUM-6/7 all resolved by the prior revision (see `<review_dispositions>`
      Round 1); cycle-2's 1 HIGH + 6 MEDIUM/LOW findings all trace to this task and Task 2 — see
      `<review_dispositions>` Round 2 for the full ledger, including exactly how each is addressed.
  </read_first>
  <files>.github/workflows/ci.yml, tools/ci-classify-e2e-output.sh, tools/ci-resolve-wezterm-cache-key.sh, tools/ci-verify-wezterm-mux.sh, tests/ci_classify_test.lua</files>
  <action>
    **Part A — `tools/ci-classify-e2e-output.sh` (new, chmod +x).** A small, reusable Lua-battery
    log classifier — the SINGLE source of truth for the D-02 skip-gate decision, reused identically
    by this workflow's e2e step, Task 2's local dry-run, `tests/ci_classify_test.lua`'s durable
    regression coverage, and Task 3's remote-log verification (never divergent copies of the same
    grep logic). `set -euo pipefail`. Takes one arg, the log file path (`${1:?usage: ...}`); errors
    if it does not exist. Defines `EXPECTED_SKIP_LABEL="keys-siblings macOS bundle-symlink contract"`
    (the ONE tolerated self-skip) and a `REQUIRED_LIVE_LABELS` array of the four gate-bound labels
    named in `<read_first>` above. Three checks, each contributing to a single `status` variable
    (never `exit`ing early — every check runs and reports, so a multi-cause failure shows all
    causes at once):
    1. **Exactly-once required self-skip (cycle-2 MEDIUM-3).** `expected_count="$(grep -cF
       "${EXPECTED_SKIP_LABEL}: SKIPPED" "${LOG}" || true)"` (the `|| true` is REQUIRED — a
       zero-match `grep -c` exits 1, which under `set -e` inside a `var="$(...)"` assignment WOULD
       otherwise abort the whole script; guarding every such substitution this way is non-negotiable
       throughout this script). If `expected_count != 1`, print an `::error::` naming the actual
       count and set `status=1` — self-skip evidence is now REQUIRED, not merely permitted.
    2. **No unexpected skip anywhere (cycle-1 HIGH-2, sharpened by cycle-2 MEDIUM-1).**
       `unexpected="$(grep -F 'SKIPPED (reason=' "${LOG}" | grep -vF "${EXPECTED_SKIP_LABEL}:
       SKIPPED" || true)"`. This is INDEPENDENT of the live-label check below — it fires even when a
       gate-bound label's `LIVE-ASSERTED` line is ALSO present elsewhere in the log (the
       `scene_motd_race_e2e_test.lua` dual-emit shape from `<read_first>`), because it scans the
       WHOLE log for any disallowed `SKIPPED` line, not just lines tied to a still-missing live
       label. If non-empty, print an `::error::` listing every such line and set `status=1`.
    3. **Every gate-bound label shows `LIVE-ASSERTED` at least once (D-02).** For each label in
       `REQUIRED_LIVE_LABELS`, `if ! grep -qF "${label}: LIVE-ASSERTED" "${LOG}"; then` (the `if !`
       form is exempt from `set -e`'s errexit trap by bash's own grammar — no `|| true` needed here
       because it is already inside an `if` condition); missing any prints an `::error::` and sets
       `status=1`.
    Print an OK summary line and `exit "${status}"` at the end — a real exit code, no echo-only
    diagnostics (this is what makes it directly reusable as a hard CI gate).

    **Part B — `tests/ci_classify_test.lua` (new, durable — cycle-2 MEDIUM-2: "no committed
    regression test discovered by `make test`").** Auto-discovered by `tools/run-tests.sh` with no
    registration step (confirmed in `<read_first>`). Follows `tests/cli/bootstrap_update_test.lua`'s
    exact idiom: a `check(label, ok, detail)` counter, a `write_log(lines)` helper writing a table of
    strings to an `os.tmpname()` file (one line each, `os.remove`d after use), and a
    `classify_ok(path)` helper running `tools/ci-classify-e2e-output.sh <path>` via `os.execute(...)`
    and returning `ok == true or ok == 0` (the SAME boolean-normalization idiom
    `bootstrap_update_test.lua`'s `bash_ok` already uses). Exercises the FULL discrimination matrix
    as five fixtures built from the SAME `EXPECTED_SKIP` string and `LIVE` table of the four labels
    (never re-typing the label strings a third time — construct each fixture by slicing/extending
    that one shared table):
    1. Exactly-once expected skip + all 4 required lives -> `classify_ok` true (PASS).
    2. One required live label replaced by an unexpected `SKIPPED` -> `classify_ok` false (FAIL).
    3. All 4 lives present, PLUS an extra unexpected `SKIPPED` line for an ALREADY-live label
       (`tier2 scene-launch-motd-race regression` printing both `LIVE-ASSERTED` and, separately,
       `SKIPPED (reason=no bash on PATH)` — the real dual-emit shape from `<read_first>`) ->
       `classify_ok` false (FAIL; this is the cycle-2 MEDIUM-1 fixture the prior revision never
       exercised).
    4. Expected macOS self-skip line entirely absent -> `classify_ok` false (FAIL; cycle-2 MEDIUM-3).
    5. Expected macOS self-skip line appears TWICE -> `classify_ok` false (FAIL; exactly-once, not
       "at least once").
    Ends with the same `print(string.format("\n%d passed, %d failed", passed, failed));
    os.exit(failed == 0 and 0 or 1)` footer every other `tests/*_test.lua` file uses.

    **Part C — `tools/ci-resolve-wezterm-cache-key.sh` (new, chmod +x — cycle-2 MEDIUM-5).**
    `set -uo pipefail`; sources `tools/bootstrap-wezterm.sh` (relative to its own
    `dirname "${BASH_SOURCE[0]}"`, so `main` never runs — the guard confirmed in `<read_first>`) to
    reuse `latest_nightly_datestamp` VERBATIM, never re-implemented; `datestamp="$(latest_nightly_datestamp
    || true)"`; if empty, falls back to `datestamp="$(date -u +%Y%m%d)"` (T-06-06-01-style graceful
    degradation — the upstream API's `uploader` object can legitimately outgrow this function's own
    `-A40` grep window and return empty; this is a pre-existing, out-of-scope characteristic of
    `bootstrap-wezterm.sh` itself, not something this task fixes). `printf '%s\n' "${datestamp}"` to
    stdout — this is the ENTIRE contract; the calling YAML step just captures it. This is now the
    ONLY place this resolution logic (including the UTC-date fallback) lives — never duplicated
    inline in YAML again.

    **Part D — `tools/ci-verify-wezterm-mux.sh` (new, chmod +x — cycle-2 MEDIUM-5).**
    `set -euo pipefail`. `command -v wezterm >/dev/null 2>&1 || { echo "::error::wezterm not
    resolvable on PATH after provisioning" >&2; exit 1; }`; print `wezterm --version`. Then mirror
    `mux.lua`'s `resolve_mux_server` order EXACTLY: if `command -v wezterm-mux-server` succeeds,
    print its `--version` and exit 0; otherwise resolve `dir="$(dirname "$(readlink -f "$(command -v
    wezterm)")")"`, and if `"${dir}/wezterm-mux-server"` is executable, run and print its
    `--version` and exit 0; otherwise print an `::error::` naming both lookup paths tried and exit
    1. This is now the ONLY place this discovery logic lives — never mirrored inline in YAML again.

    **Part E — `.github/workflows/ci.yml` (new).** `name: ci`. Header comment cites D-01 through
    D-05, restates the D-01 boundary rule from `release.yml`'s own header (pure glue — no
    install/version/update decision in YAML beyond invoking `tools/*.sh` / `make`), and notes this
    workflow is separate from the tag-triggered `release.yml` (never touched here). ALSO documents,
    right at D-01's mention, that D-01 was FORMALLY CORRECTED in `06.8-CONTEXT.md` this cycle (the
    pin-doesn't-exist finding, live-verified twice) and states the PRECISE determinism claim —
    reproducible-within-a-UTC-day, never a cryptographic pin — so a future reader isn't confused why
    `WEZTERM_TARGET=nightly` appears instead of the originally-worded `pinned`, and isn't misled into
    believing more determinism is guaranteed than actually is. IMPORTANT: write this header comment
    WITHOUT repeating the bare tokens `WEZTERM_TARGET=nightly` / `run: make test` / `make e2e`
    verbatim in prose ahead of their real step lines below — describe the mechanism in words instead
    (e.g. "the nightly-target provisioning step" rather than embedding `WEZTERM_TARGET=nightly`
    itself) so this task's own line-order `<verify>` assertions (which grep for those EXACT strings
    to prove step ordering) match the real steps, never an earlier mention in a comment.

    Triggers (D-03): `on.push.branches: [main]` and `on.pull_request` (no path/branch filter).
    `permissions: contents: read` at the workflow level. `concurrency: group:
    ci-${{ github.workflow }}-${{ github.ref }}` with `cancel-in-progress: true`.

    One job, id `test-and-e2e`, `name: unit tests + e2e battery (Tier 1+2+3-registration)`,
    `runs-on: ubuntu-latest`, `timeout-minutes: 25`. Steps, in this EXACT order (D-04: `make test`
    strictly before ANY WezTerm work):

    1. `Checkout` — `uses: actions/checkout@v4.2.2`.
    2. `Install Lua toolchain (lua5.4 + luastatic)` — `run: ./tools/ci-setup-toolchain.sh` (reused
       verbatim from `release.yml`; `make test` needs `lua5.4` on `PATH`, which this step provides —
       confirmed via `tools/run-tests.sh` needing ONLY `lua5.4`, nothing WezTerm-related).
    3. `Unit tests (make test)` — `run: make test` (now genuinely first, before WezTerm is touched
       at all — a unit regression, INCLUDING a regression in the new `tests/ci_classify_test.lua`,
       fails fast without spending any mux-provisioning time).
    4. `Add ~/.local/bin to PATH` — appends `$HOME/.local/bin` to `$GITHUB_PATH` so a cache-restored
       or freshly-symlinked `wezterm` resolves for the steps below.
    5. `Resolve WezTerm cache key (D-01)` — `id: wezterm_pin`; `run: echo
       "cache_key=$(./tools/ci-resolve-wezterm-cache-key.sh)" >> "$GITHUB_OUTPUT"` — ALL resolution
       policy (including the UTC-date fallback) lives in Part C's script, never inline here.
    6. `Cache WezTerm nightly install (D-01)` — `uses: actions/cache@v4.2.0`; `path:` both
       `~/.local/opt/wezterm` and `~/.local/bin/wezterm`; `key:
       wezterm-nightly-${{ steps.wezterm_pin.outputs.cache_key }}`.
    7. `Provision WezTerm (bootstrap-wezterm.sh, nightly target — D-01, corrected)` — `run:
       WEZTERM_TARGET=nightly ./tools/bootstrap-wezterm.sh </dev/null` (non-interactive; `nightly`
       is the ONLY genuinely fetchable target on the real repo AND is already this project's own
       default, so this is not a new install path — `install_linux` reuses the identical
       fetch/verify/extract/symlink logic either way).
    8. `Verify wezterm + wezterm-mux-server are resolvable (D-02 precondition)` — `run:
       ./tools/ci-verify-wezterm-mux.sh` — ALL discovery-order logic lives in Part D's script, never
       inline here.
    9. `Build dist/wez` — `run: make build`.
    10. `E2E battery — Tier 1 + Tier 2 + Tier 3-registration (D-02 gate)` — `run:` `set +e` (this
        step manages its own exit explicitly rather than relying on the job-default `-e`);
        `WEZ_BIN=./dist/wez make e2e 2>&1 | tee e2e-output.log`; capture
        `e2e_exit="${PIPESTATUS[0]}"`; run `./tools/ci-classify-e2e-output.sh e2e-output.log` and
        capture `classify_exit=$?`; if `e2e_exit` is nonzero, print an `::error::` (a genuine e2e
        assertion failed) and `exit "${e2e_exit}"`; otherwise `exit "${classify_exit}"` — so a
        genuine assertion failure OR an unexpected-skip/missing-live-assert/missing-exactly-once
        verdict from the classify script both fail the job for real, and an all-green run exits 0.

    `WEZ_BIN` targets the freshly built `./dist/wez` (this convention is CONTEXT.md's unnumbered
    "Claude's Discretion" section, NOT D-05 — D-05 covers branch protection; citation corrected per
    cycle-2 LOW-6). Task 2 corrects `tests/e2e/README.md`'s stale `./dist/wez-linux-x86_64` example.
    Per D-05 this workflow makes NO repository-settings calls and adds no merge-gating
    configuration — describe that in the header comment as "report-only, no repo-settings changes"
    (never naming the specific GitHub feature by name, so the prose can't collide with a later grep
    for that feature's own config keys).
  </action>
  <reversibility rating="reversible">D-01 (target keyword + cache-key shape — `nightly` + resolved
  datestamp instead of the literal `pinned`, now formally recorded as a corrected decision in
  `06.8-CONTEXT.md`, not just a plan-level deviation), D-03 (trigger list), D-04 (step ordering), and
  D-05 (no branch-protection wiring) are each a one-line YAML/workflow edit with no downstream code
  contract depending on it.</reversibility>
  <acceptance_criteria>
    - `tools/ci-classify-e2e-output.sh` exists, is executable, and its logic is independently proven
      by `tests/ci_classify_test.lua` (Part B) across all 5 fixtures: pass (exactly-once skip + 4
      lives), fail (missing a live label), fail (extra unexpected skip alongside all 4 lives
      present — the `scene_motd_race` dual-emit shape, cycle-2 MEDIUM-1), fail (expected skip
      absent), fail (expected skip duplicated, cycle-2 MEDIUM-3).
    - `tests/ci_classify_test.lua` is auto-discovered and passes under `make test` (cycle-2
      MEDIUM-2) — no manual registration step required.
    - `tools/ci-resolve-wezterm-cache-key.sh` and `tools/ci-verify-wezterm-mux.sh` exist, are
      executable, and are the ONLY places their respective resolution/discovery logic appears — the
      workflow YAML contains NEITHER `latest_nightly_datestamp` NOR a mirrored `resolve_mux_server`
      lookup inline (cycle-2 MEDIUM-5).
    - `.github/workflows/ci.yml` exists, is valid YAML, and passes `actionlint` pinned to a specific
      released version (`v1.7.12`) — installed via `go install
      github.com/rhysd/actionlint/cmd/actionlint@v1.7.12` or, if `go` is unavailable, `brew install
      actionlint`; if NEITHER succeeds, the verify step FAILS — no silent "unavailable, falling back
      to structural checks only" path (cycle-2 MEDIUM-4).
    - Triggers on `push` to `main` and on `pull_request` (D-03); `permissions: contents: read` only.
    - A step running `make test` appears strictly before BOTH the WezTerm-provisioning step
      (its real `run: WEZTERM_TARGET=nightly ./tools/bootstrap-wezterm.sh` line, not any prose
      mention of that string) and the `WEZ_BIN=./dist/wez make e2e` step (D-04 fail-fast ordering).
    - The cache-key step delegates to `tools/ci-resolve-wezterm-cache-key.sh`; the mux-precondition
      step delegates to `tools/ci-verify-wezterm-mux.sh`; the e2e step delegates to
      `tools/ci-classify-e2e-output.sh` — no inline resolution/discovery/classification logic
      duplicated in the YAML.
    - No repository-settings API calls or merge-gating config keys anywhere in the file (D-05).
  </acceptance_criteria>
  <verify>
    <automated>set -uo pipefail; export PATH="${HOME}/go/bin:${PATH}"; if ! command -v actionlint >/dev/null 2>&1; then if command -v go >/dev/null 2>&1; then go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12 || { echo "actionlint install via go failed" >&2; exit 1; }; elif command -v brew >/dev/null 2>&1; then brew install actionlint || { echo "actionlint install via brew failed" >&2; exit 1; }; else echo "::error::actionlint is mandatory and neither go nor brew is available to install it" >&2; exit 1; fi; fi; command -v actionlint >/dev/null 2>&1 || { echo "::error::actionlint still not on PATH after install attempt" >&2; exit 1; }; actionlint .github/workflows/ci.yml || { echo "actionlint reported issues" >&2; exit 1; }; yq eval '.' .github/workflows/ci.yml > /dev/null || { echo "invalid YAML" >&2; exit 1; }; test "$(yq eval '.on.push.branches[0]' .github/workflows/ci.yml)" = "main" || { echo "missing push.branches:[main]" >&2; exit 1; }; yq eval '.on' .github/workflows/ci.yml | grep -q '^pull_request:' || { echo "missing pull_request trigger" >&2; exit 1; }; test "$(yq eval '.permissions.contents' .github/workflows/ci.yml)" = "read" || { echo "permissions.contents not read-only" >&2; exit 1; }; TEST_LINE="$(grep -n 'run: make test' .github/workflows/ci.yml | head -1 | cut -d: -f1)"; PROVISION_LINE="$(grep -n 'run: WEZTERM_TARGET=nightly ./tools/bootstrap-wezterm.sh' .github/workflows/ci.yml | head -1 | cut -d: -f1)"; E2E_LINE="$(grep -n 'WEZ_BIN=./dist/wez make e2e' .github/workflows/ci.yml | head -1 | cut -d: -f1)"; [ -n "${TEST_LINE}" ] && [ -n "${PROVISION_LINE}" ] && [ -n "${E2E_LINE}" ] || { echo "a required step line is missing" >&2; exit 1; }; test "${TEST_LINE}" -lt "${PROVISION_LINE}" || { echo "make test not before WezTerm provisioning (D-04)" >&2; exit 1; }; test "${PROVISION_LINE}" -lt "${E2E_LINE}" || { echo "provisioning not before make e2e" >&2; exit 1; }; grep -q 'ci-resolve-wezterm-cache-key.sh' .github/workflows/ci.yml || { echo "cache-key resolution not delegated to tools/ci-resolve-wezterm-cache-key.sh (MEDIUM-5)" >&2; exit 1; }; grep -q 'ci-verify-wezterm-mux.sh' .github/workflows/ci.yml || { echo "mux precondition not delegated to tools/ci-verify-wezterm-mux.sh (MEDIUM-5)" >&2; exit 1; }; grep -q 'ci-classify-e2e-output.sh' .github/workflows/ci.yml || { echo "e2e step does not delegate to the classify script (D-02)" >&2; exit 1; }; ! grep -q 'latest_nightly_datestamp' .github/workflows/ci.yml || { echo "cache-key resolution still inlined in YAML (MEDIUM-5)" >&2; exit 1; }; ! grep -q 'resolve_mux_server\|readlink -f' .github/workflows/ci.yml || { echo "mux discovery still inlined in YAML (MEDIUM-5)" >&2; exit 1; }; grep -q 'actions/checkout@v4' .github/workflows/ci.yml || { echo "checkout action missing/unpinned" >&2; exit 1; }; grep -q 'actions/cache@v4' .github/workflows/ci.yml || { echo "cache action missing/unpinned" >&2; exit 1; }; ! grep -qi 'required_status_checks\|branches/.*protection\|gh api.*protection' .github/workflows/ci.yml || { echo "branch-protection config leaked into workflow (D-05)" >&2; exit 1; }; test -x tools/ci-classify-e2e-output.sh || { echo "classify script missing or not executable" >&2; exit 1; }; test -x tools/ci-resolve-wezterm-cache-key.sh || { echo "cache-key script missing or not executable" >&2; exit 1; }; test -x tools/ci-verify-wezterm-mux.sh || { echo "mux-verify script missing or not executable" >&2; exit 1; }; test -f tests/ci_classify_test.lua || { echo "durable classifier regression test missing (MEDIUM-2)" >&2; exit 1; }; lua5.4 tests/ci_classify_test.lua || { echo "durable classifier regression test failed" >&2; exit 1; }; echo "ci-yml-structure-ok"</automated>
    <fails_when>exits non-zero, or does not print the final "ci-yml-structure-ok" line — e.g.
    actionlint cannot be installed/run at all (no silent structural-only fallback), invalid YAML,
    missing pull_request trigger, permissions not read-only, `make test` not ordered before the
    real WezTerm-provisioning step or before the real `make e2e` step, the cache-key/mux-verify/
    classify steps not delegating to their respective `tools/` scripts, cache-key or mux-discovery
    logic still inlined in the YAML, an unpinned action version, a branch-protection API call
    present, any of the three new `tools/*.sh` scripts missing/non-executable, or
    `tests/ci_classify_test.lua` missing or failing any of its 5 fixtures.</fails_when>
  </verify>
  <done>`.github/workflows/ci.yml` exists, is valid YAML, passes a MANDATORY pinned-version
  `actionlint`, and structurally implements D-01 through D-05: nightly-target + resolved-datestamp
  cached WezTerm provisioning (delegated to `tools/ci-resolve-wezterm-cache-key.sh`), push+PR
  triggers, unit-tests-before-everything-else ordering, the freshly built `dist/wez` binary, a
  mux precondition delegated to `tools/ci-verify-wezterm-mux.sh`, and a hard failure on any
  UNEXPECTED tier self-skip (including the exactly-once-required expected self-skip) via
  `tools/ci-classify-e2e-output.sh` — with no branch-protection wiring and no resolution/discovery
  logic inlined in the YAML. `tests/ci_classify_test.lua` exists, is auto-discovered by `make test`,
  and passes all 5 fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WEZ_BIN doc drift + local dry-run proof of the CI gate logic against a REAL e2e run (D-02; WEZ_BIN convention is Claude's Discretion, not D-05 — cycle-2 LOW-6)</name>
  <read_first>
    - `tests/e2e/README.md` lines 12-48 — the exact stale `./dist/wez-linux-x86_64` WEZ_BIN example
      and the "gates CI once 06.8 lands" footnote wording to update.
    - `tools/build.sh` — confirms `OUT="${DIST_DIR}/wez"` is the SOLE output path `make build`
      writes; it NEVER produces a per-platform-named file under `dist/`.
    - `tools/ci-classify-e2e-output.sh` + `tests/ci_classify_test.lua` (Task 1's output) — the exact
      classification script and its durable regression coverage this task's local dry-run and
      re-proof both build on, so the SAME single-source-of-truth logic is exercised on this dev box
      before it is ever pushed to GitHub — never a THIRD divergent copy of the classification/fixture
      logic authored inline here.
    - `.github/workflows/ci.yml` (Task 1's output) — the exact `WEZ_BIN` value and step names this
      doc must describe accurately.
  </read_first>
  <files>tests/e2e/README.md</files>
  <action>
    In `tests/e2e/README.md`, replace the `WEZ_BIN`-override bullet's stale example
    `WEZ_BIN=./dist/wez-linux-x86_64 make e2e` with the value `tools/build.sh` actually produces —
    `WEZ_BIN=./dist/wez make e2e` — and add a parenthetical pointing at
    `.github/workflows/ci.yml` as the live consumer of this convention. Update the tier-matrix
    footnote sentence "`M` = must-pass (gates CI once 06.8 lands)" to "`M` = must-pass (gates CI —
    wired in `.github/workflows/ci.yml`, 06.8)" so the doc reflects that 06.8 has landed. Touch
    NOTHING else in the file.

    Then produce TWO pieces of proof:

    1. **The real local dry-run** — reproduce ci.yml's steps 3/9/10 on this dev box (which already
       has a resolvable `wezterm` + `wezterm-mux-server`): run `make test`, then `make build`, then
       `WEZ_BIN=./dist/wez make e2e > .tmp/e2e-local-proof.log 2>&1` (capturing its real exit code,
       not swallowing it behind a semicolon-echo), then `./tools/ci-classify-e2e-output.sh
       .tmp/e2e-local-proof.log` (capturing ITS real exit code too) against the REAL, non-synthetic
       output. Both must be 0 — this is the genuine, non-synthetic proof that Task 2 uniquely
       contributes (the synthetic fixture matrix is Task 1's `tests/ci_classify_test.lua`'s job,
       never re-authored here).
    2. **Re-confirm the classifier's full discrimination matrix still holds (cycle-2 MEDIUM-1/2/3)**
       — run `lua5.4 tests/ci_classify_test.lua` again on this dev box and assert it exits 0. This
       is a cheap, single-source-of-truth re-check (the fixture definitions live ONLY in that one
       file, per Task 1) that the pass/missing-live/extra-unexpected-skip-alongside-live/
       missing-expected-skip/duplicated-expected-skip discrimination genuinely holds on THIS box
       before anything is pushed — never a second, divergently-authored copy of those fixtures.

    Every step above asserts its real exit code via `test`/`||{ ...; exit 1; }`, never a bare
    `cmd; echo "$?"` that gets ignored (the exact false-green pattern cycle-1 HIGH-4 flagged).
  </action>
  <acceptance_criteria>
    - `tests/e2e/README.md`'s WEZ_BIN example now reads `./dist/wez` — no reference to the
      nonexistent `./dist/wez-linux-x86_64` path remains anywhere in the file.
    - The tier-matrix footnote reflects 06.8 as wired (references `.github/workflows/ci.yml`).
    - `make test` still exits 0 after the doc edit (`tests/e2e/README.md` is not scanned by
      `tests/docs_drift_test.lua`'s `governed_docs()` — confirmed by reading that test before editing).
    - A real local run of `make test` && `make build` && `WEZ_BIN=./dist/wez make e2e` exits 0, AND
      `tools/ci-classify-e2e-output.sh` against that REAL log also exits 0.
    - `tests/ci_classify_test.lua` (Task 1's durable fixture matrix, covering the pass case and all
      four fail cases including the cycle-2 MEDIUM-1 dual-emit and MEDIUM-3 exactly-once fixtures)
      re-passes on this dev box, exit 0.
  </acceptance_criteria>
  <verify>
    <automated>set -uo pipefail; mkdir -p .tmp; ! grep -q 'dist/wez-linux-x86_64' tests/e2e/README.md || { echo "stale WEZ_BIN reference still present" >&2; exit 1; }; grep -q 'wired in `.github/workflows/ci.yml`' tests/e2e/README.md || { echo "footnote not updated" >&2; exit 1; }; make test > .tmp/unit-test-proof.log 2>&1 || { echo "make test failed" >&2; tail -40 .tmp/unit-test-proof.log >&2; exit 1; }; make build > .tmp/build-proof.log 2>&1 || { echo "make build failed" >&2; tail -40 .tmp/build-proof.log >&2; exit 1; }; WEZ_BIN=./dist/wez make e2e > .tmp/e2e-local-proof.log 2>&1; e2e_exit=$?; test "${e2e_exit}" -eq 0 || { echo "real local e2e run failed (exit=${e2e_exit})" >&2; tail -60 .tmp/e2e-local-proof.log >&2; exit 1; }; ./tools/ci-classify-e2e-output.sh .tmp/e2e-local-proof.log; classify_exit=$?; test "${classify_exit}" -eq 0 || { echo "classify verdict on the real local e2e run was non-zero" >&2; exit 1; }; lua5.4 tests/ci_classify_test.lua; fixture_matrix_exit=$?; test "${fixture_matrix_exit}" -eq 0 || { echo "tests/ci_classify_test.lua's discrimination matrix failed to re-confirm on this box" >&2; exit 1; }; echo "task2-verify-ok"</automated>
    <fails_when>exits non-zero, or does not print the final "task2-verify-ok" line — e.g. the stale
    WEZ_BIN reference is still present, the footnote was not updated, `make test`/`make build`/the
    real `make e2e` run fails, the classify script rejects the real green e2e log, or
    `tests/ci_classify_test.lua`'s discrimination matrix (pass + all 4 fail fixtures) fails to
    re-confirm on this box.</fails_when>
  </verify>
  <done>`tests/e2e/README.md` correctly documents `WEZ_BIN=./dist/wez` as the CI override and marks
  06.8 as wired; a local dry-run of `make test` -> `make build` -> `WEZ_BIN=./dist/wez make e2e`
  exits 0 against the REAL e2e output and `tools/ci-classify-e2e-output.sh` confirms it;
  `tests/ci_classify_test.lua`'s full discrimination matrix (pass, missing-live,
  extra-unexpected-skip-alongside-live, missing-expected-skip, duplicated-expected-skip) re-passes
  on this box — proving the workflow's gate logic before it is ever pushed, from a single
  source of truth.</done>
</task>

<task type="auto">
  <name>Task 3: Push + live-verify the CI gate on GitHub Actions, correlated to the pushed SHA (D-01..D-05, phase goal)</name>
  <read_first>
    - `.github/workflows/ci.yml` (Task 1's output) — the exact workflow `name: ci` this task polls
      for via `gh run list --workflow=ci.yml`.
    - `tools/ci-classify-e2e-output.sh` (Task 1's output) — reused here against the REMOTE run's
      fetched log, the same script Task 2 already proved locally (single source of truth, never a
      third divergent copy).
    - `git remote -v` / `gh auth status` — origin is `git@github.com:castocolina/wezterm-setup.git`
      with local `main` already in sync with `origin/main`, and `gh` is authenticated with `repo`
      scope (confirmed working during planning) — this is the SAME box/session that will execute
      this task.
  </read_first>
  <precondition>assumes `gh auth status` succeeds and `git push` to `origin main` is permitted from this environment (already confirmed working during planning — origin is `git@github.com:castocolina/wezterm-setup.git`, HEAD in sync with `origin/main`).</precondition>
  <files>(no source files — commits Task 1 + Task 2's changes and pushes)</files>
  <action>
    Commit this plan's changes (`.github/workflows/ci.yml`, `tools/ci-classify-e2e-output.sh`,
    `tools/ci-resolve-wezterm-cache-key.sh`, `tools/ci-verify-wezterm-mux.sh`,
    `tests/ci_classify_test.lua`, `tests/e2e/README.md`) as ONE logical commit on `main` (per
    project commit discipline), capture `PUSHED_SHA="$(git rev-parse HEAD)"` BEFORE pushing, then
    `git push origin main`.

    Poll `gh run list --workflow=ci.yml --branch=main --limit=10 --json databaseId,headSha,status`
    in a bounded loop (every 20s, up to ~15 minutes — the FIRST run is slower, cold cache) filtering
    for the run whose `headSha` EXACTLY equals `PUSHED_SHA` (HIGH-5: never just "the latest run on
    main" — a concurrent or stale completed run must not be misattributed as evidence for THIS
    change). Stop polling once that specific run's `status` is `completed`; fail loudly (exit
    non-zero, not just log a message) if no run for `PUSHED_SHA` ever appears, or if 45 polls elapse
    without it reaching `completed`.

    Once found and completed, fetch that run's `conclusion` and re-confirm its `headSha` via `gh run
    view <databaseId> --json conclusion,headSha` (defense in depth against the run list changing
    between polls), and HARD-ASSERT both: `headSha == PUSHED_SHA` and `conclusion == success` (HIGH-4:
    a real `test`/`exit 1` assertion, never a bare `echo` of the value that nothing then checks).
    Fetch the full log via `gh run view <databaseId> --log` into `.tmp/ci-run-<id>.log`, then run
    `tools/ci-classify-e2e-output.sh` against THAT remote log (the SAME script Task 1's YAML and
    Task 2's local proof both used) and hard-assert its exit code is 0 — this is the authoritative,
    live confirmation that Tier 2/3 genuinely ran against the CI-provisioned mux (no unexpected
    self-skip) and every gate-bound label shows `LIVE-ASSERTED`.

    If `headSha` doesn't match, `conclusion` is anything other than `success`, or the classify
    script rejects the remote log, fetch the failing step's log via `gh run view <databaseId>
    --log-failed`, diagnose and fix the workflow, commit the fix, push again (capturing a NEW
    `PUSHED_SHA`), and re-poll — iterate until the run is genuinely green AND correlated to the SHA
    that was actually pushed. Do not declare this task done on a failing, self-skipped, or
    SHA-mismatched run. Note in the SUMMARY that the `push` trigger was the one actually exercised
    live; the `pull_request` trigger runs the identical job body and was verified structurally in
    Task 1.
  </action>
  <acceptance_criteria>
    - The commit containing `.github/workflows/ci.yml` + `tools/ci-classify-e2e-output.sh` +
      `tools/ci-resolve-wezterm-cache-key.sh` + `tools/ci-verify-wezterm-mux.sh` +
      `tests/ci_classify_test.lua` + the `tests/e2e/README.md` fix is pushed to `origin/main`.
    - A GitHub Actions run for `ci.yml` whose `headSha` EXACTLY matches the SHA this task pushed
      (captured via `git rev-parse HEAD` immediately before `git push`) reaches `status=completed`
      with `conclusion=success` — never "the latest run on main" without that correlation (HIGH-5).
    - `tools/ci-classify-e2e-output.sh`, run against that SPECIFIC run's fetched log, exits 0.
    - Every hard check above is a real `test`/`exit 1` assertion — no bare `echo "$VAR"` that
      nothing then verifies (HIGH-4).
    - If an intermediate run failed, self-skipped unexpectedly, or was SHA-mismatched, the workflow
      was fixed and re-pushed until a genuinely green, correctly-correlated run exists.
  </acceptance_criteria>
  <verify>
    <automated>
set -uo pipefail
PUSHED_SHA="$(git rev-parse HEAD)"
if ! git push origin main; then
  echo "git push to origin main failed" >&2
  exit 1
fi
echo "pushed_sha=${PUSHED_SHA}"
RUN_ID=""
STATUS=""
for i in $(seq 1 45); do
  MATCH="$(gh run list --workflow=ci.yml --branch=main --limit=10 --json databaseId,headSha,status --jq ".[] | select(.headSha==\"${PUSHED_SHA}\")" 2>/dev/null | head -1 || true)"
  if [ -n "${MATCH}" ]; then
    RUN_ID="$(printf '%s' "${MATCH}" | jq -r '.databaseId')"
    STATUS="$(printf '%s' "${MATCH}" | jq -r '.status')"
  fi
  echo "poll:${i} run_id=${RUN_ID:-none} status=${STATUS:-none}"
  [ "${STATUS}" = "completed" ] && break
  sleep 20
done
if [ -z "${RUN_ID}" ]; then
  echo "no GitHub Actions run for pushed SHA ${PUSHED_SHA} was ever found after 45 polls" >&2
  exit 1
fi
if [ "${STATUS}" != "completed" ]; then
  echo "run ${RUN_ID} for SHA ${PUSHED_SHA} did not reach status=completed after ~15 min" >&2
  exit 1
fi
CONCLUSION="$(gh run view "${RUN_ID}" --json conclusion --jq '.conclusion')"
HEAD_SHA_CONFIRM="$(gh run view "${RUN_ID}" --json headSha --jq '.headSha')"
echo "run_id=${RUN_ID} conclusion=${CONCLUSION} head_sha=${HEAD_SHA_CONFIRM}"
if [ "${HEAD_SHA_CONFIRM}" != "${PUSHED_SHA}" ]; then
  echo "run ${RUN_ID}'s headSha (${HEAD_SHA_CONFIRM}) does not match the pushed SHA (${PUSHED_SHA})" >&2
  exit 1
fi
if [ "${CONCLUSION}" != "success" ]; then
  echo "run ${RUN_ID} concluded '${CONCLUSION}', not 'success'" >&2
  exit 1
fi
mkdir -p .tmp
gh run view "${RUN_ID}" --log > ".tmp/ci-run-${RUN_ID}.log" 2>&1
./tools/ci-classify-e2e-output.sh ".tmp/ci-run-${RUN_ID}.log"
classify_exit=$?
if [ "${classify_exit}" -ne 0 ]; then
  echo "classify script rejected the remote run's log (exit=${classify_exit})" >&2
  exit 1
fi
echo "ci-run-verified-green"
</automated>
    <fails_when>exits non-zero, or does not print the final "ci-run-verified-green" line — e.g. no
    run for the pushed SHA ever appears within 45 polls (~15 min), the matched run never reaches
    `status=completed`, the re-confirmed `headSha` does not match the pushed SHA, `conclusion` is
    anything other than `success`, or `tools/ci-classify-e2e-output.sh` exits non-zero against the
    fetched remote log (an unexpected self-skip or a missing `LIVE-ASSERTED` marker leaked into the
    real run).</fails_when>
  </verify>
  <done>A GitHub Actions `ci` run whose `headSha` matches the exact SHA this task pushed completed
  with `conclusion=success`; `tools/ci-classify-e2e-output.sh` against that run's fetched log exits
  0 — live, remote, SHA-correlated evidence that the deterministic tiers now gate every push/PR (the
  phase goal), not just a local simulation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR branch -> CI runner | An external PR's code/workflow-defined steps execute on a GitHub-hosted runner using the repo's default `GITHUB_TOKEN` |
| Internet -> CI runner | The `nightly` WezTerm archive is fetched over HTTPS from `github.com/wez/wezterm` releases during provisioning (`tools/bootstrap-wezterm.sh`) |
| Actions Marketplace -> CI runner | Third-party actions (`actions/checkout`, `actions/cache`) execute arbitrary code inside the job |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.8-01 | Tampering | `.github/workflows/ci.yml` `permissions:` | high | mitigate | `permissions: contents: read` (least privilege) — a malicious PR's tampered workflow steps run with a read-only `GITHUB_TOKEN` and can't push, release, or modify the repo even if the job itself is compromised |
| T-06.8-02 | Tampering | `actions/checkout`, `actions/cache` version pins | medium | accept | Pinned to exact TAGS (`checkout@v4.2.2` matching `release.yml`'s existing pin; `cache@v4.2.0`), matching this project's own established convention (`release.yml` itself uses tag-pins, not SHA-pins — its own header explicitly names full-commit-SHA pinning as a "recommended follow-up once registry access exists", not yet done). CORRECTED from the prior plan version (MEDIUM-7): tags ARE mutable on GitHub — a tag-pin does NOT fully prevent a compromised-account re-push of that tag from silently swapping the action's code on a later run. Accepted as residual risk consistent with the existing `release.yml` precedent, not overstated as immutable; SHA-pinning both workflows together is the honest follow-up, tracked as a future hardening pass rather than done piecemeal in this CI-wiring phase. |
| T-06.8-03 | Tampering | WezTerm archive download (`tools/bootstrap-wezterm.sh`) | medium | accept | Inherited from the existing Phase-1 bootstrap script (`verify_tarxz` checks magic bytes + plausible size only, no published checksum pin) — official HTTPS-only host already enforced upstream (T-02-05). ADDITIONALLY (per the corrected D-01, `06.8-CONTEXT.md`): the `nightly` tag's CONTENT is mutable by design (a rolling release, live-verified across two independent review cycles) — no immutable dated release with the needed `Ubuntu24.04` asset exists upstream to pin to instead. `tools/ci-resolve-wezterm-cache-key.sh`'s cache key captures the resolved nightly-asset datestamp so a same-UTC-day rerun reuses byte-identical bits, but this is reproducible-within-a-day, NOT a cryptographic hash pin to a known-good build — stated honestly rather than oversold (the exact language cycle-2's reviewer correctly flagged as HIGH when the plan asserted "deterministic" without this qualifier). Accepted: redesigning WezTerm's own release-integrity model is out of this phase's scope. |
| T-06.8-04 | Denial of Service | CI job (mux provisioning / e2e run) | medium | mitigate | `timeout-minutes: 25` job cap so a hung mux-server or runaway retry loop on a broken/malicious PR can't consume Actions minutes indefinitely |
| T-06.8-05 | Information Disclosure | job step logs | low | accept | No secrets beyond the default read-only `GITHUB_TOKEN`; nothing sensitive is echoed by any step |
| T-06.8-06 | Spoofing | e2e battery self-skip reported as a pass | high | mitigate | `tools/ci-classify-e2e-output.sh` (D-02) distinguishes the ONE intentional macOS-only self-skip (REQUIRED to appear exactly once, cycle-2 MEDIUM-3) from an unexpected gate-bound self-skip — an unexpected `SKIPPED (reason=` line ANYWHERE in the log (even alongside an already-printed `LIVE-ASSERTED` for the same label — the `scene_motd_race` dual-emit shape, cycle-2 MEDIUM-1), or a missing `LIVE-ASSERTED` marker for any of the four gate-bound labels, is a hard failure. Durable regression coverage lives in `tests/ci_classify_test.lua` under `make test` (cycle-2 MEDIUM-2), not just one-time plan verification. CORRECTED from the original plan version (cycle-1 HIGH-2): a blanket rejection of ANY `SKIPPED` line would have made this workflow permanently red on Linux (the macOS-only Tier-1 check self-skips there by design). |
| T-06.8-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package-manager installs beyond `tools/ci-setup-toolchain.sh`'s existing `apt`/`luarocks` calls, reused verbatim from `release.yml`, unmodified. `actionlint` is pinned to a specific released version (`v1.7.12`, via `go install`/`brew` in Task 1's own MANDATORY verify step — no silent fallback if unavailable, cycle-2 MEDIUM-4) and is a PLANNING-TIME/verify-time lint tool, never a runtime dependency of `ci.yml` itself. |
</threat_model>

<verification>
- `tools/ci-classify-e2e-output.sh` correctly classifies its full 5-fixture discrimination matrix
  via `tests/ci_classify_test.lua`, auto-discovered and re-run under `make test` (Task 1's verify
  command, cycle-2 MEDIUM-1/2/3).
- `.github/workflows/ci.yml` parses as valid YAML, passes a MANDATORY pinned-version `actionlint`
  (cycle-2 MEDIUM-4), and structurally satisfies D-01 through D-05 with cache-key/mux/classify
  resolution all delegated to dedicated `tools/` scripts, never inlined (Task 1's verify command,
  cycle-2 MEDIUM-5).
- `tests/e2e/README.md`'s `WEZ_BIN` example matches `tools/build.sh`'s real output path; a local
  dry-run of `make test` -> `make build` -> `WEZ_BIN=./dist/wez make e2e` proves the gate logic
  against a REAL e2e log, and `tests/ci_classify_test.lua`'s full discrimination matrix re-confirms
  on this box (Task 2's verify command).
- A live GitHub Actions run of the new `ci` workflow, correlated by exact `headSha` to this plan's
  own pushed commit, completes with `conclusion=success` and its fetched log passes
  `tools/ci-classify-e2e-output.sh` (Task 3's verify command) — the authoritative end-to-end proof.
</verification>

<success_criteria>
- The deterministic tiers (Tier 1 + Tier 2 + Tier 3-registration) now run automatically on every `push` to `main` and every `pull_request`.
- WezTerm is provisioned via a genuinely fetchable, cached target (`nightly` + resolved datestamp) — D-01 formally corrected in `06.8-CONTEXT.md` this cycle — so the gate's pass/fail never depends on a non-existent pin; the determinism guarantee is stated precisely (reproducible-within-a-UTC-day), never oversold as an immutable pin.
- Unit tests gate everything else in the same job (fail fast, before WezTerm is even touched).
- An UNEXPECTED self-skip in CI is structurally impossible to pass quietly (even one appearing alongside an already-satisfied `LIVE-ASSERTED` for the same label), the expected macOS self-skip is REQUIRED exactly once (not merely tolerated), and this full discrimination matrix has permanent regression coverage under `make test` — proven in every direction, not just the happy path.
- All resolution/discovery/classification logic lives in dedicated, reusable `tools/` scripts that the YAML only invokes — honoring the project's existing thin-glue boundary, with no policy inlined in bash within the workflow file.
- `actionlint` is a mandatory, pinned-version verification gate with no silent structural-only fallback.
- No branch-protection required-status-check wiring exists yet (D-05) — the gate is additive, run-and-report only.
- A real, live GitHub Actions run, correlated to the exact pushed commit SHA, proves all of the above — not just a local simulation and not a misattributed stale/concurrent run.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

- `.github/workflows/ci.yml` — NEW workflow. `name: ci`; triggers `push` (branches: `[main]`) +
  `pull_request`; top-level `permissions: contents: read`; `concurrency` group
  `ci-${{ github.workflow }}-${{ github.ref }}`. One job, id `test-and-e2e`
  (`runs-on: ubuntu-latest`, `timeout-minutes: 25`), steps: `Checkout`, `Install Lua toolchain`,
  `Unit tests (make test)`, `Add ~/.local/bin to PATH`, `Resolve WezTerm cache key (D-01)` (id
  `wezterm_pin`, delegates to `tools/ci-resolve-wezterm-cache-key.sh`), `Cache WezTerm nightly
  install (D-01)`, `Provision WezTerm (bootstrap-wezterm.sh, nightly target — D-01, corrected)`,
  `Verify wezterm + wezterm-mux-server are resolvable (D-02 precondition)` (delegates to
  `tools/ci-verify-wezterm-mux.sh`), `Build dist/wez`, `E2E battery — Tier 1 + Tier 2 + Tier
  3-registration (D-02 gate)` (delegates to `tools/ci-classify-e2e-output.sh`).
- `tools/ci-classify-e2e-output.sh` — NEW, executable. Classifies an e2e battery log: exit 0 iff the
  expected macOS self-skip appears EXACTLY ONCE, no OTHER `SKIPPED (reason=` line appears anywhere
  (even alongside an already-`LIVE-ASSERTED` label), and every gate-bound label shows
  `LIVE-ASSERTED`; exit 1 otherwise. Single source of truth reused by `ci.yml`, the local dry-run,
  `tests/ci_classify_test.lua`, and the remote-log check.
- `tools/ci-resolve-wezterm-cache-key.sh` — NEW, executable. Sources `bootstrap-wezterm.sh`'s
  `latest_nightly_datestamp`, falls back to today's UTC date, prints the cache key. The ONLY place
  this resolution logic lives.
- `tools/ci-verify-wezterm-mux.sh` — NEW, executable. Mirrors `mux.lua`'s `resolve_mux_server` order
  (PATH, then sibling of the resolved `wezterm`) as a standalone precondition check. The ONLY place
  this discovery logic lives.
- `tests/ci_classify_test.lua` — NEW. Durable, `make test`-discovered regression coverage for
  `tools/ci-classify-e2e-output.sh`'s full 5-fixture discrimination matrix.
- `tests/e2e/README.md` — MODIFIED: `WEZ_BIN` CI-override example corrected to `./dist/wez`; tier
  matrix footnote updated to note 06.8 is wired. No new symbols.
- One live GitHub Actions run of the `ci` workflow against `origin/main`, correlated to the exact
  pushed SHA, captured under `.tmp/ci-run-<id>.log` (gitignored — not committed) as this plan's
  execution evidence.
</artifacts_produced>

<review_dispositions>
## Review Dispositions Ledger

### Round 1 — 1bd2c81

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Pinned WezTerm release tag is not a fetchable release (missing short-SHA suffix) | HIGH | Task 1: went a layer deeper than the suggested SHA-suffix fix — live verification during replanning (`api.github.com/repos/wez/wezterm/releases`, `.../releases/tags/nightly`) showed NEITHER `20260604-145453` NOR `20260604-145453-eeb80972` has ever existed as a real release, and no dated release ever shipped an `Ubuntu24.04` asset. Switched the provisioning target to `WEZTERM_TARGET=nightly` (this project's own already-established default, P6-D09/06-06) with the cache key derived from `tools/bootstrap-wezterm.sh`'s own `latest_nightly_datestamp` (falling back to today's UTC date). Confirmed live: the `nightly` `Ubuntu24.04.tar.xz` asset HEAD-requests `200`. |
| Blanket rejection of any `SKIPPED (reason=` line conflicts with the intentional macOS-only Tier-1 self-skip on Linux | HIGH | Task 1: created `tools/ci-classify-e2e-output.sh`, a dedicated classification script with an explicit allowlist for the ONE expected label (`keys-siblings macOS bundle-symlink contract`) and a required-`LIVE-ASSERTED` check for the four gate-bound Tier 2/3 labels — reused identically by the workflow (Task 1), the local dry-run (Task 2), and the remote-log check (Task 3). |
| Task ordering reverses D-04 (WezTerm provisioned/verified before `make test`) | HIGH | Task 1: reordered `ci.yml` so `Install Lua toolchain` -> `make test` run FIRST, strictly before ANY WezTerm-related step (resolve/cache/provision/verify) or `make build`/`make e2e` — verified by Task 1's own `<verify>` line-order assertions. |
| Task 2/3 verification can false-green (semicolon+echo swallows non-zero exit codes; no explicit `CONCLUSION == success` assertion; poll-loop exhaustion never fails) | HIGH | Task 2 and Task 3: rewrote every verify block to use explicit `test`/`\|\| { ...; exit 1; }` assertions on every command's real exit code — no bare `cmd; echo "$?"` left anywhere. Task 3 explicitly asserts `conclusion == success`, asserts the poll loop found a run before checking status, and fails loudly if 45 polls elapse without `completed`. |
| Task 3's remote verification doesn't correlate to the pushed commit (uses "latest run on main") | HIGH | Task 3: captures `PUSHED_SHA="$(git rev-parse HEAD)"` immediately before `git push`, polls filtering `gh run list`'s `headSha` field for an exact match, and re-confirms `headSha == PUSHED_SHA` via `gh run view` before trusting that run's `conclusion`. |
| Plan never exercises the skip gate's negative path (unexpected skip should FAIL; expected skip should still PASS) | MEDIUM | Task 2: added an explicit negative-path proof — two synthetic log fixtures (one expected-skip-only, one with an injected unexpected skip) run through `tools/ci-classify-e2e-output.sh`, asserting the PASS and FAIL directions both classify correctly. Task 1's own `<verify>` also runs this same two-fixture check on the freshly created script. |
| Threat model overstates that action-tag pinning prevents silent replacement; Task 1's workflow validation is grep/yq-only, not `actionlint` | MEDIUM | Threat model: corrected T-06.8-02's wording to state tags are mutable and that SHA-pinning is an accepted, not-yet-done future hardening (consistent with `release.yml`'s own header, which says the same about itself) — disposition changed from `mitigate` to `accept` to match the honest claim. Task 1's `<verify>` now installs (via `go install`/`brew`, both available in this environment — confirmed live) and runs `actionlint .github/workflows/ci.yml`, with a documented structural-check-only fallback if neither installer is available. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none) | Every HIGH and MEDIUM finding from this round was incorporated above; nothing was rejected or deferred. |

### Round 2 — 5acc46e

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| The Round-1 fix for the unfetchable pin (switching to `nightly`) itself abandons the LOCKED `D-01` decision and contradicts `06.8-CONTEXT.md:20-28` + `ROADMAP.md:215-224`'s "pinned" wording; a date-based cache key does not make the artifact deterministic | HIGH | Resolved via the option-2 path (formal decision correction, not a technical patch): `06.8-CONTEXT.md`'s D-01 is now marked `(corrected)` with an inline **Correction note** explaining that NO fetchable, immutable dated `wez/wezterm` release with the needed `Ubuntu24.04` asset exists — live-verified independently in both review cycles — so "pinned" was never achievable as originally worded. `.planning/ROADMAP.md`'s Phase 06.8 plan-list bullet is updated to match. The plan's `must_haves`, `<objective>`, Task 1's `<read_first>`/`<action>`, and the threat model (T-06.8-03) now all state the PRECISE guarantee — reproducible-within-a-UTC-day, never a cryptographic pin — instead of re-asserting the "deterministic" language this reviewer twice correctly rejected. This is the honest, durable fix the reviewer's own Suggestions list preferred ("do not treat a cache key as artifact pinning... explicitly revise CONTEXT.md and ROADMAP.md with user approval"). |
| The negative fixture only proves "missing a required live label", not "unexpected skip after all live markers present" — a real path `scene_motd_race_e2e_test.lua:84-96` can produce (mux gate LIVE-ASSERTED, then a separate SKIPPED if bash is absent) | MEDIUM | Task 1 Part B (`tests/ci_classify_test.lua`) adds fixture 3: all 4 required `LIVE-ASSERTED` labels present, PLUS an extra unexpected `SKIPPED` line for an ALREADY-live label, mirroring `scene_motd_race`'s real dual-emit shape exactly — asserted to classify FAIL. Verified for real (not just described) during planning: the existing classify logic already rejects this case correctly (the unexpected-skip check scans the whole log independently of the live-label check), so this closes a testing gap, not a logic bug. |
| `tools/ci-classify-e2e-output.sh` has no durable regression test discovered by `make test` — only one-time plan-verify fixtures exist | MEDIUM | Task 1 Part B creates `tests/ci_classify_test.lua`, auto-discovered by `tools/run-tests.sh`'s existing `tests/`+`cli/`+`config/` `*_test.lua` glob (no registration step needed — confirmed by reading `run-tests.sh`), following `tests/cli/bootstrap_update_test.lua`'s established real-subprocess-assertion idiom. Exercises the full 5-fixture discrimination matrix permanently under `make test`, not just Task 1/2's one-time `<verify>` blocks. |
| The expected Linux self-skip marker is tolerated but never explicitly REQUIRED/asserted present exactly once | MEDIUM | `tools/ci-classify-e2e-output.sh`'s Part A now runs an explicit `expected_count -ne 1` check as its FIRST failure condition (guarded with `\|\| true` inside the command substitution so a zero-match `grep -c` never trips `set -e`); `tests/ci_classify_test.lua` fixtures 4 (absent) and 5 (duplicated) both assert this fails correctly. |
| `actionlint` remains fail-open — proceeds with structural-only checks if unavailable | MEDIUM | Task 1's `<verify>` now pins `go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12` (confirmed installable live during this replanning session) with a `brew install actionlint` fallback path, and HARD-FAILS (`exit 1`) if neither installer succeeds — the prior "falling back to structural checks only" silent-success branch is removed entirely. |
| The proposed YAML embeds nightly-fallback policy (the `latest_nightly_datestamp` sourcing + UTC-date fallback) and a duplicated mux-discovery algorithm (mirroring `mux.lua`'s `resolve_mux_server`), contradicting the locked thin-glue boundary cited by the reviewer at `06.8-CONTEXT.md:120-124` (now `06.8-CONTEXT.md:149-153` — the D-01 correction note added above it shifted every subsequent line) | MEDIUM | Confirmed the citation is ACCURATE by reading that exact section (`### Established Patterns` — "no bash logic inlined in the YAML beyond invoking the existing `tools/`/`make` entry points"). Extracted both into new focused scripts: `tools/ci-resolve-wezterm-cache-key.sh` (Part C) and `tools/ci-verify-wezterm-mux.sh` (Part D); `ci.yml`'s corresponding steps now just invoke them. Task 1's `<verify>` asserts NEITHER `latest_nightly_datestamp` NOR `resolve_mux_server`/`readlink -f` appears inline in the workflow file anymore. |
| D-05 is cited for the built-binary (`dist/wez`) convention, but D-05 actually covers branch protection — the real basis is CONTEXT.md's unnumbered "Claude's Discretion" section | LOW | Corrected every WEZ_BIN/binary-choice citation in `must_haves`, `<objective>`, Task 1's `<action>`/`<acceptance_criteria>`, and Task 2's `<name>` to cite "CONTEXT.md's Claude's Discretion section", not D-05. D-05 now appears ONLY next to genuine branch-protection statements, verified by re-reading every remaining D-05 occurrence in the revised plan. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| (none) | Every HIGH, MEDIUM, and LOW finding from cycle 2 was incorporated above; nothing was rejected or deferred. |
</review_dispositions>

<output>
Create `.planning/phases/06.8-e2e-linux-ci-gate-phased-ci-wire-the-deterministic-tiers-tie/06.8-01-SUMMARY.md` when done.
</output>