---
phase: 06.8
reviewers: [opencode]
reviewed_at: 2026-09-08T18:34:00Z
plans_reviewed: [06.8-01-PLAN.md]
models:
  opencode: "openai/gpt-5.6-sol (reasoning=high)"
model_sources:
  opencode: "pinned"
cycles: 2
---

# Cross-AI Plan Review — Phase 06.8

<!-- gsd:plan-revision-conflicts:begin -->
## Plan-Revision Conflicts
<!-- gsd:plan-revision-conflicts:end -->

## Cycle 2 — OpenCode Review (2026-09-08T18:34:00Z)

Using `ai-kit-spec-review-checklist` to audit the revised GSD plan against its source files and the seven cycle-1 findings.
Using `graphify` to ground codebase relationships before checking the plan's factual and sequencing claims.
The revised plan is present on disk and is a GSD `plan` archetype. No CodeGraph or existing Graphify index is available, so the review is grounded directly against the locked phase context, bootstrap implementation, E2E skip paths, build targets, and workflow conventions.

## 06.8-01

### Summary

The revision fully fixes four cycle-1 findings and partially fixes three. Ordering, expected Linux skip handling, fail-closed exit assertions, and pushed-SHA correlation are now sound. The principal blocker is the replacement of the locked pinned WezTerm target with a mutable nightly artifact: this makes provisioning fetchable but contradicts the phase context and roadmap, and the date-based cache key does not make the downloaded artifact deterministic. Overall risk remains **HIGH** until D-01 is reconciled.

### Strengths

- Unit tests are now correctly ordered before every WezTerm operation and E2E execution at `.planning/phases/06.8-e2e-linux-ci-gate-phased-ci-wire-the-deterministic-tiers-tie/06.8-01-PLAN.md:195-240`, matching the fail-fast requirement in `06.8-CONTEXT.md:42-47`.
- The expected Linux skip label matches the actual macOS-only gate at `tests/e2e/tier1/keys_siblings_e2e_test.lua:78-97`, while the four required live labels match their source gates at `tests/e2e/tier2/scene_reuse_e2e_test.lua:74-84`, `tests/e2e/tier2/scene_newtab_e2e_test.lua:83-91`, `tests/e2e/tier2/scene_motd_race_e2e_test.lua:81-87`, and `tests/e2e/tier3/registration_e2e_test.lua:141-160`.
- The workflow correctly preserves the E2E runner boundary through `make e2e`; `tools/run-e2e.sh:87-106` already aggregates every case and exits nonzero on any test-file failure.
- `WEZ_BIN=./dist/wez` is correct because `tools/build.sh:67-69` defines that exact output. The stale documentation currently names a nonexistent platform-specific path at `tests/e2e/README.md:19-22`.
- Task 3 now captures, filters by, and re-confirms the pushed SHA before accepting a successful run at `.planning/phases/06.8-e2e-linux-ci-gate-phased-ci-wire-the-deterministic-tiers-tie/06.8-01-PLAN.md:374-394` and `:419-465`.
- The threat model now accurately acknowledges mutable action tags and points to full-SHA pinning as future hardening at `.planning/phases/06.8-e2e-linux-ci-gate-phased-ci-wire-the-deterministic-tiers-tie/06.8-01-PLAN.md:493-501`, consistent with `.github/workflows/release.yml:55-56`.

### Concerns

- **HIGH: The fetchability fix abandons the locked deterministic-pin requirement.** Context D-01 expressly selects `WEZTERM_TARGET=pinned` because a moving nightly can change outcomes without a repository change (`06.8-CONTEXT.md:20-28`), and the roadmap still promises pinned provisioning (`.planning/ROADMAP.md:215-224`). The revised plan instead downloads the literal rolling `nightly` tag at `.planning/phases/06.8-e2e-linux-ci-gate-phased-ci-wire-the-deterministic-tiers-tie/06.8-01-PLAN.md:218-225`. The bootstrap emits literal `nightly` on this path (`tools/bootstrap-wezterm.sh:246-265`), and the download URL is constructed from that mutable tag (`tools/bootstrap-wezterm.sh:403-419`, `tools/lib/wezterm-release.sh:129-141`). A date-based cache key does not bind a cold or cache-miss download to particular bytes. The plan itself acknowledges this residual mutability at `06.8-01-PLAN.md:496-497`.
- **MEDIUM: The negative fixture does not isolate unexpected-skip detection.** The failing fixture replaces a required `LIVE-ASSERTED` marker with `SKIPPED`, so it fails both the unexpected-skip rule and the missing-live rule (`06.8-01-PLAN.md:318-325`). An actual secondary skip can contain both markers: the MOTD test first emits its mux `LIVE-ASSERTED`, then separately emits `SKIPPED` when Bash is unavailable (`tests/e2e/tier2/scene_motd_race_e2e_test.lua:84-96`). The proposed fixtures do not prove that this extra-skip-only case fails.
- **MEDIUM: The classifier is not covered by a durable repository test.** The plan calls it "unit-tested" at `06.8-01-PLAN.md:26`, but the declared files contain no test artifact (`06.8-01-PLAN.md:7-10`). Synthetic fixtures exist only in one-time plan verification. Future `make test` runs merely apply `bash -n` and advisory shellcheck to shell scripts (`tools/run-tests.sh:103-133`), so an accidental semantic regression in the classifier could validate itself in CI.
- **MEDIUM: Self-skip presence is tolerated but not explicitly asserted.** The classifier removes the expected skip from consideration and requires only the four live labels (`06.8-01-PLAN.md:165-178`). The phase goal specifically requires self-skip verification (`.planning/ROADMAP.md:215-218`; `docs/prds/e2e-testing-battery-v1.0-prd.md:242-246`). Requiring the expected Linux skip marker exactly once would make that evidence explicit.
- **MEDIUM: `actionlint` remains fail-open.** The verifier attempts to install and run it but accepts structural checks when installation is unavailable (`06.8-01-PLAN.md:257-273`). That is better than cycle 1, but it does not guarantee semantic workflow validation before push. The existing workflow treats actionlint as part of correctness verification (`.github/workflows/release.yml:19-21`).
- **MEDIUM: The proposed YAML contains policy and duplicated discovery logic despite the locked thin-glue boundary.** Context requires no inline Bash logic beyond invoking existing tools or Make targets (`06.8-CONTEXT.md:120-124`). The plan nevertheless places nightly fallback policy and a duplicated mux-discovery algorithm directly in YAML (`06.8-01-PLAN.md:208-231`) while simultaneously describing the workflow as pure glue (`06.8-01-PLAN.md:182-189`).
- **LOW: D-05 is used for two unrelated decisions.** The plan attributes the built-binary convention to D-05 at `06.8-01-PLAN.md:25`, `:66`, and `:242`, but context D-05 concerns branch protection (`06.8-CONTEXT.md:49-55`). The binary choice is in the unnumbered discretion section at `06.8-CONTEXT.md:62-65`.

### Cycle-1 Findings Resolution

1. **PARTIALLY RESOLVED: pinned target not fetchable.** The nonexistent pin is no longer downloaded, and `nightly` follows the bootstrap's valid fetch path (`06.8-01-PLAN.md:221-225`; `tools/bootstrap-wezterm.sh:250-265`). However, this resolves fetchability by dropping the pinned behavior required by `06.8-CONTEXT.md:20-28` and `.planning/ROADMAP.md:224`; it does not provide a fetchable immutable pin.
2. **RESOLVED: expected Linux self-skip rejected.** The classifier now explicitly tolerates only `keys-siblings macOS bundle-symlink contract` while requiring the four gate-bound live labels (`06.8-01-PLAN.md:165-178`). That expected label is exactly what the Linux path emits at `tests/e2e/tier1/keys_siblings_e2e_test.lua:94-97`.
3. **RESOLVED: fail-fast ordering reversed.** The ordered steps now run toolchain setup and `make test` before cache resolution, provisioning, mux checks, build, and E2E (`06.8-01-PLAN.md:195-240`), satisfying `06.8-CONTEXT.md:42-47`.
4. **RESOLVED: Task 2/3 can false-green.** Task 2 captures and asserts E2E and classifier statuses (`06.8-01-PLAN.md:312-329`, `:343-349`). Task 3 fails on missing or exhausted polling, SHA mismatch, non-success conclusion, and classifier rejection (`06.8-01-PLAN.md:419-471`).
5. **RESOLVED: remote run not correlated to pushed commit.** Task 3 captures `PUSHED_SHA`, filters run results by exact `headSha`, and re-confirms it through `gh run view` before accepting the conclusion (`06.8-01-PLAN.md:374-394`, `:420-455`).
6. **PARTIALLY RESOLVED: skip gate negative path unproven.** The pass/fail fixtures establish the overall expected-skip-versus-required-self-skip result (`06.8-01-PLAN.md:318-325`), but the failing fixture also omits a required live marker. It therefore does not independently prove rejection of an extra unexpected skip after all four live markers, a real path demonstrated by `tests/e2e/tier2/scene_motd_race_e2e_test.lua:84-96`.
7. **PARTIALLY RESOLVED: action security overstated and validation too weak.** The security wording is fully corrected and now admits tag mutability (`06.8-01-PLAN.md:493-501`). Actionlint was added, but the verifier may still proceed without running it (`06.8-01-PLAN.md:257-273`).

### Suggestions

- Either preserve D-01 with a genuinely immutable, fetchable artifact, or explicitly revise `06.8-CONTEXT.md` and `.planning/ROADMAP.md` with user approval before execution. Do not treat a cache key as artifact pinning.
- Add a committed classifier regression test discovered by `make test`.
- Use three independent fixtures: expected skip plus all live markers passes; all live markers plus an extra unexpected skip fails; a missing live marker with no skip fails.
- Require the expected Linux platform skip marker in the remote log, preferably exactly once.
- Make actionlint mandatory before push, with a pinned installation version and no structural-only success fallback.
- Move nightly identity resolution and mux precondition checks into a focused `tools/` script, or explicitly approve those YAML exceptions in context.
- Correct the built-binary decision references so D-05 refers only to branch protection.

### Risk Assessment

**HIGH.** The revised plan is substantially safer, but it still contradicts the locked provisioning decision and cannot guarantee repeatability across cold/cache-miss runs because `nightly` remains mutable. Once that decision is reconciled, the remaining concerns are medium-risk verification and maintainability gaps rather than blockers.

### Status: Issues Found — fix and re-invoke

---

## Consensus Summary (Cycle 2)

Only one reviewer (OpenCode / gpt-5.6-sol) ran this cycle, so there is no cross-reviewer consensus to synthesize. The single reviewer produced a source-grounded review (citing `file:line` evidence throughout) confirming that 4 of the 7 cycle-1 findings are now fully resolved, while 3 are only partially resolved. One new HIGH-severity concern was raised: the fix for the unfetchable pin swapped it for a mutable `nightly` target, which contradicts the locked D-01 decision and the roadmap's "pinned+cached" promise. Six MEDIUM/LOW concerns remain or were newly surfaced.

### Agreed Strengths
Not applicable — single reviewer.

### Agreed Concerns
Not applicable — single reviewer. See the HIGH/MEDIUM/LOW concerns listed above under the Cycle 2 OpenCode Review.

### Divergent Views
Not applicable — single reviewer.

---

## Cycle 1 — OpenCode Review (2026-09-08T17:30:00Z, superseded — retained for history)

Using `ai-kit-spec-review-checklist` to audit the GSD plan against the repository's actual workflow, bootstrap, build, and E2E contracts.
The source confirms a material ordering contradiction: the plan promises unit-test fail-fast before WezTerm provisioning, but its required step order provisions and verifies WezTerm first (`06.8-01-PLAN.md:142-167`). I'm tracing the runner and tier outputs next to determine whether the proposed log gate and live-run verification are equally reliable.
A second blocking issue is confirmed: the global `SKIPPED (reason=` prohibition would reject an expected Linux run. The battery deliberately emits that marker for the macOS-only Tier 1 sibling test on non-macOS hosts, so the proposed CI gate and both local/remote acceptance checks cannot pass as written.

### Summary

The plan is well-grounded in existing scripts and correctly reuses the repository's build and E2E entry points. However, it cannot achieve a green CI gate as written: the selected pinned WezTerm value is not a downloadable release, and the global skip check rejects an intentional Linux skip. Several verification commands can also report success after underlying failures.

### Strengths

- The proposed `WEZ_BIN=./dist/wez` correction is accurate. `tools/build.sh:67-69` defines `dist/wez` as its output, while the current example incorrectly names `dist/wez-linux-x86_64` at `tests/e2e/README.md:19-22`.
- Reusing `make test`, `make build`, and `make e2e` preserves the repository's thin-glue boundary established in `Makefile:27-43` and `.github/workflows/release.yml:9-12`.
- The mux discovery design is grounded correctly. `tests/e2e/lib/mux.lua:72-89` checks `PATH` and then the resolved WezTerm binary's sibling directory.
- Requiring live markers is directionally sound. `tests/e2e/lib/skip.lua:44-58` clearly distinguishes `SKIPPED (reason=...)` from `LIVE-ASSERTED (...)`.
- Read-only permissions and a job timeout appropriately limit the impact of running PR-controlled code (`06.8-01-PLAN.md:131-140`).

### Concerns

- **HIGH: The pinned WezTerm provisioning target is not fetchable.** The plan requires `WEZTERM_TARGET=pinned` at `06.8-01-PLAN.md:150-156`. The pin is `20260604-145453` at `tools/lib/wezterm-release.sh:24-30`, but the same file says release URLs require the full tag including its short SHA at `tools/lib/wezterm-release.sh:129-141`. The audited version is actually `20260604-145453-eeb80972` at `.planning/decisions/wezterm-cli-surface.md:3`. A cold runner passes the incomplete value through `select_release` and directly into the download URL (`tools/bootstrap-wezterm.sh:250-260`, `tools/bootstrap-wezterm.sh:403-419`). Live GitHub API checks also returned 404 for both candidate 2026 tags. Existing evidence only proves reuse of a locally installed build, not clean installation. The first uncached CI run will fail before tests start.
- **HIGH: The global skip prohibition rejects an expected Linux result.** The plan rejects every `SKIPPED (reason=` line at `06.8-01-PLAN.md:170-178`, and repeats that assertion in Tasks 2 and 3. On Linux, `tests/e2e/tier1/keys_siblings_e2e_test.lua:78-97` intentionally emits exactly that marker for its deferred macOS-only layer and exits successfully. A direct execution during review reproduced the marker. Consequently, the proposed local proof and remote CI run cannot satisfy `no_self_skip`.
- **HIGH: The required fail-fast ordering is reversed.** Context D-04 requires unit tests before spending WezTerm provisioning time (`06.8-CONTEXT.md:42-47`), but the plan provisions and verifies WezTerm in steps 5-6 and does not run unit tests until step 8 (`06.8-01-PLAN.md:142-167`). The plan nevertheless claims unit failure avoids mux provisioning. `make test` only requires Lua 5.4 (`tools/run-tests.sh:28-31`), so the toolchain and unit tests can run before WezTerm provisioning.
- **HIGH: Task 2 and Task 3 verification can false-green.** Task 2 separates commands with semicolons and follows each failure-prone command with `echo` (`06.8-01-PLAN.md:257-259`), so failed tests, builds, or E2E runs do not determine the final shell status. Task 3 prints `CONCLUSION` but never asserts that it equals `success`, and it does not fail when the polling loop exhausts (`06.8-01-PLAN.md:307-309`). A failed run containing `LIVE-ASSERTED` and no skip marker can therefore pass the automated verifier.
- **HIGH: The remote verification is not correlated to the pushed commit.** Task 3 always selects the latest run on `main` (`06.8-01-PLAN.md:279-286`, `06.8-01-PLAN.md:301-308`). It never captures the pushed SHA or compares it with the run's `headSha`. A concurrent push or stale completed run could be accepted as evidence for this plan.
- **MEDIUM: The plan does not prove the skip gate's negative path.** The PRD requires that self-skip behavior be verified and that only real failures block (`docs/prds/e2e-testing-battery-v1.0-prd.md:242-246`). Tasks 2 and 3 exercise only a normal successful battery. Structural grep confirms that strings exist, but does not prove that an applicable Tier 2/3 skip fails while an expected platform skip remains allowed.
- **MEDIUM: Action tag security is overstated and workflow validation is too weak.** The threat model claims version tags prevent silent action replacement (`06.8-01-PLAN.md:327-331`), but tags are mutable; the existing workflow acknowledges that full commit-SHA pinning remains necessary (`.github/workflows/release.yml:55-56`). Task 1 also uses only `yq` and text checks (`06.8-01-PLAN.md:207-209`) despite `actionlint` being the established semantic workflow validator (`.github/workflows/release.yml:19-21`).

### Suggestions

- Establish a genuinely downloadable, immutable WezTerm artifact before implementing CI. Add a clean-prefix bootstrap probe and update the pin/bootstrap logic as a prerequisite.
- Require the specific expected Tier 2 and Tier 3 `LIVE-ASSERTED` labels, and reject `SKIPPED` only for those labels. Preserve the intentional macOS-only skip.
- Reorder the job to toolchain setup, `make test`, WezTerm cache/provisioning, mux verification, `make build`, then `make e2e`.
- Make every verifier fail closed with `set -euo pipefail`, explicit exit-code assertions, a completed-status assertion, and `test "$CONCLUSION" = success`.
- Capture `PUSHED_SHA="$(git rev-parse HEAD)"` and select the Actions run whose `headSha` exactly matches it.
- Add synthetic pass/fail fixtures for the marker-classification logic and run `actionlint` before pushing.
- Pin `actions/checkout` and `actions/cache` by full commit SHA, or accurately document mutable tags as accepted residual risk.

### Risk Assessment

**HIGH.** Two independent defects make the planned CI run fail deterministically: the invalid cold-install pin and the expected Linux `SKIPPED` marker. The verification commands can then obscure other failures or associate evidence with the wrong commit. The plan needs revision before execution.

### Status: Issues Found — fix and re-invoke

---

## Consensus Summary (Cycle 1, superseded)

Only one reviewer (OpenCode / gpt-5.6-sol) ran this cycle, so there is no cross-reviewer consensus to synthesize. The single reviewer produced a source-grounded review (citing `file:line` evidence throughout, including a live reproduction of the Linux self-skip marker) and found five HIGH-severity, source-verified defects in plan `06.8-01`, plus two MEDIUM concerns.

### Agreed Strengths
Not applicable — single reviewer.

### Agreed Concerns
Not applicable — single reviewer. See the HIGH/MEDIUM concerns listed above under the Cycle 1 OpenCode Review.

### Divergent Views
Not applicable — single reviewer.
