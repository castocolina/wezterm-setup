---
phase: 00-spikes-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .tmp/probes/phase-0/02-cwd-wezterm-pane-env.md
  - .tmp/probes/phase-0/03-cwd-osc7.md
  - .tmp/probes/phase-0/04-cwd-cli-introspection.md
  - .tmp/h03-cwd-mechanism/repro.md
  - .tmp/h03-cwd-mechanism/run.sh
  - .tmp/h03-cwd-mechanism/expected.txt
  - .tmp/h03-cwd-mechanism/actual.txt
  - .planning/decisions/cwd-mechanism.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "The CWD inheritance candidates ($WEZTERM_PANE env var, OSC 7 escape sequence, wezterm cli introspection) have each been probed for whether they survive a real pane split on Linux"
    - "A standalone experiment script exists that drives a real WezTerm session via wezterm cli and observably proves which mechanism survives pane splits on Linux"
    - "macOS proof is explicitly recorded as deferred to the batched Mac pass (D-04/D-05), not silently skipped"
    - "A decision record exists naming the chosen CWD inheritance mechanism, citing the captured Linux evidence, and stating the macOS verification checkpoint that could trigger reconsideration"
  artifacts:
    - path: ".tmp/h03-cwd-mechanism/run.sh"
      provides: "Standalone experiment script driving a real WezTerm session via wezterm cli to test pane-split CWD survival"
    - path: ".tmp/h03-cwd-mechanism/actual.txt"
      provides: "Captured evidence of which mechanism(s) survived a real pane-split test on Linux"
    - path: ".planning/decisions/cwd-mechanism.md"
      provides: "Committed decision record: chosen CWD mechanism + Linux evidence + explicit macOS-deferred verification checkpoint"
      contains: "Decision:"
  key_links:
    - from: ".planning/decisions/cwd-mechanism.md"
      to: ".tmp/h03-cwd-mechanism/actual.txt"
      via: "decision record cites the captured pane-split survival evidence as justification"
      pattern: "h03-cwd-mechanism"
    - from: ".planning/decisions/cwd-mechanism.md"
      to: "macOS batched verification pass"
      via: "explicit deferred-checkpoint statement per D-04/D-05"
      pattern: "macOS|Mac pass|deferred"
</must_haves>
---

<objective>
Resolve the open "CWD inheritance mechanism" decision (`$WEZTERM_PANE` env var vs OSC 7 escape
sequence vs `wezterm cli` introspection) with a real, standalone, pane-split-driving experiment on
Linux, per R6 (probe each candidate's external-system shape first) then R1 (hypothesis-grade
script proving observable survival), and record the outcome with macOS explicitly deferred per
D-04/D-05.

Purpose: FOUND-01 ("New tabs and panes open in the cwd of the active pane") is the cornerstone of
Phase 1 — the mechanism choice made here is what Phase 1 scaffolds directly into `cwd.lua`
(per `docs/agent-iteration.md` file-basename convention).

Output: Three probe artifacts (one per candidate mechanism) recording their real external shape,
one hypothesis-grade `.tmp/h03-cwd-mechanism/` directory with a green pane-split-survival
experiment script and captured evidence, and a committed `.planning/decisions/cwd-mechanism.md`
naming the winner with Linux evidence and an explicit macOS verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-spikes-alignment/00-CONTEXT.md
@docs/agent-iteration.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Probe each CWD-mechanism candidate's real shape on Linux</name>
  <files>.tmp/probes/phase-0/02-cwd-wezterm-pane-env.md, .tmp/probes/phase-0/03-cwd-osc7.md, .tmp/probes/phase-0/04-cwd-cli-introspection.md</files>
  <read_first>
    @docs/agent-iteration.md (R6: six-field probe format — assumption, command/snippet,
    exit-code/result, output, verdict, design decision; "any task that depends on the
    behavior of a WezTerm CLI command, an escape sequence, or an OS-level contract requires
    a probe artefact ... before the task is executed")
    @.planning/phases/00-spikes-alignment/00-CONTEXT.md (the three CWD candidates named in
    the validation targets: `$WEZTERM_PANE` env var, OSC 7 escape sequence, `wezterm cli`
    introspection — D-04: Linux-first, macOS batched later)
  </read_first>
  <action>
    Create `.tmp/probes/phase-0/` if absent (it may already exist from Plan 01's probe —
    do not overwrite that file). Write three separate probe artifacts, each following the
    exact six-field R6 format on one screen:
    `02-cwd-wezterm-pane-env.md` — Assumption: "`$WEZTERM_PANE` is set in the pane's shell
    environment and identifies the originating pane uniquely enough to look up its CWD via
    `wezterm cli list --format json`"; Command: the literal shell snippet that prints
    `$WEZTERM_PANE` from an active pane and cross-references it against the `pane_id` field
    in `wezterm cli list --format json` output; capture the real exit code and output;
    Verdict + design decision based on what is actually observed (does the env var survive
    into a freshly-split pane's shell, and does it map to a `cwd` field in the JSON?).
    `03-cwd-osc7.md` — Assumption: "WezTerm emits/honors OSC 7 (`\033]7;file://HOST/PATH\033\\`)
    to communicate the current working directory, and a newly split pane inherits or can read
    the parent's last-reported OSC 7 path"; Command: the literal escape-sequence probe (e.g.
    printing OSC 7 from the shell prompt hook and checking whether `wezterm cli list --format
    json` surfaces a `cwd` field derived from it); capture real output; Verdict + design
    decision based on what is observed.
    `04-cwd-cli-introspection.md` — Assumption: "`wezterm cli list --format json` (or a more
    specific subcommand such as `get-pane-direction` referenced in ROADMAP) exposes a `cwd`
    field per pane that can be read by a newly-spawned pane to inherit its parent's directory";
    Command: the literal `wezterm cli list --format json` invocation with the JSON path/field
    that would carry CWD info; capture real output; Verdict + design decision. For each probe,
    RUN the command for real against the active WezTerm session before writing the verdict —
    do not guess any of the three shapes. If a candidate's assumption fails outright, record
    `fails` and note it; that is a valid, useful probe outcome that narrows Task 2's scope.
  </action>
  <acceptance_criteria>
    All three probe files exist under `.tmp/probes/phase-0/`, each containing the six R6
    fields (assumption, command, exit-code/result, output, verdict, design decision) with a
    verdict of `holds`, `fails`, or `partial-with-workaround` based on a command that was
    actually executed against the live WezTerm session (the captured "Output" field contains
    real command output, not a hypothetical example).
  </acceptance_criteria>
  <verify>
    <automated>for f in 02-cwd-wezterm-pane-env 03-cwd-osc7 04-cwd-cli-introspection; do rg -c "^(Assumption|Command|Exit|Output|Verdict|Design decision)" ".tmp/probes/phase-0/$f.md" | rg -q "^[6-9]$" && echo "$f: ok" || echo "$f: MISSING FIELDS"; done</automated>
  </verify>
  <done>All three CWD-candidate probes exist with six R6 fields each and a real recorded verdict from a command executed against the live WezTerm session.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the pane-split CWD-survival experiment script (Linux)</name>
  <files>.tmp/h03-cwd-mechanism/repro.md, .tmp/h03-cwd-mechanism/run.sh, .tmp/h03-cwd-mechanism/expected.txt, .tmp/h03-cwd-mechanism/actual.txt</files>
  <read_first>
    @.tmp/probes/phase-0/02-cwd-wezterm-pane-env.md (probe verdict — narrows which candidate(s) are viable)
    @.tmp/probes/phase-0/03-cwd-osc7.md (probe verdict)
    @.tmp/probes/phase-0/04-cwd-cli-introspection.md (probe verdict)
    @docs/agent-iteration.md (R1: hypothesis dir layout — repro.md + run script + expected.txt + actual.txt; the run script is the source of truth, the manual repro is documentation)
    @.planning/phases/00-spikes-alignment/00-CONTEXT.md (D-04/D-05: Linux-first, macOS verification deferred and batched)
  </read_first>
  <action>
    Create `.tmp/h03-cwd-mechanism/`. Write `repro.md` describing the observable behavior under
    test: "a new pane split from a parent pane whose shell is in directory X opens with its own
    cwd also at X." Document the manual repro steps (cd to a known temp dir in pane A, split,
    observe pane B's cwd) and the expected observable outcome. Write `expected.txt` with the
    structured expectation (the split pane's reported `cwd` equals the parent pane's `cwd`, exit 0).
    Write `run.sh` as a standalone bash experiment that: (a) records a known parent cwd (e.g. a
    freshly created temp dir), (b) drives a real pane split via `wezterm cli split-pane`, (c) reads
    the new pane's cwd using whichever mechanism(s) the Task 1 probes marked `holds` (e.g. parse
    the `cwd` field for the new `pane_id` from `wezterm cli list --format json`), (d) asserts the
    new pane's cwd matches the parent's and writes the comparison result to `actual.txt`. The run
    script is the gate (R1): iterate `run.sh` until it exits 0 with a matching cwd and capture that
    green run into `actual.txt`. If MORE than one mechanism survives, record each mechanism's result
    in `actual.txt` so the decision in Task 3 can pick on simplicity. macOS is NOT tested here —
    note in `repro.md` that the same `run.sh` must be re-run on macOS during the batched Mac pass
    (D-04/D-05) before FOUND-01 ships.
  </action>
  <acceptance_criteria>
    Running `bash .tmp/h03-cwd-mechanism/run.sh` on Linux exits 0 and `.tmp/h03-cwd-mechanism/actual.txt`
    records, for at least one candidate mechanism, that a freshly split pane's reported cwd equals the
    parent pane's cwd (the survival assertion). `repro.md` contains the literal string "macOS" noting
    the deferred re-run requirement. `expected.txt` and `actual.txt` describe the same observable shape.
  </acceptance_criteria>
  <verify>
    <automated>bash .tmp/h03-cwd-mechanism/run.sh; echo "exit=$?"; bat .tmp/h03-cwd-mechanism/actual.txt; rg -q "macOS" .tmp/h03-cwd-mechanism/repro.md && echo "macos-note: ok"</automated>
  </verify>
  <done>The pane-split experiment runs green on Linux (exit 0), `actual.txt` proves at least one mechanism's cwd survives a real split, and `repro.md` records the deferred macOS re-run.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record the CWD-mechanism decision with deferred macOS checkpoint</name>
  <files>.planning/decisions/cwd-mechanism.md</files>
  <read_first>
    @.tmp/h03-cwd-mechanism/actual.txt (the captured Linux survival evidence to cite)
    @.tmp/probes/phase-0/02-cwd-wezterm-pane-env.md (candidate verdicts feeding the choice)
    @.tmp/probes/phase-0/03-cwd-osc7.md
    @.tmp/probes/phase-0/04-cwd-cli-introspection.md
    @.planning/phases/00-spikes-alignment/00-CONTEXT.md (D-05: commit on Linux evidence now; macOS distribution/behavior is a verification checkpoint that can trigger reconsideration; D-07: decision records live in `.planning/decisions/`)
  </read_first>
  <action>
    Create `.planning/decisions/` if absent. Write `.planning/decisions/cwd-mechanism.md` with: a
    "Decision:" line naming the chosen mechanism (e.g. `wezterm cli` JSON introspection of the
    `cwd` field, or OSC 7, or `$WEZTERM_PANE` lookup); an "Evidence:" section quoting the captured
    Linux survival result from `.tmp/h03-cwd-mechanism/actual.txt` (since `.tmp/` is deleted after
    promotion, the record must stand alone); a "Probe summary:" noting each candidate's verdict; a
    "macOS verification checkpoint:" section stating the SAME experiment must pass on macOS during
    the batched Mac pass before FOUND-01 ships, and that a macOS failure could trigger reconsideration
    (D-05); and a "Phase 1 implication:" line stating this mechanism is what `cwd.lua` scaffolds.
  </action>
  <acceptance_criteria>
    `.planning/decisions/cwd-mechanism.md` exists, names exactly one chosen mechanism on a
    "Decision:" line, quotes concrete captured evidence from `.tmp/h03-cwd-mechanism/actual.txt`,
    and contains a "macOS" verification-checkpoint section explicitly deferring both-platform proof
    to the batched Mac pass per D-04/D-05.
  </acceptance_criteria>
  <verify>
    <automated>rg -q "^Decision:" .planning/decisions/cwd-mechanism.md && rg -c "macOS|Mac pass|deferred" .planning/decisions/cwd-mechanism.md && rg -c "h03-cwd-mechanism" .planning/decisions/cwd-mechanism.md</automated>
  </verify>
  <done>Decision record names the chosen CWD mechanism, cites real Linux evidence, and explicitly defers macOS verification to the batched pass per D-04/D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

A decisions-and-evidence phase: it produces local experiment scripts that drive the user's own
already-running WezTerm session and a committed Markdown decision record. No shipped code, no
network exposure, no authentication surface, no untrusted-input handling. Scratch scripts are
gitignored and deleted after promotion (R5).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-05 | Tampering | `.tmp/h03-cwd-mechanism/run.sh` drives `wezterm cli split-pane` against the local session | accept | Script uses fixed, hardcoded `wezterm cli` subcommands against the user's own local session; no user-supplied command strings reach a shell; scratch artifact deleted post-promotion (R5) |
</threat_model>

<verification>
All three CWD-candidate probes record real verdicts. The pane-split experiment exits 0 on Linux
and `actual.txt` proves at least one mechanism's cwd survives a real split. The decision record
names the winner, cites the captured Linux evidence, and explicitly defers macOS verification to
the batched Mac pass (D-04/D-05).
</verification>

<success_criteria>
- ROADMAP Phase 0 Success Criterion 2 satisfied (Linux portion): "CWD inheritance mechanism is
  proven ... with a standalone experiment script" — proven on Linux now; macOS recorded as a
  deferred verification checkpoint per the user's macOS-later decision (D-04/D-05).
- `.planning/decisions/cwd-mechanism.md` is written to disk (not committed — user commits after
  reviewing `git diff --stat`, per the No-auto-commit rule).
</success_criteria>

<output>
Create `.planning/phases/00-spikes-alignment/00-02-SUMMARY.md` when done
</output>