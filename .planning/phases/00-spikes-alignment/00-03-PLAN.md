---
phase: 00-spikes-alignment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .tmp/probes/phase-0/05-wezterm-cli-help-surface.md
  - .planning/decisions/wezterm-cli-surface.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "Every subcommand enumerated by `wezterm cli --help` is catalogued with its purpose and flags"
    - "Each catalogued subcommand has a Linux availability verdict captured from a real invocation"
    - "macOS availability is present as an explicit column marked pending the batched Mac pass (D-04/D-05), not silently omitted"
    - "Known gaps (subcommands that do not exist or behave unexpectedly) are documented with workarounds — at minimum the confirmed `wezterm cli set-user-var` absence and the OSC 1337 workaround"
  artifacts:
    - path: ".planning/decisions/wezterm-cli-surface.md"
      provides: "Durable committed catalogue of the entire wezterm cli command surface with per-platform availability columns and a known-gaps section"
      contains: "set-user-var"
  key_links:
    - from: ".planning/decisions/wezterm-cli-surface.md"
      to: ".tmp/probes/phase-0/05-wezterm-cli-help-surface.md"
      via: "catalogue is built from the captured real --help enumeration recorded in the probe"
      pattern: "wezterm-cli-help-surface"
</must_haves>
---

<objective>
Produce a durable, committed catalogue of the ENTIRE `wezterm cli` command surface (per D-09 —
full sweep, not just what Phases 1-5 need), with per-subcommand flags, a Linux availability column
filled now, a macOS column marked pending the batched Mac pass, and a "Known gaps & workarounds"
section. This satisfies ROADMAP Phase 0 Success Criterion 3.

Purpose: every later phase issues `wezterm cli` calls (pane/tab color, titles, scene splits). A
single audited reference prevents the rework the bash predecessor hit (e.g. assuming a non-existent
`set-user-var` subcommand). The catalogue is the shared source of truth Phases 1-5 lean on.

Output: one probe artifact capturing the raw `wezterm cli --help` enumeration, and a committed
`.planning/decisions/wezterm-cli-surface.md` catalogue.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-spikes-alignment/00-CONTEXT.md
@docs/agent-iteration.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enumerate the full wezterm cli surface on Linux</name>
  <files>.tmp/probes/phase-0/05-wezterm-cli-help-surface.md</files>
  <read_first>
    @docs/agent-iteration.md (R6 probe format — six fields; probe captures external-system shape before it is encoded)
    @.planning/phases/00-spikes-alignment/00-CONTEXT.md (D-09: full surface sweep, bounded by what `wezterm cli --help` enumerates; D-04: Linux now, macOS later)
    @.planning/PROJECT.md (Context section — the confirmed `wezterm cli set-user-var` absence and OSC 1337 workaround that the audit must record as a known gap)
  </read_first>
  <action>
    Create `.tmp/probes/phase-0/` if absent (shared with Plans 01/02 — do not overwrite their
    probes). Run `wezterm cli --help` for real and capture the full list of subcommands. Then run
    `wezterm cli <subcommand> --help` for EVERY enumerated subcommand and capture each one's flags
    and one-line purpose. Write `05-wezterm-cli-help-surface.md` in the six-field R6 shape:
    Assumption = "`wezterm cli --help` enumerates the complete subcommand surface available in this
    WezTerm version"; Command = the literal `wezterm cli --help` plus the per-subcommand help loop;
    Exit code/Result = the captured exit status and WezTerm version (`wezterm --version`); Output =
    the captured raw enumeration (subcommand list + per-subcommand flag summaries); Verdict =
    `holds` (or note any subcommand whose `--help` errored); Design decision = "this enumeration is
    the source for the committed catalogue in Task 2." Record the exact WezTerm version so the
    catalogue is version-anchored.
  </action>
  <acceptance_criteria>
    `.tmp/probes/phase-0/05-wezterm-cli-help-surface.md` exists with the six R6 fields, records the
    real `wezterm --version`, and its Output field contains the actual `wezterm cli --help`
    subcommand list (real captured text, not a hypothetical example). At least every top-level
    subcommand printed by `wezterm cli --help` appears in the captured output.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "^(Assumption|Command|Exit|Output|Verdict|Design decision)" .tmp/probes/phase-0/05-wezterm-cli-help-surface.md; wezterm cli --help | rg -c "^\s+\w"</automated>
  </verify>
  <done>Probe captures the real full `wezterm cli` subcommand enumeration with the WezTerm version recorded and a real verdict.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the durable wezterm cli surface catalogue</name>
  <files>.planning/decisions/wezterm-cli-surface.md</files>
  <read_first>
    @.tmp/probes/phase-0/05-wezterm-cli-help-surface.md (the captured enumeration to transcribe into the catalogue)
    @.planning/phases/00-spikes-alignment/00-CONTEXT.md (D-07: decision/reference records live in `.planning/decisions/`; D-09: macOS column pending the Mac pass)
    @.planning/PROJECT.md (the `set-user-var` gap + OSC 1337 workaround to record in Known gaps)
  </read_first>
  <action>
    Create `.planning/decisions/` if absent. Write `.planning/decisions/wezterm-cli-surface.md`
    containing: a header recording the audited WezTerm version (from the probe); a table with one
    row per subcommand and columns: Subcommand, Purpose, Key flags, Linux (✓/✗/partial from the
    captured Linux verdict), macOS (literal "pending — Mac pass" placeholder per D-04/D-05); and a
    "Known gaps & workarounds" section documenting at minimum the confirmed absence of
    `wezterm cli set-user-var` and its OSC 1337 `SetUserVar` workaround (from PROJECT.md), plus any
    other subcommand the probe found missing or surprising. Note at the top that the macOS column is
    to be filled during the batched Mac pass before Phase 1 closes.
  </action>
  <acceptance_criteria>
    `.planning/decisions/wezterm-cli-surface.md` exists, contains a per-subcommand table with both a
    "Linux" and a "macOS" column (macOS values literally marked pending), records the audited
    WezTerm version, and contains a "Known gaps" section that includes the literal string
    "set-user-var" with its OSC 1337 workaround. Every top-level subcommand from the probe's
    captured enumeration appears as a table row.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "set-user-var" .planning/decisions/wezterm-cli-surface.md && rg -c "pending" .planning/decisions/wezterm-cli-surface.md && rg -q "macOS" .planning/decisions/wezterm-cli-surface.md && echo "catalogue: ok"</automated>
  </verify>
  <done>Catalogue lists every audited subcommand with Linux availability and a pending macOS column, is version-anchored, and documents known gaps (including set-user-var) with workarounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

A read-only audit phase: it runs `wezterm cli --help` (and per-subcommand `--help`) against the
local WezTerm install and writes a Markdown reference. No shipped code, no network, no auth, no
untrusted input. The `--help` invocations are non-mutating.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-06 | Information Disclosure | catalogue records local `wezterm cli` surface | accept | `--help` output is non-sensitive tool documentation; the catalogue is a committed project reference with no secrets |
</threat_model>

<verification>
The probe captures the real full `wezterm cli` enumeration with the WezTerm version. The catalogue
transcribes every subcommand with Linux availability, a pending macOS column, and a known-gaps
section covering the `set-user-var` absence and its OSC 1337 workaround.
</verification>

<success_criteria>
- ROADMAP Phase 0 Success Criterion 3 satisfied: "The full `wezterm cli` command surface is
  audited and any gaps (missing subcommands) are documented with workarounds."
- `.planning/decisions/wezterm-cli-surface.md` is written to disk (not committed — user commits
  after reviewing `git diff --stat`, per the No-auto-commit rule).
</success_criteria>

<output>
Create `.planning/phases/00-spikes-alignment/00-03-SUMMARY.md` when done
</output>
