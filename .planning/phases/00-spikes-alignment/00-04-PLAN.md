---
phase: 00-spikes-alignment
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - .planning/decisions/tab-title-format.md
  - .planning/PROJECT.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "The already-proven tab-title prefix convention (`color:title`) is documented and locked as a formal format spec — no re-proving, document-only per the CONTEXT code_context note"
    - "Every Phase 0 decision record is promoted into the PROJECT.md Key Decisions table before Phase 1 planning begins"
    - "The PROJECT.md 'Companion CLI language' Key Decision row is updated from 'Pending' to the chosen language from the cli-language decision record"
  artifacts:
    - path: ".planning/decisions/tab-title-format.md"
      provides: "Locked format spec for the tab-title color-prefix convention"
      contains: "color:title"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table updated with all four Phase 0 decisions promoted from .planning/decisions/"
      contains: "Key Decisions"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/decisions/"
      via: "Key Decisions table rows reference the four Phase 0 decision records (cli-language, cwd-mechanism, wezterm-cli-surface, tab-title-format)"
      pattern: "cli-language|cwd-mechanism|wezterm-cli-surface|tab-title-format"
</must_haves>
---

<objective>
Lock the already-proven tab-title color-prefix convention into a formal format spec (document-only
— it was proven 2026-06-07, no re-proving needed), then promote all four Phase 0 decision records
into the PROJECT.md "Key Decisions" table. This satisfies ROADMAP Phase 0 Success Criterion 4 and
closes the phase's alignment mandate.

Purpose: ROADMAP requires "All Phase 0 decisions are written to PROJECT.md Key Decisions before
Phase 1 planning begins." This plan is the promotion gate — it depends on Plans 01-03 having
produced their decision records first (declared via depends_on + wave 2).

Output: a committed `.planning/decisions/tab-title-format.md` format spec, and an updated
`.planning/PROJECT.md` Key Decisions table reflecting all Phase 0 outcomes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-spikes-alignment/00-CONTEXT.md
@docs/agent-iteration.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lock the tab-title color-prefix format spec</name>
  <files>.planning/decisions/tab-title-format.md</files>
  <read_first>
    @.planning/PROJECT.md (Validated section + Key Decisions: tab color stored in tab-title prefix `"color:title"`, proven 2026-06-07; pane-level color via OSC 1337 SetUserVar)
    @.planning/phases/00-spikes-alignment/00-CONTEXT.md (code_context: this target is DOCUMENT-AND-LOCK only, no re-proving; D-07: decision records live in `.planning/decisions/`)
  </read_first>
  <action>
    Create `.planning/decisions/` if absent. Write `.planning/decisions/tab-title-format.md`
    documenting the locked format: the tab-title string encodes color as a prefix in the form
    `color:title` (and bare `color` when no title is set); enumerate the supported color profile
    names (red, orange, yellow, green, teal, cyan, blue, navy, purple, pink — from PROJECT.md
    Validated); state the parse rule (split on the first `:`; left = color profile, right = display
    title); note WHY this mechanism is used (the tab title survives pane switches whereas pane user
    vars do not — already proven, cite PROJECT.md Key Decisions); and note the companion pane-level
    override path (OSC 1337 `SetUserVar` `WEZTERM_TAB_COLOR`). Mark this as a locked, no-re-prove
    decision (status: proven 2026-06-07).
  </action>
  <acceptance_criteria>
    `.planning/decisions/tab-title-format.md` exists, contains the literal string "color:title",
    enumerates the supported color profile names, states the first-`:`-split parse rule, and notes
    the OSC 1337 pane-level override. It records status as already-proven (no re-proving task).
  </acceptance_criteria>
  <verify>
    <automated>rg -c "color:title" .planning/decisions/tab-title-format.md && rg -c "SetUserVar|WEZTERM_TAB_COLOR" .planning/decisions/tab-title-format.md && rg -q "green" .planning/decisions/tab-title-format.md && echo "format-spec: ok"</automated>
  </verify>
  <done>Tab-title format spec is written, enumerates the parse rule and color profiles, cites the proven persistence rationale, and is marked locked/no-re-prove.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Promote all Phase 0 decisions into PROJECT.md Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    @.planning/decisions/cli-language.md (chosen CLI language — promote and flip the 'Pending' Key Decision row)
    @.planning/decisions/cwd-mechanism.md (chosen CWD mechanism — promote; note macOS-deferred checkpoint)
    @.planning/decisions/wezterm-cli-surface.md (audited surface reference — promote a pointer row)
    @.planning/decisions/tab-title-format.md (locked format spec — promote a pointer row)
    @.planning/PROJECT.md (current Key Decisions table to amend — the "Companion CLI language" row currently reads "— Pending (spike required)")
    @.planning/phases/00-spikes-alignment/00-CONTEXT.md (D-05: CLI language committed on Linux evidence now, macOS distribution is a verification checkpoint — reflect provenance honestly)
  </read_first>
  <action>
    Edit `.planning/PROJECT.md` "Key Decisions" table. Update the existing "Companion CLI language:
    Lua preferred, Python/uv fallback" row's Outcome from "Pending" to the chosen language with a
    pointer to `.planning/decisions/cli-language.md` and a note that macOS distribution is a deferred
    verification checkpoint (D-05). Add new rows for: the CWD inheritance mechanism (with the
    macOS-deferred note, pointer to `cwd-mechanism.md`); the `wezterm cli` surface audit (pointer to
    `wezterm-cli-surface.md`, note macOS column pending); and the tab-title format lock (pointer to
    `tab-title-format.md`, status proven). Do NOT delete or rewrite existing proven rows. Keep the
    table's existing column shape (Decision | Rationale | Outcome). This is the ROADMAP Criterion 4
    promotion — every Phase 0 decision must appear here before Phase 1 planning.
  </action>
  <acceptance_criteria>
    `.planning/PROJECT.md` Key Decisions table no longer contains a "Pending" outcome for the
    companion CLI language row, and contains pointer references to all four decision records
    (`cli-language`, `cwd-mechanism`, `wezterm-cli-surface`, `tab-title-format`). The CWD and CLI
    rows mention the deferred macOS verification per D-05.
  </acceptance_criteria>
  <verify>
    <automated>rg -c "cli-language|cwd-mechanism|wezterm-cli-surface|tab-title-format" .planning/PROJECT.md && ! rg -q "Companion CLI language.*Pending" .planning/PROJECT.md && echo "promotion: ok"</automated>
  </verify>
  <done>All four Phase 0 decisions are present in the PROJECT.md Key Decisions table, the CLI-language 'Pending' outcome is resolved, and macOS-deferred provenance is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

A documentation/promotion phase: it writes one Markdown format spec and edits the PROJECT.md
Key Decisions table. No shipped code, no network, no auth, no untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-07 | Tampering | edit to `.planning/PROJECT.md` | accept | Edit appends/updates Key Decisions rows only; existing proven rows are preserved; change is reviewed via `git diff --stat` before the user commits (No-auto-commit rule) |
</threat_model>

<verification>
The tab-title format spec is written with the parse rule, color profiles, and override path. The
PROJECT.md Key Decisions table is updated so all four Phase 0 decisions are promoted, the CLI
language 'Pending' state is resolved, and macOS-deferred provenance is recorded per D-05.
</verification>

<success_criteria>
- ROADMAP Phase 0 Success Criterion 4 satisfied: "All Phase 0 decisions are written to PROJECT.md
  Key Decisions before Phase 1 planning begins."
- Tab-title-prefix validation target closed as document-and-lock (no re-proving), per CONTEXT.
- All edits are written to disk (not committed — user commits after reviewing `git diff --stat`,
  per the No-auto-commit rule).
</success_criteria>

<output>
Create `.planning/phases/00-spikes-alignment/00-04-SUMMARY.md` when done
</output>
