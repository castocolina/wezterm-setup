---
phase: 06.4-user-documentation-audit-and-refactor
plan: 04
subsystem: documentation
tags: [docs-prune, drift-check, migration-doc, repro-evidence, git-rm]

# Dependency graph
requires:
  - phase: 06.4-01
    provides: durable tests/docs_drift_test.lua drift-check (documented→exists gate)
  - phase: 06.4-02
    provides: focused docs/ pages incl. docs/troubleshooting.md (salvaged doctor no-shadowing-handler nugget)
  - phase: 06.4-03
    provides: lean README hub + CONTRIBUTING.md (final governed doc surface)
provides:
  - Refreshed, drift-verified user-doc set with all stale "what was" narrative pruned
  - docs/migration-tab-color-decouple.md removed (D-07; current nugget salvaged into docs/troubleshooting.md)
  - 12 dated docs/repro/h-*.md dev-hypothesis evidence files removed (D-06)
  - Final whole-suite drift gate green over README + 5 docs/ pages + CONTRIBUTING.md
affects: [phase-7-macos-parity, future-doc-audits]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Salvage-before-prune: confirm still-current value lives in a governed doc BEFORE git rm-ing the legacy source"
    - "Blocking maintainer checkpoint gates irreversible doc deletion; git rm keeps deletions history-recoverable"

key-files:
  created:
    - .planning/phases/06.4-user-documentation-audit-and-refactor/06.4-04-PRUNE-DOSSIER.md
  modified: []

key-decisions:
  - "Pruned the full default list of 13 files (migration doc + 12 repro docs) per maintainer approval"
  - "docs/agent-iteration.md and docs/macos-verification.md kept (out of the user-doc refactor scope)"

patterns-established:
  - "Salvage-before-prune: the drift-prone legacy doc is deleted only after its still-current nugget is confirmed present in a governed doc"
  - "Live ls docs/repro/ governs the prune set (not the planning-time frontmatter snapshot)"

requirements-completed: []

# Metrics
duration: ~6min
completed: 2026-06-19
---

# Phase 06.4 Plan 04: Execute Approved Prune + Final Drift-Check Gate Summary

**Pruned the migration doc + 12 dated repro hypothesis docs via `git rm` (salvage-confirmed first), then closed the documentation phase with `make test` green over the refreshed doc set (31 files; docs_drift_test.lua passes, governed_docs tolerates the now-absent files).**

## Performance

- **Duration:** ~6 min (Task 3 continuation only; Tasks 1–2 completed in prior agent runs)
- **Completed:** 2026-06-19
- **Tasks:** 1 (Task 3; Tasks 1–2 were done before this continuation)
- **Files modified:** 13 deletions (staged + committed via `git rm`)

## Accomplishments

- Removed `docs/migration-tab-color-decouple.md` — pure Phase 6.1 migration narrative ("what was", D-07). Its still-current doctor-gate nugget ("no shadowing tab-bar handler") was confirmed already present in `docs/troubleshooting.md` (salvage verified in Task 1) before deletion.
- Removed the 12 dated `docs/repro/h-*.md` dev-hypothesis evidence files (D-06); `docs/repro/` is now empty. Current user-facing behavior for each is covered by the refreshed `docs/` pages (cli.md / scenes.md / keybindings.md / install.md / troubleshooting.md).
- Ran the final phase gate: `make test` exits 0 — the Plan 01 drift-check (`tests/docs_drift_test.lua`) is green over the whole refreshed doc set, and its `governed_docs()` scan tolerated the now-absent files without error.
- Guards held: `docs/agent-iteration.md` (contributor process, linked from CONTRIBUTING.md) and `docs/macos-verification.md` (Phase 7 runbook) were NOT pruned.

## Prune List (13 files, all staged + committed as `D`)

| File | Category |
|---|---|
| `docs/migration-tab-color-decouple.md` | Migration narrative (D-07) |
| `docs/repro/h-tab-color.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-tab-completion.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-diag-keys.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-diag-doctor.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-diag-completions.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-inst06-bootstrap.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-scene-grid.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-scene-horizontal.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-scene-materialization.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-scene-tall.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-scene-tall-mirrored.md` | Dev-hypothesis evidence (D-06) |
| `docs/repro/h-06.2-identity-orthogonality.md` | Dev-hypothesis evidence (D-06) |

**Kept (explicitly out of scope):** `docs/agent-iteration.md`, `docs/macos-verification.md`.

## Final Gate Result

```
PASS  tests/docs_drift_test.lua
run-tests: all 31 file(s) passed
FINAL_GATE_EXIT=0
```

- `make test` exited 0 (full suite, 31 files).
- The drift-check is green over README + the 5 `docs/` pages + CONTRIBUTING.md — zero documented-but-nonexistent surface.
- `governed_docs()` reads a fixed `GOVERNED_DOC_PATHS` allowlist (none of the pruned paths are in it) and skips any file that `io.open` returns nil for, so the removed files cause no scan error.

## Task Commits

1. **Task 1 (prior agent): prune dossier + salvage confirmation** — `16ed7ac` (docs, non-destructive)
2. **Task 2 (prior agent): blocking maintainer checkpoint** — APPROVED the full default prune list (no commit; checkpoint)
3. **Task 3: execute approved prune + final drift gate** — `965ee84` (docs; 13 `git rm` deletions, atomic)

## Decisions Made

- Pruned the full default list of 13 files exactly as the maintainer approved at the Task 2 checkpoint; the live `ls docs/repro/` matched the dossier's 12-file GO set with no divergence, so no reconciliation adjustment was needed.
- Kept `docs/agent-iteration.md` and `docs/macos-verification.md` per the Task 3 guard (out of the user-doc refactor scope).

## Deviations from Plan

None - plan executed exactly as written. The live `docs/repro/` list matched the approved dossier's GO classification one-for-one (12 files), so no live-vs-dossier reconciliation was required.

## Deferred Backlog (whole-phase audit, D-10 — NOT fixed here)

No new code-gap backlog items surfaced during the prune. Pre-existing deferred work remains tracked elsewhere and is unaffected by this doc prune:
- macOS parity / on-Mac verification — Phase 7 (hardware-blocked; `docs/macos-verification.md` runbook intentionally kept).
- UX backlog (`wez scene list`, did-you-mean, unified error prefixes) — tracked in `.planning/MACOS-PARITY-AND-FOLLOWUPS.md` §A-2; out of scope for the doc audit.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 06.4 (User Documentation Audit and Refactor) is COMPLETE — all 4 plans done, doc set machine-verified clean by the drift-check.
- Phase 7 (macOS Parity Pass) remains the final v1 close gate; it is hardware-blocked (needs a Mac).

## Self-Check: PASSED

- Removed files absent on disk: confirmed (`docs/migration-tab-color-decouple.md` gone; `docs/repro/` empty).
- Out-of-scope guards present: `docs/agent-iteration.md` + `docs/macos-verification.md` still exist.
- Prune commit `965ee84` exists in git log with 13 deletions.

---
*Phase: 06.4-user-documentation-audit-and-refactor*
*Completed: 2026-06-19*
