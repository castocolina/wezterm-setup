---
phase: 06.4-user-documentation-audit-and-refactor
plan: 04
type: execute
wave: 4
depends_on: ["06.4-01", "06.4-02", "06.4-03"]
# NOTE: the docs/repro/* paths below are a PLANNING-TIME SNAPSHOT of the repro set.
# The live `ls docs/repro/` at execution time GOVERNS the actual prune candidates
# (re-derived in Task 1); treat this list as illustrative, not authoritative.
files_modified:
  - docs/migration-tab-color-decouple.md
  - docs/repro/h-tab-color.md
  - docs/repro/h-tab-completion.md
  - docs/repro/h-diag-keys.md
  - docs/repro/h-diag-doctor.md
  - docs/repro/h-diag-completions.md
  - docs/repro/h-inst06-bootstrap.md
  - docs/repro/h-scene-grid.md
  - docs/repro/h-scene-horizontal.md
  - docs/repro/h-scene-materialization.md
  - docs/repro/h-scene-tall.md
  - docs/repro/h-scene-tall-mirrored.md
  - docs/repro/h-06.2-identity-orthogonality.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "docs/migration-tab-color-decouple.md is removed; its still-current doctor-gate nugget already lives in docs/troubleshooting.md (D-07)"
    - "Stale docs/repro/* dev-hypothesis evidence is pruned; surviving user-facing value (if any) is captured in a current doc, not deleted blindly (D-06)"
    - "After pruning, the full drift-check + suite is green and no governed user doc references a nonexistent surface"
    - "A maintainer checkpoint confirms WHICH repro files are pruned vs. kept before deletion"
  artifacts:
    - path: "docs/troubleshooting.md"
      provides: "Confirmed home of the salvaged doctor no-shadowing-handler nugget (created in Plan 02; this plan verifies the nugget survived before deleting the migration doc)"
      contains: "format-tab-title"
  key_links:
    - from: "docs/troubleshooting.md"
      to: "the deleted migration doc's doctor-gate guidance"
      via: "salvage-before-prune (nugget present before migration doc removed)"
      pattern: "no shadowing|format-tab-title"
---

<objective>
Prune the stale, "what was" docs that violate the governing principle (document what the tool IS
now): delete `docs/migration-tab-color-decouple.md` as a pure migration narrative (D-07) — but
ONLY after confirming its still-current doctor-gate nugget already lives in `docs/troubleshooting.md`
(salvaged in Plan 02) — and prune the dated `docs/repro/*` dev-hypothesis evidence (D-06), some of
which references pre-6.1 behavior. Then run the Plan 01 drift-check + full suite as the final
phase gate so the refreshed doc set is machine-verified clean before the phase closes.

A blocking maintainer checkpoint confirms the prune list (which repro files go vs. stay) before
any deletion, since "which `docs/repro/*` are stale enough to prune" is explicitly Claude's
discretion (D-06) and deletion is irreversible without git.

NOTE ON THE `files_modified` LIST: the `docs/repro/*` paths enumerated in this plan's frontmatter
are a PLANNING-TIME SNAPSHOT. The authoritative prune set is whatever a live `ls docs/repro/` at
execution time reports (re-derived in Task 1 and confirmed at the Task 2 checkpoint) — do NOT
trust the frozen count.

Purpose: remove drift-prone legacy narrative and lock in a verified-clean doc set.
Output: pruned `docs/`, salvage confirmed, drift-check + suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.4-user-documentation-audit-and-refactor/06.4-CONTEXT.md
@.planning/phases/06.4-user-documentation-audit-and-refactor/06.4-PATTERNS.md
@docs/migration-tab-color-decouple.md
@docs/troubleshooting.md
@tests/docs_drift_test.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the prune candidates and confirm the salvage landed</name>
  <read_first>
    - docs/migration-tab-color-decouple.md (the doctor-gate nugget lines 42-64 that MUST already exist in troubleshooting.md before this doc is deleted)
    - docs/troubleshooting.md (confirm the salvaged nugget is present — created in Plan 02)
    - docs/repro/ listing — the `files_modified` frontmatter lists a PLANNING-TIME SNAPSHOT (12 files: h-tab-color, h-tab-completion, h-diag-keys, h-diag-doctor, h-diag-completions, h-inst06-bootstrap, h-scene-grid, h-scene-horizontal, h-scene-materialization, h-scene-tall, h-scene-tall-mirrored, h-06.2-identity-orthogonality). RE-DERIVE the authoritative list from a live `ls docs/repro/` at execution time rather than trusting this count.
    - .planning/phases/06.4-user-documentation-audit-and-refactor/06.4-CONTEXT.md (D-06/D-07 prune rules)
  </read_first>
  <action>
    Build the prune dossier (NO deletions yet). For `docs/migration-tab-color-decouple.md`: confirm
    `docs/troubleshooting.md` ALREADY carries the still-current doctor "no shadowing tab-bar handler"
    gate + remediation (the Plan 02 salvage). If any current-value sentence in the migration doc is
    NOT yet reflected in troubleshooting.md, port it there FIRST (the salvage must be complete before
    the source is deleted) — only the migration/"what was" framing is discarded.

    For `docs/repro/*`: re-derive the live list with `ls docs/repro/` (the frontmatter list is a
    planning-time snapshot; the runtime `ls` governs) and classify each file. All are dated `h-*`
    dev-hypothesis evidence (repro scripts + captured output), not user docs; per D-06 these are
    prune candidates. Flag any that still document CURRENT user-facing behavior with unique value not
    captured elsewhere (expected: none — scene layout/tab-color/completion behavior is now covered by
    docs/scenes.md + docs/keybindings.md + docs/cli.md). Produce the explicit go/stay list with a
    one-line rationale each, ready for the checkpoint.
  </action>
  <verify>
    <automated>grep -niE 'no shadowing|format-tab-title' docs/troubleshooting.md; echo "nugget-present-above (non-empty=salvage confirmed)"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/troubleshooting.md` contains the doctor no-shadowing-handler gate + remediation: `grep -niE 'format-tab-title' docs/troubleshooting.md` is non-empty (salvage confirmed BEFORE deletion).
    - A written prune dossier lists every live `docs/repro/*` file (from `ls docs/repro/`) + the migration doc with a per-file go/stay decision and rationale.
    - Any current-value sentence found ONLY in the migration doc has been ported into a current doc (none expected; if found, troubleshooting.md is updated).
  </acceptance_criteria>
  <done>The salvage is confirmed present in troubleshooting.md and a complete prune dossier exists; nothing deleted yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Maintainer confirms the prune list</name>
  <what-built>
    A prune dossier: `docs/migration-tab-color-decouple.md` slated for deletion (its current nugget
    confirmed salvaged into `docs/troubleshooting.md`), and a go/stay classification of every live
    `docs/repro/*` dev-hypothesis file (default: prune all as dated `h-*` evidence per D-06).
  </what-built>
  <how-to-verify>
    1. Read the prune dossier from Task 1 (the go/stay list with rationale, derived from the live `ls docs/repro/`).
    2. Confirm `docs/troubleshooting.md` carries the doctor-gate guidance you want kept:
       `grep -n 'format-tab-title' docs/troubleshooting.md`.
    3. Decide: approve pruning the migration doc + the listed `docs/repro/*` files, OR name any
       `docs/repro/*` file to KEEP (with why). Deletion is git-reversible but the working tree
       changes here.
  </how-to-verify>
  <resume-signal>Type "approved" to prune the full default list, or list the files to keep / exclude.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the approved prune + final drift-check gate</name>
  <read_first>
    - The Task 1 dossier + the Task 2 approval (the confirmed go/stay list)
    - tests/docs_drift_test.lua (the final gate; its governed_docs() scan must stay green after files are removed)
  </read_first>
  <action>
    Delete the migration doc and the maintainer-approved `docs/repro/*` files via `git rm` (so the
    removal is staged and reversible). Honor any keep exceptions from Task 2 exactly. Do NOT delete
    `docs/agent-iteration.md` or `docs/macos-verification.md` (out of the user-doc refactor set per
    CONTEXT canonical_refs — agent-iteration is contributor process now linked from CONTRIBUTING.md;
    macos-verification is the Phase 7 runbook).

    Run the full suite (`make test`) as the final phase gate: the Plan 01 drift-check must be green
    across the entire refreshed doc set (README + the five docs/ pages + CONTRIBUTING.md), and no
    governed doc may reference a nonexistent command/flag/keybinding/scene-field. Confirm the
    drift-check's `governed_docs()` tolerates the now-absent files (no broken scan). Record the final
    green run + the prune list in the SUMMARY; note any deferred code-gap backlog items surfaced
    during the whole-phase audit (D-10) without fixing non-trivial ones here.
  </action>
  <verify>
    <automated>test ! -f docs/migration-tab-color-decouple.md &amp;&amp; echo "migration-doc-removed"; make test 2>&amp;1 | tail -15; echo "suite-done"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/migration-tab-color-decouple.md` no longer exists (`test ! -f` succeeds) and the deletion is staged (`git status --porcelain` shows it as `D`).
    - The maintainer-approved `docs/repro/*` files are removed; any keep exceptions still exist.
    - `docs/agent-iteration.md` and `docs/macos-verification.md` still exist (NOT pruned — out of scope).
    - `make test` exits 0 with the drift-check green over the full refreshed doc set; `governed_docs()` handles the removed files without error.
    - No governed user doc references a nonexistent surface (the drift-check is the proof).
  </acceptance_criteria>
  <done>The approved files are pruned, the salvage is intact, and the full suite + drift-check is green — the phase doc set is machine-verified clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prune action -> repo history | Deletions are staged via `git rm` so they remain recoverable from history; the working-tree change is the only effect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.4-07 | Tampering/Repudiation | irreversible doc deletion | mitigate | Deletions go through `git rm` (history-recoverable) and are gated behind a blocking maintainer checkpoint that confirms the exact go/stay list; the salvage is verified present BEFORE the source is removed. |
| T-06.4-08 | Information disclosure | pruned repro evidence | accept | The `docs/repro/*` files contain only dev hypothesis output (keybinding/scene/install traces), no secrets; their removal discloses nothing. |

No HIGH-severity threats introduced: deletions are git-staged and checkpoint-gated; the still-current doctor-gate value is salvaged before its source doc is removed.
</threat_model>

<verification>
- `docs/troubleshooting.md` carries the salvaged doctor-gate nugget BEFORE the migration doc is deleted.
- The full `make test` (incl. the Plan 01 drift-check) is green over the entire refreshed doc set after pruning.
- `docs/agent-iteration.md` + `docs/macos-verification.md` are untouched (out of scope).
</verification>

<success_criteria>
- `docs/migration-tab-color-decouple.md` is pruned (migration framing gone), its current nugget salvaged into `docs/troubleshooting.md` (D-07).
- Stale `docs/repro/*` are pruned per the maintainer-approved list (D-06).
- The drift-check + full suite is green as the final phase gate — the refreshed doc set has zero documented-but-nonexistent surface.
</success_criteria>

<output>
Create `.planning/phases/06.4-user-documentation-audit-and-refactor/06.4-04-SUMMARY.md` when done.
</output>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- **Deletions** (staged): `docs/migration-tab-color-decouple.md` + the maintainer-approved `docs/repro/*` files.
- **`docs/troubleshooting.md`** — confirmed (and if needed, topped-up) home of the salvaged doctor no-shadowing-handler nugget (the doc itself is created in Plan 02; this plan guarantees the salvage is complete before deletion).
- No new code symbols; the final gate is the Plan 01 `tests/docs_drift_test.lua` run green over the refreshed doc set.
</artifacts_this_phase_produces>
</output>
