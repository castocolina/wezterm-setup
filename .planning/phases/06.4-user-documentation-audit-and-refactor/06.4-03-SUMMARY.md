---
phase: 06.4-user-documentation-audit-and-refactor
plan: 03
subsystem: docs
tags: [readme, progressive-disclosure, contributing, drift-check, crafting-effective-readmes, agent-md-refactor]

# Dependency graph
requires:
  - phase: 06.4-01
    provides: durable docs drift-check (tests/docs_drift_test.lua) wired into make test
  - phase: 06.4-02
    provides: focused docs/ pages (cli, scenes, keybindings, install, troubleshooting) that own the deep content
provides:
  - Lean progressive-disclosure README hub (overview + quickstart + compact tables + links)
  - CONTRIBUTING.md (moved Development content; hypothesis loop + make targets)
  - Stale "Status / pre-release" section removed entirely (D-03)
  - icon-as-own-attribute example (replaces the icon-baked-into-title example)
affects: [phase-07-macos-verification, future-docs-audits]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Progressive-disclosure README hub: essentials in root, depth delegated to docs/ pages + CONTRIBUTING.md"
    - "Doc-content contract tests target the doc that OWNS the content, not a fixed file, so they survive a hub split"

key-files:
  created:
    - CONTRIBUTING.md
  modified:
    - README.md
    - tests/cli/install_sh_test.lua

key-decisions:
  - "README condensed to a 77-line hub (<= 120-line acceptance); deep Install/CLI/Scenes/Keybindings prose delegated to the Plan 02 docs/ pages"
  - "Kept a condensed 'Why not a WezTerm plugin?' framing in the hub (D-04 discretion); dropped the standalone 'Platform' section by folding parity into the opening paragraph"
  - "Search overlay described as prose only (no literal chord) because Ctrl+R / Ctrl+Shift+F are embraced WezTerm defaults absent from keybindings.lua data — a literal chord token would fail the drift-check"
  - "Re-targeted install_sh_test README assertions to docs/install.md + docs/troubleshooting.md (the docs that now own that content post-split) rather than weakening or deleting the guarantees"

patterns-established:
  - "Hub README links every focused docs/ page + CONTRIBUTING.md via progressive-disclosure links"
  - "Doc contract assertions follow content across a refactor by asserting against the owning doc"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-06-19
---

# Phase 06.4 Plan 03: README hub refactor + CONTRIBUTING split Summary

**Refactored README from a ~222-line monolith into a 77-line progressive-disclosure hub (overview + quickstart + compact feature/keybinding tables linking the five docs/ pages), moved Development to CONTRIBUTING.md, removed the stale Status section, and reworked the icon-baked example to the 6.2 icon-as-own-attribute model — drift-check green.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-19T22:30:11Z
- **Completed:** 2026-06-19T22:34:23Z
- **Tasks:** 2
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- README is now a lean 77-line hub: one-paragraph overview, quickstart (`curl|bash` + `wez doctor`), a compact feature/command table and an essential-chords table, and a documentation index linking all five `docs/` pages + `CONTRIBUTING.md`.
- Removed the stale "Status / pre-release / phases 0–5 pending" section entirely (D-03) — not rewritten.
- Moved the Development content into a new `CONTRIBUTING.md` (D-04): hypothesis-first loop, `make doctor/test/clean/install` targets, commit-discipline + verify-before-done rules, and links to `docs/agent-iteration.md` and `.planning/REQUIREMENTS.md`.
- Fixed the concrete drift: the `wez pane title "🔥 build"` icon-baked example is replaced by the icon-as-own-attribute form `wez pane title api --icon python`.
- Surfaced previously-omitted surface at teaser depth with links: icon attribute, scene `cwd`/`focus`/`size`/alpha/`follow_pane_color`, `WEZ_CHANNEL`, `wez update`, `wez uninstall`, and the `Ctrl+Shift+K`/`Ctrl+Shift+W` + `Alt+Shift+R`/`Alt+Shift+E` chords.
- `make test` exits 0 — the docs drift-check (documented→exists) and the install contract test are both green over the refreshed hub + docs.

## Task Commits

Each task was committed atomically:

1. **Task 1: Move Development to CONTRIBUTING.md and remove the Status section** - `7472eb7` (docs)
2. **Task 2: Slim the README into a hub, link the docs/ pages, fix the icon example** - `f3cea57` (docs)

## Files Created/Modified
- `CONTRIBUTING.md` (new) — moved contributor/Development content: hypothesis-first loop, make targets, working agreements, links to docs/agent-iteration.md + CLAUDE.md.
- `README.md` (refactored) — lean progressive-disclosure hub; no `## Status`, no `## Development`; icon example fixed; links to all five docs/ pages + CONTRIBUTING.md.
- `tests/cli/install_sh_test.lua` (modified) — re-targeted the README install/trust/post-install contract assertions to `docs/install.md` + `docs/troubleshooting.md`, the docs that now own that content after the Plan 02 split.

## Skills Applied (D-02)
Both mandated skills were read from the agent-toolkit plugin cache and applied:

- **`agent-md-refactor`** (split planning): Phase 1 surfaced the Status/pre-release claim as a contradiction with shipped reality (resolved by D-03 — delete). Phase 2 extracted the hub essentials (overview, quickstart, compact tables, links). Phase 3's "group the rest" mapped cleanly onto the Plan 02 docs/ pages (Install→install.md, CLI→cli.md, Scenes→scenes.md, Keybindings→keybindings.md, doctor troubleshooting→troubleshooting.md, Development→CONTRIBUTING.md). Phase 5 pruned the Status section.
- **`crafting-effective-readmes`** (authoring AND review — D-02 requires both):
  - *Authoring*: project type = Personal / config distribution, task = Updating. Kept Name + one-sentence description + quickest path to "it works" + compact usage + links.
  - *Review pass*: re-read the finished hub against the skill checklist — confirmed audience match, no stale dates, all six link targets exist, no vague filler, the `curl|bash` quickstart always carries the `docs/install.md` trust-model pointer (T-06.4-05 mitigation), examples carry no secrets (T-06.4-06), and the previously-omitted surface is now present at teaser depth.

## Decisions Made
- README condensed to 77 lines (well under the 120-line acceptance); depth delegated to the Plan 02 docs/ pages.
- Kept a condensed "Why not a WezTerm plugin?" framing in the hub (D-04 discretion); folded "Platform" parity into the opening paragraph rather than keeping a standalone section.
- Described the embraced search overlay as prose with no literal chord token (Ctrl+R / Ctrl+Shift+F are WezTerm defaults absent from `keybindings.lua`; a literal chord would have failed the documented→exists drift-check), pointing readers to `wez keys` / keybindings.md instead.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Stale install contract test still asserted the deep install content lived in README.md**
- **Found during:** Task 2 (the README slim made `make test` fail)
- **Issue:** `tests/cli/install_sh_test.lua` (lines 108–136) asserted README.md still carried the inline one-liners, the `### Trust model` heading, inspect-before-run, `WEZ_REF=v1.0.0`, the SHA-256 note, the `~/.local/bin` post-install step, and the nightly/update-in-place behavior. Plan 02 had already relocated that content into `docs/install.md` (+ the PATH step into `docs/troubleshooting.md`), and this plan's hub refactor (D-01/D-04) finalized the move — so those README-targeted assertions failed even though every guarantee still exists in the owning doc.
- **Fix:** Re-targeted each assertion to the doc that now owns the content (`docs/install.md` for one-liners/trust/WEZ_REF/SHA-256/nightly+update-in-place; `docs/troubleshooting.md` for the `~/.local/bin` PATH step), kept a hub assertion that the quickstart `curl|bash` one-liner still appears in README, and matched the install.md `## Trust model` H2 heading. No guarantee was weakened or dropped — the install.sh script-text and headless-abort behavior assertions were left untouched.
- **Files modified:** tests/cli/install_sh_test.lua
- **Verification:** `make test` exits 0 — install_sh_test PASS and docs_drift_test PASS; all 31 test files pass.
- **Committed in:** f3cea57 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** The fix was required to complete the planned hub refactor without losing test coverage; it follows the content across the Plan 02 split rather than relaxing any guarantee. No scope creep.

## Issues Encountered
None beyond the deviation above. The drift-check (`tests/docs_drift_test.lua`) passed unchanged — every command/flag/keybinding/scene-field the refreshed hub names exists in source.

## Guard Compliance (D-05)
`git status --porcelain CLAUDE.md .planning/STATE.md docs/agent-iteration.md` was empty throughout execution (before the post-plan STATE.md/ROADMAP bookkeeping commit). `docs/agent-iteration.md` was linked from CONTRIBUTING.md, never modified.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The README hub + CONTRIBUTING.md + the five docs/ pages form the complete v1 user-facing doc set; the documented→exists drift-check guards them in CI via `make test`.
- Plan 04 (if any in this phase) can proceed; the macOS-specific doc verification remains deferred to Phase 7 (D-18 / docs/macos-verification.md).

## Self-Check: PASSED
- Files verified on disk: `CONTRIBUTING.md`, `README.md`, `06.4-03-SUMMARY.md`.
- Commits verified in git log: `7472eb7` (Task 1), `f3cea57` (Task 2).
