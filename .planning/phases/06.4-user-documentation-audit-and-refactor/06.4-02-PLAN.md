---
phase: 06.4-user-documentation-audit-and-refactor
plan: 02
type: execute
wave: 2
depends_on: ["06.4-01"]
files_modified:
  - docs/cli.md
  - docs/scenes.md
  - docs/keybindings.md
  - docs/install.md
  - docs/troubleshooting.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "docs/scenes.md is a FULL tab-level + pane-level field reference matching the real scene.lua/recipe.lua schema (D-13)"
    - "docs/cli.md documents the common workflows (icon, color split, follow_pane_color, channels, update, uninstall) and points to `wez <cmd> --help` for exhaustive flags (D-11)"
    - "docs/keybindings.md is a short essential-chords table that points to `wez keys` for the complete list (D-12)"
    - "docs/install.md covers curl|bash + wget + WEZ_CHANNEL + WEZ_REF + update + uninstall + trust model"
    - "docs/troubleshooting.md carries the still-current `wez doctor` no-shadowing-handler gate guidance (salvaged per D-07)"
    - "Every command/flag/keybinding/scene-field named across these pages exists in source (the drift-check is green)"
  artifacts:
    - path: "docs/scenes.md"
      provides: "Full scene TOML reference: tab-level layout/color/title/icon/cwd/follow_pane_color + [[panes]] command/color/title/icon/cwd/focus/size, accepted values incl. #RRGGBBAA alpha and cwd grammar, worked examples"
      min_lines: 80
    - path: "docs/cli.md"
      provides: "Task-oriented CLI guide for pane/tab/scene/doctor/keys/update/uninstall"
      min_lines: 60
    - path: "docs/keybindings.md"
      provides: "Essential-chords table + pointer to `wez keys`"
      min_lines: 30
    - path: "docs/install.md"
      provides: "Install/channels/update/uninstall/trust-model guide"
      min_lines: 50
    - path: "docs/troubleshooting.md"
      provides: "Doctor no-shadowing-handler gate + remediation (salvaged nugget)"
      min_lines: 25
  key_links:
    - from: "docs/scenes.md"
      to: "cli/lib/scene.lua + cli/lib/recipe.lua"
      via: "field reference mirrors parse_pane_spec + recipe field set"
      pattern: "follow_pane_color|focus|size|cwd|icon"
    - from: "docs/keybindings.md"
      to: "config/wezterm-setup/keybindings.lua"
      via: "essential chords match kb.keys entries"
      pattern: "Ctrl%+Shift%+K|Alt%+Shift%+R"
---

<objective>
Author the focused, progressive-disclosure `docs/` pages that the lean README hub (Plan 03) will
link to, reflecting the FINAL 6.1/6.2/6.3/6.5 reality and the governing principle "document what
the tool IS now, not what it was" (no legacy/migration narrative).

Page set (Claude's discretion per D-01, fixed here):
- `docs/cli.md` — task-oriented CLI guide; common workflows + important flags, deferring the
  exhaustive flag list to `wez <cmd> --help` + completion (D-11).
- `docs/scenes.md` — the FULL scene TOML field reference (the one place a complete reference is
  warranted, D-13's exception to D-11).
- `docs/keybindings.md` — a SHORT essential-chords table that points to `wez keys` (D-12).
- `docs/install.md` — install (curl|bash + wget + process-substitution), `WEZ_CHANNEL`, `WEZ_REF`
  pinning, trust model, `wez update`, `wez uninstall`.
- `docs/troubleshooting.md` — current troubleshooting incl. the salvaged `wez doctor`
  no-shadowing-handler gate nugget (D-07; the migration framing is dropped, the current gate
  guidance is kept).

Purpose: the deep content the README hub points to, each cheap to keep accurate and validated by
the Plan 01 drift-check.
Output: five new `docs/` pages, all drift-check green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06.4-user-documentation-audit-and-refactor/06.4-CONTEXT.md
@.planning/phases/06.4-user-documentation-audit-and-refactor/06.4-PATTERNS.md
@cli/spec.lua
@cli/lib/scene.lua
@cli/lib/recipe.lua
@config/wezterm-setup/keybindings.lua
@docs/migration-tab-color-decouple.md
@docs/agent-iteration.md
@tests/docs_drift_test.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/scenes.md (full schema) + docs/keybindings.md (essential chords)</name>
  <read_first>
    - cli/lib/scene.lua (M.LAYOUTS line 243; M.COLOR_NAMES line 272; M.parse_pane_spec fields line 152-end; cwd/focus/size semantics in the header comment lines 135-150)
    - cli/lib/recipe.lua (tab-level keys: color/title/cwd/icon/follow_pane_color; per-pane fields incl. icon lines 82-119)
    - config/wezterm-setup/keybindings.lua (kb.keys lines 81-129 — the real chords incl. Ctrl+Shift+K clear, Ctrl+Shift+W close-tab, Alt+Shift+H/V split, Alt+Shift+X close-pane, Alt+Shift+Z zoom, Alt+Shift+R/E RotatePanes, Ctrl+Tab nav, Alt+arrows)
    - .planning/STATE.md decisions (06.1 #RRGGBBAA accepted+preserved; 06.2 icon-as-own-attribute; follow_pane_color WEZTERM_TAB_FOLLOW_PANE default OFF)
    - tests/docs_drift_test.lua (the membership the new pages must satisfy)
  </read_first>
  <action>
    Create `docs/scenes.md` as the FULL reference (D-13). Include: a tab-level field table
    (`layout`, `color`, `title`, `icon`, `cwd`, `follow_pane_color`) and a `[[panes]]` field table
    (`command`, `color`, `title`, `icon`, `cwd`, `focus`, `size`), each with meaning + accepted
    values. Document the special `command = "shell"` pane value (a plain interactive shell — no
    command sent; equivalently the bare `shell` pane spec, the form the seeded scenes use for a
    working shell pane). Document the 4 layouts EXACTLY as `scene.LAYOUTS` (`tall`, `tall:mirrored`, `grid`,
    `horizontal`), the 10 color names EXACTLY as `scene.COLOR_NAMES`, that colors accept
    `#RRGGBBAA` alpha (preserved end-to-end; alpha only renders with window transparency), the
    `cwd` grammar (literal / `~` / `$ENV` / relative where `.`=launch dir and `..`=parent; NO shell
    `$(...)`), `focus` (selects active pane on spawn), `size` (split percent 1..100), and
    `follow_pane_color` (default OFF; when ON a colorless tab tracks its active pane). Add 2 worked
    example recipes mirroring the seeded `dev`/`docker` scenes. Use NO "previously"/"before"/legacy
    framing.

    Create `docs/keybindings.md` (D-12): a SHORT essential-chords table (the handful a user reaches
    for: clear screen+scrollback, split H/V, zoom, close pane, close tab, next/prev tab, rotate
    panes, word-left/right), each rendered with Linux (`Super`) and macOS (`Cmd`) columns matching
    the real `kb.keys`. End with a one-line pointer: "Run `wez keys` for the complete, always-current
    curated list" (the 6.5 curated-first output is the source of truth). Do NOT mirror every binding.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -i 'docs_drift\|fail\|passed' | tail -10; echo "exit=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/scenes.md` tabulates ALL of: layout, color, title, icon, cwd, follow_pane_color (tab) AND command, color, title, icon, cwd, focus, size (pane). `grep -cE 'follow_pane_color|focus|size|icon|cwd' docs/scenes.md` >= 6.
    - `docs/scenes.md` documents the `command = "shell"` / bare-`shell` plain-shell pane value (`grep -nE 'shell' docs/scenes.md` is non-empty).
    - Every layout/color named in `docs/scenes.md` is a member of `scene.LAYOUTS`/`scene.COLOR_NAMES` (the drift-check passes — no invented layout/color).
    - `docs/scenes.md` documents `#RRGGBBAA` alpha and the no-`$(...)` cwd grammar (`grep -E '#RRGGBBAA|RRGGBBAA' docs/scenes.md` and `grep -E 'cwd' docs/scenes.md` both non-empty).
    - `docs/keybindings.md` lists Ctrl+Shift+K (clear), Ctrl+Shift+W (close tab), Alt+Shift+R/E (rotate); every documented chord normalizes to a member of source_chords() (drift-check green).
    - `make test` exits 0 with the drift-check including these two pages.
    - Zero legacy/migration framing: `grep -niE 'previously|used to|migration|before 6\.|legacy' docs/scenes.md docs/keybindings.md` returns nothing.
  </acceptance_criteria>
  <done>Both pages exist, the scene reference is complete and accurate, the keybinding table is short + accurate, and the drift-check is green.</done>
</task>

<task type="auto">
  <name>Task 2: docs/cli.md + docs/install.md + docs/troubleshooting.md (salvaged doctor nugget)</name>
  <read_first>
    - cli/spec.lua (subcommands + flags: pane/tab icon+color+--follow-pane-color, scene new/launch, doctor, keys --json/--platform, update, uninstall --yes/--keep-*; lines 56-153)
    - README.md (current Install section lines 30-124 — the install/trust/uninstall prose to lift + refresh; the `wez pane title "🔥 build"` icon-baked example lines 16-17 to REWORK to the icon attribute)
    - docs/migration-tab-color-decouple.md (lines 42-64 — the still-current "no shadowing tab-bar handler" doctor gate + remediation to SALVAGE; the rest is migration framing to drop)
    - .planning/STATE.md decisions (06.3 WEZ_CHANNEL=nightly|stable|<vX.Y.Z>; wez uninstall front door; wez update)
  </read_first>
  <action>
    Create `docs/cli.md` (D-11): a task-oriented guide grouped by `wez pane`, `wez tab`,
    `wez scene`, `wez doctor`, `wez keys`, `wez update`, `wez uninstall`. Cover the important
    workflows + flags — pane/tab `icon` as its OWN attribute (NOT baked into a title; explicitly
    rework any title-with-emoji example to use the icon attribute, per the 6.2 model), the tab/pane
    color split, `--follow-pane-color`, `wez keys --json`/`--platform`, `wez update`,
    `wez uninstall` (+ `--keep-config`/`--keep-cli`/`--keep-backup`/`--yes`). State once that
    `wez <cmd> --help` + tab-completion is the exhaustive flag reference; do NOT mirror every flag
    in prose.

    Create `docs/install.md`: the one-line `curl|bash` + `wget` + `bash <(curl …)` process-sub
    forms, the `WEZ_CHANNEL` selector (`nightly` default / `stable` / `<vX.Y.Z>`), `WEZ_REF`
    pinning, the trust model (inspect-before-run, pin-to-tag/commit, SHA-256-before-chmod),
    `make install` local path, `wez update`, and `wez uninstall` / `make uninstall KEEP_*`.

    Create `docs/troubleshooting.md`: SALVAGE the still-current nugget from
    `docs/migration-tab-color-decouple.md` (D-07) — the `wez doctor` "no shadowing tab-bar handler"
    core gate, the symptom, and exactly what to remove from `wezterm.lua` when it fires (delete the
    inline `wezterm.on("format-tab-title", …)` registration outside the managed sentinel block +
    any duplicate keybindings; keep personal settings; the installer's timestamped backup is the
    recovery path). Frame it as CURRENT troubleshooting — no "what changed / before vs after"
    narrative. (The migration doc itself is pruned in Plan 04; this is where its surviving value lands.)
  </action>
  <verify>
    <automated>make test 2>&1 | grep -i 'docs_drift\|fail\|passed' | tail -10; echo "exit=${PIPESTATUS[0]}"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/cli.md` covers pane/tab/scene/doctor/keys/update/uninstall and documents `icon` as its own attribute; `grep -nE 'icon' docs/cli.md` is non-empty AND no example bakes an emoji into a `title "…"` string (`grep -nE 'title "[^"]*[🔥💚🐳]' docs/cli.md` returns nothing).
    - Every `wez <cmd>`/`--flag` named in `docs/cli.md` and `docs/install.md` parses cleanly via the spec (drift-check `flag_accepts` green — e.g. `wez uninstall --keep-config`, `wez keys --platform`, `wez update` all valid; no invented flag).
    - `docs/install.md` documents `WEZ_CHANNEL` (nightly/stable/`<vX.Y.Z>`), `WEZ_REF`, SHA-256-before-chmod, and `wez uninstall`.
    - `docs/troubleshooting.md` names the "no shadowing tab-bar handler" doctor gate and the exact remediation (delete inline `format-tab-title` handler outside the managed block); `grep -niE 'format-tab-title|shadow' docs/troubleshooting.md` is non-empty.
    - Zero migration framing: `grep -niE 'before:|after:|what changed|previously|used to' docs/troubleshooting.md docs/cli.md docs/install.md` returns nothing.
    - `make test` exits 0 with the drift-check including all three pages.
  </acceptance_criteria>
  <done>All three pages exist, the doctor nugget is salvaged as current troubleshooting, no surface is invented, no migration framing, and the drift-check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → user shell | `docs/install.md` instructs `curl|bash`; the page must accurately represent the trust model so a user can make an informed decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.4-03 | Spoofing/Tampering | docs/install.md `curl|bash` guidance | mitigate | Carry forward the existing accurate trust model: HTTPS=transport-only, inspect-before-run, pin via `WEZ_REF` to tag/commit, SHA-256-before-chmod. No weakening of the install security guidance. |
| T-06.4-04 | Information disclosure | example recipes / commands in docs | mitigate | Use placeholder/illustrative values only; no real tokens, hostnames, or secrets in any example (scene/CLI examples use generic dirs + commands). |

No HIGH-severity threats introduced: docs-only changes. The install trust model is preserved exactly, not relaxed; no secrets are embedded in examples.
</threat_model>

<verification>
- `make test` exits 0 with the Plan 01 drift-check validating all five new pages.
- No documented command/flag/keybinding/scene-field is absent from source.
- No legacy/migration framing survives in the new pages (grep gates above).
</verification>

<success_criteria>
- Five focused `docs/` pages exist (cli, scenes, keybindings, install, troubleshooting).
- `docs/scenes.md` is a complete, accurate tab+pane field reference (D-13); `docs/keybindings.md` is the short essential table (D-12); `docs/cli.md` is the task-oriented guide deferring to `--help` (D-11).
- The salvaged doctor no-shadowing-handler nugget lives in `docs/troubleshooting.md` (D-07).
- The drift-check is green against all pages.
</success_criteria>

<output>
Create `.planning/phases/06.4-user-documentation-audit-and-refactor/06.4-02-SUMMARY.md` when done.
</output>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- **`docs/cli.md`** (new) — task-oriented CLI guide (pane/tab/scene/doctor/keys/update/uninstall).
- **`docs/scenes.md`** (new) — full tab + pane scene TOML field reference (D-13).
- **`docs/keybindings.md`** (new) — essential-chords table → `wez keys` (D-12).
- **`docs/install.md`** (new) — install/channels/update/uninstall/trust-model guide.
- **`docs/troubleshooting.md`** (new) — salvaged `wez doctor` no-shadowing-handler gate guidance (D-07).
- No new code symbols; all five are prose validated by the Plan 01 drift-check.
</artifacts_this_phase_produces>
