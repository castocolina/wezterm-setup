# 06.4-04 Prune Dossier (Task 1 output — NO deletions performed)

> Built by Plan 06.4-04 Task 1. This is the **non-destructive** audit that the
> blocking maintainer checkpoint (Task 2) approves before any `git rm` (Task 3).
> The `docs/repro/*` list below is **re-derived live** from `ls docs/repro/` at
> execution time (2026-06-19), NOT the planning-time frontmatter snapshot.

## 1. Salvage confirmation (D-07 — required BEFORE the migration doc is deleted)

`docs/migration-tab-color-decouple.md`'s still-current doctor-gate nugget **already
lives** in `docs/troubleshooting.md`. Grep evidence:

```
$ grep -niE 'no shadowing|format-tab-title' docs/troubleshooting.md
12:## Doctor gate: "no shadowing tab-bar handler"
28:`format-tab-title` registration (or a duplicate keybinding block) sitting **outside** the
36:1. **Delete the inline `wezterm.on("format-tab-title", ...)` registration** (single- or
```

`docs/troubleshooting.md` § "Doctor gate: 'no shadowing tab-bar handler'" reproduces
every current-value sentence of the migration doc's "no shadowing handler" section:

| Migration-doc content | Reflected in troubleshooting.md? |
|---|---|
| Symptoms (`<name>:` prefix, accent never applies, pane wrong cwd) | Yes — § Symptoms |
| Cause: `wezterm.on` **appends** (no replace) → inline handler shadows managed | Yes — § Cause |
| Fix step 1: delete inline `format-tab-title` outside the managed-block markers | Yes — § Fix #1 |
| Fix step 2: remove duplicate keybindings that re-bind managed keys | Yes — § Fix #2 |
| Fix step 3: keep everything else untouched | Yes — § Fix #3 |
| Recovery: timestamped `wezterm.lua.bak.<timestamp>` + reinstall reset | Yes — § Recovery |
| Re-run `wez doctor` to confirm the gate passes | Yes — § Fix (closing line) |

**Nothing to port.** The only content NOT carried over is the pure migration /
"what was" framing — the "What changed: Before/After" narrative and the "Live tabs
reset on the next recolor / restart" transition note. That is exactly the
drift-prone legacy narrative D-07 directs us to discard. **Salvage is complete.**

## 2. Migration doc — go/stay

| File | Decision | Rationale |
|---|---|---|
| `docs/migration-tab-color-decouple.md` | **GO** | Pure Phase 6.1 migration narrative ("what was", D-07). Current nugget salvaged into `docs/troubleshooting.md` (confirmed §1). Not in the drift `GOVERNED_DOC_PATHS` allowlist; no inbound links from any governed doc/README/CONTRIBUTING/STATE — safe to delete with the suite staying green. |

## 3. `docs/repro/*` — go/stay (live `ls docs/repro/`, 12 files, 2026-06-19)

All 12 are dated `h-*` dev-hypothesis evidence (promoted/manual repro scripts +
captured output, each tied to a specific plan with a date) — the exact D-06 prune
category. None is in the drift `GOVERNED_DOC_PATHS` allowlist; none is link-targeted
by a governed doc (`docs/agent-iteration.md` references the `docs/repro/h<NN>.md`
*directory convention* generically, not any specific file, so the dir stays and no
link breaks). Current user-facing behavior is covered elsewhere as noted.

| File | Decision | Rationale (current coverage) |
|---|---|---|
| `h-tab-color.md` (Plan 03-02, 06-11) | **GO** | `wez tab color` behavior now covered by `docs/cli.md` / `docs/keybindings.md`; dated pre-context repro evidence. |
| `h-tab-completion.md` (Plan 03-04, 06-12) | **GO** | `wez tab` dynamic completion covered by `docs/cli.md`; dated repro. |
| `h-diag-keys.md` (DIAG-02/03/04) | **GO** | `wez keys` grouped classification covered by `docs/cli.md`/`docs/keybindings.md`; dated repro. |
| `h-diag-doctor.md` (DIAG-01) | **GO** | `wez doctor` gates covered by `docs/cli.md` + `docs/troubleshooting.md`; dated repro. |
| `h-diag-completions.md` (DIAG-05) | **GO** | shell completions covered by `docs/cli.md`/`docs/install.md`; dated repro. |
| `h-inst06-bootstrap.md` (INST-06) | **GO** | sudo-free bootstrap covered by `docs/install.md`; dated repro. |
| `h-scene-grid.md` (Plan 04-02, 06-13) | **GO** | `wez scene new --layout grid` covered by `docs/scenes.md`; dated repro. |
| `h-scene-horizontal.md` (Plan 04-02) | **GO** | `--layout horizontal` covered by `docs/scenes.md`; dated repro. |
| `h-scene-materialization.md` (Plan 04-02) | **GO** | `wez scene new` materialization covered by `docs/scenes.md`; dated repro. |
| `h-scene-tall.md` (Plan 04-02) | **GO** | `--layout tall` covered by `docs/scenes.md`; dated repro. |
| `h-scene-tall-mirrored.md` (Plan 04-02) | **GO** | `--layout tall:mirrored` covered by `docs/scenes.md`; dated repro. |
| `h-06.2-identity-orthogonality.md` (Plan 06.2-05, 06-16) | **GO** | Phase 6.2 identity-orthogonality repro; behavior covered by `docs/keybindings.md` + tab-identity docs; dated repro. |

**Default recommendation: prune ALL 12 repro files + the migration doc** (13 deletions).
No file holds unique CURRENT user-facing value absent from the refreshed doc set.

## 4. Explicitly OUT of scope (do NOT delete — Task 3 guard)

- `docs/agent-iteration.md` — contributor process, linked from CONTRIBUTING.md.
- `docs/macos-verification.md` — Phase 7 runbook.

## 5. Suite-safety pre-check (so Task 3's `make test` stays green)

- `migration-tab-color-decouple.md` NOT in drift `GOVERNED_DOC_PATHS` allowlist (`grep` rc=1).
- No `docs/repro/*` path in the allowlist; `governed_docs()` reads a fixed list and
  skips absent optional pages — removed files cause no scan error.
- No inbound links to the migration doc or to any specific repro file from governed docs.
- `make test` is currently GREEN (31 files) and stays green after the approved prune.
