---
phase: 06.4-user-documentation-audit-and-refactor
plan: 03
type: execute
wave: 3
depends_on: ["06.4-01", "06.4-02"]
files_modified:
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "README is a lean hub: overview + quickstart + compact feature/keybinding summary, deep content delegated to docs/ pages (D-01)"
    - "The README 'Status' section (the per-phase pre-release table) is REMOVED entirely, not rewritten (D-03)"
    - "The 'Development' content is MOVED OUT of README into a contributor-facing doc (D-04)"
    - "Both `/crafting-effective-readmes` and `/agent-md-refactor` skills are applied during execution AND `/crafting-effective-readmes` during review (D-02)"
    - "Every command/flag/keybinding/scene-field the README still names exists in source (drift-check green)"
    - "The README links to the focused docs/ pages created in Plan 02"
  artifacts:
    - path: "README.md"
      provides: "Lean hub README (overview, quickstart, compact summary, links to docs/ pages); no Status section; no Development section; icon example uses the icon attribute"
      min_lines: 60
    - path: "CONTRIBUTING.md"
      provides: "Moved Development/contributor content (hypothesis loop, make targets)"
      min_lines: 20
  key_links:
    - from: "README.md"
      to: "docs/cli.md, docs/scenes.md, docs/keybindings.md, docs/install.md, docs/troubleshooting.md"
      via: "progressive-disclosure links"
      pattern: "docs/(cli|scenes|keybindings|install|troubleshooting)%.md"
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "contributor pointer"
      pattern: "CONTRIBUTING%.md"
---

<objective>
Refactor `README.md` from the current bloated, partly-stale document into a lean progressive-
disclosure HUB (D-01) using BOTH mandated skills, and MOVE the contributor-facing "Development"
content out into `CONTRIBUTING.md` (D-04). Remove the stale "Status" section entirely (D-03 — do
NOT replace it with a fresh table that just re-rots). Governing principle: document what the tool
IS now (v0.1.0 shipped, 0-6.5 done), not what it was — no legacy/migration narrative.

Concrete drift to fix while refactoring (audit starting points): the false "Status / pre-release"
section (removed by D-03), the `wez pane title` icon-baked example (rework to the icon attribute),
and any keybinding/feature mentions that drift from the real `kb.keys` / shipped surface (the
README keeps only a COMPACT summary and links out to the Plan 02 pages for depth).

Purpose: a fast, accurate front door that delegates depth to the focused docs/ pages.
Output: refactored `README.md` (lean hub) + new `CONTRIBUTING.md`, drift-check green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06.4-user-documentation-audit-and-refactor/06.4-CONTEXT.md
@.planning/phases/06.4-user-documentation-audit-and-refactor/06.4-PATTERNS.md
@README.md
@docs/cli.md
@docs/scenes.md
@docs/keybindings.md
@docs/install.md
@docs/troubleshooting.md
@tests/docs_drift_test.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Development to CONTRIBUTING.md and remove the Status section</name>
  <read_first>
    - README.md (the `## Development` section lines 197-210 to lift; the `## Status` section lines 180-193 to delete; the `## Why not a WezTerm plugin?` + `## Platform` sections lines 212-221 to keep-or-condense per D-04)
    - docs/agent-iteration.md (the contributor process this references; CONTRIBUTING.md links to it — link/lift it, do NOT modify it)
    - CLAUDE.md (project commit/hypothesis rules the contributor doc should reflect)
  </read_first>
  <action>
    Apply the `/agent-md-refactor:agent-md-refactor` skill to plan the README split (D-02): the
    skill drives WHICH content stays in the hub vs. moves to focused docs. Create `CONTRIBUTING.md`
    and MOVE the README `## Development` content into it (D-04): the hypothesis-first loop in
    `.tmp/hNN-slug/`, the `make doctor` / `make test` (incl. `WEZTERM_INTEGRATION=1`) / `make clean`
    targets, and a link to `docs/agent-iteration.md`. Reflect the CLAUDE.md commit-discipline +
    verify-before-done rules briefly so contributors see them.

    DELETE the README `## Status` section in full (D-03) — do NOT replace it with a rewritten phase
    table. If a reader wants roadmap state, the existing `.planning/ROADMAP.md` link suffices; add a
    single sentence pointer at most. At Claude's discretion (D-04) keep a CONDENSED "Why not a
    plugin?" / "Platform" framing in the README or fold it into the hub — pick one and keep it tight.
    Do NOT touch CLAUDE.md or `.planning/*` (out of scope, D-05). Do NOT modify
    `docs/agent-iteration.md` either — it is the source-of-truth contributor process; CONTRIBUTING.md
    LINKS to it (and may quote it), it does not rewrite it.
  </action>
  <verify>
    <automated>grep -niE 'pre-release|nothing is installable' README.md; echo "status-line-hits-above (none=good)"; test -f CONTRIBUTING.md &amp;&amp; grep -niE 'make test|hypothesis|agent-iteration' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^## Status' README.md` returns NOTHING (section fully removed, D-03).
    - `grep -niE 'pre-release|phases? (0|1|2|3|4|5).*pending' README.md` returns NOTHING (the false status claim is gone).
    - `CONTRIBUTING.md` exists and contains the moved Development content: `grep -niE 'make test|hypothesis|agent-iteration' CONTRIBUTING.md` is non-empty.
    - `grep -nE '^## Development' README.md` returns NOTHING (moved out, D-04).
    - CLAUDE.md, `.planning/`, and `docs/agent-iteration.md` are unchanged (the latter is lifted/linked into CONTRIBUTING.md — this guard catches accidental modification while moving contributor content): `git status --porcelain CLAUDE.md .planning/STATE.md docs/agent-iteration.md` is empty.
  </acceptance_criteria>
  <done>Development content lives in CONTRIBUTING.md; the Status section is gone; CLAUDE.md/.planning/docs/agent-iteration.md untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Slim the README into a hub, link the docs/ pages, fix the icon example</name>
  <read_first>
    - README.md (current command table lines 12-22 incl. the `wez pane title` icon-baked row; Install lines 30-124; Keybindings lines 128-143; Scenes lines 147-177 — all to COMPACT and delegate)
    - docs/cli.md, docs/scenes.md, docs/keybindings.md, docs/install.md, docs/troubleshooting.md (the Plan 02 pages to LINK to)
    - tests/docs_drift_test.lua (the README is one of the governed docs the drift-check scans)
    - config/wezterm-setup/keybindings.lua (the real chords for the compact summary)
  </read_first>
  <action>
    Apply `/crafting-effective-readmes:crafting-effective-readmes` to author the lean hub (D-02,
    MANDATORY during execution). Reduce the README to: a one-paragraph overview, a quickstart
    (the `curl|bash` one-liner + run `wez doctor`), a COMPACT feature/command summary table, and a
    COMPACT essential-keybinding teaser — then link out to the focused pages: `docs/install.md`,
    `docs/cli.md`, `docs/scenes.md`, `docs/keybindings.md`, `docs/troubleshooting.md`, and
    `CONTRIBUTING.md`. Move the deep Install/Scenes/Keybindings prose OUT (it now lives in the Plan
    02 pages) — keep only the teaser + link in README.

    Fix the concrete drift: rework the icon-baked `wez pane title` example row so the icon is its
    OWN attribute (per the 6.2 model) rather than baked into the title; ensure every command/flag
    the compact table names exists in the spec. Add the new surface the old README omitted, at
    teaser depth with links: the icon attribute, scene cwd/focus/size/alpha/follow_pane_color
    (link scenes.md), WEZ_CHANNEL + `wez update` + `wez uninstall` (link install.md), the
    Ctrl+Shift+K / Ctrl+Shift+W / Alt+Shift+R/E chords + Ctrl+R search toggle (link keybindings.md).
    Keep ZERO legacy/migration framing. Then RE-REVIEW the result with
    `/crafting-effective-readmes` (D-02 requires it during review too) and note the review pass in
    the SUMMARY.
  </action>
  <verify>
    <automated>make test 2>&amp;1 | grep -iE 'docs_drift|fail|passed' | tail -10; echo "suite-exit=done"; grep -oE 'docs/(cli|scenes|keybindings|install|troubleshooting)[.]md|CONTRIBUTING[.]md' README.md | sort -u; wc -l README.md</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with the drift-check green — every command/flag/keybinding/scene-field the README still names exists in source.
    - The README links to ALL five docs pages + CONTRIBUTING.md: the `grep -oE` in verify lists `docs/cli.md`, `docs/scenes.md`, `docs/keybindings.md`, `docs/install.md`, `docs/troubleshooting.md`, `CONTRIBUTING.md`.
    - No icon is baked into a title example: `grep -nE 'pane title "[^"]*[🔥💚🐳]' README.md` returns NOTHING; the icon attribute is shown instead (`grep -niE 'icon' README.md` non-empty).
    - The README names the previously-omitted surface at teaser depth: `grep -niE 'wez update|wez uninstall|WEZ_CHANNEL|follow_pane_color|Ctrl\+Shift\+K' README.md` is non-empty.
    - Zero legacy/migration framing: `grep -niE 'previously|used to|migration|legacy|before 6[.]' README.md` returns NOTHING.
    - The README is materially leaner than the original ~222 lines (deep prose delegated to docs/): `wc -l README.md` <= 120.
  </acceptance_criteria>
  <done>README is a lean, accurate hub linking the docs/ pages + CONTRIBUTING.md; the icon example is fixed; both skills were applied (exec + review); the drift-check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README quickstart -> user shell | The README still shows the `curl|bash` quickstart; it must not weaken the trust guidance (full trust model now lives in docs/install.md, linked). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.4-05 | Spoofing/Tampering | README `curl|bash` quickstart | mitigate | The README quickstart links to `docs/install.md` for the full trust model (inspect-before-run, pin via WEZ_REF, SHA-256-before-chmod); the one-liner is never presented without that pointer. |
| T-06.4-06 | Information disclosure | README examples | mitigate | Examples use generic illustrative values only; no real tokens, hostnames, or secrets. |

No HIGH-severity threats introduced: a docs-only refactor. The install trust model is preserved (relocated to install.md and linked), not removed or weakened; examples carry no secrets.
</threat_model>

<verification>
- `make test` exits 0 with the Plan 01 drift-check validating the refreshed README.
- The README links every Plan 02 docs page + CONTRIBUTING.md; the Status section is gone; Development is moved out.
- No legacy/migration framing survives (grep gates above); the icon example uses the icon attribute.
</verification>

<success_criteria>
- README is a lean progressive-disclosure hub (D-01) authored + reviewed with `/crafting-effective-readmes` and split with `/agent-md-refactor` (D-02).
- The Status section is removed entirely (D-03); Development is moved to CONTRIBUTING.md (D-04).
- Every surface the README names exists in source (drift-check green); the icon-in-title example is reworked to the icon attribute.
</success_criteria>

<output>
Create `.planning/phases/06.4-user-documentation-audit-and-refactor/06.4-03-SUMMARY.md` when done.
</output>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- **`README.md`** (refactored) — lean progressive-disclosure hub linking the five docs/ pages + CONTRIBUTING.md; Status section removed; Development moved out; icon example fixed.
- **`CONTRIBUTING.md`** (new) — the moved contributor/Development content (hypothesis loop, make targets, links to docs/agent-iteration.md).
- No new code symbols; both are prose validated by the Plan 01 drift-check.
</artifacts_this_phase_produces>
</output>
