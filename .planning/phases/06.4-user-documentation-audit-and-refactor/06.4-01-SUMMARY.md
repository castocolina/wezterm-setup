---
phase: 06.4-user-documentation-audit-and-refactor
plan: 01
subsystem: testing
tags: [lua, docs-drift, drift-check, argparse, keybindings, scene-schema, make-test]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: "cli/spec.lua single-source command contract; spec.subcommand_names()/build_parser()"
  - phase: 01-foundation
    provides: "config/wezterm-setup/keybindings.lua key DATA table; cli/commands/keys.lua exported normalizers (canonicalize_key/norm_mods/id_of)"
  - phase: 06.1-tab-and-scene-identity-redesign
    provides: "cli/lib/scene.lua LAYOUTS/COLOR_NAMES/parse_pane_spec + cli/lib/recipe.lua field set"
provides:
  - "Durable tests/docs_drift_test.lua: documented->exists drift-check auto-discovered by make test (zero runner/Makefile edits)"
  - "Programmatic source-surface enumerators reused-from-source (subcommands, flags, chords, layouts, colors, scene fields)"
  - "fires_on_bad_reference firing proof (D-09) the downstream doc plans 02/03/04 run after editing prose"
affects: [06.4-02, 06.4-03, 06.4-04, documentation, verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Inverse spec-driven test: derive the SOURCE set programmatically, iterate DOCUMENTED tokens, assert membership (documented->exists)"
    - "Command-context scoping: extract command/flag/chord tokens only from inline-backtick + fenced code, never prose"
    - "Tolerate-if-absent governed-doc list so the check is valid before later waves author the focused pages"

key-files:
  created:
    - "tests/docs_drift_test.lua"
    - ".planning/phases/06.4-user-documentation-audit-and-refactor/deferred-items.md"
  modified:
    - "README.md"

key-decisions:
  - "Chord identity reuses the EXPORTED keys.id_of; documented capital-letter labels are lowercased before id_of so Super+K matches the lowercase data entry without spuriously implying SHIFT"
  - "Token extraction is scoped to command-context spans (inline backticks + fenced blocks) so prose mentions like 'the wez binary' are never validated as subcommands"
  - "D-10 trivial README fixes applied inline so the docs are truthful and make test is green at end-of-plan (deeper README refactor remains plans 02/03/04)"
  - "Pre-existing cli/lib/recipe_test.lua failure logged to deferred-items.md, not fixed (out of scope per phase doc-boundary D-10)"

patterns-established:
  - "Drift-check derives every surface from source modules (spec/keybindings/scene), never a hardcoded list"
  - "Firing proof: planted bad subcommand/flag/chord/field/layout/color asserted REJECTED through the same predicates the real scan uses"

requirements-completed: []

# Metrics
duration: ~25min
completed: 2026-06-19
---

# Phase 06.4 Plan 01: Documentation Drift-Check Backbone Summary

**Durable `tests/docs_drift_test.lua` that derives the real command/flag/keybinding/scene surface from source and fails `make test` whenever a doc names a nonexistent token — auto-discovered, zero runner edits, with a planted-bad-reference firing proof.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-06-19
- **Tasks:** 2 (both TDD)
- **Files modified:** 3 (1 created test, 1 README fix, 1 deferred-items log)

## Accomplishments
- Built the zero-config drift-check (`tests/docs_drift_test.lua`, 476 lines) auto-discovered by `tools/run-tests.sh` / `make test` with NO Makefile or runner edit.
- Enumerated every documented surface PROGRAMMATICALLY from source: `spec.subcommand_names()` + `spec.build_parser():pparse`, `require("keybindings").keys` via the exported `keys.id_of`, and `scene.LAYOUTS`/`scene.COLOR_NAMES`/`scene.parse_pane_spec`.
- Wired a governed-doc scanner (README + tolerated-if-absent `docs/*.md`) that extracts subcommands/flags/chords/TOML-fields/layout+color values from command-context spans only and asserts each EXISTS in source (documented -> exists ONLY, D-09).
- Added the `fires_on_bad_reference` block proving the check FAILS on a planted bad subcommand, flag, chord, scene field, layout, and color — plus a live scratch-copy proof that a real planted token produces a non-zero exit.
- The check immediately caught real README drift and the D-10 trivial fixes were applied so the docs are truthful: `--panes` -> `--pane shell --pane shell`, `Alt+Shift+\` / `Alt+-` -> `Alt+Shift+H` / `Alt+Shift+V`, zoom `Ctrl+Shift+Z` -> `Alt+Shift+Z`.

## Task Commits

Each task was committed atomically (both TDD; enumerator self-tests then doc-scan assertions land green so RED/GREEN collapse into one commit per task):

1. **Task 1: Source-surface enumerators + harness** - `ab8716d` (test)
2. **Task 2: Doc scanner + documented-to-exists assertions + firing guard** - `7cd6944` (feat)

**Plan metadata:** (final docs commit — STATE/ROADMAP/SUMMARY)

## Files Created/Modified
- `tests/docs_drift_test.lua` - The durable drift-check: `check()` harness, `source_subcommands`/`flag_accepts`/`source_chords`/`source_layouts`/`source_colors`/`source_scene_fields` enumerators, `governed_docs`/`command_spans`/token extractors, and the `fires_on_bad_reference` self-test block. 71 passed / 0 failed.
- `README.md` - D-10 trivial truthfulness fixes (scene `--pane` flag + three keybinding chords) so the docs match source and the check is green.
- `.planning/phases/06.4-user-documentation-audit-and-refactor/deferred-items.md` - logs the pre-existing `recipe_test.lua` failure as out-of-scope.

## Decisions Made
- **Reuse exported `keys.id_of` for chord identity** — documented chords are normalized through the same canonicalizer the live `wez keys` path uses; a single-letter doc label is lowercased first so `Super+K` matches the `mapped:k` + `SUPER` data entry without falsely implying SHIFT.
- **Command-context scoping** — subcommand/flag/chord extraction runs only over inline-backtick spans and fenced code-block lines (with shell-comment tails stripped), so prose like "the `wez` binary" and `# keep wez binary` comments never become false subcommand claims.
- **Tolerate-if-absent governed docs** — `docs/cli.md`/`scenes.md`/`keybindings.md`/`install.md` are listed but skipped when absent, so the check is valid in Wave 1 before later plans author them.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] README documented nonexistent keybinding chords and a wrong scene flag**
- **Found during:** Task 2 (the drift-check fired on the live README)
- **Issue:** README named `--panes 2` (real flag is repeatable `--pane`), split chords `Alt+Shift+\` / `Alt+-` (replaced by `Alt+Shift+H` / `Alt+Shift+V` per D-10), and zoom `Ctrl+Shift+Z` (real is `Alt+Shift+Z`) — all absent from source, so the docs were untruthful.
- **Fix:** Applied minimal D-10-sanctioned inline corrections to the README "What it delivers" table, the Keybindings table, and the Scenes example so each documented token resolves in source. Deeper README restructuring remains plans 02/03/04.
- **Files modified:** README.md
- **Verification:** `lua5.4 tests/docs_drift_test.lua` -> 71 passed / 0 failed (exit 0); scratch-copy planted-token proof returns non-zero.
- **Committed in:** `7cd6944` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — doc-truthfulness, the exact D-10 trivial-fix disposition the phase context sanctions).
**Impact on plan:** Necessary for the plan's own success criterion (`make test` green at end-of-plan). No scope creep — only the tokens the check flagged were corrected; the lean-hub refactor stays in later waves.

## Issues Encountered
- **Pre-existing `cli/lib/recipe_test.lua` failure** ("2.9d ai.toml maps to ...") surfaced when running the full `make test`. Reproduced at commit `ab8716d` and earlier — NOT caused by this plan. The new `tests/docs_drift_test.lua` itself passes 71/71 (exit 0). Logged to `deferred-items.md` and left unfixed per the scope boundary; the suite-level non-zero is solely this pre-existing recipe failure, not the drift-check.

## Known Stubs
None — the test is fully wired to source; no placeholder data paths.

## Threat Flags
None — the test only `require`s in-repo pure modules and reads in-repo docs as data (no network, no `load()`/eval of doc content), matching the plan's accept/mitigate threat register with no new surface.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plans 02/03/04 now have a machine-checkable gate: after editing any governed doc, `lua5.4 tests/docs_drift_test.lua` (or `make test`) asserts every documented command/flag/chord/scene field exists in source.
- The `governed_docs()` list already names the focused pages (`docs/cli.md`/`scenes.md`/`keybindings.md`/`install.md`) so they are validated automatically the moment Plan 02/03 creates them — no test edit needed.
- Open item carried forward (not a blocker for this plan): the pre-existing `cli/lib/recipe_test.lua` failure (deferred-items.md) — a scene/recipe seed↔test mismatch to repair outside 06.4.

## Self-Check: PASSED

- FOUND: tests/docs_drift_test.lua
- FOUND: .planning/phases/06.4-user-documentation-audit-and-refactor/06.4-01-SUMMARY.md
- FOUND: .planning/phases/06.4-user-documentation-audit-and-refactor/deferred-items.md
- FOUND commit: ab8716d (Task 1)
- FOUND commit: 7cd6944 (Task 2)

---
*Phase: 06.4-user-documentation-audit-and-refactor*
*Completed: 2026-06-19*
