---
status: testing
phase: 07.1-macos-post-v1-follow-ups
source: [07.1-VERIFICATION.md, live-mux-test 2026-06-23]
started: 2026-06-23T12:26:29Z
updated: 2026-06-23T16:10:00Z
---

# Phase 07.1 UAT — macOS live verification

Verification status legend:
- **AUTO-LIVE** — confirmed against the live WezTerm mux / unit suite by the agent on 2026-06-23 (Intel macOS, `wezterm 20260622-120102`).
- **HUMAN-GUI** — needs your eyes on the GUI (rendered tab bar / pressing real keys) — the CLI cannot observe rendered glyph widths or inject GUI keystrokes.
- **HW-ARM64** — needs real Apple-Silicon hardware (non-gating).

## Current Test

number: 2
name: Tab-title rendering after the column-aware fix (#3)
expected: |
  Open 3+ tabs in the same project with different icons (e.g. ai/docker/shell scenes).
  Each tab title should now show MORE of the title and the SAME length across tabs
  with the same title (no more @wez vs @wezte vs @wezter unevenness). tab_max_width
  now defaults to 32.
awaiting: user response

## Tests

### 1. `wez keys` works (regression fixed) — AUTO-LIVE ✓
expected: `wez keys` lists bindings; `wez keys --json` emits valid JSON.
result: PASS — keys plain = 174 lines; `--json` = 22.9 KB valid parseable JSON. Root cause fixed: install_macos now symlinks all WezTerm bundle siblings (wezterm-gui, wezterm-mux-server, strip-ansi-escapes), not just `wezterm`, so `wezterm show-keys --lua` can re-exec `wezterm-gui`. (commits 959dac0/4c765e1)

### 2. Tab-title rendering (#3) — HUMAN-GUI
expected: titles fill more width and truncate to equal column width across same-title tabs; emoji icons (1- vs 2-codepoint) no longer cause uneven truncation.
result: [pending — needs your eyes] — logic fixed + unit-tested (column-aware single-pass truncation; tab_max_width default 32; commits 8498a93/3d4f072). The CLI cannot read the rendered tab-bar text, so please confirm visually.

### 3. Scene styling no longer leaks `printf`/`quote>` (#4) — AUTO-LIVE ✓
expected: launching ai/dev/docker scenes leaves clean panes — no literal `printf '\033…'` and no `quote>`.
result: PASS — launched all 3 scenes live (claude→shell to avoid spawning a real agent); 0 panes leaked after the chunking fix (was: ai scene leaked in both panes). Root cause: one ~1.1 KB `printf` line typed into a fresh shell; now chunked to one short `printf` per OSC. (commits 76020c8/53a49cf)

### 4. `wez scene launch` all recipes — AUTO-LIVE ✓
expected: ai/dev/docker launch (exit 0), spawn the right pane count, open in the launch dir (D-08), reject unknown/traversal names.
result: PASS — ai=2 panes, dev=3, docker=3, all exit 0; all panes opened in the launch dir; unknown-name→exit 1, empty-recipes→exit 2, path-traversal→exit 1.

### 5. `wez tab` / `wez pane` identity (color/title/icon + reset) — AUTO-LIVE ✓
expected: each set/clear emits its OSC and exits 0.
result: PASS — tab color/title/icon, pane color/title/icon, and the reset/clear paths all exit 0.

### 6. `wez doctor`, `wez version`, `wez completions`, `wez seed-scenes` — AUTO-LIVE ✓
result: PASS — doctor exit 0 + gates; version `wez 0.1.0`; bash+zsh completions emit; seed-scenes copies 3 recipes.

### 7. Keybinding inventory — AUTO-LIVE (data) / HUMAN-GUI (keypress)
expected: load-bearing bindings present (clear, Super, Alt+Shift+R/E rotate, Alt+Shift+Z zoom).
result: PASS for presence in `wez keys` output. Pressing the keys in the GUI to confirm the bound ACTIONS fire is HUMAN-GUI (the CLI cannot inject keystrokes).

### 8. `make uninstall` idempotent (quick-gn7) — AUTO-LIVE ✓
expected: `make uninstall` warns + exit 0 when `wez` is already gone (so `make uninstall install` works from clean).
result: PASS — `WEZ_BIN_DIR=<empty> PATH= bash tools/uninstall.sh` exits 0.

### 9. Dev-launcher Lua 5.4 resolution (quick-gbk) — AUTO-LIVE ✓
expected: `make install` builds a working dev `wez` on keg-only Homebrew lua@5.4.
result: PASS — build resolves `/usr/local/opt/lua@5.4/bin/lua5.4`; `./dist/wez version` exit 0.

### 10. Scene PATH symlink (07.1-01) — AUTO-LIVE ✓
result: PASS — `wezterm` resolvable on PATH; `wez scene` works out-of-box.

### 11. Apple-Silicon first-launch self-check (INST-08, non-gating) — HW-ARM64
expected: on arm64 hardware, `bash tools/silicon-check.sh` downloads v1.0.0 `wez-macos-aarch64`, verifies sha256 before chmod/run, observes (not mutates) quarantine + codesign, runs `wez version`/`doctor`, prints a pasteable report → paste into docs/macos-verification.md Section 1a.
result: [pending — needs arm64 hardware]

## Summary

total: 11
passed: 8        # AUTO-LIVE confirmed
issues: 0
pending: 3       # 2 HUMAN-GUI (tab rendering, key-press actions), 1 HW-ARM64
skipped: 0
blocked: 0

## Gaps

None functional. Remaining items are observation-only: GUI rendering of the tab-title fix (#3),
pressing real keybindings, and the non-gating arm64 self-check.
