---
phase: 07.1-macos-post-v1-follow-ups
plan: 01
subsystem: infra
tags: [macos, installer, bash, symlink, wez-scene, tdd, lua-test]

# Dependency graph
requires:
  - phase: 07-macos-parity
    provides: "install_macos() real .app placement + run-the-binary evidence; macOS deviation #6 / A-4 follow-up filed"
provides:
  - "install_macos symlinks the bundled WezTerm CLI onto the user PATH (~/.local/bin/wezterm), mirroring install_linux"
  - "wez scene new / wez scene launch resolve wezterm by name out-of-box on a clean macOS install (no 'non-numeric pane id')"
  - "TEXT no-run regression assertions proving the macOS symlink block exists and is ordered correctly"
affects: [07.1-03-consolidation, macos-verification-runbook, INST-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "install_macos mirrors install_linux's ln -sfn user-PATH symlink idiom (Linux<->macOS install symmetry)"
    - "TEXT no-run unit gate: assert script body shape (ln -sfn / mkdir -p ordering) without triggering a live download"

key-files:
  created: []
  modified:
    - tools/bootstrap-wezterm.sh
    - tests/cli/bootstrap_macos_test.lua

key-decisions:
  - "Mirror install_linux's exact ln -sfn idiom (idempotent -f -n flags, BSD-ln safe) rather than introduce GNU-only flags"
  - "Bind a local wez_bin to the bundle CLI path and reuse the already-defined app_dir/BIN_DIR locals — no new download or fetcher"
  - "Include the install_linux not-on-PATH advisory (case ':${PATH}:') for parity (RESEARCH Open Q1) — cheap, no behavior risk"
  - "Keep the test a TEXT-only no-run gate (no real download), consistent with the file's stated contract"

patterns-established:
  - "Plan-level TDD gate: RED (test asserts absent block, exits non-zero on exactly the new checks) -> GREEN (mirror precedent, full suite exits 0)"

requirements-completed: [INST-08]

# Metrics
duration: 6min
completed: 2026-06-23
---

# Phase 07.1 Plan 01: macOS scene-PATH symlink Summary

**install_macos now symlinks the bundled WezTerm CLI onto the user PATH (~/.local/bin/wezterm), so `wez scene` works out-of-box on a clean macOS install — closing deviation #6 / A-4 and restoring Linux<->macOS install symmetry.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-06-23
- **Completed:** 2026-06-23
- **Tasks:** 2 completed (TDD RED + GREEN)
- **Files modified:** 2

## Accomplishments
- `install_macos()` now creates `~/.local/bin/wezterm -> ~/Applications/WezTerm.app/Contents/MacOS/wezterm`, sudo-free and idempotent, after the run-the-binary evidence block.
- `wez scene new` / `wez scene launch <name>` resolve `wezterm` by name out-of-box on macOS (no `mux returned a non-numeric pane id`).
- Added a TEXT no-run regression (3 assertions) that proves the symlink block exists and that `mkdir -p` precedes `ln -sfn`, mirroring install_linux ordering.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — add the macOS symlink TEXT assertions** - `688f260` (test)
2. **Task 2: GREEN — mirror install_linux's ln -sfn block inside install_macos** - `d085e7d` (feat)

_TDD: RED commit added 3 failing assertions; GREEN commit landed the symlink block and turned them green. No REFACTOR needed — the block is a minimal direct mirror of install_linux._

## Files Created/Modified
- `tools/bootstrap-wezterm.sh` - Added the `ln -sfn` user-PATH symlink block (+ not-on-PATH advisory) to `install_macos()`, mirroring `install_linux:449-451/465-468`.
- `tests/cli/bootstrap_macos_test.lua` - Added 3 `check(...)` assertions scoped to `macos_body()`: `ln -sfn` present, `${BIN_DIR}/wezterm` target, and `mkdir -p` precedes `ln -sfn`.

## Decisions Made
- Reused install_linux's exact `ln -sfn` idiom (BSD-`ln`-safe, idempotent) — no GNU-only flags.
- Reused existing `app_dir` / `BIN_DIR` locals; bound a local `wez_bin` to the bundle CLI path — no new download/fetcher/scratch dir.
- Added the install_linux not-on-PATH advisory for parity (RESEARCH Open Q1).
- Kept the test a TEXT-only no-run gate — no live WezTerm download is triggered.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `tools/run-tests.sh` aborts with `'lua5.4' not found on PATH` under the default shell; resolved by prepending `$(brew --prefix lua@5.4)/bin` to PATH (matches the plan's verify commands). No change to the project; environment-only.

## TDD Gate Compliance
- RED gate: `688f260` (`test(07.1-01): ...`) — added 3 assertions; test exited non-zero with exactly those 3 checks failing, 26 pre-existing checks green.
- GREEN gate: `d085e7d` (`feat(07.1-01): ...`) — landed the symlink block; macOS bootstrap test 29/29 (exit 0), full suite 31 files pass.
- REFACTOR gate: not required (minimal mirror, no cleanup needed).

## Verification
- `bash -n tools/bootstrap-wezterm.sh` clean.
- `lua5.4 tests/cli/bootstrap_macos_test.lua` exits 0 — 29 passed, 0 failed (3 new symlink assertions green; security greps — no `/Applications`, `sudo`, `hdiutil`, `xattr` — unchanged).
- `tools/run-tests.sh` (full suite): all 31 files passed.
- Security invariants preserved: no `xattr`/quarantine mutation, no `sudo`/`hdiutil`, no system `/Applications` — only `~/.local/bin` (user-path) write added (T-07.1-02 mitigation honored).
- Live/manual (real Mac) check is owned by Plan 03's runbook §6/§7 (human_verify_mode=end-of-phase) — out of scope here.

## Known Stubs
None.

## Self-Check: PASSED
- Files: `tools/bootstrap-wezterm.sh`, `tests/cli/bootstrap_macos_test.lua`, `07.1-01-SUMMARY.md` all present.
- Commits: `688f260` (RED), `d085e7d` (GREEN) both in git history.
