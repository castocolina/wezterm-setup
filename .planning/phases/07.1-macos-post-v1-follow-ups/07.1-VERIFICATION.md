---
phase: 07.1-macos-post-v1-follow-ups
verified: 2026-06-23T12:25:02Z
status: human_needed
score: 8/8 must-haves verified
overrides_applied: 0
human_verification:
  - test: "On real Apple Silicon (Darwin arm64), run `bash tools/silicon-check.sh` and paste the report into docs/macos-verification.md Section 1a"
    expected: "Checksum verifies, `codesign --verify` OK, `wez version` = v1.0.0, `wez doctor` exits 0, quarantine absent on gh/curl download — a clean report flips INST-08's macOS status"
    why_human: "The project's agents have no Apple-Silicon hardware. INST-08's live first-launch run is explicitly NON-gating and is the single deferred item; shipping the kit (not running it) is the phase deliverable. This item's absence does NOT downgrade the phase to gaps_found."
---

# Phase 07.1: macOS post-v1 follow-ups Verification Report

**Phase Goal:** Close the macOS items deferred out of the Phase 7 v1 gate (NON-gating; v1.0.0 already shipped): (1) make `wez scene` work out-of-box on a clean macOS install by symlinking `wezterm` onto PATH in `install_macos`; (2) fix the cross-platform `wez keys --json` dkjson require-path bug; (3) ship an Apple-Silicon end-user first-launch self-check kit (INST-08); (4) record the keep-both keybindings rationale durably.
**Verified:** 2026-06-23T12:25:02Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | `install_macos` symlinks the bundled WezTerm CLI onto the user PATH (`~/.local/bin/wezterm`), mirroring `install_linux` | ✓ VERIFIED | `tools/bootstrap-wezterm.sh:573-576`: `local wez_bin="${app_dir}/WezTerm.app/Contents/MacOS/wezterm"; mkdir -p "${BIN_DIR}"; ln -sfn "${wez_bin}" "${BIN_DIR}/wezterm"; log ...` — exact mirror of `install_linux:449-451`, `mkdir -p` precedes `ln -sfn` |
| 2 | `wez scene new`/`launch` resolve `wezterm` by name out-of-box on a clean macOS install (no "non-numeric pane id") | ✓ VERIFIED | Symlink places `wezterm` on `${BIN_DIR}` (`~/.local/bin`, user PATH); not-on-PATH advisory at `:578-581`; idempotent `ln -sfn` (`-f -n`, BSD-ln safe). Live arm64 confirmation is the non-gating human item below |
| 3 | The symlink is sudo-free, user-path only, idempotent on re-install/update | ✓ VERIFIED | `bootstrap_macos_test.lua` security greps green: no system `/Applications`, no `sudo`/`hdiutil`, no `xattr` strip (29/29 pass); writes only `~/.local/bin`; `ln -sfn` idempotent |
| 4 | `wez keys --json` resolves dkjson via the bundle module name `cli.vendor.dkjson` (dual-resolve, bare-name dev fallback); no fatal bare require in the shipped binary | ✓ VERIFIED | `cli/commands/keys.lua:266-271`: `local ok, json = pcall(require, "cli.vendor.dkjson")` then bare fallback + `type(json) ~= "table"` guard returning 1. `grep` confirms NO bare `require("dkjson")` remains. `tools/build.sh:219` globs all `cli/**/*.lua` so `cli/vendor/dkjson.lua` bundles as `cli.vendor.dkjson` |
| 5 | A regression pins the bundle require path so the bug cannot silently recur | ✓ VERIFIED | `keys_test.lua` source-guard + bundle round-trip assertions present and green (25/25 pass): "uses the bundle require path, not bare require(\"dkjson\")" + "round-trips through the BUNDLE module cli.vendor.dkjson" |
| 6 | `tools/silicon-check.sh` ships: exists, executable, verifies sha256 BEFORE chmod/run, sudo-free, observe-only | ✓ VERIFIED | `-rwxr-xr-x`, 178 lines, `bash -n` clean. Checksum verified at `:91-106` BEFORE `chmod +x` (`:111`) and run (`:144`). Non-comment greps: zero `spctl`/`sudo`/`hdiutil`, zero `xattr -d`/`xattr -c`, no system `/Applications`. mktemp scratch + trap cleanup |
| 7 | docs/macos-verification.md has the Apple-Silicon (INST-08) self-check section + paste-report landing spot; keep-both keybindings rationale recorded | ✓ VERIFIED | Section 1a (`:246-288`) invokes `tools/silicon-check.sh`, documents spctl/quarantine caveats, has a fenced "paste the report here" landing spot referencing INST-08. Keep-both rationale at `:402-419` (SUPER=Cmd, Ctrl+Shift intentional fallbacks, D-08, no behavior change) |
| 8 | Deviation #5/#6 + SCEN/A-4 footers flipped to fixed-in-07.1 across runbook/REQUIREMENTS/ROADMAP/MACOS-PARITY; INST-08 NOT falsely flipped to verified | ✓ VERIFIED | Runbook deviation rows #5 (`:641,:664`) + #6 (`:665`) read "FIXED in 07.1"; MACOS-PARITY A-4 RESOLVED + checkbox ticked (`:53,:62`), --json fixed (`:133`). INST-08 per-ID row byte-IDENTICAL in REQUIREMENTS:94 and ROADMAP:427 (`Done (Phase 6; macOS on-Mac verify deferred D-18)`) — NOT flipped to verified. SCEN-05/06 coverage rows byte-identical (REQ:119-120 / RMP:423-424) |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `tools/bootstrap-wezterm.sh` | install_macos `ln -sfn` symlink block | ✓ VERIFIED | Lines 573-576; mirrors install_linux; `bash -n` clean; wired via main() macos branch (`:598`) |
| `tests/cli/bootstrap_macos_test.lua` | TEXT no-run symlink assertions | ✓ VERIFIED | `ln -sfn`, `${BIN_DIR}/wezterm`, `mkdir -p` ordering checks green; 29/29 pass |
| `cli/commands/keys.lua` | dual-resolve `cli.vendor.dkjson` in --json branch | ✓ VERIFIED | Lines 266-271; no bare require; guard returns 1 |
| `tests/cli/keys_test.lua` | bundle-require regression | ✓ VERIFIED | Source-guard + round-trip assertions green; 25/25 pass |
| `tools/silicon-check.sh` | verify-before-run self-check (≥30 lines, `shasum -a 256`, `wez-macos-aarch64`) | ✓ VERIFIED | 178 lines, executable, `shasum -a 256` + `wez-macos-aarch64` present, verify-before-run order correct |
| `docs/macos-verification.md` | Apple-Silicon section + keybindings note + flipped rows | ✓ VERIFIED | Section 1a + keep-both rationale + flipped deviation #5/#6 rows all present |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `install_macos()` | `${BIN_DIR}/wezterm` symlink | `ln -sfn ${app_dir}/WezTerm.app/Contents/MacOS/wezterm` | ✓ WIRED | `bootstrap-wezterm.sh:573-575` |
| `keys.lua --json branch` | `cli/vendor/dkjson.lua` (as `cli.vendor.dkjson`) | `pcall(require, "cli.vendor.dkjson")` + bundle glob in build.sh:219 | ✓ WIRED | require + build.sh `find cli ... *.lua` confirms bundle name |
| `docs Section 1a` | `tools/silicon-check.sh` | runbook instructs `bash tools/silicon-check.sh` + paste report | ✓ WIRED | `macos-verification.md:263,:281-288` |
| `silicon-check.sh` | v1.0.0 `wez-macos-aarch64` asset | gh/curl download + `shasum -a 256` verify-before-run | ✓ WIRED | `silicon-check.sh:39-106` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| keys --json bundle-require regression | `lua5.4 tests/cli/keys_test.lua` | 25 passed, 0 failed, exit 0 | ✓ PASS |
| macOS bootstrap symlink + security greps | `lua5.4 tests/cli/bootstrap_macos_test.lua` | 29 passed, 0 failed, exit 0 | ✓ PASS |
| Full unit suite (no regression) | `bash tools/run-tests.sh` | all 31 files passed | ✓ PASS |
| silicon-check.sh syntax | `bash -n tools/silicon-check.sh` | clean | ✓ PASS |
| bootstrap syntax | `bash -n tools/bootstrap-wezterm.sh` | clean (implied by test sourcing) | ✓ PASS |
| Live arm64 silicon-check run | `bash tools/silicon-check.sh` on real Mac | no arm64 hardware | ? SKIP (non-gating human item) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| INST-08 | 07.1-01, 07.1-02, 07.1-03 | Apple-Silicon end-user first-launch check (non-gating) | ✓ SATISFIED (kit shipped) | `tools/silicon-check.sh` + runbook Section 1a shipped. Live arm64 run remains a deferred/non-gating human item by design — per-ID INST-08 row correctly stays `deferred D-18`, NOT flipped to verified |

No orphaned requirements: ROADMAP maps only INST-08 to Phase 07.1, and all three plans declare `requirements: [INST-08]`. Deviation #5/#6 + SCEN/A-4 are doc-footer closures, not new requirement IDs (consistent with ROADMAP line 440).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| tools/silicon-check.sh | 48 | `XXXXXX` (in `mktemp -d` template) | ℹ️ Info | False positive — `mktemp` random-suffix template, not a debt marker |

No real `TBD`/`FIXME`/`XXX` debt markers; no `TODO`/`HACK`/`PLACEHOLDER`; no stub returns. The Section 1a "paste the report here" fenced block is an intentional, documented INST-08 flip landing spot — not a stub.

### Human Verification Required

#### 1. Apple-Silicon first-launch self-check (INST-08, NON-gating)

**Test:** On a real Apple-Silicon Mac (Darwin arm64), run `bash tools/silicon-check.sh` and paste the printed report into `docs/macos-verification.md` Section 1a.
**Expected:** Checksum verifies; `codesign --verify` OK; `wez version` = v1.0.0; `wez doctor` exits 0; quarantine absent on gh/curl download. A clean report flips INST-08's macOS status from deferred to verified.
**Why human:** The project's agents have no Apple-Silicon hardware. This live run is explicitly non-gating and is the single intentionally-deferred item. Shipping the kit (script + runbook + landing spot) — which is fully verified above — is the phase deliverable, NOT the real-hardware run.

### Gaps Summary

No gaps. All 8 observable truths are verified against the codebase:

- **Scene PATH fix (Plan 01):** `install_macos` symlinks `~/.local/bin/wezterm` → the bundle CLI, a byte-faithful mirror of `install_linux` with correct `mkdir -p` → `ln -sfn` ordering, sudo-free, idempotent, security invariants intact (29/29 test pass).
- **keys --json fix (Plan 02):** dual-resolve `cli.vendor.dkjson` (bare dev fallback + unavailable-encoder guard) replaces the bare require; no bare `require("dkjson")` remains; `build.sh:219` confirms the vendored module bundles under the resolved name; regression pinned (25/25 test pass); `build.sh` correctly left untouched.
- **Silicon kit (Plan 03):** `tools/silicon-check.sh` ships executable, verify-before-run (checksum at :91-106 strictly precedes chmod :111 and run :144), sudo-free, observe-only (no spctl/sudo/hdiutil/xattr-strip), with runbook Section 1a + paste-report landing spot.
- **Docs (Plan 03):** keep-both keybindings rationale recorded durably (D-08); deviation #5/#6 + SCEN/A-4 footers flipped to fixed-in-07.1 across all four docs; INST-08 per-ID rows byte-identical between REQUIREMENTS.md:94 and ROADMAP.md:427 and correctly NOT flipped to verified.

Full unit suite (31 files) passes with no regression. All six phase commits exist in git history.

The phase is **human_needed** solely because the live arm64 silicon-check run is a legitimate non-gating deferred item — its absence does NOT block goal achievement. The phase goal (shipping the kit + the two code fixes + the durable docs) is fully achieved in the codebase.

---

_Verified: 2026-06-23T12:25:02Z_
_Verifier: Claude (gsd-verifier)_
