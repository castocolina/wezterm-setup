# Phase 07.1: macOS post-v1 follow-ups - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-22
**Phase:** 07.1-macos-post-v1-follow-ups
**Areas discussed:** Scene PATH fix (A-4), `wez keys --json` bug membership (#5), Apple-Silicon end-user check (INST-08), Keybindings documentation

---

## Scene PATH fix (A-4 / deviation #6)

| Option | Description | Selected |
|--------|-------------|----------|
| Symlink wezterm→~/.local/bin | Mirror install_linux:450 (`ln -sfn` the bundled CLI). Sudo-free, user-path, symmetric, no shell-rc edits. Smallest correct change. | ✓ |
| Prepend .app/Contents/MacOS to managed PATH | Add bundle MacOS dir to managed shell PATH. Broader, touches shell rc, PATH-ordering risk. | |

**User's choice:** Symlink wezterm→~/.local/bin (Recommended)
**Notes:** Restores Linux↔macOS symmetry; `wez scene` resolves `wezterm` out-of-box once shipped.

---

## `wez keys --json` bug membership (#5 / deviation #5)

| Option | Description | Selected |
|--------|-------------|----------|
| Fold into 7.1 | Fix here alongside the macOS work, despite being cross-platform. One cycle. | ✓ |
| Standalone bugfix, keep 7.1 macOS-only | Handle outside 7.1 to keep scope pure. | |

**User's choice:** Fold into 7.1
**Notes:** The dkjson require-path bug is cross-platform (Linux equally broken) but surfaced during the Phase 7 macOS pass; user opted to fix it within 7.1. Fix benefits both platforms.

---

## Apple-Silicon end-user check (INST-08)

| Option | Description | Selected |
|--------|-------------|----------|
| Documented self-run checklist + script | End-user runs on real Silicon: download arm64 → Gatekeeper → wez doctor → report. Actionable now; non-gating. | ✓ |
| Defer until Silicon hardware available | Placeholder only. | |
| Report-based (ship + collect) | Rely on shipped asset + issue template. | |

**User's choice:** Documented self-run checklist + script (Recommended)
**Notes:** No agent hardware available; non-gating. INST-08 macOS status flips only when a real Silicon run is reported.

---

## Keybindings documentation

| Option | Description | Selected |
|--------|-------------|----------|
| Note in docs + runbook | Durable, discoverable record of the keep-both rationale. | ✓ |
| Config comment only | Lives in keybindings.lua, less discoverable. | |
| No-op — existing comments suffice | Treat as already recorded. | |

**User's choice:** Note in docs + runbook (Recommended)
**Notes:** Keep-both decided earlier (no behavior change); `SUPER`=Cmd already works, Ctrl+Shift are intentional cross-platform fallbacks. Rationale currently only in chat/handoff — make it durable.

---

## Claude's Discretion

- Exact test file placement/names and the precise `cli/vendor/` require idiom.
- Whether the Silicon self-check ships as a standalone script, a runbook section, or both.

## Deferred Ideas

- Dropping macOS `Ctrl+Shift` fallbacks for Cmd-only ergonomics (needs `target_triple` gating) — NOT done; keep-both chosen.
- Broader bootstrapper UX (version listing, `--branch` flag, branch-default install) — separate cross-platform installer follow-up, not 07.1.
