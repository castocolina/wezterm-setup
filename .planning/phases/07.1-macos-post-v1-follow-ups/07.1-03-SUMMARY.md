---
phase: 07.1-macos-post-v1-follow-ups
plan: 03
subsystem: docs
tags: [macos, apple-silicon, inst-08, self-check, bash, runbook, keybindings, deviation-flip]

# Dependency graph
requires:
  - phase: 07.1-01
    provides: "install_macos scene-PATH symlink (~/.local/bin/wezterm) — deviation #6 fix to footer-flip"
  - phase: 07.1-02
    provides: "wez keys --json dual-resolve cli.vendor.dkjson — deviation #5 fix to footer-flip"
provides:
  - "tools/silicon-check.sh — sudo-free, verify-before-run Apple-Silicon first-launch self-check kit (INST-08)"
  - "docs/macos-verification.md Section 1a (Apple-Silicon first-launch, INST-08) with a paste-report landing spot"
  - "keep-both keybindings rationale recorded durably in the runbook (D-08)"
  - "macOS deviation #5/#6 + SCEN/A-4 footers flipped to fixed-in-07.1 across REQUIREMENTS/ROADMAP/runbook/MACOS-PARITY"
affects: [INST-08, macos-verification-runbook, requirements-traceability, roadmap-coverage]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "verify-before-run self-check: shasum -a 256 BEFORE chmod +x/run (build.sh idiom); exit non-zero on mismatch"
    - "observe-not-mutate macOS probes: xattr -p (report) + codesign --verify (evidence), never spctl/xattr -d (D-07, Pitfall 1)"
    - "gh-release-download with curl fallback into a mktemp -d scratch dir with trap cleanup"

key-files:
  created:
    - tools/silicon-check.sh
  modified:
    - docs/macos-verification.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/MACOS-PARITY-AND-FOLLOWUPS.md

key-decisions:
  - "spctl intentionally NOT used (always rejects ad-hoc, Pitfall 1); evidence is codesign --verify + the binary running"
  - "Quarantine OBSERVED (xattr -p), never stripped (D-07) — reworded the report strings so no non-comment xattr -d / spctl token trips the acceptance grep"
  - "INST-08 stays DEFERRED/non-gating; only the scene-PATH caveat + #5/#6 deviations flip — INST-08 flips only on a reported real-Silicon run"
  - "REQUIREMENTS Traceability + ROADMAP Coverage footers kept byte-for-byte consistent for shared scene-PATH wording; per-ID INST-08 rows byte-identical"

requirements-completed: [INST-08]

# Metrics
duration: 6min
completed: 2026-06-23
---

# Phase 07.1 Plan 03: macOS docs consolidation + Apple-Silicon self-check kit Summary

**Shipped the sudo-free, verify-before-run Apple-Silicon first-launch self-check kit (`tools/silicon-check.sh` + runbook Section 1a with a paste-report landing spot, INST-08), recorded the keep-both keybindings rationale durably (D-08), and flipped macOS deviation #5 (`wez keys --json`) + #6 (scene PATH) plus the SCEN/A-4 footers to fixed-in-07.1 across the four docs — INST-08 stays deferred/non-gating.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-06-23
- **Completed:** 2026-06-23
- **Tasks:** 2 completed
- **Files modified:** 4 (1 created, 3 modified) + REQUIREMENTS/ROADMAP/MACOS-PARITY

## Accomplishments

- **`tools/silicon-check.sh`** (NEW, executable): downloads the published `v1.0.0` `wez-macos-aarch64` + `.sha256` via `gh release download` with a `curl -fsSL` fallback into a `mktemp -d` scratch dir (trap cleanup); verifies the checksum with `shasum -a 256` **before** `chmod +x`/run (exit non-zero on mismatch); OBSERVES (never mutates) `xattr -p com.apple.quarantine` + `codesign --verify --verbose`; runs `wez version` / `wez doctor` for real evidence; and prints a copy-pasteable REPORT block (arch, checksum, quarantine, codesign, version, doctor exit + tail). `spctl` intentionally NOT used (Pitfall 1); no `sudo`/`hdiutil`/system `/Applications`.
- **`docs/macos-verification.md` Section 1a** "Apple-Silicon first-launch self-check (INST-08)": one-command invocation, expected outcomes, the spctl/quarantine caveats, and a "paste the report here" fenced landing spot whose filling flips INST-08.
- **Keep-both keybindings rationale (D-08)** recorded durably in Section 4: `SUPER` = Cmd on macOS; the `Ctrl+Shift` chords are intentional cross-platform fallbacks kept on macOS; keep-both is locked, no behavior change, `target_triple` gating is a deferred idea.
- **Deviation #5 (`wez keys --json`)** + the DIAG-04 result row flipped to fixed-in-07.1 (Plan 07.1-02, dual-resolve `cli.vendor.dkjson`); historical root cause kept + the truthful "shipped v1.0.0 binary still carries the bug, fix reaches end users via a new release" note added.
- **Deviation #6 (scene PATH)** flipped to fixed-in-07.1 (Plan 07.1-01, `install_macos` symlink); MACOS-PARITY §A-4 checkbox ticked + marked RESOLVED, §C-4/§C-7 updated.
- **SCEN/INST-08 footers** in REQUIREMENTS.md + ROADMAP.md no longer call the scene-PATH fix "not yet shipped" — they read fixed-in-07.1; the two footers stay byte-for-byte consistent for the shared sentence; **INST-08 per-ID rows stay `deferred D-18` (NOT flipped to verified)**.

## Task Commits

1. **Task 1: Apple-Silicon self-check script + runbook section (INST-08)** — `fdf3faf` (feat)
2. **Task 2: keep-both keybindings rationale + flip deviation #5/#6 + SCEN/A-4 footers** — `d4980c8` (docs)

## Files Created/Modified

- `tools/silicon-check.sh` (NEW, +193) — the verify-before-run, observe-only Apple-Silicon self-check kit.
- `docs/macos-verification.md` — Section 1a (Apple-Silicon first-launch, INST-08) + keep-both keybindings note + flipped DIAG-04 result row + flipped deviation #5/#6 rows.
- `.planning/MACOS-PARITY-AND-FOLLOWUPS.md` — §A-4 ticked + RESOLVED-in-07.1; §C-4 (`--json` fixed) + §C-7 (scene caveat resolved) updated.
- `.planning/REQUIREMENTS.md` — Traceability footer: scene-PATH fixed in 07.1, `--json` fixed in 07.1, INST-08 stays deferred/non-gating.
- `.planning/ROADMAP.md` — Coverage footer mirrored byte-for-byte for the shared wording.

## Decisions Made

- `spctl` intentionally NOT used in the script (always rejects ad-hoc — Pitfall 1); evidence is `codesign --verify` + the binary running. Report strings reworded so no non-comment `spctl`/`xattr -d` token trips the acceptance grep, while keeping the documentation meaning.
- Quarantine OBSERVED, never stripped (D-07).
- INST-08 stays DEFERRED/non-gating; only the scene-PATH caveat and deviations #5/#6 flip.
- REQUIREMENTS Traceability and ROADMAP Coverage footers kept byte-for-byte consistent for shared wording; per-ID INST-08 rows byte-identical.

## Deviations from Plan

None — plan executed exactly as written. (Two acceptance-grep-driven rewordings of report strings inside `silicon-check.sh` — `spctl`→"Gatekeeper-assessment", literal `xattr -d`→"see the runbook fallback" — were normal iteration to satisfy the plan's own "no non-comment spctl/xattr -d" acceptance criteria, not behavioral deviations.)

## TDD Gate Compliance

This plan is doc/config + new-bash-script only (no behavior-adding source under test). It is exempt from the RED/GREEN gate per the executor's doc-only exemption; verification is `bash -n` + `shellcheck` + the plan's grep acceptance checks + the full unit suite (no regression).

## Verification

- `bash -n tools/silicon-check.sh` clean; `test -x` passes; `shellcheck tools/silicon-check.sh` clean (SC2329 trap false-positive + SC2034 resolved).
- Task 1 grep gate: `shasum -a 256` present, `wez-macos-aarch64` present, `codesign --verify` on a non-comment line, **zero** non-comment `spctl|sudo |hdiutil`, **zero** non-comment `xattr -d|xattr -c`, runbook has "Apple-Silicon".
- Task 2 grep gate: keep-both rationale + `fixed in 07.1` present in runbook, MACOS-PARITY, REQUIREMENTS, ROADMAP.
- INST-08 per-ID Traceability rows confirmed `deferred D-18` (NOT verified) and byte-identical across REQUIREMENTS/ROADMAP; shared scene-PATH footer sentence byte-identical.
- Full unit suite (`tools/run-tests.sh` under lua5.4 5.4.8): **all 31 files passed** — no regression from the docs/new-script changes.
- Live (real Apple Silicon, NON-gating, human-verify end-of-phase): an end-user runs `bash tools/silicon-check.sh` and pastes the report into Section 1a — that report is what flips INST-08's macOS status (D-07). Not exercisable by the agent (no arm64 hardware).

## Known Stubs

None. The Section 1a "paste the report here" fenced block is an intentional landing spot (documented as the INST-08 flip trigger), not a stub.

## Self-Check: PASSED
